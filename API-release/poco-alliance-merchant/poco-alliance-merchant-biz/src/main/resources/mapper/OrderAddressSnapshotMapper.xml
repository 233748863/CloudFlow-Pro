<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.joywon.poco.merchant.OrderModule.mapper.OrderAddressSnapshotMapper">

    <resultMap id="orderAddressSnapshotVOMap" type="cn.joywon.poco.merchant.OrderModule.vo.OrderAddressSnapshotVO">
        <id property="id" column="id"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="receiverPhone" column="receiver_phone"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="detailAddress" column="detail_address"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
    </resultMap>

    <!-- 根据订单ID查询最新地址快照 -->
    <select id="getAddressSnapshotByOrderId" resultMap="orderAddressSnapshotVOMap">
        SELECT 
            id,
            receiver_name,
            receiver_phone,
            province,
            city,
            district,
            detail_address,
            latitude,
            longitude
        FROM order_address_snapshots
        WHERE order_id = #{orderId} AND is_deleted = 0
        ORDER BY created_time DESC
        LIMIT 1
    </select>

</mapper>