spring:
  data:
    redis:
      host: poco-redis
      database: 5
  # 数据库相关配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: root
      url: jdbc:mysql://poco-mysql:3306/pocox_boot?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&allowMultiQueries=true&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true


# 文件上传配置
file:
  local:
    enable: true
    base-path: /Users/poco/Downloads/img

# 验证码配置
aj:
  captcha:
    water-mark: pig4cloud

# 登录报文加密根密钥 ，必须是16位
security:
  encodeKey: pigxpigxpigxpigx

# 配置文件加密根密码
jasypt:
  encryptor:
    password: poco

# swagger token url 配置
swagger:
  token-url: ${swagger.gateway}/admin/oauth2/token

