# CloudFlow Pro P0-P3 实施计划（2026-05-20）

- 编制日期：2026-05-20
- 关联审计：[审计报告-2026-05-20.md](./审计报告-2026-05-20.md)
- 适用基线：`dev` 分支
- 编排原则：先稳定 → 再统一 → 再优化；P0 必须先于 P1；同级内"后端 → 前端 → 配置"顺序推进
- 范围声明（per memory）：**P2 不纳入补测试工作**；偏好"一次性推进关键重构与数据库大改"，所以同一 PR 内可包含数据库变更

---

## 一、节奏与里程碑

| 阶段 | 工期 | 目标 | 关键交付 |
|---|---|---|---|
| P0 | 1–2 周 | 修补回调链路与权限/任务体验"漏血点" | 回调幂等+死信、对账 Job、任务完成事件协议、前端权限单源 |
| P1 | 2–4 周 | 统一接入模式与契约 | 公共 Handler 模板 v2、Workflow `/wf` 前缀迁移、鉴权补齐、大 service 拆分 |
| P2 | 4–8 周 | 常量与文件治理 | businessType 注册表、inner 白名单配置化、前端冗余清理 |
| P3 | 持续 | 长期演进 | common-base 抽底、SSE 决策、CI 守护 |

---

# 二、P0 实施步骤（高优先）

## P0-1 工作流回调消费幂等改造

**目标**：消除"重复消费覆写业务状态"与"异常静默丢弃"两个风险。

**改造文件**：
- 主改：[cloudflow-backend/cloudflow-common/cloudflow-common-workflow-callback/src/main/java/com/cloudflow/common/workflow/callback/listener/WorkflowApprovalCallbackStreamConsumer.java](cloudflow-backend/cloudflow-common/cloudflow-common-workflow-callback/src/main/java/com/cloudflow/common/workflow/callback/listener/WorkflowApprovalCallbackStreamConsumer.java)
- 新增：`cloudflow-common-workflow-callback/.../service/CallbackIdempotentStore.java`（基于 Redis Set/SETNX）
- 配置：[cloudflow-common/cloudflow-common-workflow-callback/.../WorkflowCallbackProperties.java](cloudflow-backend/cloudflow-common/cloudflow-common-workflow-callback/src/main/java/com/cloudflow/common/workflow/callback/config/WorkflowCallbackProperties.java) 新增 `idempotentTtlHours=72`

**具体步骤**：

1. 新增幂等存储接口 `CallbackIdempotentStore`：
   ```java
   boolean acquire(String streamKey, String msgId, Duration ttl);  // SETNX
   void release(String streamKey, String msgId);                    // 失败时回滚
   ```
2. 在 `WorkflowApprovalCallbackStreamConsumer.onMessage` 进入时调用 `acquire(streamKey, msgId, 72h)`；获取失败直接 ACK+DEL 并 warn 日志（说明已消费过）
3. 调用 `workflowCallbackService.handleApprovalResult(dto)` 包在 try 中：
   - 成功 → ACK+DEL，**保留幂等键**直到 TTL 过期
   - 异常 → **不 ACK**，让 Redis Stream 走 pending 重投递；同时 `release(streamKey, msgId)` 释放幂等键避免下次直接跳过
4. 在 `WorkflowCallbackAutoConfiguration` 注入 `CallbackIdempotentStore` Bean
5. 在 `XADD` 端（`ApprovalResultStreamPublisher`）保证幂等键的源是 `processInstanceId + approvalResult`（避免 Stream msgId 变化但业务等价的情况），把 hash 写进消息体的 `bizMsgKey` 字段，幂等键改为 `bizMsgKey`

**验收**：
- 同一 `bizMsgKey` 重复投递 5 次，业务表只更新 1 次
- Handler 抛异常时消息进入 pending 列表，重启服务后可重投递
- 手工 `XADD` 一条故意构造的异常消息，未走入静默丢弃

---

## P0-2 死信队列与人工补单入口

**目标**：消费连续失败 N 次后转 DLQ，提供查询/重投/补单接口。

**改造文件**：
- 主改：`WorkflowApprovalCallbackStreamConsumer.java`（增加重试计数）
- 新增：`cloudflow-common-workflow-callback/.../listener/CallbackDeadLetterRecorder.java`
- 新增：`cloudflow-common-workflow-callback/.../controller/CallbackDlqAdminController.java`
- 新增表：`wf_callback_dead_letter`

**数据库变更**（per memory: 允许一次性数据库大改）：
```sql
-- 02.cloudflow-workflow.sql 追加
CREATE TABLE wf_callback_dead_letter (
  id            BIGINT PRIMARY KEY AUTO_INCREMENT,
  stream_key    VARCHAR(128) NOT NULL,
  biz_msg_key   VARCHAR(128) NOT NULL,
  process_instance_id VARCHAR(64),
  business_type VARCHAR(64),
  business_id   BIGINT,
  payload_json  JSON NOT NULL,
  retry_count   INT DEFAULT 0,
  last_error    TEXT,
  status        VARCHAR(16) DEFAULT 'PENDING',   -- PENDING / REPLAYED / IGNORED
  create_time   DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time   DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_stream_msg (stream_key, biz_msg_key)
);
```

**具体步骤**：
1. Redis Stream 改用 `XREADGROUP + NOACK 重投递` 实现自动重试，每条消息维护 `XPENDING` 计数
2. 在 Consumer 中读取 `pending.deliveryCount`，超过 `maxRetry=5` 时转 DLQ：
   - 写入 `wf_callback_dead_letter` 表
   - ACK + DEL 原 Stream 消息（避免无限重投占用 pending 列表）
3. 在工作流服务新增 `CallbackDlqAdminController`（路径 `/dlq`，`@SaCheckRole("admin")`）：
   - `GET /dlq/list` 分页查询
   - `POST /dlq/{id}/replay` 重新 `XADD` 到原 Stream
   - `POST /dlq/{id}/ignore` 标记 IGNORED
4. 前端在 WorkflowMonitor 增加 DLQ Tab（可直接复用 admin 路由）

**验收**：
- 模拟 Handler 永久失败，第 5 次重试后写入 dead_letter 表且原 Stream 被 ACK
- 调用 `/dlq/{id}/replay` 后业务表回写成功

---

## P0-3 流程-业务状态对账 Job

**目标**：每 5 分钟扫描"流程已完成但业务表状态未结束"的不一致。

**改造文件**：
- 新增：`cloudflow-service-workflow/.../job/ProcessBusinessReconcileJob.java`
- 新增：`cloudflow-service-workflow/.../mapper/ProcessBusinessReconcileMapper.xml`
- 新增表：`wf_reconcile_alert`

**步骤**：
1. SQL：左连接 `wf_process_instance` 与每个业务表（按 `process_instance_id` 关联，业务表 status 仍为审批中）
2. 命中后写入 `wf_reconcile_alert(process_instance_id, business_type, business_id, expected_status, actual_status)`
3. 通过 `cloudflow-common-job` 注册 `@Scheduled(cron="0 */5 * * * *")`，租户隔离用 `TenantContext.runWithTenant`
4. 命中即推送 SSE 通知给 admin 角色（复用 `cloudflow-common-sse` 模块，顺便激活 P3-2 的孤立模块）

**业务表清单**（按已实现 Handler 计 21+ 张表）：
- OA：oa_expense_claim、oa_payment_request、oa_purchase_request、oa_contract、oa_seal_application、oa_business_trip、oa_budget_plan、oa_budget_adjust、oa_project、oa_vehicle_usage、oa_knowledge、oa_license_renewal、oa_license_borrow、oa_seal_renewal
- CRM：crm_approval（payload_json 内 type）
- HR：hr_lifecycle_application、hr_time_request、hr_offer、hr_comp_change、hr_performance_objective、hr_recruitment_requisition

**验收**：人为造一条"流程已完成但业务表 status=PENDING"，5 分钟内 `wf_reconcile_alert` 出现该记录。

---

## P0-4 前端任务完成-业务页刷新协议

**目标**：审批后业务页自动刷新，无需手工 F5。

**改造文件**：
- 主改：[cloudflow-frontend/src/components/TaskHandleModal.tsx](cloudflow-frontend/src/components/TaskHandleModal.tsx)
- 主改：[cloudflow-frontend/src/hooks/useWebSocket.ts](cloudflow-frontend/src/hooks/useWebSocket.ts)
- 主改：[cloudflow-frontend/src/stores/workflowStore.ts](cloudflow-frontend/src/stores/workflowStore.ts)
- 后端：[cloudflow-service-workflow/.../event/OaWorkflowEventListener.java](cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/event/OaWorkflowEventListener.java) → 任务完成时推 WS 消息

**协议设计**：
```ts
// WS 消息体
{
  topic: "workflow.task.completed",
  payload: {
    processInstanceId: string,
    businessType: string,
    businessId: number,
    approvalResult: "APPROVED" | "REJECTED",
    completedAt: string
  }
}
```

**步骤**：
1. 后端：在 `WorkflowEventPublisher` 发布 `TaskCompletedEvent` 时同步通过 `/ws/notifications` 广播到当前租户的所有 admin/发起人
2. 前端 `useWebSocket` 新增 `subscribeTo(topic, handler)` 方法；统一在 `App.tsx` 注册 `workflow.task.completed`
3. 注册后调用 `queryClient.invalidateQueries(["business", businessType, businessId])` 触发对应业务列表/详情自动刷新
4. 所有业务页面在 `useQuery` 时 queryKey 统一约定 `["business", "<businessType>", id]`
5. `TaskHandleModal` 完成后 `mutate.onSuccess` 内：
   - `closeModal()`
   - `queryClient.invalidateQueries(["workflow", "todo"])`
   - 不再自己跳转业务页，由 WS 协议触发

**验收**：用户在 A 浏览器审批 ExpenseClaim，B 浏览器打开同一报销单，状态在 1-2 秒内自动变为"已通过"。

---

## P0-5 前端权限源单一化

**目标**：删除 `PermissionRouteGuard` 静态权限码列表，统一以 `/auth/info` 返回为唯一源。

**改造文件**：
- 主改：[cloudflow-frontend/src/components/PermissionRouteGuard.tsx](cloudflow-frontend/src/components/PermissionRouteGuard.tsx)
- 主改：[cloudflow-frontend/src/stores/authStore.ts](cloudflow-frontend/src/stores/authStore.ts)（或 context）
- 主改：[cloudflow-frontend/src/router.tsx](cloudflow-frontend/src/router.tsx)

**步骤**：
1. 登录成功后 `getUserInfo()` 返回的 `permissions: string[]`、`roles: string[]` 写入 `authStore`，并 persist 到 sessionStorage
2. `PermissionRouteGuard` 改为：
   ```tsx
   const { permissions } = useAuthStore();
   if (required && !permissions.includes(required)) return <Navigate to="/403" />;
   ```
3. 删除所有静态权限码常量（搜 `PERMISSION_` / `PERM_` 收口到 `authStore.ts`）
4. 路由声明改为 `{ path: '/workflow/design', element: <Guard required="workflow:design:edit">...</Guard> }`
5. **后端**配合：确保 `/auth/info` 返回的 `permissions` 包含菜单权限码 + 按钮权限码（已有，验证一遍）

**验收**：
- 后台改一个角色权限码立即生效（用户重新登录后），无需前端发版
- 通过删 `PermissionRouteGuard` 中所有写死的字符串验证

---

# 三、P1 实施步骤（统一与契约）

## P1-1 工作流回调接入模板 v2（双兼容）

**目标**：给新业务模块统一推荐 `ApprovalResultHandler` 派；HR 现状保留为"高级用法"。

**新增文件**：
- `cloudflow-common-workflow-callback/.../template/AbstractApprovalResultHandler.java`
- `cloudflow-common-workflow-callback/.../template/BusinessStatusUpdater.java`（业务状态字段约定接口）
- `cloudflow-common-workflow-callback/README.md`（接入指南）

**模板约定**：
```java
public abstract class AbstractApprovalResultHandler implements ApprovalResultHandler {
  // 子类只需实现两个方法：
  protected abstract String supportedBusinessType();
  protected abstract BusinessStatusUpdater updater();
  // 模板自动负责：日志、租户上下文、状态映射、异常向上抛
}
```

**步骤**：
1. 抽 `BusinessStatusUpdater`：`void update(Long bizId, String approvalResult, String instanceId, String comment);`
2. 实现 `AbstractApprovalResultHandler.handle(dto)`：调用 `updater.update`，租户上下文复用 HR 现有逻辑
3. CRM 现有 `AbstractCrmApprovalHandler` 继承新模板（保持向后兼容，让 CRM 子类无感知）
4. README 加 5 个步骤接入指南：新增 `XxxApprovalHandler extends AbstractApprovalResultHandler` → 注册 Bean → yaml 配 stream-key/group → DB 表加 `process_instance_id` 列 → 完事
5. HR 派保留为高级用法，README 标注"仅当业务表跨多实体路由时使用"

---

## P1-2 WorkflowController 加类级前缀迁移

**目标**：统一加 `@RequestMapping("/wf")` 前缀，消除裸路径耦合。**重大破坏性变更，需配合 Feign 客户端同 PR 修改**。

**改造文件**：
- 后端：[WorkflowController.java](cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/controller/WorkflowController.java) + 同包其他 Controller（如 WorkflowEnhanceController、WorkflowMonitorController 等已有前缀的忽略）
- 三个 Feign 客户端：[OA RemoteWorkflowService](cloudflow-backend/cloudflow-service-oa/src/main/java/com/cloudflow/oa/service/remote/RemoteWorkflowService.java)、[CRM RemoteWorkflowService](cloudflow-backend/cloudflow-service-crm/src/main/java/com/cloudflow/crm/service/remote/RemoteWorkflowService.java)、[HR WorkflowServiceClient](cloudflow-backend/cloudflow-service-hr/src/main/java/com/cloudflow/hr/client/WorkflowServiceClient.java)
- 网关：[config/cloudflow-gateway.yaml](config/cloudflow-gateway.yaml) 中 `/workflow/**` → 改 `StripPrefix=1` 后下游收到 `/wf/start`
- 前端：[cloudflow-frontend/src/services/api/workflow.ts](cloudflow-frontend/src/services/api/workflow.ts)

**步骤**：
1. 在 `WorkflowController` 类上加 `@RequestMapping("/wf")`
2. 同步把所有 Feign 客户端的 `@PostMapping("/start")` → `@PostMapping("/wf/start")`，`/complete`→`/wf/complete`，`/todo`、`/tasks/count`、`/tasks/statistics`、`/tasks/groups`、`/instance/{id}` 同理
3. 网关 `/workflow/start` 限流路由也改 `StripPrefix=1` 不变（前端继续调 `/workflow/wf/start`，下游收到 `/wf/start`）
4. 前端 `workflow.ts` 所有 `request.post("/workflow/start", ...)` → `/workflow/wf/start`
5. 删除 OA `RemoteWorkflowService.java:14-19` 的提醒注释

**回滚策略**：把改动单独成 PR，CI 通过后再合并。Feign 与 Controller 变更必须**同 commit**，避免半截状态。

---

## P1-3 OA/CRM Controller 鉴权注解扫描与补齐

**步骤**：
1. 写一个 `BatchAuthorityChecker.java` 单元测试（或 Maven plugin），扫描两个服务的所有 `@RestController`，检查每个 `@PostMapping/@GetMapping/@PutMapping/@DeleteMapping` 方法是否有 `@SaCheckPermission` 或 `@SaCheckLogin`
2. 输出缺失列表 → 人工补齐（GET 查询用 `@SaCheckPermission("oa:xxx:query")`，POST 用 `:add/:edit/:remove`）
3. 把扫描脚本加入 `mvn verify` phase（P3-4 把它升级为 CI 守护）

---

## P1-4 限流策略去重

**改造文件**：[config/cloudflow-gateway.yaml](config/cloudflow-gateway.yaml) 与 AuthController

**决策**：以网关为限流唯一源，删除 Controller 上 `@RateLimiter` 注解。

**步骤**：
1. 网关 yaml 把现有 login/register/captcha-slider/captcha-check 的 `RequestRateLimiter` 保留；并按需补 `/workflow/wf/start` 限流
2. 后端搜索 `@RateLimiter` 注解，删除所有 Controller 方法上的（保留 `cloudflow-common-ratelimiter` 模块本身不动，留作未来"细粒度业务层限流"用）

---

## P1-5 OA 工作台接入 HR 提醒数据源

**改造文件**：
- 主改：[WorkplaceServiceImpl.java:227-228](cloudflow-backend/cloudflow-service-oa/src/main/java/com/cloudflow/oa/service/impl/WorkplaceServiceImpl.java)
- 新增 Feign：`cloudflow-service-oa/.../service/remote/RemoteHrWorkplaceService.java`
- HR 新增内部接口：`cloudflow-service-hr/.../controller/HrInnerWorkplaceController.java`

**步骤**：
1. HR 新增 `@Inner` 控制器 `/inner/hr/workplace/reminders`，返回当前用户的入职/转正/合同到期/排班/请假提醒
2. OA 新增 Feign `getHrReminders(userId, tenantId)`
3. `WorkplaceServiceImpl` 在原 `markService("hr", true, "未接入HR提醒数据源")` 处改为真实拉取 + `mergeTodayItems(..., hrReminders)`
4. 接口失败时降级为空列表 + `markService("hr", false, ...)`

---

## P1-6 前端大 service 拆分

**目标**：`hr.ts` 1656 行、`workflow.ts` 1256 行、`crm.ts` 836 行拆为子模块。

**拆分约定**：以业务子领域为单位，每个文件 ≤ 400 行。

**HR 拆分**（`src/services/api/hr/`）：
```
hr/index.ts                 // 重导出，保持外部 import 不变
hr/employee.ts              // 员工档案
hr/attendance.ts            // 考勤
hr/leave.ts                 // 请假/加班/调休
hr/lifecycle.ts             // 入职/转正/调岗/离职
hr/compensation.ts          // 薪酬/社保/个税
hr/performance.ts           // 绩效（含 CRM 绩效聚合）
hr/recruitment.ts           // 招聘/Offer/面试
hr/_shared/types.ts         // 类型集中
hr/_shared/defaults.ts      // 业务默认值
hr/_shared/normalize.ts     // 字段兼容
```

**workflow 拆分**（`src/services/api/workflow/`）：
```
workflow/index.ts
workflow/instance.ts        // start/recall/invalidate/get
workflow/task.ts            // todo/done/complete/transfer
workflow/definition.ts      // 流程定义/模板/版本
workflow/monitor.ts         // 监控/统计/告警
workflow/_shared/extract.ts // extractList/extractPageMeta/normalize 收口
```

**CRM 拆分**（`src/services/api/crm/`）：customer / contact / opportunity / quote / receivable / renewal / ticket / dashboard

**步骤**：
1. 先建子目录与 index.ts，re-export 原符号
2. 移动函数（用编辑器搬运，不改实现）
3. 把 `extractList`/`extractPageMeta`/`normalize` 收到 `_shared/`，避免每个文件复制
4. 改完保证 `npm run type-check && npm run build` 通过

---

## P1-7 WebSocket 路由 StripPrefix

**改造**：[config/cloudflow-gateway.yaml:105-108](config/cloudflow-gateway.yaml)

**两个方案任选其一**：
- A：网关加 `StripPrefix=1`，**下游 mapping 改为 `/notifications`**（去掉 `/ws` 前缀）
- B：保持现状但**显式加注释**说明对齐，并在网关加 `Path=/ws/notifications` 精确匹配避免误路由

推荐 A：彻底对齐，避免歧义。

---

## P1-8 配置敏感信息密文化

**改造文件**：`config/*.yaml`、Nacos 命名空间

**步骤**：
1. Nacos 启用 KMS 集成（或本地 `jasypt-spring-boot` 占位 `ENC()` ）
2. `config/cloudflow-common.yaml:34`、`cloudflow-auth.yaml:17` 等所有明文密码占位 `${DB_PASSWORD}` 改为 `ENC(...)` 或 Vault 引用
3. README 增加"首次部署密钥下发流程"
4. CI 增加 `git diff` 守护，禁止 `password:` 后跟非 `ENC(` 的字符串

---

# 四、P2 实施步骤（治理与清理）

> **per memory: P2 不补测试**；以下步骤仅做代码治理，不要求新增 UT。

## P2-1 businessType 中心化注册表

**新增**：`cloudflow-common-workflow-callback/.../registry/BusinessTypeRegistry.java`

**步骤**：
1. 定义 `BusinessTypeRegistry`：`registerType(String code, String displayName, String ownerService)` + Spring `ApplicationListener<ContextRefreshedEvent>` 收集
2. 各业务模块的 `OaBusinessTypes`/`CrmBusinessTypes` 静态字段保留，但启动期被自动注册
3. 工作流服务暴露 `/wf/business-types` 接口，前端在发起流程时下拉显示

---

## P2-2 RemoteAuthController inner 白名单配置化

**改造**：`cloudflow-auth/.../controller/RemoteAuthController.java`

**步骤**：
1. `@Inner(allowedServices = "${cloudflow.inner.allowed-services:cloudflow-service-hr,cloudflow-service-workflow,cloudflow-service-oa,cloudflow-service-crm}")`
2. yaml `cloudflow-auth.yaml` 配 `cloudflow.inner.allowed-services` 为完整列表
3. 同步检查 OA/CRM/HR 是否也有类似硬编码白名单

---

## P2-3 FeignClient 显式 contextId

**改造**：所有 `@FeignClient` 增加 `contextId="..."`，命名规范 `<owner>-<target>`，例如：
- `@FeignClient(name="cloudflow-service-workflow", contextId="oa-workflow-client")`

避免 Bean 冲突，并使 actuator metrics 区分调用方。

---

## P2-4 网关 ComponentScan 排除规则收敛

**改造**：[GatewayApplication.java:23-38](cloudflow-backend/cloudflow-gateway/src/main/java/com/cloudflow/gateway/GatewayApplication.java)

**步骤**：
1. 把"排除规则"反转为"显式扫描"：`@SpringBootApplication(scanBasePackages={"com.cloudflow.gateway", "com.cloudflow.common.ratelimiter", "com.cloudflow.common.security.gateway"})`
2. 公共模块改成 `@AutoConfiguration` + `@ConditionalOnWebApplication(type=REACTIVE)`，让网关自动只装配响应式 Bean

---

## P2-5 前端版本历史三文件合并

**改造**：
- 删 [VersionHistory.improved.tsx](cloudflow-frontend/src/pages/VersionHistory.improved.tsx)
- 把 `.improved.tsx` 中已被采用的 UI/逻辑合并到 [VersionHistoryPage.tsx](cloudflow-frontend/src/pages/VersionHistoryPage.tsx)
- 决定保留 `VersionHistory.tsx` 还是 `VersionHistoryPage.tsx` 作为路由入口（看 `router.tsx` 中实际用的是谁）→ 留路由用的，删另一个

**步骤**：
1. `grep -r "VersionHistory" src/router.tsx` 找实际入口
2. 比对三个文件 diff，挑出最完整的实现作为基线
3. 删除剩余两个，搜索引用一并删

---

## P2-6 backend_data.ts 隔离

**改造**：[cloudflow-frontend/src/backend_data.ts](cloudflow-frontend/src/backend_data.ts)

**步骤**：
1. 新建目录 `src/pages/CodeGeneration/demo-data/`
2. 把 `backend_data.ts`（1900+ 行）移入，重命名为 `code-generation-samples.ts`
3. 引用方 `import { BACKEND_SOURCE } from '@/backend_data'` 改 `from '@/pages/CodeGeneration/demo-data/code-generation-samples'`
4. 在文件顶部加注释 `// 仅用于 CodeGeneration 页面演示，非真实业务数据`

---

## P2-7 前端表单组件三件套职责梳理

**改造**：
- [FormBuilder.tsx](cloudflow-frontend/src/components/FormBuilder.tsx)：拖拽设计器
- [FormRenderer.tsx](cloudflow-frontend/src/components/FormRenderer.tsx)：根据 schema 渲染填报
- [DynamicFormViewer.tsx](cloudflow-frontend/src/components/DynamicFormViewer.tsx)：只读查看

**步骤**：
1. 文件顶部统一注释职责单行说明
2. 新建 `src/components/form/README.md`，把三者的"什么时候用哪个"写清
3. 若发现 `DynamicFormViewer` 与 `FormRenderer` 80% 重复，合并为 `FormRenderer` 加 `readonly` prop（看具体重复度决定）

---

## P2-8 resilience4j 熔断阈值配置

**改造**：`config/cloudflow-common.yaml` 或各服务 yaml 增加：
```yaml
resilience4j:
  circuitbreaker:
    instances:
      workflow-client:
        slidingWindowSize: 20
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
  timelimiter:
    instances:
      workflow-client:
        timeoutDuration: 5s
```

各 Feign 上加 `@CircuitBreaker(name="workflow-client", fallbackMethod="xxxFallback")`。

---

# 五、P3 实施步骤（长期演进）

## P3-1 common 模块层级精简

**目标**：消除 common-core 与 common-sensitive 间接环，提炼 common-base。

**步骤**：
1. 新增 `cloudflow-common-base`，只放：常量、异常基类、`R<T>`、`PageQuery/PageResult`、`UserContext`、`TenantContext`
2. `cloudflow-common-core` 改为依赖 `common-base`，不再依赖 `common-sensitive`
3. `cloudflow-common-sensitive` 改为只依赖 `common-base`
4. `cloudflow-common-security` 拆出网关专用部分 `common-security-gateway`，业务专用 `common-security-server`
5. 全量编译 + 一键启动验证

---

## P3-2 SSE 模块去留决策

**当前**：`cloudflow-common-sse` 在前端无真实调用。

**决策树**：
- 如果 P0-3 对账 Job 决定走 SSE 推送 → 留下并完善
- 否则 → 删除模块，简化依赖

---

## P3-3 网关 /inner/** 黑名单兜底

**改造**：网关增加 GlobalFilter，命中 `/.+/inner/**` 直接 403。

```java
@Component
public class InnerPathBlockFilter implements GlobalFilter, Ordered {
  @Override public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
    String path = exchange.getRequest().getURI().getPath();
    if (path.contains("/inner/")) {
      exchange.getResponse().setStatusCode(HttpStatus.FORBIDDEN);
      return exchange.getResponse().setComplete();
    }
    return chain.filter(exchange);
  }
  @Override public int getOrder() { return -100; }
}
```

---

## P3-4 鉴权注解 CI 守护

**改造**：把 P1-3 的扫描脚本升级为 Maven enforcer rule 或 GitHub Action：
- 每次 PR 自动扫描新增 Controller 方法是否带 `@SaCheckPermission`
- 缺失即 fail build

---

## P3-5 孤儿接口梳理

**步骤**：
1. 后端：`SysPostController`、`SysConfigController` 在前端搜索调用点
2. 前端：`src/services/api/system.ts` 中声明但无业务页调用的函数标记 `@deprecated`
3. 二选一：补 UI 或删后端接口

---

## P3-6 WebSocket 鉴权改造

**目标**：避免 token 进 access log。

**方案**：
1. 网关握手时不校验 token
2. 客户端建立连接后发送第一帧 `{ "type": "auth", "token": "..." }`
3. 服务端 5 秒未收到 auth 帧则主动 close
4. 通过的连接绑定 userId/tenantId 上下文

---

# 六、推进规则与协作约定

1. **PR 颗粒**：P0/P1 每项独立 PR；P2 同主题可合并；P3 按主题拉长周期
2. **提交命名**：`feat(p0-1): workflow callback idempotent + dlq`、`refactor(p1-6): split hr.ts`
3. **数据库变更**：必须落在 `02.cloudflow-workflow.sql` 等结构脚本中（保持 README 提到的"01–04 结构脚本"约定），不要新建散落 SQL
4. **per memory**：不要求 P2 补测试；P0/P1 关键改动**手工回归脚本**写进 PR 描述（启动哪几个服务、调哪几个接口、看哪几个表）
5. **回滚预案**：每个 P0/P1 PR 描述底部必须有"回滚步骤"段落

---

# 七、风险与依赖关系图

```
P0-1 幂等 ──┐
P0-2 死信 ──┼─→ P1-1 公共 Handler 模板（必须先有 P0 才能写入模板）
P0-3 对账 ──┘                  │
                                ↓
                     P1-2 /wf 前缀迁移（独立可推）
                                │
P0-4 任务事件 ─→ P0-5 权限单源（前端两个独立任务，可并行）
                                │
                                ↓
P1-3/4/5/6/7/8（各自独立，可并行）
                                ↓
                       P2 / P3（长期）
```

依赖项：
- P0-1 与 P0-2 强相关，必须同一 PR 合并
- P1-1 必须在 P0-1/2 落地后启动，避免模板下沉错的设计
- P1-2 是破坏性变更，建议在 P0 全部上线后单独发版

---

**审计完成 → 实施开始：建议本周内启动 P0-1/2/3 联合 PR（工作流回调三件套+对账 Job+DB 变更）。**

---

# 八、P0 落地状态（2026-05-21 更新）

| 项 | 状态 | 落地与计划差异 |
|---|---|---|
| P0-1 幂等存储 | ✅ 完成 | 按计划落地。`CallbackIdempotentStore` 基于 Redis SETNX，TTL 默认 72h；`WorkflowApprovalCallbackStreamConsumer` 在入口 acquire、异常时 release 并不 ACK 走 pending 重投递；`ApprovalResultStreamPublisher` 将 `bizMsgKey = processInstanceId + approvalResult` 写入消息体 |
| P0-2 死信队列 | ✅ 完成 | 落地路径与计划略不同：DLQ 采用"二级 Redis Stream + DB 表"组合。失败超过 maxRetry 后由 `DefaultDeadLetterHandler` XADD 到 `wf:dlq:callback`；`CallbackDlqStreamConsumer` 消费该 Stream 写入 `wf_callback_dead_letter` 表；管理接口 `CallbackDlqAdminController` 提供 `/wf/callback/dlq/list|replay|ignore`，`@SaCheckRole("admin")`；前端 DLQ Tab 未做（计划项保留） |
| P0-3 对账 Job | ✅ 完成 | `ProcessBusinessReconcileJob` 5 分钟 cron 通过 `@DistributedJob` 分布式调度；`WfReconcileAlertMapper.xml` 用 UNION ALL 关联 21 张业务表；落表 `wf_reconcile_alert`。**未启用 SSE 推送 admin**（决策推迟到 P3-2，与孤立 SSE 模块去留绑定） |
| P0-4 任务完成-业务刷新协议 | ✅ 完成 | 后端钩子点改为 `ApprovalResultStreamPublisher`（而非计划的 `OaWorkflowEventListener`），原因：该类已在解析 businessType/businessId 并能取到 `instance.startUserId`，复用更精准。`NotificationWebSocketHandler` 新增 `sendTopicMessage(userId, topic, payload)`，topic = `workflow.task.completed`。前端不复用 `useWebSocket.subscribeTo`，改为 `useWorkflowRefresh(onRefresh, businessType?)` Hook 监听 `window` 上的 `workflow-task-completed` CustomEvent；businessType 通过 `normalize()`（小写+短横线转下划线）兼容前后端大小写差异。LeaveApplicationPage / OvertimeApplicationPage / BusinessTripPage 已接入 |
| P0-5 前端权限源单一化 | ✅ 完成 | 计划描述的"静态权限码常量"在仓库实际不存在（`grep PERMISSION_/PERM_` 无命中）。**实际问题是 `useAppStore` 维护了一份 AuthState slice + 一个 `usePermission` hook 读 store 空数组，构成隐性双轨**。落地动作：①删除 `src/hooks/usePermission.ts`；②`PermissionGuard` / `PermissionButton` / `PermissionRouteGuard` / `RouteGuard` 全部切到 `useAuth()`（`/auth/info` 返回的 `user.permissions`）；③**物理删除** `workflowStore.ts` 中的 AuthState 段（接口/初始值/Action 全部移除），不保留废弃占位；④`types/props.ts` 清理过时的 `PermissionGuardProps` |

**未完成的计划副项**（不阻塞 P0 收尾，登记到 P1 继续推进）：
- P0-2：前端 DLQ Tab UI（WorkflowMonitor 内）
- P0-3：对账告警 SSE 推送 admin（与 P3-2 SSE 模块决策合并）
- P0-4：`useWebSocket.subscribeTo` 通用订阅机制（当前用 CustomEvent 桥接，已能用但不是协议级 API）

**type-check 状态**：本会话期间因 Bash 分类器不可用未跑成；代码已静态自检（grep 全量交叉验证）。建议人工执行 `cd cloudflow-frontend && npx tsc --noEmit -p tsconfig.json` 把关。
