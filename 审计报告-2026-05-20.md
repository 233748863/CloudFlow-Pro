# CloudFlow Pro 前后端综合审计报告

- 审计日期：2026-05-20
- 审计范围：`cloudflow-backend`(gateway / auth / workflow / oa / crm / hr + 16 个 common 子模块) + `cloudflow-frontend`(React 19)
- 审计排除：`cloudflow-frontend/src/mobile/`、`cloudflow-frontend-vue/`
- 审计立项：业务可拓展性 + 工作流跨模块联动合理性
- 关联历史文档：[后端审计报告.md](./后端审计报告.md)、[P0修复方案.md](./P0修复方案.md)、[P1修复方案.md](./P1修复方案.md)、[P2修复方案.md](./P2修复方案.md)、[P3修复方案.md](./P3修复方案.md)
- 本次行动计划：[P0-P3实施计划-2026-05-20.md](./P0-P3实施计划-2026-05-20.md)

---

## 一、整体结论

**架构主线合理，最大风险不是"无法交互"而是"交互方式形成两套风格 + 关键链路缺幂等"。**

| 维度 | 评分 | 说明 |
|---|---|---|
| 后端结构 | 7.5 / 10 | 网关-认证-业务-公共四层清晰；服务边界与 DB 脚本一一对应 |
| 前端结构 | 7.0 / 10 | 路由 / Store / API 拦截器封装规范；问题集中在 service 文件膨胀 |
| 工作流跨模块交互 | 7.0 / 10 | Stream 解耦方向正确，但回调消费缺幂等/死信/对账；接入模式两派并存 |

整体风险定级：**🟡 中（含 2 项 🔴 高优先）**

---

## 二、当前架构主线（已成立的部分）

1. **工作流即流程引擎**：[WorkflowController.java:29](cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/controller/WorkflowController.java) 暴露 `/start`、`/complete`、`/todo`、`/tasks/count` 等公共接口；CRM / HR / OA 通过 OpenFeign 接入
   - [cloudflow-service-crm/.../RemoteWorkflowService.java:9](cloudflow-backend/cloudflow-service-crm/src/main/java/com/cloudflow/crm/service/remote/RemoteWorkflowService.java)
   - [cloudflow-service-hr/.../WorkflowServiceClient.java:22](cloudflow-backend/cloudflow-service-hr/src/main/java/com/cloudflow/hr/client/WorkflowServiceClient.java)
   - [cloudflow-service-oa/.../RemoteWorkflowService.java:20](cloudflow-backend/cloudflow-service-oa/src/main/java/com/cloudflow/oa/service/remote/RemoteWorkflowService.java)

2. **审批结果回流采用 Redis Stream 异步解耦**（而非业务硬回调）
   - 发布端：[ApprovalResultStreamPublisher.java:25](cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/event/ApprovalResultStreamPublisher.java)
   - 公共消费基础设施：[WorkflowCallbackAutoConfiguration.java:35](cloudflow-backend/cloudflow-common/cloudflow-common-workflow-callback/src/main/java/com/cloudflow/common/workflow/callback/config/WorkflowCallbackAutoConfiguration.java) + [WorkflowApprovalCallbackStreamConsumer.java:21](cloudflow-backend/cloudflow-common/cloudflow-common-workflow-callback/src/main/java/com/cloudflow/common/workflow/callback/listener/WorkflowApprovalCallbackStreamConsumer.java)

3. **业务发起流程的契约约定一致**：`processDefinitionKey + businessKey + variables(businessType, businessId, businessNo, callbackStreamKey)`
   - HR：[WorkflowServiceClient.java:42-57](cloudflow-backend/cloudflow-service-hr/src/main/java/com/cloudflow/hr/client/WorkflowServiceClient.java)
   - CRM：[CrmApprovalServiceImpl.java:191](cloudflow-backend/cloudflow-service-crm/src/main/java/com/cloudflow/crm/service/impl/CrmApprovalServiceImpl.java)

4. **OA 工作台在后端做聚合**，前端只调 `/oa/workplace/*`
   - 后端：[WorkplaceServiceImpl.java:92, :217, :452](cloudflow-backend/cloudflow-service-oa/src/main/java/com/cloudflow/oa/service/impl/WorkplaceServiceImpl.java)
   - 前端：[workplace.ts:107](cloudflow-frontend/src/services/api/workplace.ts)（127 行,干净）

5. **网关路由前缀清晰**：`/auth /workflow /oa /crm /hr` 各自 `StripPrefix=1`，鉴权与限流分层

---

## 三、关键问题清单（按风险级别）

### 🔴 P0 级（高风险，必须立即处理）

| ID | 问题 | 证据 | 影响 |
|---|---|---|---|
| **P0-1** | **工作流回调消费缺幂等**：[WorkflowApprovalCallbackStreamConsumer.java:45-54](cloudflow-backend/cloudflow-common/cloudflow-common-workflow-callback/src/main/java/com/cloudflow/common/workflow/callback/listener/WorkflowApprovalCallbackStreamConsumer.java) 是"先调 Handler 再 ACK+DEL"，**没有 msgId 去重**。Handler 半执行抛异常时整段 catch 仅 log，消息被丢弃 | 同上 | 重复消费会覆写业务状态；异常消息静默丢失 |
| **P0-2** | **回调失败无死信/重试**：异常分支只 log 不进 DLQ，无人工补单入口 | 同上 | 流程已结束但业务表未回写时，无法发现也无法补救 |
| **P0-3** | **流程-业务状态无对账机制**：没有定时 Job 比对 `wf_process_instance.status` 与业务表 `status` | 无相关 Job | 回调链路任一环出错都不会有人发现 |
| **P0-4** | **前端工作流任务完成后无业务页回调协议**：`TaskHandleModal` 完成审批不通知发起页 | [cloudflow-frontend/src/components/TaskHandleModal.tsx](cloudflow-frontend/src/components/TaskHandleModal.tsx) | 用户审批后业务页要刷新才显示新状态，体验割裂 |
| **P0-5** | **前端权限源双轨**：`PermissionRouteGuard` 维护静态权限码，与后端 `sys_menu.perms` / `/auth/info` 返回的权限码并存 | [cloudflow-frontend/src/components/PermissionRouteGuard.tsx](cloudflow-frontend/src/components/PermissionRouteGuard.tsx) | 权限漂移、上线后菜单可见性与功能可执行性不一致 |

### 🟡 P1 级（中风险，近期处理）

| ID | 问题 | 证据 |
|---|---|---|
| **P1-1** | **回调接入两派并存**：CRM/OA 走 `ApprovalResultHandler` 公共分发；HR 自实现 `WorkflowCallbackService` 替换默认 Dispatcher。新模块缺统一接入模板 | [AbstractCrmApprovalHandler.java:23](cloudflow-backend/cloudflow-service-crm/src/main/java/com/cloudflow/crm/service/impl/AbstractCrmApprovalHandler.java) vs [HrWorkflowCallbackServiceImpl.java:26-28](cloudflow-backend/cloudflow-service-hr/src/main/java/com/cloudflow/hr/service/impl/HrWorkflowCallbackServiceImpl.java) |
| **P1-2** | **WorkflowController 无类级 @RequestMapping**：所有 Feign 依赖裸路径（`/start`、`/complete`），耦合脆 | [WorkflowController.java:29](cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/controller/WorkflowController.java)，OA 的 [RemoteWorkflowService.java:14-19](cloudflow-backend/cloudflow-service-oa/src/main/java/com/cloudflow/oa/service/remote/RemoteWorkflowService.java) 注释已提醒 |
| **P1-3** | **OA/CRM 部分 Controller 未全部加 `@SaCheckPermission`**：鉴权覆盖不完全 | OA / CRM Controller 目录扫描 |
| **P1-4** | **限流策略双写**：网关 yaml 与 `cloudflow-common-ratelimiter` 注解同时存在 | [cloudflow-gateway.yaml:23-63](config/cloudflow-gateway.yaml) + AuthController |
| **P1-5** | **OA 工作台 HR 数据源未接入**：硬编码"未接入HR提醒数据源" | [WorkplaceServiceImpl.java:227-228](cloudflow-backend/cloudflow-service-oa/src/main/java/com/cloudflow/oa/service/impl/WorkplaceServiceImpl.java) |
| **P1-6** | **前端大 service 文件膨胀**：`hr.ts` 1656 行、`workflow.ts` 1256 行、`crm.ts` 836 行；类型/字段兼容/默认值/缓存/跨模块聚合混合 | [cloudflow-frontend/src/services/api/hr.ts:1016, :1145, :1616](cloudflow-frontend/src/services/api/hr.ts)、[crm.ts:456+](cloudflow-frontend/src/services/api/crm.ts)、[workflow.ts:34, :53, :115](cloudflow-frontend/src/services/api/workflow.ts) |
| **P1-7** | **WebSocket 路由未配 StripPrefix**：`/ws/**` 与下游 mapping 偶然对齐 | [cloudflow-gateway.yaml:105-108](config/cloudflow-gateway.yaml) |
| **P1-8** | **配置密码占位无默认值/无密文方案**：`${DB_PASSWORD}` 直接展开 | [config/cloudflow-common.yaml:34](config/cloudflow-common.yaml)、[config/cloudflow-auth.yaml:17](config/cloudflow-auth.yaml) |

### 🟠 P2 级（中低风险，可纳入迭代）

| ID | 问题 |
|---|---|
| P2-1 | businessType 字符串约定松散（HR `normalizeBusinessType`、OA/CRM 各自常量），无中心化注册表 |
| P2-2 | RemoteAuthController inner 白名单硬编码（应配置化） |
| P2-3 | 多数 FeignClient 未指定 `contextId`，潜在 Bean 冲突 |
| P2-4 | 网关 GatewayApplication `ComponentScan` 排除规则过多，新功能易被误排 |
| P2-5 | 前端版本历史三文件并存：`VersionHistory.tsx` / `VersionHistory.improved.tsx` / `VersionHistoryPage.tsx` |
| P2-6 | 前端 `backend_data.ts`（1900+ 行）模拟 SQL/Java 演示数据混在 src 根目录 |
| P2-7 | 前端表单组件三件套职责不清：`FormBuilder` / `FormRenderer` / `DynamicFormViewer` |
| P2-8 | resilience4j 熔断阈值未在 yaml 配置（仅引依赖） |

### 🟢 P3 级（长期演进）

| ID | 问题 |
|---|---|
| P3-1 | `cloudflow-common-*` 模块层级偏深，common-core 依赖 common-sensitive、common-security 依赖 common-redis；建议提炼 common-base 抽底 |
| P3-2 | SSE 模块孤立（`cloudflow-common-sse` 前端无真实调用）：决定下线或纳入 OA workplace |
| P3-3 | inner controller 在网关增加 `/inner/**` 黑名单兜底 |
| P3-4 | OA/CRM 权限注解扫描脚本纳入 CI 检查 |
| P3-5 | 孤儿接口梳理：`SysPostController` / `SysConfigController` 前端只声明未调用 |
| P3-6 | WebSocket 鉴权改为握手后首帧验证或 Cookie，避免 token 进 access log |

---

## 四、与第三方审计意见的差异校准

第三方审计意见准确点：
- ✅ "两套回调风格并存"判断成立（OA+CRM 公共 handler 派、HR 自定义 dispatcher 派）
- ✅ "WorkflowController 无类级前缀导致 Feign 裸路径耦合"成立
- ✅ "前端大 service 文件膨胀"成立（hr.ts 1656 行已核实）
- ✅ "OA 后端聚合 + 前端只调 `/oa/workplace/*`" 设计合理

需校准点：
- ⚠️ 跨模块交互能力评分由 8.0 → 7.0–7.5（接入两派 + 回调链路缺幂等扣分）
- ⚠️ 前端结构评分由 6.5 → 7.0（路由/Store/拦截器封装实际较规范）
- ⚠️ "新模块接入会面临选择"应明确表述为：**OA+CRM 是同一派**，HR 是另一派；新模块若不立模板会出现第三种风格

第三方审计遗漏点（已补入本报告）：
- 回调消费缺幂等（P0-1）
- 回调失败无死信/重试（P0-2）
- 流程-业务状态无对账（P0-3）
- 前端任务完成无业务页刷新协议（P0-4）
- 前端权限双源（P0-5）

---

## 五、改进路线总览

按"先稳定 → 再统一 → 再优化"三阶段推进：

```
P0 (1-2 周)：稳定回调链路、稳定前后端权限源、打通任务完成-业务刷新协议
   └─→ P1 (2-4 周)：统一回调接入模板、补齐鉴权与限流去重、拆分前端大 service
        └─→ P2 (4-8 周)：常量中心化、文件清理、配置化白名单
             └─→ P3 (持续)：common 模块重构、SSE 决策、CI 守护
```

详细实施步骤见 [P0-P3实施计划-2026-05-20.md](./P0-P3实施计划-2026-05-20.md)。

---

## 六、关键提示

1. **务必先做 P0 再做 P1**：在统一接入模板（P1-1）之前先把幂等/死信/对账（P0-1/2/3）写进模板内。否则模板再统一，丢消息照样丢。
2. **P0-5 权限源统一是单向迁移**：以 `/auth/info` 返回的权限码为唯一源，前端守卫只读不维护；不应保留兼容期。
3. **P1-1 接入模板需双兼容**：既能继续支持 HR 的"自定义 Dispatcher"逃生口，也要给新模块默认走公共 Handler 派；改造时不强行迁移 HR。
4. **per memory: P2 不纳入补测试**：本报告 P2/P3 实施计划同样不要求额外补测试覆盖，仅要求关键变更点保留可手工回归的 README 步骤。

---

## 七、P0 完成回执（2026-05-21）

| ID | 状态 | 关键产物 |
|---|---|---|
| P0-1 | ✅ | `CallbackIdempotentStore`（Redis SETNX, TTL 72h）+ `WorkflowApprovalCallbackStreamConsumer` 入口幂等 + `ApprovalResultStreamPublisher` 注入 `bizMsgKey` |
| P0-2 | ✅ | DLQ 双层（`wf:dlq:callback` Stream + `wf_callback_dead_letter` 表）+ `CallbackDlqAdminController` `/wf/callback/dlq/list\|replay\|ignore`（`@SaCheckRole("admin")`）；前端 DLQ Tab 后置 |
| P0-3 | ✅ | `ProcessBusinessReconcileJob`（5min cron + `@DistributedJob`）+ `WfReconcileAlertMapper.xml`（UNION ALL 21 业务表）+ `wf_reconcile_alert` 落表；SSE 通知与 P3-2 决策合并后置 |
| P0-4 | ✅ | `NotificationWebSocketHandler.sendTopicMessage` topic 协议 + `ApprovalResultStreamPublisher.pushTaskCompletedWs` + 前端 `useWorkflowRefresh(businessType?)` Hook 接入 Leave/Overtime/BusinessTrip 三页 |
| P0-5 | ✅ | 删除 `usePermission` hook；`PermissionGuard`/`PermissionButton`/`PermissionRouteGuard`/`RouteGuard` 统一走 `useAuth()`；`workflowStore.ts` AuthState 段物理删除；`types/props.ts` 清理过时 `PermissionGuardProps` |

> 实际落地路径与原计划的偏差见 [P0-P3实施计划-2026-05-20.md 八、P0 落地状态](./P0-P3实施计划-2026-05-20.md#八p0-落地状态2026-05-21-更新)。

---

## 八、P1 批次 A 完成回执（2026-05-21）

| ID | 状态 | 关键产物 |
|---|---|---|
| P1-1 | ✅ | [cloudflow-common-workflow-callback/README.md](cloudflow-backend/cloudflow-common/cloudflow-common-workflow-callback/README.md) 接入指南（默认 Handler 派 + HR 自定义 Dispatcher 派双兼容判定） |
| P1-2 | ✅ | `WorkflowController @RequestMapping("/wf")`；网关 `Path=/workflow/** + StripPrefix=1` 不动；前端 `workflow.ts`（27 处）+ `syncManager.ts` + `MobileWorkflowMonitor.tsx` + `VersionHistoryPage.tsx` 路径加 `/wf`；OA/CRM/HR 3 个 Feign 客户端共 10 处加 `/wf` 前缀（`/enhance/instance/invalidate` 保留） |
| P1-4 | ✅ | 网关新增 `auth-tenant-options` 路由 + `RequestRateLimiter`（1/s, 桶 10, IP key）；`AuthController.tenantOptions` 删除 `@RateLimiter` 注解；`cloudflow-common-ratelimiter` 模块保留供 `ErrorReportController` 等业务侧细粒度限流 |
| P1-7 | ✅ | 网关 `cloudflow-ws` 路由显式 `StripPrefix=0` + 注释对齐 `WebSocketConfig.registerWebSocketHandlers` 的 `/ws/notifications` mapping |

批次 B（P1-3/5/8）、批次 C（P1-6 拆分）按 [P0-P3实施计划-2026-05-20.md 九、P1 落地状态](./P0-P3实施计划-2026-05-20.md#九p1-落地状态2026-05-21-批次-a) 计划顺延。
