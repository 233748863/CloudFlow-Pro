<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.hr.mapper.SchedulePlanMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.cloudflow.hr.domain.entity.SchedulePlan">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="plan_name" property="planName"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="shift_id" property="shiftId"/>
        <result column="schedule_date" property="scheduleDate"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
    </resultMap>

    <!-- 查询指定日期范围内的排班计划 -->
    <select id="selectByDateRange" resultMap="BaseResultMap">
        SELECT *
        FROM hr_schedule_plan
        WHERE tenant_id = #{tenantId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
          AND schedule_date BETWEEN #{startDate} AND #{endDate}
          AND status != 'CANCELLED'
        ORDER BY schedule_date ASC
    </select>

    <!-- 查询指定员工在指定日期的排班计划 -->
    <select id="selectByEmployeeAndDate" resultMap="BaseResultMap">
        SELECT *
        FROM hr_schedule_plan
        WHERE tenant_id = #{tenantId}
          AND target_type = 'EMPLOYEE'
          AND target_id = #{employeeId}
          AND schedule_date = #{scheduleDate}
          AND status != 'CANCELLED'
        LIMIT 1
    </select>

    <!-- 批量插入排班计划 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO hr_schedule_plan (
            tenant_id, plan_name, target_type, target_id, shift_id,
            schedule_date, status, create_time, update_time, create_by, update_by
        )
        VALUES
        <foreach collection="plans" item="plan" separator=",">
            (
                #{plan.tenantId}, #{plan.planName}, #{plan.targetType}, #{plan.targetId}, #{plan.shiftId},
                #{plan.scheduleDate}, #{plan.status}, #{plan.createTime}, #{plan.updateTime}, #{plan.createBy}, #{plan.updateBy}
            )
        </foreach>
    </insert>

</mapper>
