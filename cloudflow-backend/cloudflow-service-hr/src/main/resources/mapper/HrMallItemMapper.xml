<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.hr.mapper.HrMallItemMapper">

    <update id="deductStock">
        UPDATE hr_mall_item
        SET stock = stock - #{quantity},
            sales_count = sales_count + #{quantity},
            update_time = NOW()
        WHERE id = #{itemId}
          AND tenant_id = #{tenantId}
          AND stock &gt;= #{quantity}
          AND deleted = 0
          AND status = 'ON_SHELF'
    </update>

    <update id="restoreStock">
        UPDATE hr_mall_item
        SET stock = stock + #{quantity},
            sales_count = sales_count - #{quantity},
            update_time = NOW()
        WHERE id = #{itemId}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </update>

</mapper>
