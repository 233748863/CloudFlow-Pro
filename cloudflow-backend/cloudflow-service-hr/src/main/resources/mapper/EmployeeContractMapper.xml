<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.hr.mapper.EmployeeContractMapper">

    <sql id="BaseContractColumns">
        id,
        tenant_id,
        employee_id,
        contract_type,
        contract_no,
        sign_date,
        start_date,
        end_date,
        duration,
        status,
        create_time,
        update_time,
        create_by,
        update_by,
        deleted
    </sql>

    <select id="selectContractsByEmployeeId" resultType="com.cloudflow.hr.domain.entity.EmployeeContract">
        SELECT
        <include refid="BaseContractColumns"/>
        FROM hr_employee_contract
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
          AND employee_id = #{employeeId}
        ORDER BY start_date DESC
    </select>

    <select id="selectContractById" resultType="com.cloudflow.hr.domain.entity.EmployeeContract">
        SELECT
        <include refid="BaseContractColumns"/>
        FROM hr_employee_contract
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
          AND id = #{id}
        LIMIT 1
    </select>

    <select id="selectExpiringContracts" resultType="com.cloudflow.hr.domain.entity.EmployeeContract">
        SELECT
        <include refid="BaseContractColumns"/>
        FROM hr_employee_contract
        WHERE deleted = 0
          AND status = 'ACTIVE'
          AND end_date BETWEEN #{currentDate} AND DATE_ADD(#{currentDate}, INTERVAL #{days} DAY)
        ORDER BY end_date ASC
    </select>

</mapper>
