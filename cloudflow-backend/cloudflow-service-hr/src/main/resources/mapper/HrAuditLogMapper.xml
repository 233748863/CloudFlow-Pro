<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.hr.mapper.HrAuditLogMapper">

    <insert id="insertLog">
        INSERT INTO hr_audit_log (
            tenant_id, business_domain, business_id, operation_type,
            operator_id, operator_name, before_data, after_data
        ) VALUES (
            #{tenantId}, #{businessDomain}, #{businessId}, #{operationType},
            #{operatorId}, #{operatorName}, #{beforeData}, #{afterData}
        )
    </insert>

</mapper>
