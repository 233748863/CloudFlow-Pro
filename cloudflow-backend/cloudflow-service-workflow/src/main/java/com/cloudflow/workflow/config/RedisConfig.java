package com.cloudflow.workflow.config;

/**
 * Redis 配置已由 cloudflow-common 模块的 com.cloudflow.common.config.RedisConfig 提供。
 * 此文件已清空以避免 Bean 名称冲突 (ConflictingBeanDefinitionException)。
 * 
 * 如需自定义 workflow 模块的 Redis 配置，请使用不同的类名或 Bean 名称。
 */
