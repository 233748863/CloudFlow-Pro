<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.workflow.mapper.PerformanceStatsMapper">

    <resultMap id="PerformanceStatsResult" type="com.cloudflow.workflow.domain.monitor.PerformanceStats">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="statDate" column="stat_date"/>
        <result property="processDefKey" column="process_def_key"/>
        <result property="processDefName" column="process_def_name"/>
        <result property="processName" column="process_def_name"/>
        <result property="totalCount" column="total_count"/>
        <result property="completedCount" column="completed_count"/>
        <result property="failedCount" column="failed_count"/>
        <result property="timeoutCount" column="timeout_count"/>
        <result property="anomalyCount" column="anomaly_count"/>
        <result property="avgDuration" column="avg_duration_ms"/>
        <result property="minDuration" column="min_duration_ms"/>
        <result property="maxDuration" column="max_duration_ms"/>
        <result property="avgDurationMs" column="avg_duration_ms"/>
        <result property="minDurationMs" column="min_duration_ms"/>
        <result property="maxDurationMs" column="max_duration_ms"/>
        <result property="successRate" column="success_rate"/>
        <result property="timeoutRate" column="timeout_rate"/>
        <result property="anomalyRate" column="anomaly_rate"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 查询性能统计数据 -->
    <select id="selectPerformanceStats" resultMap="PerformanceStatsResult">
        SELECT 
            id,
            stat_date,
            process_def_key,
            process_def_name,
            total_count,
            completed_count,
            failed_count,
            timeout_count,
            anomaly_count,
            avg_duration as avg_duration_ms,
            min_duration as min_duration_ms,
            max_duration as max_duration_ms,
            create_time,
            update_time,
            CASE WHEN total_count > 0 THEN ROUND((completed_count * 100.0 / total_count), 2) ELSE 0 END as success_rate,
            CASE WHEN total_count > 0 THEN ROUND((timeout_count * 100.0 / total_count), 2) ELSE 0 END as timeout_rate,
            CASE WHEN total_count > 0 THEN ROUND((anomaly_count * 100.0 / total_count), 2) ELSE 0 END as anomaly_rate
        FROM wf_performance_stats
        <where>
            <if test="startDate != null">
                AND stat_date >= #{startDate}
            </if>
            <if test="endDate != null">
                AND stat_date &lt;= #{endDate}
            </if>
            <if test="processDefKey != null and processDefKey != ''">
                AND process_def_key = #{processDefKey}
            </if>
        </where>
        ORDER BY stat_date DESC
    </select>

</mapper>
