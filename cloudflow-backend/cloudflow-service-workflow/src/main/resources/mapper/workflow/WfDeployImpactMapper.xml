<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.workflow.mapper.WfDeployImpactMapper">

    <select id="listByDeployId" resultType="com.cloudflow.workflow.domain.WfDeployImpact">
        SELECT *
        FROM wf_deploy_impact
        WHERE deploy_id = #{deployId}
        ORDER BY impact_level DESC
    </select>

    <select id="listHighImpactByDeployId" resultType="com.cloudflow.workflow.domain.WfDeployImpact">
        SELECT *
        FROM wf_deploy_impact
        WHERE deploy_id = #{deployId}
          AND impact_level IN ('HIGH', 'CRITICAL')
    </select>

</mapper>
