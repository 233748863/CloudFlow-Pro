<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.workflow.mapper.WfProcessVersionSnapshotMapper">

    <select id="listByProcessDefId" resultType="com.cloudflow.workflow.domain.WfProcessVersionSnapshot">
        SELECT *
        FROM wf_process_version_snapshot
        WHERE process_def_id = #{processDefId}
        ORDER BY version DESC
    </select>

    <select id="listByProcessKey" resultType="com.cloudflow.workflow.domain.WfProcessVersionSnapshot">
        SELECT *
        FROM wf_process_version_snapshot
        WHERE process_key = #{processKey}
        ORDER BY version DESC, id DESC
    </select>

    <select id="selectByProcessDefIdAndVersion" resultType="com.cloudflow.workflow.domain.WfProcessVersionSnapshot">
        SELECT *
        FROM wf_process_version_snapshot
        WHERE process_def_id = #{processDefId}
          AND version = #{version}
    </select>

    <select id="selectByProcessKeyAndVersion" resultType="com.cloudflow.workflow.domain.WfProcessVersionSnapshot">
        SELECT *
        FROM wf_process_version_snapshot
        WHERE process_key = #{processKey}
          AND version = #{version}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectByDeployId" resultType="com.cloudflow.workflow.domain.WfProcessVersionSnapshot">
        SELECT s.*
        FROM wf_process_version_snapshot s
        INNER JOIN wf_deploy_record d
            ON d.process_def_id = s.process_def_id
           AND d.version = s.version
        WHERE d.id = #{deployId}
        ORDER BY s.id DESC
        LIMIT 1
    </select>

    <select id="selectLatestByProcessDefId" resultType="com.cloudflow.workflow.domain.WfProcessVersionSnapshot">
        SELECT *
        FROM wf_process_version_snapshot
        WHERE process_def_id = #{processDefId}
        ORDER BY version DESC
        LIMIT 1
    </select>

</mapper>
