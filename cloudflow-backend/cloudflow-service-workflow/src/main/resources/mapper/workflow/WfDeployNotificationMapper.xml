<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.workflow.mapper.WfDeployNotificationMapper">

    <select id="listByDeployId" resultType="com.cloudflow.workflow.domain.WfDeployNotification">
        SELECT *
        FROM wf_deploy_notification
        WHERE deploy_id = #{deployId}
        ORDER BY created_time DESC
    </select>

    <select id="listByStatus" resultType="com.cloudflow.workflow.domain.WfDeployNotification">
        SELECT *
        FROM wf_deploy_notification
        WHERE send_status = #{status}
        ORDER BY created_time ASC
    </select>

    <update id="updateSendStatus">
        UPDATE wf_deploy_notification
        SET send_status = #{status},
            send_time = NOW(),
            error_message = #{errorMessage}
        WHERE id = #{id}
    </update>

</mapper>
