<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.oa.mapper.OaSealMapper">

    <select id="getTodayApplicationMaxSeq" resultType="java.lang.Integer">
        SELECT MAX(CAST(SUBSTRING(application_no, 11) AS UNSIGNED))
        FROM oa_seal_application
        WHERE application_no LIKE CONCAT('YY', DATE_FORMAT(NOW(), '%Y%m%d'), '%')
    </select>

</mapper>
