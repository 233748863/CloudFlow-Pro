<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudflow.oa.mapper.ContactMapper">

    <sql id="contactFromWhere">
        FROM sys_user u
        LEFT JOIN sys_dept d
            ON u.dept_id = d.dept_id
        LEFT JOIN sys_user_post up
            ON u.user_id = up.user_id
        LEFT JOIN sys_post p
            ON up.post_id = p.post_id
        WHERE u.del_flag = '0'
          AND u.status = '0'
          <if test="keyword != null and keyword != ''">
              AND (
                  u.nick_name LIKE CONCAT('%', #{keyword}, '%')
                  OR u.user_name LIKE CONCAT('%', #{keyword}, '%')
                  OR u.phonenumber LIKE CONCAT('%', #{keyword}, '%')
                  OR u.email LIKE CONCAT('%', #{keyword}, '%')
                  OR d.dept_name LIKE CONCAT('%', #{keyword}, '%')
                  OR p.post_name LIKE CONCAT('%', #{keyword}, '%')
              )
          </if>
          <if test="deptId != null">
              AND (
                  u.dept_id = #{deptId}
                  OR FIND_IN_SET(#{deptId}, d.ancestors) > 0
              )
          </if>
    </sql>

    <select id="countContacts" resultType="int">
        SELECT COUNT(DISTINCT u.user_id)
        <include refid="contactFromWhere" />
    </select>

    <select id="selectContacts" resultType="java.util.HashMap">
        SELECT
            u.user_id,
            u.nick_name,
            u.user_name,
            u.email,
            u.phonenumber,
            u.sex,
            u.avatar,
            u.status,
            d.dept_id,
            d.dept_name,
            GROUP_CONCAT(DISTINCT p.post_name ORDER BY p.post_sort SEPARATOR '、') AS post_name
        <include refid="contactFromWhere" />
        GROUP BY
            u.user_id,
            u.nick_name,
            u.user_name,
            u.email,
            u.phonenumber,
            u.sex,
            u.avatar,
            u.status,
            d.dept_id,
            d.dept_name,
            d.order_num
        ORDER BY d.order_num, u.user_id
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectDeptTree" resultType="java.util.HashMap">
        SELECT
            dept_id,
            parent_id,
            dept_name,
            order_num,
            leader,
            phone,
            email
        FROM sys_dept
        WHERE del_flag = '0'
          AND status = '0'
        ORDER BY parent_id, order_num
    </select>

    <select id="selectUserDetail" resultType="java.util.HashMap">
        SELECT
            u.user_id,
            u.nick_name,
            u.user_name,
            u.email,
            u.phonenumber,
            u.sex,
            u.avatar,
            u.status,
            u.remark,
            d.dept_id,
            d.dept_name,
            GROUP_CONCAT(DISTINCT p.post_name ORDER BY p.post_sort SEPARATOR '、') AS post_name
        FROM sys_user u
        LEFT JOIN sys_dept d
            ON u.dept_id = d.dept_id
        LEFT JOIN sys_user_post up
            ON u.user_id = up.user_id
        LEFT JOIN sys_post p
            ON up.post_id = p.post_id
        WHERE u.user_id = #{userId}
          AND u.del_flag = '0'
        GROUP BY
            u.user_id,
            u.nick_name,
            u.user_name,
            u.email,
            u.phonenumber,
            u.sex,
            u.avatar,
            u.status,
            u.remark,
            d.dept_id,
            d.dept_name
        LIMIT 1
    </select>
</mapper>
