# CloudFlow Pro P3 风险修复方案

- 编写日期：2026-05-13
- 适用基线：`dev` 分支（P0/P1/P2 修复完成后启动）
- 关联文档：[后端审计报告.md](./后端审计报告.md) §13.4、[P0修复方案.md](./P0修复方案.md)、[P1修复方案.md](./P1修复方案.md)、[P2修复方案.md](./P2修复方案.md)
- 修复范围：10 项 P3 风险（去重后）

> **说明**：P3 属于 **长期清理与体验优化**，多为死代码清理、文档对齐、占位模块决策。不会引发线上事故，可作为团队"破窗"治理任务，由开发同学在迭代空档完成。

---

## 一、修复优先级与建议顺序

按 **改动成本 × 长期收益** 排序，分 2 类推进：

| 类别 | 项目 | 工期 |
|---|---|---|
| **死代码与占位清理** | P3-1 cloudflow-common-seata 落地或删除 | 2 天（决策）+ 实施 |
| | P3-5 TenantWebFilter 死代码清理 | 0.5 天 |
| | P3-6 AuthFilter.resolveCookieToken 死代码清理 | 0.5 天 |
| | P3-2 Hystrix → Sentinel 实质迁移 | 3-5 天（如 P1-4 仅删配置） |
| **代码质量与安全细节** | P3-3 HotUpdateController 高危确认 token | 1 天 |
| | P3-4 HasPermissionAspect 异常类型规范 | 0.5 天（P1-9 完成后随手清理） |
| | P3-7 HR lookupSingleValue PreparedStatement | 1 天 |
| | P3-8 OSS URL 签名/过期/租户隔离评估 | 2 天 |
| | P3-9 Prometheus 抓取认证 | 1 天 |
| | P3-10 OA allow-anonymous-read 评估 | 0.5 天 |

总工期约 **12-15 个工作日**，建议每个迭代分配 1-2 项作为技术债清理。

---

## 二、P3-1 cloudflow-common-seata 落地或删除

### 2.1 问题描述

`cloudflow-common-seata` 模块**仅 1 个空 `SeataConfiguration` 类**，注释"预留"。业务侧未引用，但 README/AGENTS 文档仍宣传"分布式事务"。

形成"宣传与现实脱节"，安全审计和新人 onboarding 困惑。

### 2.2 修复思路（决策驱动）

**先决策：是否真的需要 Seata？**

| 选项 | 适用场景 | 建议 |
|---|---|---|
| **删除模块** | 当前 SAGA 模式（`WorkflowSagaServiceImpl`）+ Redis Stream 本地消息表已能覆盖业务需求 | 推荐 |
| **落地集成** | HR/OA 跨服务流程审批确实需要强一致性（如调薪同时改 hr_employee_salary 与 oa_budget） | 需评估 RT 成本 |

### 2.3 修复步骤（删除路径）

1. **删除模块目录**：
   ```bash
   rm -rf cloudflow-backend/cloudflow-common/cloudflow-common-seata
   ```
2. **修改父 POM**：移除 `<module>cloudflow-common-seata</module>`
3. **更新文档**：
   - `README.md` 移除"Seata 分布式事务"宣传
   - `AGENTS.md` 同步移除
4. **回归编译**：`mvn clean install -pl cloudflow-common -am`

### 2.4 修复步骤（落地路径）

1. **新增 Seata Server 容器**（docker-compose.yml）：
   ```yaml
   seata-server:
     image: seataio/seata-server:2.0.0
     environment:
       - SEATA_IP=seata-server
       - STORE_MODE=db
       - SEATA_CONFIG_NAME=file:/root/seata-config/registry
     volumes:
       - ./seata-config:/root/seata-config
   ```
2. **完善 `SeataConfiguration`**：注册数据源代理、TM/RM 配置
3. **业务侧标注 `@GlobalTransactional`**：
   ```java
   @GlobalTransactional
   public void adjustSalaryAndBudget(Long employeeId, BigDecimal newSalary) {
       hrService.updateSalary(employeeId, newSalary);
       oaService.deductBudget(deptId, monthlyDiff);
   }
   ```
4. **回归测试**：故意让 OA 抛异常，验证 HR 端回滚

### 2.5 验证方式（删除路径）

- `mvn dependency:tree | grep seata` 期望 0 匹配
- README 中无 Seata 相关字样
- 应用启动无 `SeataConfiguration` 类加载

---

## 三、P3-2 Hystrix → Sentinel 实质迁移

### 3.1 问题描述

P1-4 已删除 Hystrix 死配置，但若未引入熔断器，业务服务在依赖服务宕机时会**线程堆积、级联雪崩**。

### 3.2 修复思路

**两个选项**（与 P1-4 联动）：

| 选项 | 优点 | 缺点 |
|---|---|---|
| **Resilience4j** | 与 Spring Cloud 2023 兼容好，社区活跃 | 配置式编程为主 |
| **Sentinel** | 阿里出品，与 Nacos 集成完美，可视化控制台 | 学习曲线略陡 |

CloudFlow 已用 Spring Cloud Alibaba 2023.0.1.0 + Nacos，推荐 **Sentinel**。

### 3.3 修复步骤（Sentinel 路线）

1. **依赖引入**：
   ```xml
   <dependency>
       <groupId>com.alibaba.cloud</groupId>
       <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
   </dependency>
   ```
2. **Sentinel Dashboard 部署**（docker-compose.yml）：
   ```yaml
   sentinel-dashboard:
     image: bladex/sentinel-dashboard:1.8.6
     ports: ["8718:8858"]
   ```
3. **业务服务配置**：
   ```yaml
   spring:
     cloud:
       sentinel:
         transport:
           dashboard: sentinel-dashboard:8858
         eager: true
         datasource:
           ds1:
             nacos:
               server-addr: ${NACOS_ADDR}
               data-id: ${spring.application.name}-sentinel-rules
               group-id: SENTINEL_GROUP
               rule-type: flow
   ```
4. **Feign 整合 Sentinel**：
   ```yaml
   feign:
     sentinel:
       enabled: true
   ```
   ```java
   @FeignClient(name = "cloudflow-auth", fallback = AuthServiceFallback.class)
   public interface AuthServiceClient { ... }
   ```
5. **熔断规则配置**（Dashboard 操作）：
   - QPS 阈值
   - 异常比例熔断
   - 慢调用熔断（RT > 500ms 比例 > 0.5 触发）

### 3.4 验证方式

- Dashboard 可视化看到各服务流量
- 故意让 auth 服务 sleep(10s)，调用方触发慢调用熔断
- Nacos 中规则持久化

---

## 四、P3-3 HotUpdateController 高危确认 token

### 4.1 问题描述

`HotUpdateController.execute` 直接对运行中流程做节点追加/删除/替换，**改动直接生效**。误操作可能导致：
- 已运行实例错乱
- 任务流转死锁
- 审批链路丢失

当前仅靠 admin 角色控制，缺二次确认。

### 4.2 修复思路

引入 **两步确认机制**：

1. 第一步：POST `/hotupdate/prepare` 返回 confirmToken（30s 有效）
2. 第二步：POST `/hotupdate/execute` 携带 confirmToken 才真正执行

### 4.3 修复步骤

```java
@RestController
@RequestMapping("/hotupdate")
@SaCheckRole("admin")
public class HotUpdateController {

    @PostMapping("/prepare")
    public R<HotUpdatePreviewVO> prepare(@RequestBody HotUpdateRequest req) {
        // 1. 校验流程实例存在
        // 2. 模拟执行 diff
        HotUpdatePreviewVO preview = hotUpdateService.simulate(req);
        // 3. 生成确认 token
        String token = IdUtils.simpleUUID();
        redisTemplate.opsForValue().set(
            "HOTUPDATE:CONFIRM:" + token,
            JSONUtil.toJsonStr(req),
            30, TimeUnit.SECONDS
        );
        preview.setConfirmToken(token);
        // 4. 记录预备操作到审计
        auditService.record("HOTUPDATE_PREPARE", req);
        return R.ok(preview);
    }

    @PostMapping("/execute")
    @Audit(businessType = BusinessType.UPDATE, businessKey = "#token")
    public R<Void> execute(@RequestParam String token) {
        String reqJson = redisTemplate.opsForValue().get("HOTUPDATE:CONFIRM:" + token);
        if (reqJson == null) {
            throw new ServiceException("CONFIRM_TOKEN_EXPIRED");
        }
        // 一次性消费
        redisTemplate.delete("HOTUPDATE:CONFIRM:" + token);
        HotUpdateRequest req = JSONUtil.toBean(reqJson, HotUpdateRequest.class);
        hotUpdateService.execute(req);
        return R.ok();
    }
}
```

### 4.4 验证方式

- `/hotupdate/prepare` 返回 confirmToken + diff 预览
- 30s 内调 `/hotupdate/execute` 成功
- 30s 后调用 → "CONFIRM_TOKEN_EXPIRED"
- 同 token 重复调用 → 失败（一次性消费）

---

## 五、P3-4 HasPermissionAspect 异常类型规范

### 5.1 问题描述

`HasPermissionAspect` 抛 `RuntimeException("没有访问权限")`，全局异常处理需匹配字符串识别。

### 5.2 修复思路

如 P1-9 已完成（所有 `@HasPermission` 替换为 `@SaCheckPermission`），则**直接删除 `HasPermissionAspect` 与注解定义**。

如出于兼容仍保留，则改抛 `SaTokenException`：

```java
// 改前
throw new RuntimeException("没有访问权限");

// 改后
throw new NotPermissionException(requiredPerms, "user");
```

### 5.3 修复步骤

1. 确认 P1-9 完成度：`grep -rn "@HasPermission" cloudflow-backend/`
2. 若 0 匹配 → 删除 `HasPermissionAspect.java` + `HasPermission.java`
3. 若有残留 → 改抛 `NotPermissionException`，与 Sa-Token 异常体系统一

### 5.4 验证方式

- 全局异常处理 case 简化（无需字符串匹配）
- 单元测试无权限场景，返回 403 + 标准 SaTokenException

---

## 六、P3-5 TenantWebFilter 死代码清理

### 6.1 问题描述

网关 `TenantWebFilter` 把租户 ID 写到 Reactor Context，但**下游 servlet 服务读不到**（servlet 与 reactor 上下文隔离），实际依赖 Token 内嵌的 tenantId。

`TenantWebFilter` 是半完成功能 → 死代码。

### 6.2 修复思路

**两种处理**（择一）：

**选项 A：删除（推荐）**
- 直接删除 `TenantWebFilter`，租户解析全部依赖 Sa-Token Session

**选项 B：完整实现**
- 在 Filter 中把 `X-Tenant-Id` 写回 Request Headers
- 下游 `UserContextInterceptor` 优先读 Header，回退 Session

### 6.3 修复步骤（删除路径）

1. 删除 `cloudflow-backend/cloudflow-gateway/.../TenantWebFilter.java`
2. 更新 `cloudflow-backend/cloudflow-gateway/src/main/java/.../config/`（如有显式注册）
3. 启动 gateway，验证业务请求租户解析正常

### 6.4 验证方式

- 业务接口正常访问，租户隔离生效（仍走 Sa-Token Session）
- gateway 日志无 `TenantWebFilter` 相关记录

---

## 七、P3-6 AuthFilter.resolveCookieToken 死代码清理

### 7.1 问题描述

`AuthFilter` 中 `resolveCookieToken` 私有方法已写但**未被调用**——`resolveRawToken` 只读 Header。

### 7.2 修复思路

**判断是否真需要 Cookie 兜底**：

- 若前端确实只用 `Authorization` Header → 删除 `resolveCookieToken`
- 若前端依赖 Cookie（如 OA 系统的 SSO）→ 在 `resolveRawToken` 中调用 `resolveCookieToken` 作 fallback

### 7.3 修复步骤（删除路径）

```java
// 改前
private String resolveRawToken(ServerHttpRequest req) {
    String header = req.getHeaders().getFirst("Authorization");
    if (StrUtil.isNotBlank(header) && header.startsWith("Bearer ")) {
        return header.substring(7);
    }
    return null;
}

private String resolveCookieToken(ServerHttpRequest req) {
    // ⚠️ 死代码
    HttpCookie cookie = req.getCookies().getFirst("Authorization");
    return cookie == null ? null : cookie.getValue();
}

// 改后（删除 resolveCookieToken）
```

### 7.4 修复步骤（启用路径）

```java
private String resolveRawToken(ServerHttpRequest req) {
    String header = req.getHeaders().getFirst("Authorization");
    if (StrUtil.isNotBlank(header) && header.startsWith("Bearer ")) {
        return header.substring(7);
    }
    return resolveCookieToken(req);
}
```

### 7.5 验证方式

- 前端登录后，刷新页面（仅 Cookie 中有 token）能维持登录态（启用路径）
- 或：纯 Header 调用工作正常（删除路径）

---

## 八、P3-7 HR lookupSingleValue PreparedStatement 改造

### 8.1 问题描述

`HrDomainCrudService.lookupSingleValue` 等少量内部方法**直接字符串拼接 idColumn / valueColumn**：

```java
String sql = "SELECT " + valueColumn + " FROM " + tableName + " WHERE " + idColumn + " = ?";
```

虽然当前调用方都传字面量，但**未来扩展可能引入业务参数 → SQL 注入风险**。

### 8.2 修复思路

**双重防护**：

1. 列名校验：仅允许 `getColumns(tableName)` 已存在的列名
2. PreparedStatement 占位：表名/列名经白名单后拼接，参数全部走占位

### 8.3 修复步骤

```java
public Object lookupSingleValue(String tableName, String idColumn,
                                String valueColumn, Object idValue) {
    // 1. 表名白名单
    if (!getRegisteredTables().contains(tableName)) {
        throw new IllegalArgumentException("Unknown table: " + tableName);
    }
    // 2. 列名白名单
    Set<String> cols = getColumns(tableName);
    if (!cols.contains(idColumn) || !cols.contains(valueColumn)) {
        throw new IllegalArgumentException("Invalid column");
    }
    // 3. 标识符二次校验（仅字母数字下划线）
    if (!IDENTIFIER_PATTERN.matcher(idColumn).matches() ||
        !IDENTIFIER_PATTERN.matcher(valueColumn).matches() ||
        !IDENTIFIER_PATTERN.matcher(tableName).matches()) {
        throw new IllegalArgumentException("Invalid identifier");
    }
    // 4. 拼接 SQL（参数仍占位）
    String sql = String.format("SELECT `%s` FROM `%s` WHERE `%s` = ?",
        valueColumn, tableName, idColumn);
    return jdbcTemplate.queryForObject(sql, Object.class, idValue);
}

private static final Pattern IDENTIFIER_PATTERN = Pattern.compile("^[a-zA-Z][a-zA-Z0-9_]*$");
```

### 8.4 验证方式

- 单元测试：
  - 传入合法 column → 正常返回
  - 传入 `"id; DROP TABLE hr_employee --"` → 抛 IllegalArgumentException
  - 传入不存在的 column → 抛 IllegalArgumentException

### 8.5 注意事项

P0-2 HR 第二波重构完成后，`HrDomainCrudService` 整体废弃，此项可省略。

---

## 九、P3-8 OSS URL 签名/过期/租户隔离评估

### 9.1 问题描述

`hr_employee_document.attachment_urls`（JSON）存储 OSS URL，承载身份证扫描件、合同附件等敏感文件。

需要评估：
- URL 是否签名（防直接拿 URL 公网访问）
- 签名 URL 是否有过期时间
- 不同租户的文件是否物理隔离（按 Bucket 或路径前缀）

### 9.2 修复步骤

1. **现状调研**：
   ```bash
   grep -rn "generatePresignedUrl\|getObject\|putObject" \
       cloudflow-backend/cloudflow-common/cloudflow-common-oss/
   ```
2. **改造 `OssClient.generatePresignedUrl`**：
   ```java
   public String generatePresignedUrl(String bucket, String key, Duration expiration) {
       GetObjectPresignRequest presignReq = GetObjectPresignRequest.builder()
           .signatureDuration(expiration)   // 默认 15min
           .getObjectRequest(b -> b.bucket(bucket).key(key))
           .build();
       return s3Presigner.presignGetObject(presignReq).url().toString();
   }
   ```
3. **租户路径隔离**：
   ```java
   public String buildObjectKey(Long tenantId, String module, String fileName) {
       return String.format("tenant/%d/%s/%s/%s",
           tenantId, module, LocalDate.now(), IdUtils.simpleUUID() + "_" + fileName);
   }
   ```
4. **下载接口加签名 URL 返回**：
   ```java
   @GetMapping("/file/{id}/download-url")
   public R<String> getDownloadUrl(@PathVariable Long id) {
       SysFile file = fileService.getById(id);
       // 校验当前用户能否访问此文件（租户 + 业务权限）
       assertAccessible(file);
       String url = ossClient.generatePresignedUrl(
           file.getBucket(), file.getObjectKey(), Duration.ofMinutes(15));
       return R.ok(url);
   }
   ```
5. **附件 URL 不直接持久化签名版**，每次访问按需生成

### 9.3 验证方式

- 直接访问 `https://oss.cloudflow/tenant/100000/hr/...` → 403 AccessDenied
- 通过接口拿签名 URL，15 分钟内可访问，过期后失败
- 用 A 租户 token 调 B 租户文件 → 403

---

## 十、P3-9 Prometheus 抓取认证

### 10.1 问题描述

`/actuator/prometheus` 当前无任何认证（容器内网信任），但若 Prometheus 节点被入侵或网络配置不当，可暴露：
- JVM 内存/线程指标
- HTTP 请求 path/method/count
- 业务自定义 metrics

### 10.2 修复步骤

1. **Spring Security 接管 actuator 端点**（独立 dependency）：
   ```xml
   <dependency>
       <groupId>org.springframework.boot</groupId>
       <artifactId>spring-boot-starter-security</artifactId>
   </dependency>
   ```
2. **配置 basic auth**：
   ```java
   @Configuration
   public class ActuatorSecurityConfig {
       @Bean
       SecurityFilterChain actuatorFilterChain(HttpSecurity http) throws Exception {
           http
               .securityMatcher("/actuator/**")
               .authorizeHttpRequests(a -> a
                   .requestMatchers("/actuator/health").permitAll()
                   .anyRequest().hasRole("MONITOR"))
               .httpBasic(Customizer.withDefaults());
           return http.build();
       }

       @Bean
       UserDetailsService monitorUser(
               @Value("${monitor.username}") String username,
               @Value("${monitor.password}") String password) {
           UserDetails user = User.builder()
               .username(username)
               .password("{bcrypt}" + bcrypt.encode(password))
               .roles("MONITOR")
               .build();
           return new InMemoryUserDetailsManager(user);
       }
   }
   ```
3. **Prometheus 配置 basic_auth**：
   ```yaml
   scrape_configs:
     - job_name: 'cloudflow-auth'
       basic_auth:
         username: prometheus
         password_file: /etc/prometheus/prom_passwd
       static_configs:
         - targets: ['auth:9100']
   ```
4. **密码走环境变量**（与 P0-1 联动）

### 10.3 验证方式

- 匿名访问 `/actuator/prometheus` → 401
- 带 basic auth 访问 → 200 + metrics 数据
- Prometheus dashboard 数据正常

### 10.4 注意事项

若 P2-14 已实现"独立 management 端口 + 内网隔离"，此项可降级为可选。

---

## 十一、P3-10 OA allow-anonymous-read 评估

### 11.1 问题描述

`cloudflow.oa.announcement.allow-anonymous-read: false` 配置存在，但**未验证 `SysAnnouncementController` 是否真的执行了此配置**。

可能是死配置，也可能是漏校验。

### 11.2 修复步骤

1. **代码搜索配置消费点**：
   ```bash
   grep -rn "allow-anonymous-read\|allowAnonymousRead" cloudflow-backend/
   ```
2. **若无消费**：删除配置 + 删除 `OaProperties` 中字段
3. **若需启用**：
   ```java
   @GetMapping("/announcement/public")
   public R<List<AnnouncementVO>> publicList() {
       if (!oaProperties.getAnnouncement().isAllowAnonymousRead()) {
           StpUtil.checkLogin();  // 显式校验
       }
       return R.ok(announcementService.listPublic());
   }
   ```
4. **网关白名单同步**：若允许匿名读，加 `/oa/announcement/public/**` 到 AuthFilter 白名单

### 11.3 验证方式

- `allow-anonymous-read: true` → 未登录可访问 `/oa/announcement/public`
- `allow-anonymous-read: false` → 401

---

## 十二、总修复时间线

```
Iteration A (技术债清理周):
  Day 1-2 P3-1 Seata 决策 + 实施（删除路径）
  Day 3   P3-5 TenantWebFilter 清理
  Day 4   P3-6 AuthFilter.resolveCookieToken 清理
  Day 5   P3-4 HasPermissionAspect 收尾（P1-9 联动）

Iteration B (安全细节加固):
  Day 1-2 P3-2 Sentinel 实质迁移（如选择落地）
  Day 3   P3-3 HotUpdateController 二次确认
  Day 4   P3-7 lookupSingleValue 改造（如 P0-2 未完成）
  Day 5   P3-10 allow-anonymous-read 评估

Iteration C (基础设施完善):
  Day 1-2 P3-8 OSS URL 签名 + 租户隔离
  Day 3   P3-9 Prometheus 抓取认证
  Day 4-5 全量回归 + 文档更新
```

---

## 十三、配套机制

1. **破窗治理**：每个迭代允许分配 10-15% 工时给 P3 清理
2. **新人 onboarding 项目**：P3 中的清理任务（如 P3-5/P3-6）适合作为新人首个 PR
3. **代码扫描 CI**：
   - 定期扫描死代码（IDE inspection 报告周报）
   - 定期扫描配置文件，识别未消费的属性
4. **审计报告同步**：每完成一项，在 `后端审计报告.md` §13.6 修复记录表追加一行

---

## 十四、附录：快速命令清单

```bash
# 扫描 Seata 残留
grep -rn "seata\|@GlobalTransactional" cloudflow-backend/ --include="*.java" --include="*.xml"

# 扫描死代码（IDE 工具更靠谱）
grep -rn "@SuppressWarnings.*unused\|TODO.*remove\|FIXME.*delete" cloudflow-backend/

# 扫描 HasPermission 残留
grep -rn "@HasPermission\|HasPermissionAspect" cloudflow-backend/

# 扫描 Hystrix 配置残留
grep -rn "hystrix\|Hystrix" cloudflow-backend/ config/

# 扫描 OSS 直链使用（应改签名 URL）
grep -rn "getObjectUrl\|.com/.*\.\(jpg\|pdf\|doc" cloudflow-backend/

# 扫描 actuator 配置
grep -rn "endpoints.web.exposure\|management.endpoints" cloudflow-backend/ config/

# 扫描未消费的配置属性
grep -rn "@ConfigurationProperties\|@Value" cloudflow-backend/ --include="*.java" | \
  awk -F'"' '{print $2}' | sort -u
```

---

## 十五、CRM 模块整改阶段 1–3 联动（2026-05-13 已落地）

CRM 模块整改阶段 1–3（见 `docs/CRM-模块评估与整改方案.md`）已落地。与本 P3 文档的关系：

### 15.1 直接关系

| P3 条目 | 联动情况 |
|---|---|
| P3-2 Hystrix → Sentinel 实质迁移 | 无直接关系；阶段 2/3 新增的 Feign（`RemoteHrService`、`CrmPerformanceClient`）全部使用 `FallbackFactory` / `fallback`，未引入 Hystrix |
| P3-3 HotUpdateController 高危确认 token | 无直接关系 |
| P3-4 HasPermissionAspect 异常类型规范 | CRM 阶段 3 新增 Controller（`CrmApprovalController`、`CrmHandoverTaskController`、`CrmPerformanceInnerController`）均未使用 `@HasPermission`；P3-4 迁移后不额外涉及 CRM |
| P3-5 TenantWebFilter 死代码清理 | 无直接关系 |
| P3-6 AuthFilter.resolveCookieToken 死代码清理 | 无直接关系 |
| P3-8（若涉及 README 一致性）| **强相关**。README 中 "CRM 8 张表、10 个 Controller、约 77 Java 文件" 的描述已过时。当前数据（阶段 3 后）：**10 张表**（原 8 + `oa_crm_handover_task` + `oa_crm_approval`）、**约 20 个 Controller**（原 10 + 阶段 2/3 新增 `CrmPerformanceInnerController` / `CrmHandoverTaskController` / `CrmApprovalController`）、**118 个 Java 文件**（原 77）。P3 清理文档时应一并对齐 |
| P3-9 Prometheus 抓取认证 | 无直接关系 |
| P3-10 OA allow-anonymous-read 评估 | 无直接关系 |

### 15.2 阶段 1/2/3 完结后需要同步更新的文档位点

1. **`README.md`**（若存在）/ **`AGENTS.md`**：CRM 表数、文件数、Controller 数。
2. **`docs/CRM-模块评估与整改方案.md`**：阶段 1/2/3 标注"已完成"。本报告已同步审计报告 §8.10 的状态表。
3. **工作流模板导入脚本**（若有）：补齐 `customer_claim_review` / `customer_level_change` / `opportunity_downgrade_review` / `crm_refund_review` 4 个流程定义。

### 15.3 时间线

不占用 P3 的任何一波。P3 属于长期清理，CRM 阶段 1–3 的完工为后续的代码清理提供了更清晰的边界（拆分后的 Service、抽出的常量、独立的事件流）。

> 本节仅作进度同步。详细内容见 `后端审计报告.md` §8.10、§9.11 与 §13.6。

---

*文档版本：v1.0 — 2026-05-13。每完成一项 P3，请同步更新本文档对应章节为"✅ 已完成"并在审计报告 §13.6 追加修复记录。*
