# CloudFlow Pro P3 风险修复方案（状态化台账）

- 初版日期：2026-05-13
- 最新刷新：2026-05-20
- 适用基线：`dev` 分支（P0/P1/P2 已完成后的当前仓库）
- 关联文档：[后端审计报告.md](./后端审计报告.md) §13.4、§13.6；[P0修复方案.md](./P0修复方案.md)；[P1修复方案.md](./P1修复方案.md)；[P2修复方案.md](./P2修复方案.md)
- 当前范围：原始 10 项 P3 风险，现按“已完成 / 失效关闭 / 历史待实施项收口结果”持续维护

> **说明**：本文不再把 10 项 P3 一律视为“待办”，而是以当前仓库现状为准维护状态。
>
> **状态口径**：
> - ✅ **已完成**：当前主路径或本轮变更已落地
> - 🛑 **失效关闭**：原问题对象已下线或已不再成立
> - 🟡 **待实施**：历史状态口径，表示当时仍有实际收口动作；截至本次刷新已清零
> - ✍️ **待改写**：仅用于发现文案与现状漂移、但尚未完成重写的中间态；本次刷新后已清零

---

## 一、当前总表

| 条目 | 当前状态 | 当前结论 | 现状锚点 |
|---|---|---|---|
| P3-1 `cloudflow-common-seata` 去留决策 | ✅ 已完成 | `cloudflow-common-seata` 占位模块、父 POM module 与根 `dependencyManagement` 已一并移除 | `cloudflow-backend/pom.xml`、`cloudflow-backend/cloudflow-common/pom.xml` |
| P3-2 熔断治理路线决策 | ✅ 已完成 | 已统一选定并落地 Resilience4j 路线：4 个 Feign 服务补齐 `spring-cloud-starter-circuitbreaker-resilience4j` 与 `spring.cloud.openfeign.circuitbreaker.enabled=true` | `cloudflow-service-{crm,oa,hr,workflow}/pom.xml`、相关 `application.yml` / `config/*.yaml` |
| P3-3 热更新二次确认 | ✅ 已完成 | 已改为“分析 → 生成确认令牌 → 执行”三步流 | `HotUpdateController.java`、`HotUpdateServiceImpl.java`、前端热更新页/弹窗 |
| P3-4 `HasPermissionAspect` 收尾 | ✅ 已完成 | 主代码路径 `@HasPermission` / `HasPermissionAspect` 已清零；不再需要兼容异常类型修复 | `P1修复方案.md` P1-9、当前代码检索结果 |
| P3-5 租户透传链路补齐 | ✅ 已完成 | 不是删除 `TenantWebFilter`，而是补齐可信 `X-Tenant-Id` 透传链路 | `AuthFilter.java`、`TenantWebFilter.java`、`UserContextInterceptor.java` |
| P3-6 `resolveCookieToken` 死代码清理 | ✅ 已完成 | `resolveRawToken` 已回退到 Cookie，原“未使用死代码”判断不再成立 | `AuthFilter.java` |
| P3-7 HR `lookupSingleValue` 改造 | 🛑 失效关闭 | 旧 `HrDomainCrudService` 已在 P0#2 第二波下线，本项失去实施对象 | `P0修复方案.md`、`后端审计报告.md` §9.8 |
| P3-8 OSS 签名 URL / 过期 / 隔离 | ✅ 已完成 | 已收口为 auth 统一受登录态保护的文件访问入口；前端附件值改为长期持久化 raw `filePath`，访问时再转内部稳定地址，并补租户校验与历史 URL 兼容 | `SysFileController.java`、`SysFileServiceImpl.java`、`attachment.ts`、`FileUpload.tsx`、`hr.ts` |
| P3-9 Prometheus 抓取认证 | ✅ 已完成 | management 独立端口保留，`/actuator/prometheus` 已收口 basic auth，Prometheus 改为 password_file 抓取 | `PrometheusManagementServletConfiguration.java`、`PrometheusScrapeReactiveFilter.java`、`docker/monitoring/prometheus/prometheus.yml`、`docker-compose.yml` |
| P3-10 OA `allow-anonymous-read` 生效性 | ✅ 已完成 | 已新增受 `allowAnonymousRead` 驱动的 `/oa/announcement/public` 匿名只读公告入口 | `OaProperties.java`、`SysAnnouncementController.java`、`SysAnnouncementServiceImpl.java` |

当前汇总：**✅ 已完成 9 项 + 🛑 失效关闭 1 项**。

---

## 二、本轮新增收口摘要

本轮已将此前剩余的 2 项 P3 一并落地：

| 项目 | 本轮结果 | 核心落点 |
|---|---|---|
| P3-8 OSS 签名 URL / 隔离链路 | auth 统一文件访问入口 + 前端附件稳定地址收口 + 历史 URL 兼容 + 租户校验 | `cloudflow-auth`、`cloudflow-frontend/src/utils/attachment.ts` |
| P3-2 熔断治理路线决策 | 正式选择 Resilience4j，并为 4 个 OpenFeign 服务补齐 circuitbreaker 依赖与正确配置前缀 | 4 个服务 `pom.xml`、`application.yml`、`config/*.yaml` |

> 当前 P3 代码主线已全部收口；后续若有新增事项，仅按增量维护本文与审计记录。

---

## 三、P3-1 `cloudflow-common-seata` 去留决策

> **状态：✅ 已完成（2026-05-19）**

### 3.1 本轮落地结果

本轮已按“删除占位模块”路径完成收口：

1. 删除 `cloudflow-backend/cloudflow-common/cloudflow-common-seata` 模块目录
2. 从 `cloudflow-backend/cloudflow-common/pom.xml` 移除 `<module>cloudflow-common-seata</module>`
3. 从 `cloudflow-backend/pom.xml` 移除 `cloudflow-common-seata` 的内部依赖管理项
4. 回归编译通过，确认当前主路径无残余依赖

### 3.2 当前结论

P3-1 已不再是待决策事项，当前仓库已明确选择：

> **删除未启用的 Seata 空占位模块，而不是继续保留假能力入口。**

### 3.3 当前验证口径

- `cloudflow-common-seata` 目录已删除
- 父 POM module 与根 `dependencyManagement` 已同步收口
- 后端聚合编译通过
- 当前仅余历史说明类文档残留，不再属于代码整改主路径

---

## 四、P3-2 熔断治理路线决策

> **状态：✅ 已完成（2026-05-20）**

### 4.1 选型结论

P1-4 已完成 **Hystrix 死配置清理** 后，本轮不再停留在“Sentinel / Resilience4j 二选一”的文档态，而是直接按当前技术栈落地：

> **统一选择 Resilience4j，作为 Spring Cloud 2023 下的 OpenFeign circuit breaker 底座。**

### 4.2 本轮落地结果

1. 为 `cloudflow-service-crm`、`cloudflow-service-oa`、`cloudflow-service-hr`、`cloudflow-service-workflow` 补齐 `spring-cloud-starter-circuitbreaker-resilience4j`
2. 将 4 个服务的熔断开关统一切到 Spring Cloud 2023 正确前缀：
   - `spring.cloud.openfeign.circuitbreaker.enabled=true`
3. 同步更新本地 `application.yml` 与 Nacos `config/cloudflow-service-*.yaml`
4. 保留现有 `fallback` / `fallbackFactory` 语义，不再继续依赖已过期的 `feign.circuitbreaker.enabled` 旧写法

### 4.3 当前结论

当前仓库已不再是“只有 fallback、没有统一熔断底座”的状态，而是完成了：

- **统一依赖**：4 个 Feign 服务同口径引入 Resilience4j starter
- **统一配置**：本地与 Nacos 均改用 `spring.cloud.openfeign.circuitbreaker.enabled`
- **统一方向**：当前已完成 circuit breaker 底座接入；后续若需补慢调用阈值、实例级策略，可直接在 Resilience4j 配置下增量扩展

### 4.4 当前验证口径

- 关键检索已不再命中旧 `feign.circuitbreaker.enabled`
- 4 个服务 `pom.xml` 已命中 `spring-cloud-starter-circuitbreaker-resilience4j`
- 后端聚合编译通过：
  - `mvn -f cloudflow-backend/pom.xml -pl cloudflow-service-crm,cloudflow-service-oa,cloudflow-service-hr,cloudflow-service-workflow -am -DskipTests compile`

---

## 五、P3-3 工作流热更新二次确认

> **状态：✅ 已完成（2026-05-19）**

### 5.1 本轮落地结果

已将原先“管理员直接执行热更新”的高危链路，改为**服务端两步确认**：

1. `POST /workflow/hot-update/analyze`：影响分析
2. `POST /workflow/hot-update/prepare`：生成一次性确认令牌
3. `POST /workflow/hot-update/execute`：携带确认令牌后才真正执行

### 5.2 已落地的关键约束

- 确认令牌 TTL = **30 秒**
- 令牌写入 Redis，一次性消费
- 令牌与 **userId / tenantId** 绑定
- `prepare` / `execute` 均写工作流审计日志
- 前端发布页与弹窗均已改成三步流

### 5.3 主要落点

- 后端：
  - `cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/controller/HotUpdateController.java`
  - `cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/service/impl/HotUpdateServiceImpl.java`
  - `cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/domain/dto/HotUpdateRequest.java`
  - `cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/domain/dto/HotUpdateResult.java`
  - `cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/service/WorkflowAuditService.java`
- 前端：
  - `cloudflow-frontend/src/components/deploy/HotUpdatePanel.tsx`
  - `cloudflow-frontend/src/components/HotUpdateDialog.tsx`
  - `cloudflow-frontend/src/services/api/workflow.ts`

### 5.4 当前验证口径

- 同一 `confirmToken` 仅能成功执行一次
- 过期 token 返回失败
- 非同身份用户/租户不可复用 token
- 前端交互已从“分析后直接执行”改为“分析 → 确认 → 生成令牌 → 执行”

---

## 六、P3-4 `HasPermissionAspect` 收尾

> **状态：✅ 已完成**

### 6.1 当前现状

P1-9 已完成权限注解收口，当前代码主路径：

- 外部接口统一收口到 `@SaCheckPermission`
- 内部接口维持 `@Inner`
- 平台级少量固定保留 `@SaCheckRole("admin")`

当前仓库代码检索已不再命中 `@HasPermission` / `HasPermissionAspect` 的运行时使用路径。

### 6.2 当前结论

P3-4 不再需要实施“异常类型规范化”这类兼容修复；当前只剩历史文档中的说明残留，不再属于代码整改事项。

---

## 七、P3-5 租户透传链路补齐

> **状态：✅ 已完成（2026-05-19）**

### 7.1 原始问题

原始问题不是“TenantWebFilter 存在就一定该删”，而是：

- `TenantWebFilter` 只写 Reactor Context
- 下游 servlet 服务读不到 Reactor Context
- 链路缺少**可信 `X-Tenant-Id` header** 透传

### 7.2 本轮落地结果

本轮选择的不是删除路径，而是**完整实现路径**：

1. `AuthFilter` 在 `TokenService.verifyToken` 成功后，从登录态中提取 `tenantId`
2. 将可信租户值写回 `X-Tenant-Id`
3. `TenantWebFilter` 在缺失时回退默认租户，并把最终租户值同步写回 Request Headers
4. 下游 `UserContextInterceptor.fillTenantFromHeader` 可消费该 header

### 7.3 当前链路

- 网关认证成功 → `AuthFilter` 写可信 `X-Tenant-Id`
- 网关缺失租户 → `TenantWebFilter` 补默认租户并回写 header
- 下游 servlet 服务 → `UserContextInterceptor` 从 `X-Tenant-Id` 填充 `UserContext`

### 7.4 主要落点

- `cloudflow-backend/cloudflow-gateway/src/main/java/com/cloudflow/gateway/filter/AuthFilter.java`
- `cloudflow-backend/cloudflow-gateway/src/main/java/com/cloudflow/gateway/filter/TenantWebFilter.java`
- `cloudflow-backend/cloudflow-common/cloudflow-common-security/src/main/java/com/cloudflow/common/security/interceptor/UserContextInterceptor.java`

### 7.5 当前结论

P3-5 已由“半成品链路”收口为“可被下游 servlet 消费的可信租户透传链路”，不再作为死代码清理项处理。

---

## 八、P3-6 `AuthFilter.resolveCookieToken` 收尾

> **状态：✅ 已完成**

### 8.1 当前现状

`AuthFilter.resolveRawToken` 已在 Header 缺失时回退到 `resolveCookieToken`。

### 8.2 当前结论

原始文档中“`resolveCookieToken` 未被调用，是死代码”的判断已经失效。

当前该方法属于**已启用的 Cookie fallback 路径**，无需继续按“删除死代码”推进。

---

## 九、P3-7 HR `lookupSingleValue` 改造

> **状态：🛑 失效关闭**

### 9.1 当前现状

P0#2 第二波完成后：

- `HrDomainController` 已下线
- `HrDomainCrudService` 已下线
- 原始 `lookupSingleValue` 这一批 JdbcTemplate + Map 通用 CRUD 代码已不再是主路径

### 9.2 当前结论

P3-7 的原实施对象已经不存在，因此本项应改为**失效关闭**，而不是继续保留在待办列表里。

---

## 十、P3-8 OSS 签名 URL / 过期 / 租户隔离

> **状态：✅ 已完成（2026-05-20）**

### 10.1 本轮落地结果

本轮没有重复新增 presign 能力，而是把上传、持久化、访问三段链路统一收口到 auth 文件服务：

1. `cloudflow-auth` 新增受登录态保护的 `/auth/system/file/access`
2. `SysFileServiceImpl` 统一按 `sys_file.filePath` / 历史 `sys_file.url` 查找附件
3. 下载前追加 `UserContext.tenantId` 租户校验
4. LOCAL 文件走服务端直出；PRIVATE OSS 按需生成 presigned URL 后 302 跳转；公共 OSS 走直接重定向
5. 前端统一通过 `attachment.ts` 将 raw path / 历史 URL 规范成稳定内部访问地址
6. 新上传附件长期持久化 raw `filePath`，不再把 presigned URL 写入业务字段

### 10.2 前端收口范围

已同步改造：

- `FileUpload.tsx`
- `AttachmentLinks.tsx`
- `services/api/hr.ts`
- `pages/hr/HrRecruitmentPage.tsx`
- `pages/hr/HrEmployeeWorkspace.tsx`
- `BusinessTripPage.tsx`
- `ExpenseClaimPage.tsx`
- `PurchaseRequestPage.tsx`
- `PaymentRequestPage.tsx`
- `KnowledgePage.tsx`
- `TaskHandleModal.tsx`

### 10.3 当前结论

P3-8 已从“URL 是否签名”的单点问题，收口为：

- **访问入口统一**：附件统一走 auth 内部稳定地址
- **存储值稳定**：前端与业务表长期保存 raw `filePath`
- **权限前置**：访问前先过登录态与租户校验
- **历史兼容保留**：旧 `/upload/...`、旧 OSS URL、旧 access URL 均可继续访问

### 10.4 当前验证口径

- `cloudflow-auth` 模块编译通过
- 前端 `npm run type-check` 通过
- 历史 `split('/').pop()` 附件显示逻辑已从重点页面收口到统一 helper
- 新增稳定访问地址后，前端不再直接持久化 presigned URL

---

## 十一、P3-9 Prometheus 抓取认证

> **状态：✅ 已完成（2026-05-19）**

### 11.1 已完成部分

P2-14 已完成 **management 端口隔离**：

- 网关/认证/工作流/OA/CRM/HR 已分离到 **9100-9105**
- `docker/monitoring/prometheus/prometheus.yml` 已按这些端口抓取 `/actuator/prometheus`

本轮在此基础上继续收口认证：

- servlet 服务通过 `PrometheusManagementServletConfiguration` 仅对 `/actuator/prometheus` 增加 basic auth
- gateway 通过 `PrometheusScrapeReactiveFilter` 对独立 management 子上下文增加同口径保护
- Prometheus 抓取配置改为 `basic_auth + password_file`
- `docker-compose.yml` 为 6 个服务与 Prometheus 容器统一注入抓取密码

### 11.2 当前结论

P3-9 已不再保留剩余动作。当前主路径已实现：

> **management 独立端口 + metrics 抓取认证 + Prometheus 容器密码文件化**

### 11.3 当前验证口径

- 后端聚合编译通过
- `docker/monitoring/prometheus/prometheus.yml` 已包含 `basic_auth`
- `/actuator/health` 健康检查路径未被一并锁死
- 凭据不再硬编码在 Prometheus 配置文件正文中

---

## 十二、P3-10 OA `allow-anonymous-read` 生效性

> **状态：✅ 已完成（2026-05-19）**

### 12.1 本轮落地结果

本轮已选择“启用路径”完成收口：

- `SysAnnouncementController` 新增 `/announcement/public` 匿名只读接口
- 该接口受 `oaProperties.getAnnouncement().getAllowAnonymousRead()` 控制
- 仅返回 `scopeType = ALL`、`status = "1"`、未过期的公开公告
- `AuthFilter` 仅对白名单放行 `/oa/announcement/public` 与其只读子路径，不扩大其他 OA 匿名面

### 12.2 当前结论

P3-10 已从“配置已装载但无入口”收口为：

> **配置驱动的匿名公告只读入口已形成，且匿名面被限定在公开公告查询。**

### 12.3 当前验证口径

- 配置关闭时返回“当前租户未开放匿名公告访问”
- 配置开启时仅返回公开范围公告
- 管理接口、我的公告、已读标记等受权路径保持不变

---

## 十三、收口后状态

```text
P3 当前代码主线：已全部完成
剩余动作：仅在后续增量变更时同步本文与审计记录
```

---

## 十四、快速复核清单

```bash
# 1) HasPermission 运行时残留
grep -rn "@HasPermission\|HasPermissionAspect" cloudflow-backend/ --include="*.java"

# 2) Hystrix 残留
grep -rn "hystrix\|Hystrix" cloudflow-backend/ config/ --include="*.java" --include="*.yml" --include="*.yaml"

# 3) OA 匿名公告配置消费
grep -rn "allow-anonymous-read\|allowAnonymousRead" cloudflow-backend/ --include="*.java" --include="*.yml" --include="*.yaml"

# 4) Prometheus 抓取认证
grep -rn "basic_auth\|password_file\|cloudflow.security.prometheus" cloudflow-backend/ docker/monitoring/prometheus/ --include="*.java" --include="*.yml"

# 5) Seata 占位模块
grep -rn "cloudflow-common-seata\|SeataConfiguration" cloudflow-backend/ --include="*.xml" --include="*.java"
```

---

## 十五、CRM 模块整改联动说明

`docs/CRM-模块评估与整改方案.md` 仍可作为**历史背景**参考，但不再作为当前 P3 状态源。

后续判断 P3 是否完成，以以下状态源为准：

1. 当前代码主路径
2. `P0修复方案.md`
3. `P1修复方案.md`
4. `P2修复方案.md`
5. `后端审计报告.md` §13.4、§13.6

---

*文档版本：v2.1 — 2026-05-20。本文已改为状态化台账；截至本次刷新，P3 代码主线已全部收口，后续仅按增量维护对应条目与 `后端审计报告.md` §13.6。*