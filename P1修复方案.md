# CloudFlow Pro P1 风险修复方案

- 编写日期：2026-05-13
- 适用基线：`dev` 分支（P0 修复完成后启动）
- 关联文档：[后端审计报告.md](./后端审计报告.md) §13.2、[P0修复方案.md](./P0修复方案.md)
- 修复范围：16 项 P1 风险（去重后）

> **说明**：P1 是 **短期内必做** 的治理项，不会立即引发数据外泄或 RCE，但若长期不处理会演变为 P0。建议 P0 第一波/第二波完成后，立即推进 P1。

---

## 一、修复优先级与建议顺序

按 **业务影响 × 改动成本 × 是否阻塞后续工作** 排序：

| 波次 | 项目 | 工期 | 阻塞关系 |
|---|---|---|---|
| **第一波（基础设施）** | P1-1 common-sensitive 自动化 | 1 天 | 阻塞 P1-5 CRM 脱敏 |
| | P1-7 TokenService 包名修正 | 0.5 天 | 无 |
| | P1-4 删除 Hystrix 死配置 | 0.5 天 | 阻塞 P1-3 引入 Sentinel |
| | P1-14 配置双源收敛 | 1 天 | 无 |
| **第二波（业务能力）** | P1-2 业务规则 Fallback 分级 | 1 天 | 无 |
| | P1-3 定时任务 @DistributedJob | 1 天 | 多副本部署前必做 |
| | P1-9 权限注解收敛 | 1.5 天 | 阻塞 P2-9 startProcess 权限 |
| | P1-8 滑块 passToken 与 IP 绑定 | 0.5 天 | 无 |
| **第三波（业务模块）** | P1-5 CRM 敏感字段加密+脱敏 | 2 天 | 依赖 P1-1 |
| | P1-13 CRM 数据权限 | 1.5 天 | 无 |
| | P1-10 workplace/summary 缓存 | 1 天 | 无 |
| | P1-11 ErrorReport 白名单+限流 | 0.5 天 | 无 |
| | P1-12 Sync deviceId 绑定 | 1 天 | 无 |
| **第四波（部署/运维）** | P1-6 多环境隔离 | 2 天 | 无 |
| | P1-15 容器资源限制提升 | 0.5 天 | 无 |
| | P1-16 文档对齐 | 0.5 天 | 无 |

总工期约 **15 个工作日**（约 3 周）。

---

## 二、P1-1 common-sensitive 模块自动化

### 2.1 问题描述

`cloudflow-common-sensitive` 当前**只有 `SensitiveUtils` 一个工具类**，无注解、无 Jackson Serializer、无 AutoConfiguration。意味着：

- 业务侧返回脱敏需要手动调 `SensitiveUtils.maskObject(vo)`
- HR 第一波加固只能用列名硬编码（脆弱），CRM 完全没做
- 大概率在新增字段时被遗忘

### 2.2 影响范围

- `cloudflow-common-sensitive` 模块（补齐 3 个文件）
- 间接影响所有业务服务的 Controller 返回链路

### 2.3 修复思路

补齐 4 件套：**注解 + Serializer + AutoConfiguration + 集成测试**

```
cloudflow-common-sensitive/
├── annotation/Sensitive.java            (字段级注解)
├── strategy/SensitiveStrategy.java      (枚举：ID_CARD/PHONE/BANK/EMAIL/ADDRESS/CUSTOM)
├── serializer/SensitiveJsonSerializer.java  (Jackson 序列化器)
├── config/SensitiveAutoConfiguration.java   (Spring Boot 自动装配)
└── utils/SensitiveUtils.java            (已存在)
```

### 2.4 修复步骤

1. **新增 `@Sensitive` 注解**：
   ```java
   @Target(ElementType.FIELD)
   @Retention(RetentionPolicy.RUNTIME)
   @JacksonAnnotationsInside
   @JsonSerialize(using = SensitiveJsonSerializer.class)
   public @interface Sensitive {
       SensitiveStrategy strategy();
       String pattern() default "";  // CUSTOM 策略使用
   }
   ```

2. **实现 `SensitiveJsonSerializer`**：
   ```java
   public class SensitiveJsonSerializer extends JsonSerializer<String>
           implements ContextualSerializer {
       private SensitiveStrategy strategy;
       @Override
       public void serialize(String value, JsonGenerator gen, SerializerProvider sp) {
           gen.writeString(strategy.mask(value));
       }
       @Override
       public JsonSerializer<?> createContextual(SerializerProvider sp, BeanProperty prop) {
           Sensitive ann = prop.getAnnotation(Sensitive.class);
           SensitiveJsonSerializer s = new SensitiveJsonSerializer();
           s.strategy = ann.strategy();
           return s;
       }
   }
   ```

3. **AutoConfiguration 注册 + 暴露开关**：
   ```yaml
   cloudflow:
     sensitive:
       enabled: true
       skip-roles: [admin, hr_admin]   # 特权角色不脱敏
   ```

4. **改造 HR/CRM VO**：
   ```java
   @Sensitive(strategy = SensitiveStrategy.ID_CARD)
   private String idCard;
   ```

### 2.5 验证方式

- 单元测试：调用 `objectMapper.writeValueAsString(vo)`，确认字段已脱敏
- 集成测试：用普通员工 token 调 `/hr/employees/1`，返回 `idCard: "330***********1234"`
- 用 admin 角色调相同接口，返回原文

### 2.6 风险

- 序列化器仅作用于响应 JSON，DB 中仍是原文 — 该问题由 `@EncryptField` 负责
- 特权角色判断需配合 `UserContext`，跨线程场景需 `ContextTaskDecorator` 透传

---

## 三、P1-2 业务规则 Fallback 策略分级

### 3.1 问题描述

`BusinessRuleClientFallback` 默认统一 PASS（放行），不符合合规风控的"宁可错杀"原则。规则中心宕机时：

- `oa.contract.risk.threshold` 失效 → 高风险合同直接通过审批
- `hr.leave.quota.limit` 失效 → 员工可超额请假

### 3.2 修复思路

按 ruleCode 分类配置 fallback 行为：**PASS（放行）/ WARN（放行但告警）/ DENY（阻断）**

```yaml
cloudflow:
  business-rule:
    fallback-policy:
      hr.leave.quota.limit: PASS         # 业务体验优先
      oa.expense.amount.limit: WARN      # 放行但发邮件给财务
      oa.contract.risk.threshold: DENY   # 合同风险必须阻断
      default: WARN                      # 兜底
```

### 3.3 修复步骤

1. **新增 `RuleFallbackPolicy` 枚举**：`PASS / WARN / DENY`
2. **改造 `BusinessRuleClientFallback`**：
   ```java
   @Component
   public class BusinessRuleClientFallback implements BusinessRuleClient {
       @Autowired private BusinessRuleProperties props;
       @Override
       public R<RuleResult> getEffective(String ruleCode) {
           RuleFallbackPolicy policy = props.getFallbackPolicy()
               .getOrDefault(ruleCode, props.getDefaultPolicy());
           log.warn("[BusinessRule] 规则中心降级 | ruleCode={} policy={}", ruleCode, policy);
           switch (policy) {
               case DENY: return R.fail("RULE_CENTER_DOWN");
               case WARN:
                   alertService.notify("规则中心宕机", ruleCode);
                   return R.ok(RuleResult.pass());
               default: return R.ok(RuleResult.pass());
           }
       }
   }
   ```
3. **Sentry/邮件告警接入**：WARN 策略触发时发告警

### 3.4 验证方式

- 故意停掉 auth 服务，HR 请假接口仍能调用（PASS）
- 同时 OA 合同提交接口返回 400（DENY）
- 邮件/Sentry 收到告警

---

## 四、P1-3 工作流定时任务 @DistributedJob

### 4.1 问题描述

`cloudflow-service-workflow` 多副本部署时，**所有副本同时执行**：
- `TimerSchedulerService` 定时器调度
- `AnomalyDetectionServiceImpl` 异常检测
- `TimeoutDetectionServiceImpl` 超时检测
- `ProcessMonitorServiceImpl` 监控聚合

后果：**重复扫描 / 重复告警 / 重复消息推送**。

### 4.2 修复步骤

1. 全量梳理 `@Scheduled` 注解位置：
   ```bash
   grep -rn "@Scheduled" cloudflow-backend/cloudflow-service-workflow/
   ```
2. 每个 `@Scheduled` 方法加 `@DistributedJob`：
   ```java
   @DistributedJob(name = "anomaly-detect", lockExpireSeconds = 300)
   @Scheduled(cron = "0 */5 * * * ?")
   public void detectAnomaly() { ... }
   ```
3. 同时审计 OA 服务的 `@EnableScheduling` 调用点
4. 应用启动时打印分布式锁竞争结果，便于排查

### 4.3 验证方式

启 2 个 workflow 副本，定时任务到点后查看日志：
- 副本 A：`[anomaly-detect] 抢锁成功，开始执行`
- 副本 B：`[anomaly-detect] 抢锁失败，跳过本次执行`

---

## 五、P1-4 删除 Hystrix 死配置

### 5.1 问题描述

`feign.hystrix.enabled: true` + Hystrix timeout 配置在 5 个业务服务的 yaml 中存在，但 **Spring Cloud 2023 已移除 Hystrix 支持**，配置完全无效，仅是 boilerplate 噪音。

### 5.2 修复步骤

1. **删除死配置**：
   ```bash
   grep -rl "feign.hystrix\|hystrix.command" config/ cloudflow-backend/*/src/main/resources/
   # 对每个文件移除相关 yaml 块
   ```
2. **引入 Resilience4j**（推荐，与 Spring Cloud 2023 兼容更好）：
   ```xml
   <dependency>
       <groupId>org.springframework.cloud</groupId>
       <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
   </dependency>
   ```
3. 在 Feign Client 上加 `@CircuitBreaker(name = "auth-service")`
4. 配置熔断参数：
   ```yaml
   resilience4j:
     circuitbreaker:
       instances:
         auth-service:
           failure-rate-threshold: 50
           wait-duration-in-open-state: 30s
   ```

### 5.3 验证方式

- 配置扫描：`grep -r "hystrix" config/ cloudflow-backend/` 期望 0 匹配
- 故意让 auth 服务返回 500，调 5 次后熔断开启，第 6 次走 fallback

---

## 六、P1-5 CRM 敏感字段加密+脱敏

### 6.1 问题描述

CRM `oa_crm_customer` 表的 `credit_code`（统一社会信用代码）、`mobile`、`email`，以及 `oa_crm_contact.mobile/email` 全部明文存储，接口返回也未脱敏。

### 6.2 修复步骤（依赖 P1-1 完成）

1. **Entity 标注 `@EncryptField`**：
   ```java
   @TableName("oa_crm_customer")
   public class CrmCustomer {
       @EncryptField private String creditCode;
       @EncryptField private String mobile;
       @EncryptField private String email;
   }
   ```
2. **VO 标注 `@Sensitive`**：
   ```java
   public class CrmCustomerVO {
       @Sensitive(strategy = SensitiveStrategy.CREDIT_CODE)
       private String creditCode;
       @Sensitive(strategy = SensitiveStrategy.PHONE)
       private String mobile;
       @Sensitive(strategy = SensitiveStrategy.EMAIL)
       private String email;
   }
   ```
3. **数据迁移**：写 Spring Boot 命令行工具，把存量 `credit_code` / `mobile` / `email` 批量加密（参考 P0-2 §8.4.5）

### 6.3 验证方式

- DB 查询：`SELECT credit_code FROM oa_crm_customer LIMIT 5;` 返回 Base64 密文
- 接口调用：`GET /crm/customer/1`，返回 `mobile: "138****1234"`
- admin 角色调用同接口，返回原文（取决于 skip-roles 配置）

---

## 七、P1-6 多环境隔离

### 7.1 问题描述

- 全部服务 `spring.profiles.active=dev` 默认
- `config/` 下只有 prod 风格 yaml（地址 192.168.1.173）
- Nacos 单 namespace，prod/dev 共用 Data ID

### 7.2 修复思路

**两个维度隔离**：

1. **Nacos namespace 拆分**：dev / test / staging / prod 四个命名空间
2. **Spring Profile 化配置**：`cloudflow-{service}-{profile}.yaml`

### 7.3 修复步骤

1. **Nacos 控制台创建 namespace**：
   ```
   namespace-id           name
   cloudflow-dev          开发环境
   cloudflow-test         测试环境
   cloudflow-staging      预发环境
   cloudflow-prod         生产环境
   ```
2. **配置文件拆分**：
   ```
   config/
   ├── shared/                              (跨环境共享)
   │   └── cloudflow-common.yaml            (DataSource URL 占位、Sa-Token 通用)
   ├── dev/
   │   ├── cloudflow-common.yaml            (DB 地址、密码占位)
   │   ├── cloudflow-auth.yaml
   │   └── ...
   ├── test/...
   └── prod/...
   ```
3. **应用启动指定 namespace**：
   ```yaml
   spring:
     cloud:
       nacos:
         config:
           namespace: ${NACOS_NAMESPACE:cloudflow-dev}
   ```
4. **Docker Compose 注入环境**：
   ```yaml
   environment:
     SPRING_PROFILES_ACTIVE: ${ENV:-dev}
     NACOS_NAMESPACE: cloudflow-${ENV:-dev}
   ```

### 7.4 验证方式

- dev 环境启动，日志显示 `Loading config from namespace=cloudflow-dev`
- 修改 dev 的配置不影响 prod

---

## 八、P1-7 TokenService 等错位包名修正

### 8.1 问题描述

历史搬迁未同步重命名，导致 IDE 跳转迷惑：

| 类 | 声明包 | 实际所在模块 |
|---|---|---|
| `TokenService` | `com.cloudflow.common.core.utils` | `cloudflow-common-security` |
| `RedisCache` | `com.cloudflow.common.core.utils` | `cloudflow-common-redis` |
| `RedisStreamUtil` | `com.cloudflow.common.core.utils` | `cloudflow-common-redis` |
| `SysConfigHelper` | `com.cloudflow.common.core.utils` | `cloudflow-common-redis` |
| `MyMetaObjectHandler` | `com.cloudflow.common.handler` | `cloudflow-common-data` |
| `TenantRedisCacheManager` | `com.cloudflow.common.cache` | `cloudflow-common-redis` |
| `MybatisPlusConfig` | `com.cloudflow.common.config` | `cloudflow-common-data` |
| `RedisConfig` | `com.cloudflow.common.config` | `cloudflow-common-redis` |

### 8.2 修复步骤

1. **逐个类移动 + 重命名包**：
   - `TokenService`: `com.cloudflow.common.core.utils` → `com.cloudflow.common.security.token`
   - `RedisCache`: → `com.cloudflow.common.redis.cache`
   - ...（按模块归属重命名）
2. **IDE Refactor → Move + Update References** 自动改所有 import
3. **跨模块编译验证**：`mvn clean install -pl cloudflow-common -am`
4. **运行回归测试**

### 8.3 风险

- 影响所有业务服务的 import 语句，PR diff 量大但纯机械变更
- 必须用 IDE 的 Refactor 功能，不能手动改

---

## 九、P1-8 滑块 passToken 与 IP/请求来源绑定

### 9.1 问题描述

`SliderPuzzleUtil` 校验通过后生成 passToken，但 **passToken 未与 username/IP 绑定**：

- A 用户从 IP1 拿到 passToken
- B 用户从 IP2 直接拿这个 passToken 调登录
- 当前会通过校验，绕过滑块防爆破

### 9.2 修复步骤

1. **校验时同时记录 IP**：
   ```java
   public PassTokenInfo verifyCaptcha(String captchaKey, int x, HttpServletRequest req) {
       // ... 验证滑块逻辑 ...
       String passToken = IdUtils.simpleUUID();
       PassTokenInfo info = new PassTokenInfo(passToken, IpUtils.getIp(req), Instant.now());
       redisTemplate.opsForValue().set("PASS_TOKEN:" + passToken,
           JSONUtil.toJsonStr(info), 30, TimeUnit.SECONDS);
       return info;
   }
   ```
2. **登录时校验 IP 一致**：
   ```java
   public LoginVO login(LoginBody body, HttpServletRequest req) {
       PassTokenInfo info = getPassTokenInfo(body.getPassToken());
       if (info == null || !info.getIp().equals(IpUtils.getIp(req))) {
           throw new ServiceException("CAPTCHA_INVALID");
       }
       // ... 后续登录 ...
   }
   ```

### 9.3 验证方式

- 同 IP 验证 → 登录通过
- 换 IP 用同 passToken 登录 → 返回"验证码无效"

---

## 十、P1-9 权限注解收敛

### 10.1 问题描述

3 套权限校验机制并存：

| 机制 | 使用位置 | 异常类型 |
|---|---|---|
| `@HasPermission`（自研） | auth 服务部分 Controller | `RuntimeException` |
| `@SaCheckPermission`（Sa-Token） | workflow 部分 Controller | `SaTokenException` |
| 无注解 | OA/CRM/HR 大量 Controller | 依赖前端隐藏 |

### 10.2 修复思路

**统一到 Sa-Token 标准注解** + **废弃 `@HasPermission`**：

1. Sa-Token 是社区标准，文档完善
2. `CloudFlowStpInterface` 已提供权限源
3. 异常类型规范

### 10.3 修复步骤

1. **全量扫描 `@HasPermission`**：
   ```bash
   grep -rn "@HasPermission" cloudflow-backend/ --include="*.java"
   ```
2. **逐个替换为 `@SaCheckPermission`**：
   ```java
   // 改前
   @HasPermission("system:user:add")
   // 改后
   @SaCheckPermission("system:user:add")
   ```
3. **删除 `HasPermissionAspect` 与注解定义**
4. **`GlobalExceptionHandler` 增加 `SaTokenException` 处理**（若未覆盖）

### 10.4 验证方式

- 无权限用户调高敏接口 → 返回 403，body 含 `code: 403, msg: "无权限"`
- `git grep @HasPermission` 期望 0 匹配

---

## 十一、P1-10 workplace/summary 聚合接口短期缓存

### 11.1 问题描述

`/oa/workplace/summary` 一次聚合 10+ 张表（待办、抄送、公告、日程、会议室、HR 提醒、合同/车辆/证照/资产风险）。

- 移动端首屏延迟敏感
- 每个登录用户进首页触发 1 次，无缓存

### 11.2 修复思路

**Redis 短期缓存（30s）+ 用户行为驱动失效**：

```java
@Service
public class WorkplaceService {
    @Cacheable(value = "workplace:summary", key = "#userId", unless = "#result == null")
    public WorkplaceSummaryVO summary(Long userId) {
        // 原聚合逻辑
    }
}
```

`cloudflow-common-redis` 已提供 `TenantRedisCacheManager`，自动追加租户前缀。

### 11.3 修复步骤

1. `WorkplaceService` 加 `@Cacheable` 注解
2. 在产生新待办/新公告/新风险时，触发 `@CacheEvict`
3. 配置 TTL：
   ```yaml
   spring:
     cache:
       redis:
         time-to-live: 30s
   ```

### 11.4 验证方式

- 首次调用 P95 = 800ms
- 30s 内重复调用 P95 < 20ms（Redis 命中）
- 新建公告后立即调用 → 缓存失效，重新聚合

---

## 十二、P1-11 ErrorReport 加白名单 + 限流

### 12.1 问题描述

前端 `/oa/error-report` 用于捕获 JS 异常，但目前必须登录才能调用 → **登录页 JS 报错无法上报**（用户最需要捕获的恰恰是登录前异常）。

### 12.2 修复步骤

1. **网关白名单**：
   ```yaml
   # cloudflow-gateway.yaml AuthFilter 白名单
   - /oa/error-report
   ```
2. **IP 限流**（防垃圾）：
   ```java
   @RateLimiter(key = "error-report", type = LimitType.IP, count = 30, time = 60)
   @PostMapping("/oa/error-report")
   public R<Void> report(@RequestBody ErrorReportDTO dto, HttpServletRequest req) {
       dto.setIp(IpUtils.getIp(req));
       errorReportService.save(dto);
       return R.ok();
   }
   ```
3. **存储字段裁剪**：限制 stack trace 长度 < 8KB，防巨型 payload

### 12.3 验证方式

- 未登录调 `/oa/error-report` → 200 成功
- 同 IP 1 分钟内调 31 次 → 第 31 次返回 429

---

## 十三、P1-12 SyncController deviceId 绑定 + 冲突算法

### 13.1 问题描述

`POST /sync/upload` 完全依赖客户端传入的 `deviceId`，无注册/绑定。后果：
- 多人共用同一 deviceId → 数据交叉污染
- 冲突解决算法未公开说明（最后写入胜出？版本号？CRDT？）

### 13.2 修复步骤

1. **新增 `oa_sync_device` 表**：
   ```sql
   CREATE TABLE oa_sync_device (
       id BIGINT AUTO_INCREMENT PRIMARY KEY,
       device_id VARCHAR(64) NOT NULL,
       user_id BIGINT NOT NULL,
       tenant_id BIGINT NOT NULL,
       device_name VARCHAR(128),
       last_sync_time DATETIME,
       UNIQUE KEY uk_device_user (device_id, user_id, tenant_id)
   );
   ```
2. **首次同步必须先注册**：`POST /sync/register-device` 返回 deviceToken
3. **冲突算法显式化**：
   ```yaml
   cloudflow:
     oa:
       sync:
         conflict-strategy: LAST_WRITE_WINS   # 或 VERSION_VECTOR
         time-tolerance-seconds: 5
   ```
4. **lastSyncTime 改为时间戳 BIGINT**：避免时区/格式问题

### 13.3 验证方式

- 未注册 deviceId 调 `/sync/upload` → 403 + "DEVICE_NOT_REGISTERED"
- 同一用户两设备 deviceId 不同 → 数据互不污染

---

## 十四、P1-13 CRM 数据权限对接

### 14.1 问题描述

CRM `oa_crm_customer` 等表仅靠 `owner_id` 字段+SQL 实现数据权限，未通过 `RedisDataScopeHandle` 校验 `dsType`。

后果：销售 A 通过 URL 改 `customerId` 可看到销售 B 的私单。

### 14.2 修复步骤

1. **Entity 加 `@DataScope` 注解**（MyBatis-Plus 数据权限拦截器自动处理）：
   ```java
   @DataScope(deptAlias = "dept_id", userAlias = "owner_id")
   @TableName("oa_crm_customer")
   public class CrmCustomer { ... }
   ```
2. **Service 查询前确认拦截器生效**：
   ```java
   public PageResult<CrmCustomerVO> page(CrmQuery q) {
       // MyBatis-Plus DataScopeInnerInterceptor 自动追加 WHERE owner_id = ? OR dept_id IN (...)
       return baseMapper.selectPage(...);
   }
   ```
3. **Controller 加显式权限码**（结合 P0-6）：
   ```java
   @SaCheckPermission("crm:customer:list")
   ```

### 14.3 验证方式

- 销售 A 登录，调 `/crm/customer/list` → 仅返回自己 owner 的客户
- 销售 A 调 `/crm/customer/{B-customer-id}` → 返回 404
- manager 角色（dsType=2 本级及下级）→ 看到下属销售的全部客户

---

## 十五、P1-14 配置双源收敛

### 15.1 问题描述

每个服务同时加载 Nacos yaml + 本地 `application.yml`，存在覆盖关系混乱：

- gateway 本地 `is-read-cookie: true` vs Nacos `is-read-cookie: false`
- DB URL `useSSL` 在两份 yaml 中相反

### 15.2 修复思路

**Nacos 为唯一真相源**，本地 `application.yml` 只保留 fallback 极简配置（启动元信息）：

```yaml
# cloudflow-{service}/src/main/resources/application.yml （仅保留）
spring:
  application:
    name: cloudflow-{service}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:127.0.0.1:8848}
      namespace: ${NACOS_NAMESPACE:cloudflow-dev}
      username: ${NACOS_USERNAME}
      password: ${NACOS_PASSWORD}
server:
  port: ${SERVER_PORT:9001}
```

其他配置（DataSource / Redis / Sa-Token / 业务参数）全部进 Nacos。

### 15.3 修复步骤

1. 备份当前 `application.yml`
2. 逐服务精简到上述模板
3. 把删除的内容补充到 Nacos 对应 yaml
4. 启动验证

### 15.4 验证方式

- 删除本地 application.yml 中的 DB 配置，启动仍正常（从 Nacos 加载）
- Nacos 配置项变更，应用自动 refresh

---

## 十六、P1-15 容器资源限制提升

### 16.1 问题描述

| 服务 | 当前限制 | 实际负载 |
|---|---|---|
| workflow | 0.5-1.0 CPU / 0.5-1G | 41 张表 + 多线程异步 + 定时任务，**易 OOM** |
| HR | 0.25-0.5 CPU / 0.25-0.5G | 132 端点 + Map 序列化频繁，**GC 压力大** |

### 16.2 修复步骤

修改 `docker-compose.yml`：

```yaml
workflow:
  deploy:
    resources:
      limits:
        cpus: '2.0'        # 0.5-1.0 → 1.0-2.0
        memory: 2G         # 0.5-1G → 1G-2G
      reservations:
        cpus: '1.0'
        memory: 1G

hr:
  deploy:
    resources:
      limits:
        cpus: '1.0'        # 0.25-0.5 → 0.5-1.0
        memory: 1G         # 0.25-0.5G → 0.5-1G
      reservations:
        cpus: '0.5'
        memory: 512M
```

同步调整 JVM：
```dockerfile
ENTRYPOINT ["java",
    "-XX:+UseContainerSupport",
    "-XX:MaxRAMPercentage=70.0",     # 75 → 70（预留 30% 给堆外）
    "-XX:+UseG1GC",
    "-XX:MaxGCPauseMillis=200",
    ...]
```

### 16.3 验证方式

- 压测 workflow `POST /start` × 1000 并发，期望 P95 < 500ms 且无 OOM
- `docker stats` 查看实际 CPU/内存使用

---

## 十七、P1-16 文档对齐

### 17.1 问题描述

- README 中表数量与 SQL 实测偏差较大（OA 实际 52 张 vs README 36；HR 实际 37 张 vs README 43）
- 排序规则不一致（建库 `utf8mb4_unicode_ci` vs 连接串 `utf8mb4_0900_ai_ci`）

### 17.2 修复步骤

1. **重新统计建表语句**：
   ```bash
   for f in DB/*.sql; do
       echo "=== $f ==="
       grep -c "CREATE TABLE" "$f"
   done
   ```
2. **更新 README** 表数量与模块说明
3. **统一排序规则**：选 `utf8mb4_0900_ai_ci`（MySQL 8 默认），所有 SQL 与连接串对齐
4. **DB 历史迁移**（可选）：
   ```sql
   ALTER DATABASE cloud_flow_db CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
   ALTER TABLE hr_employee CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
   ...
   ```

### 17.3 验证方式

- README 表数量与 `grep -c "CREATE TABLE"` 结果一致
- `SHOW CREATE DATABASE cloud_flow_db;` 返回 `utf8mb4_0900_ai_ci`

---

## 十八、总修复时间线

```
Week 1 (第一波 - 基础设施):
  Day 1   P1-7 包名修正 + P1-4 删 Hystrix
  Day 2-3 P1-1 common-sensitive 自动化
  Day 4-5 P1-14 配置双源收敛

Week 2 (第二波 - 业务能力):
  Day 1   P1-2 Fallback 分级
  Day 2   P1-3 @DistributedJob
  Day 3-4 P1-9 权限注解收敛
  Day 5   P1-8 passToken IP 绑定

Week 3 (第三波 - 业务模块):
  Day 1-2 P1-5 CRM 加密+脱敏
  Day 3-4 P1-13 CRM 数据权限
  Day 5   P1-10 workplace 缓存 + P1-11 ErrorReport 白名单

Week 4 (第四波 - 部署/收尾):
  Day 1   P1-12 SyncController deviceId
  Day 2-3 P1-6 多环境隔离
  Day 4   P1-15 容器资源 + P1-16 文档对齐
  Day 5   全量验证 + PR 合并
```

---

## 十九、配套机制

1. **CI 加固**：
   - 提交时扫描 `Juwangkeji` / 弱密码模式（已在 P0-1）
   - 提交时检查 `@HasPermission` 使用 → 报警（P1-9 完成后）
   - 提交时检查 Hystrix 配置残留（P1-4 完成后）
2. **告警规则**：
   - `BusinessRule fallback triggered` 告警频率
   - `DistributedJob lock contention` 告警
   - `ErrorReport rate limit hit` 频率
3. **审计报告同步**：每完成一项，在 `后端审计报告.md` §13.6 修复记录表追加一行

---

## 二十、附录：快速命令清单

```bash
# 扫描 Hystrix 死配置
grep -rn "hystrix" config/ cloudflow-backend/ --include="*.yml"

# 扫描 @HasPermission 使用
grep -rn "@HasPermission" cloudflow-backend/ --include="*.java"

# 扫描包名错位的类
grep -rn "package com.cloudflow.common.core.utils" cloudflow-backend/cloudflow-common/ --include="*.java"

# 扫描 @Scheduled 未加 @DistributedJob
grep -B2 "@Scheduled" cloudflow-backend/ -r --include="*.java" | grep -v "@DistributedJob"

# CRM 敏感字段查询
grep -rn "credit_code\|mobile\|email" cloudflow-backend/cloudflow-service-crm/

# 文档表数量统计
grep -c "CREATE TABLE" DB/*.sql
```

---

## 二十一、CRM 模块整改阶段 1–3 联动（2026-05-13 已落地）

CRM 模块整改阶段 1–3（见 `docs/CRM-模块评估与整改方案.md`）已落地。与本 P1 文档的关系：

### 21.1 直接关系

| P1 条目 | 联动情况 |
|---|---|
| P1-1 common-sensitive 自动化 | CRM 阶段 1 已将敏感字段从散落字符串抽出到 `CrmConstants`（见 `cloudflow-service-crm/.../constant/CrmConstants.java`），待 P1-1 落地后即可通过 `@Sensitive` 注解直接挂字段 |
| P1-5 CRM 敏感字段加密+脱敏 | **强相关**。阶段 1 完成的巨型 Service 拆分（1080 行 → 4 个类）是 `@EncryptField` 落地的前置条件——现在客户 CRUD、工作台聚合、OA 集成三者分离，后续加密链路改造只需动 `CrmCustomerServiceImpl` 与 `CrmCustomer` 实体 |
| P1-9 权限注解收敛 | CRM 阶段 3 新增 `/approval/*`、`/handover-task/*` 接口**当前未加权限码**，应与 P1-9 收敛为 `@SaCheckPermission` 统一标记 |
| P1-13 CRM 数据权限 | 阶段 2 已把 CRM 客户归属与员工/部门联动（`RemoteHrService.getEmployeeByUserId`），为后续对接 `RedisDataScopeHandle` 打下基础；阶段 2 的离职交接（`oa_crm_handover_task`）让 ownerId 变更不再"静默"，利于数据权限审计 |
| P1-14 配置双源收敛 | 无直接关系 |

### 21.2 阶段 3 新增的"自动化链路"对运维的提示

| 自动化 | 触发条件 | 新增点 |
|---|---|---|
| 商机赢单 → 合同草稿 | `CrmOpportunityService.winOpportunity` | 发 `crm:stream:events` 的 `OpportunityWon` 事件；调 `RemoteOaService.createContract` |
| 合同审批通过 → 项目 + 预算 | `OaContract.sourceType` 以 `CRM_` 开头 | OA `ContractApprovalHandler` 广播 `ContractApproved`；CRM `CrmContractApprovedEventHandler` 消费 |
| 回款确认 → 发票绑定 | `CrmReceivable.invoiceId` 非空 | CRM `CrmReceivableConfirmedEventHandler` 调 `RemoteOaService.bindInvoice` |
| 员工离职 → 交接待办 | HR `LifecycleApplication` 的 RESIGNATION 流转到 EFFECTIVE/COMPLETED | HR → Stream `hr:stream:employee-left`；CRM 消费后写 `oa_crm_handover_task` |

上述链路均为"失败即告警、不阻塞主流程"设计。建议在 P1 交付时把它们纳入监控：Prometheus 增加 `crm_stream_consume_failure_total`、`hr_employee_left_publish_failure_total` 两个指标。

### 21.3 时间线

不占用 P1 的任何一波——阶段 1–3 是 CRM 独立演进。P1-5 / P1-9 / P1-13 开展时，建议复用阶段 1 抽出的 `CrmConstants` 与拆分后的 Service 层作为落点。

> 本节仅作进度同步。详细内容见 `后端审计报告.md` §8.10、§9.11 与 §13.6。

---

*文档版本：v1.0 — 2026-05-13。每完成一项 P1，请同步更新本文档对应章节为"✅ 已完成"并在审计报告 §13.6 追加修复记录。*
