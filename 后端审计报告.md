# CloudFlow Pro 后端审计报告

- 审计日期：2026-05-12（首版），2026-05-13（增补：P0#2 / P0#3 修复）
- 审计范围：`cloudflow-backend/` 全量代码、`config/` Nacos 配置、`docker-compose.yml`、`DB/` 数据库脚本
- 审计基线：当前 `dev` 分支（commit `e9bbe8c6` + 本次 P0#2/P0#3 增量提交）
- 后端 Maven 版本：`1.0.0`

> **修复状态摘要（2026-05-13）**
> - ✅ P0#3 网关 CORS 收敛已落地：见 §四、§十一、§十三的对应章节标注
> - ✅ P0#2 HR 第一波加固已落地：见 §九"9.10 已完成的第一波加固"
> - ⏳ 其余 P0 仍待处理

---

## 一、执行摘要

CloudFlow Pro 后端是一个 Spring Boot 3.2.6 + Spring Cloud 2023.0.1 + Spring Cloud Alibaba 2023.0.1.0 的多模块微服务工程，由 **网关 + 认证 + 4 个业务服务 + 16 个公共子模块** 组成，对应 6 个独立部署单元，共 **978 个 Java 文件、84 个 Controller**，所有服务共享单一 MySQL 库 `cloud_flow_db`。

总体结论：

| 维度 | 评估 |
|---|---|
| 模块化与组件拆分 | 良好——网关 / 认证 / 业务三层清晰，common 拆分粒度合理 |
| 工作流引擎完整度 | 较高——14 个 Controller，41 张表覆盖定义/实例/任务/会签/委派/版本/发布/监控/归档 |
| OA 业务覆盖广度 | 高——31 个 Controller 覆盖公告、行政、资产、合同、印章、证照、知识库等 |
| **HR 服务实现质量** | **严重不足**——单文件 132 个端点、`Map<String,Object>` 透传、无 DTO、无脱敏、无字段级权限 |
| 多租户机制 | 行级 tenant_id 拦截器，覆盖度较好但部分通用 SQL 旁路 MyBatis |
| 安全配置 | **明文 DB/Redis 密码硬编码于 Nacos 配置文件中**；CORS `allowed-origin-patterns:"*"` 配合 `allow-credentials:true` |
| 测试与质量门禁 | Maven enforcer + dependency-analyze 已配置；自动化测试覆盖低 |
| 监控可观测性 | Prometheus + Grafana 已就绪，应用侧 Actuator 暴露完整 |

**Top 风险（按优先级）**：

1. **Nacos 配置文件 `cloudflow-common.yaml`/`cloudflow-auth.yaml` 等明文写入生产 DB/Redis 密码 `Juwangkeji@2025`** — 已进入 git 历史，必须立即轮换并切换到密文或环境变量。
2. ✅ **[2026-05-13 已修复]** ~~HR 服务核心实现 `HrDomainCrudService` 是基于 JdbcTemplate + Map 的通用 CRUD，132 个端点未做字段级校验/脱敏，员工薪酬/社保/个税/合同表通过 HTTP 直接读写，越权与敏感数据外泄风险高。~~ —— 第一波加固已落地：class-level `@SaCheckLogin`、高敏薪税表写白名单 + 角色校验、基于 `UserContext.dsType/dsDeptIds` 的数据权限过滤、按列名脱敏返回。详见 §9.10。**仍需后续：拆 Controller、改 DTO/VO、切回 MyBatis-Plus 路径以恢复 `@EncryptField`/审计能力。**
3. ✅ **[2026-05-13 已修复]** ~~网关全局 CORS 配置 `allowed-origin-patterns: "*"` + `allow-credentials: true`，不符合 W3C 规范同时存在 CSRF 与凭证盗取风险。~~ —— 改为环境变量 `CLOUDFLOW_CORS_ALLOWED_ORIGINS` 驱动的白名单，默认仅放行 `http://localhost:3000`、`http://localhost:5173`、`https://localhost`、`http://localhost`，docker-compose 已注入对应环境变量。
4. **MySQL/Redis 密码在 `docker-compose.yml` 中的默认值（`cloudflow_2026` / `cloudflow_redis_2026`）与 Nacos 中的密码（`Juwangkeji@2025`）不一致**，意味着 docker compose 启动后服务连不上 DB，说明编排未与配置中心同步演练过。
5. **应用容器统一以 MySQL `root` 用户连接业务库**，缺乏最小权限隔离；Nacos `NACOS_AUTH_TOKEN` 默认值为示例 SecretKey，未强制要求覆盖。

---

## 二、整体架构

### 2.1 模块拓扑

```
              ┌──────────────────┐
              │   前端 :3000     │   React 19 + Vite 6 + PWA
              └────────┬─────────┘
                       │  /api  /ws
              ┌────────▼─────────┐
              │  Gateway :9000   │   Spring Cloud Gateway (WebFlux)
              │  AuthFilter      │   - Bearer Token 校验 (Sa-Token + Redis)
              │  RateLimit       │   - 删除 X-User-* 伪造头
              │  TenantWebFilter │   - 全局 CORS / 异常 / 路由
              └────┬─┬─┬─┬─┬─────┘
        ┌─────────┘ │ │ │ └──────────┐
        │           │ │ └────────┐   │
   ┌────▼────┐ ┌────▼─┐ ┌──▼──┐ ┌▼───┐ ┌▼───┐
   │ Auth    │ │Wf    │ │ OA  │ │CRM │ │HR  │
   │ :9001   │ │:9002 │ │:9003│ │:9004│ │:9005│
   └────┬────┘ └──┬───┘ └──┬──┘ └──┬─┘ └──┬─┘
        │         │        │       │      │
        └─────────┴────────┴───────┴──────┘
                          │
              ┌───────────┼───────────┐
        ┌─────▼─────┐ ┌───▼──┐  ┌─────▼─────┐
        │  MySQL    │ │Redis │  │  Nacos    │
        │  :3306    │ │:6379 │  │  :8848    │
        │cloud_flow │ └──────┘  │ 配置+注册  │
        └───────────┘           └───────────┘
```

### 2.2 服务清单与规模

| 服务 | 端口 | 启动类 | Java 文件 | Controller | 备注 |
|---|---|---|---|---|---|
| `cloudflow-gateway` | 9000 | `GatewayApplication` | 5 | 0 | WebFlux，剥离 servlet/MyBatis 自动装配 |
| `cloudflow-auth` | 9001 | `AuthApplication` | ≈100 | 19 | 认证 + 系统管理 + 文件 + 业务规则中心 |
| `cloudflow-service-workflow` | 9002 | `WorkflowApplication` | ≈230 | 14 | 流程引擎、模板、版本、发布、监控、规则、热更新 |
| `cloudflow-service-oa` | 9003 | `OaApplication` | ≈260 | 31 | 公告、行政、资产、合同、印章、证照、知识库、同步 |
| `cloudflow-service-crm` | 9004 | `CrmApplication` | ≈75 | 10 | 客户、联系人、商机、报价、回款、续约、工单、Dashboard |
| `cloudflow-service-hr` | 9005 | `HrServiceApplication` | ≈45 | 3 | **单 Controller 132 个端点**，结构异常 |

合计 84 个业务 Controller + 1 个公共 SSE Controller = README 中"业务服务 84+"印证。

### 2.3 公共模块清单（16 个）

| 模块 | 用途定位 |
|---|---|
| `cloudflow-common` | 元模块（聚合 starter / 二次封装） |
| `cloudflow-common-core` | R 响应、PageQuery/PageResult、UserContext、TenantContext/Broker/Interceptor、SecurityUtils、TokenService、全局异常、RequestLogAspect、ThreadPool |
| `cloudflow-common-data` | MyBatis-Plus 元字段填充、分页、数据权限 |
| `cloudflow-common-datasource` | dynamic-datasource 集成、`@DS` 切换 |
| `cloudflow-common-redis` | RedisTemplate、序列化、Redisson、缓存常量 |
| `cloudflow-common-security` | Sa-Token 全局过滤器、UserContextInterceptor、SaTokenExceptionHandler |
| `cloudflow-common-audit` | Javers 字段级审计 |
| `cloudflow-common-log` | 操作日志切面、Logback 公共配置 |
| `cloudflow-common-idempotent` | `@Idempotent` 注解 + Redis 防重 |
| `cloudflow-common-ratelimiter` | `@RateLimiter` 注解 + Redis 限流 |
| `cloudflow-common-encrypt` | MyBatis 字段级加密拦截器（注解驱动） |
| `cloudflow-common-sensitive` | 敏感字段脱敏序列化 |
| `cloudflow-common-excel` | FastExcel 导入导出封装 |
| `cloudflow-common-oss` | AWS SDK v2 + 本地存储统一抽象 |
| `cloudflow-common-sse` | Server-Sent Events 推送 |
| `cloudflow-common-job` | XxlJob/任务调度（占位） |
| `cloudflow-common-seata` | Seata 分布式事务（占位） |

`cloudflow-common-job` 与 `cloudflow-common-seata` 当前未见被业务服务直接依赖，疑似预留模块。

### 2.4 跨服务调用拓扑

| 调用方 | 被调用方 | 通道 | 用途 |
|---|---|---|---|
| Gateway | Auth/Workflow/OA/CRM/HR | HTTP（lb://） | 路由转发，Token 已在网关校验 |
| Workflow | Auth | Feign（`workflow.service.remote`） | 拉取用户、菜单、字典 |
| OA/CRM/HR | Workflow | Feign + Redis Stream | 主动启流程 + 异步回调审批结果 |
| CRM | OA | Feign（`RemoteOaService`） | 获取合同/项目/预算/发票链路信息 |
| HR | Auth/OA/Workflow/Auth 规则中心 | Feign（`hr.client.*`） | 用户/部门同步、会议室预订、流程发起、业务规则查询 |
| 所有 | Redis | Redis Stream | 工作流回调消息总线（`WorkflowCallbackStreamConstants`） |
| 所有 | Auth | Sa-Token Token-Session | Token 反查登录态（不发 HTTP，直读 Redis） |

工作流回调采用 **"Feign 同步发起 + Redis Stream 异步反馈"** 模式，在 CRM/OA/HR 中均存在镜像的 `WorkflowApprovalCallbackStreamConfig` + `WorkflowApprovalCallbackStreamConsumer` + `WorkflowCallbackServiceImpl` 三件套，代码结构高度重复（参见第十章重复实现）。

### 2.5 核心技术栈版本

| 技术 | 版本 | 备注 |
|---|---|---|
| JDK | 17 | LTS |
| Spring Boot | 3.2.6 | Jakarta EE 9+ |
| Spring Cloud | 2023.0.1 | |
| Spring Cloud Alibaba | 2023.0.1.0 | Nacos 注册+配置 |
| MyBatis-Plus | 3.5.7 | jsqlparser 4.9 显式锁定 |
| Sa-Token | 1.45.0 | Servlet + Reactor 双 starter |
| Redisson | 3.27.0 | 分布式锁、限流、缓存 |
| Dynamic Datasource | 4.3.1 | `@DS` 切换 |
| Knife4j | 4.5.0 | OpenAPI 3 / Jakarta |
| FastExcel | 1.0.0 | 替代 EasyExcel |
| Apache POI | 5.2.5 | |
| Javers | 7.6.2 | 审计差异 |
| Hutool | 5.8.26 | 通用工具集 |
| MySQL Connector | 8.0.33 | |
| Groovy | 3.0.19 | 流程脚本引擎 |
| AWS SDK | 2.25.16 | OSS（S3 协议） |

POM 已通过 `maven-enforcer-plugin` 启用 `banDuplicatePomDependencyVersions` / `reactorModuleConvergence` / `requireUpperBoundDeps`，并通过 `maven-dependency-plugin` 的 `analyze-only` 强制 `failOnWarning=true`，质量门禁规范度好。

### 2.6 启动方式与本地编排

- 本地一键：`start-cloudflow.bat`（实质 `start-cloudflow.ps1`）顺序启动 6 个服务 + 前端，日志落到 `.cloudflow-runtime/logs/`。
- 容器一键：`docker compose up -d` 起 13 个容器（MySQL/Redis/Nacos/6 业务服务/Prometheus/Grafana/Frontend）。
- 容器内 JVM 统一：`-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0`，配合 `deploy.resources.limits` 限制 0.5-1 CPU、256M-1G 内存。
- 健康检查统一通过 `/actuator/health`，Prometheus 抓取间隔默认 30s。

---

## 三、公共模块（cloudflow-common 16 子模块）

### 3.1 模块逐项审计

| 模块 | 关键类 / Bean | 实现状态 | 主要发现 |
|---|---|---|---|
| `cloudflow-common` | （纯 pom 聚合） | 元模块 | 仅做依赖聚合，无源码 |
| `cloudflow-common-core` | `R` / `PageQuery` / `PageResult` / `UserContext` / `TenantContext` / `TenantBroker` / `TenantInterceptor` / `SecurityUtils` / `SecurityProperties` / `RequestLogAspect` / `GlobalExceptionHandler` / `IdUtils` / `SnowflakeId` / `IpUtils` / `DateUtils` / `MimeTypeUtils` / `ThreadPoolConfig` / `ContextTaskDecorator` / `SwaggerConfig` / `CloudFlowConfig` | 完整 | 基础设施健全；`UserContext` 用 ThreadLocal，配合 `ContextTaskDecorator` 在 `@Async` 线程池中传递；`TenantBroker.applyWithoutTenant` 提供白名单临时跨租户调用 |
| `cloudflow-common-data` | `MybatisPlusConfig`（多租户+数据权限+分页 3 拦截器）、`DataScopeInnerInterceptor`、`RedisDataScopeHandle`、`MyMetaObjectHandler`、`TenantIgnoreAspect` | 完整 | 数据权限 5 类（0全部/1自定义/2本级及下级/3本级/4本人），登录时一次性算好部门列表写入 `UserContext`，**业务库不再反查 auth 库**；插件顺序"租户→数据权限→分页"正确 |
| `cloudflow-common-datasource` | `DynamicDataSourceAutoConfiguration` / `DsConstants` | 占位级 | 主要靠 baomidou `dynamic-datasource-spring-boot3-starter` 自动装配，本模块仅薄封装 |
| `cloudflow-common-redis` | `RedisConfig`（Jackson2JsonRedisSerializer）、`RedisCache`、`RedisStreamUtil`、`SysConfigHelper`、`TenantRedisCacheManager` | 完整 | `TenantRedisCacheManager` 自动在 Spring Cache key 前追加 `tenant:{id}:`，与 `@Cacheable` 联动实现多租户缓存隔离 |
| `cloudflow-common-security` | `TokenService`、`UserContextInterceptor`、`CloudFlowStpInterface`（Sa-Token 权限提供器）、`Inner` 注解 + `InnerAspect`（内部调用鉴权）、`SecurityWebMvcConfig`、`SaTokenExceptionHandler` | 完整 | **`TokenService` 类的包名 `com.cloudflow.common.core.utils.TokenService` 与所在模块 `cloudflow-common-security` 不一致，文件位置也确实落在 security 模块的 core/utils 子目录**——历史搬迁未清理，IDE 自动跳转会迷惑 |
| `cloudflow-common-audit` | `@Audit` 注解、`AuditAspect`、`JaversCompareHandle`、`SysAuditLogEntity`、`DataAuditor`、`SpelParser` | 完整 | Javers 7.6.2 对实体级 diff，通过 SpEL 解析业务键，`DefaultAuditLogHandle` 写库；与业务侧 `oa_trace_event` / `sys_audit_log` 表配合 |
| `cloudflow-common-log` | `@SysLog` 注解、`SysLogAspect`、`SysLogEvent`、`SysLogListener`、`SysLogEntity` | 完整 | 事件驱动+异步落库，规避主流程 IO；与 audit 模块的边界是"操作日志 vs 字段级审计"两套独立机制 |
| `cloudflow-common-idempotent` | `@RepeatSubmit` 注解、`RepeatSubmitAspect` | 完整 | Redis `SET NX EX` 原子防重；Key 由 `{userId}:{uri}:md5(args.hashCode())` 组成。**`arg.hashCode()` 对未重写 hashCode 的 POJO 走 identity hash，每次反序列化新对象 → digest 不同 → 防重不生效**；POJO 必须自行实现 `hashCode/equals` 才能命中 |
| `cloudflow-common-ratelimiter` | `@RateLimiter` 注解、`RateLimiterAspect`、`LimitType`（DEFAULT/IP/USER） | 完整 | Redis Lua 脚本原子计数；Redis 异常时**静默放行**（设计为可降级，应在监控告警同步报警）；IP 取 `X-Forwarded-For` 而**未校验来源是否网关**，业务服务直接暴露时可被伪造 IP 绕过 |
| `cloudflow-common-encrypt` | `@EncryptField` 注解、`MybatisEncryptInterceptor`、`MybatisDecryptInterceptor`、`EncryptUtils`、`EncryptorAutoConfiguration` | 部分高危 | 支持 AES/SM4/Base64；**`EncryptUtils.encrypt` 在异常时 `return plainText`** —— 配错密钥或密钥为空（默认值就是空串）时，原计划加密的字段会**静默以明文落库**且无错误日志可观测；自动配置 `matchIfMissing=true` 默认启用 |
| `cloudflow-common-sensitive` | `SensitiveUtils`（递归 mask Map/Bean/Collection） | **未完成** | **只有一个工具类，没有注解、Jackson Serializer、AOP、AutoConfiguration**；意味着接口返回脱敏需要业务侧手动调 `SensitiveUtils.maskObject`，实际工程中大概率被遗忘 |
| `cloudflow-common-excel` | FastExcel 二次封装、`@CellMerge`/`@ExcelDictFormat`/`@ExcelEnumFormat`、`DefaultExcelListener`、`ExcelUtil` | 完整 | 字典翻译、合并单元格、错误收集均覆盖 |
| `cloudflow-common-oss` | `OssClient`（AWS SDK v2 S3）、`OssFactory`、`OssProperties`、`UploadResult`、`OssException` | 完整 | 兼容 MinIO/阿里云/腾讯云/七牛云（任何 S3 协议）；预签名 URL 由 AccessPolicy 控制 |
| `cloudflow-common-sse` | `SseController`、`SseEmitterManager`、`SseAutoConfiguration` | 完整 | 唯一含 Controller 的 common 模块；维护用户→Emitter 列表实现按人推送 |
| `cloudflow-common-job` | `@DistributedJob` 注解、`DistributedJobAspect`、`JobAutoConfiguration` | 完整 | Redisson 分布式锁包装 `@Scheduled`，多实例只跑一份；非 XxlJob 路线 |
| `cloudflow-common-seata` | `SeataConfiguration`（空类，注释说"预留") | **占位** | 整个模块仅 1 个空类，业务侧未引用，README 仍宣传"Seata"应剔除或落地 |

### 3.2 模块间依赖

```
core ─┬─ data ─┬─ datasource
      ├─ redis ─┬─ idempotent
      │        └─ ratelimiter
      ├─ security ── (auth/oa/crm/hr/workflow)
      ├─ encrypt
      ├─ sensitive
      ├─ excel
      ├─ oss
      ├─ sse
      ├─ log
      ├─ audit
      └─ job

seata: 孤岛（无消费者）
```

`core` 是真正的根，所有模块都对它有传递依赖。`security` 间接依赖 `core`、`redis`（Sa-Token Redis 持久化），是次根。

### 3.3 设计问题与风险

1. **`cloudflow-common-security/.../com/cloudflow/common/core/utils/TokenService.java`** 包名声明在 `core.utils`，物理放在 `security` 模块——历史搬迁未同步重命名。建议同步修正为 `com.cloudflow.common.security.token.TokenService`，所有依赖方按 Maven 依赖关系不会受影响。

2. **`EncryptUtils.encrypt/decrypt` 异常时返回原文** —— 这是"防止数据丢失"的兜底，但意味着：
   - 写入时密钥配错 → 明文落库
   - 读取时密文损坏 → 暴露加密失败但仍尝试展示
   该容错策略与"加密保护敏感数据"的语义冲突。建议改为：写入失败抛异常 + 监控告警；读取失败返回 `***`。

3. **`common-sensitive` 没有自动化集成**。仅有工具类，业务侧调用率难以保证。建议补充：
   - `@Sensitive` 字段级注解 + Jackson Serializer，让 `R<UserVO>` 返回自动脱敏
   - 与 `@EncryptField` 联动：加密字段在序列化时默认 mask

4. **`@RepeatSubmit` 防重 key 用 `arg.hashCode()`**，对未重写 `hashCode()` 的 POJO 失效。建议改为 `SecureUtil.md5(JSON.toJSONString(args))` 或 Hutool 的 `BeanUtil.beanToMap` 再哈希。

5. **限流 IP 从 `X-Forwarded-For` 直接取首位** —— 业务服务端口 9001-9005 实际暴露在容器网络可达范围，并非只能通过网关访问。建议：
   - 业务服务限制只接受 Gateway 转发（network policy 或 Spring 配置可信代理）
   - 或在限流 IP 解析中先校验 `RemoteAddr` 是否网关 IP

6. **`common-seata` 占位** —— 既未启用又未删除，README/AGENTS 文档仍含"分布式事务"宣传。建议要么落地 + 集成业务（HR/OA 跨服务流程审批是真实需求），要么从 README 删除引用。

7. **`common-job` vs Spring `@Scheduled`** —— 仅工作流服务用了 `@EnableScheduling`。多实例部署时仅 workflow 有定时任务，但 OA 还有 `@EnableScheduling`——建议明确哪些服务有定时任务、并强制要求带 `@DistributedJob`，否则多副本会重复执行。

### 3.4 推荐改进（按优先级）

1. **P0** — 修复 `EncryptUtils` 异常时返回明文的兜底策略，避免敏感数据静默明文化。
2. **P1** — 把 `common-sensitive` 补齐 Jackson Serializer + `@Sensitive` 注解 + AutoConfiguration，让 Controller 返回链路自动脱敏。
3. **P1** — `TokenService` 的包名物理位置统一。
4. **P2** — `@RepeatSubmit` 换用稳定 args 摘要。
5. **P3** — `common-seata` 要么落地要么删除。

---

## 四、网关（cloudflow-gateway）

### 4.1 基本面

| 项 | 值 |
|---|---|
| 端口 | 9000 |
| 运行时 | WebFlux + Reactor Netty |
| 源码规模 | 5 个 Java 文件 |
| 关键组件 | `GatewayApplication`、`AuthFilter`、`TenantWebFilter`、`RateLimitConfig`、`GatewayExceptionHandler` |
| 配置来源 | `application.yml`（本地兜底）+ Nacos `cloudflow-gateway.yaml` + `cloudflow-common.yaml` |

`GatewayApplication` 显式 exclude 了 Servlet 体系（`GlobalExceptionHandler`、`UserContextInterceptor`、`TenantInterceptor`、`MybatisPlusConfig`、`SwaggerConfig`、`CloudFlowConfig`、`RedisStreamUtil` 等 8 个 Bean），避免 common 模块被误装配到 Reactive 环境——这点处理得很专业。

### 4.2 路由表

| RouteId | URI | 路径前缀 | StripPrefix |
|---|---|---|---|
| `cloudflow-auth` | `lb://cloudflow-auth` | `/auth/**` | 1 |
| `cloudflow-workflow` | `lb://cloudflow-service-workflow` | `/workflow/**` | 1 |
| `cloudflow-oa` | `lb://cloudflow-oa` | `/oa/**` | 1 |
| `cloudflow-crm` | `lb://cloudflow-service-crm` | `/crm/**` | 1 |
| `cloudflow-hr` | `lb://cloudflow-service-hr` | `/hr/**` | 1 |
| `cloudflow-ws` | `lb:ws://cloudflow-service-workflow` | `/ws/**` | 0（WebSocket） |

注意 OA 服务的 service-id 是 `cloudflow-oa`（无 `service-` 前缀），其他 4 个业务服务为 `cloudflow-service-*`，命名不统一。

### 4.3 AuthFilter（鉴权全局过滤器）

`AuthFilter.getOrder() == -100`，先于 Gateway 默认转发逻辑。流程：

1. 白名单放行：`/auth/login`、`/auth/register`、`/auth/tenant/options`、`/auth/captcha/**`、`/ws/**`。
2. 从 `Authorization` Header 取 Bearer Token，缺失时回退到 `Authorization` Cookie。
3. 调用 `TokenService.verifyToken(rawToken)` → Sa-Token Redis 反查会话。
4. 命中即放行，同时主动 **删除请求头中 `X-Auth-Token` / `X-User-Id` / `X-User-Name` / `X-User-Roles` / `X-User-Dept-Name` / `X-User-Tenant-Id`** —— 防止下游服务被伪造身份头入侵，这是关键的安全加固。
5. 不命中 → `401 {"code":401,"msg":"未授权或Token已过期"}`。

设计良好的几点：
- 网关只校验 Token，不把用户信息当成 header 透传，下游一律重新查 Redis 拿身份。
- WebSocket 走 `/ws/**` 白名单，由 workflow 服务的 `AuthHandshakeInterceptor` 单独握手校验。
- 移除 cookie 透传中潜在的 XSS/CSRF 隧道。

潜在问题：
- `resolveCookieToken` 私有方法已写但未被网关使用（`AuthFilter.resolveRawToken` 只读 Header）；而 OA 等下游服务可能依赖 Cookie——网关层 cookie 校验未实施，意味着 **同源 cookie 走 ws 流时不会被网关鉴权**。
- 白名单使用字符串前缀匹配，`/auth/captcha/**` 通配较宽，未来若 captcha 子路径新增高敏接口需谨慎。

### 4.4 TenantWebFilter

`Ordered.HIGHEST_PRECEDENCE + 100`（早于 AuthFilter），从 `X-Tenant-Id` Header 读取租户 ID，写入 Reactor Context；未提供时填 `100000`（默认租户）。

**注意：网关把租户 ID 写到了 Reactor Context 但**没有同步刷到 Request Headers**——下游 servlet 服务读不到 Reactor Context，只能依赖 Token 内嵌的 tenantId 或客户端直接传 `X-Tenant-Id`。当前业务服务的 `UserContextInterceptor` 是从 `loginUser.tenantId`（来自 Sa-Token Session）取的，因此实际上 TenantWebFilter 的输出**未被下游消费**——这是死代码或半完成的功能。

### 4.5 RateLimitConfig

两个 KeyResolver：

- `ipKeyResolver`（`@Primary`）：从 `RemoteAddress` 取 IP。**未读 `X-Forwarded-For`**，如果网关上层还有反向代理（如 Nginx），所有请求 IP 会被识别为代理 IP，限流失效。
- `userKeyResolver`：先从 Bearer Token 解出登录 ID，未登录回退 IP。

`application.yml` 中**只声明了 KeyResolver Bean，未配置任何 RouteDefinition 的 `RequestRateLimiter` 过滤器**——意味着限流 Bean 已就绪但**无任何路由启用限流**，仅是基础设施摆设。需要在路由定义中加 `RequestRateLimiter` + `redis-rate-limiter.replenishRate/burstCapacity` 才能生效。

### 4.6 GatewayExceptionHandler

`@Order(-1)` 抢占默认 `DefaultErrorWebExceptionHandler`，统一返回 `{code, msg, data}`，针对 400/401/403/404/429/502/503/504 给中文友好提示，且日志记录请求路径和原始异常。设计扎实。

### 4.7 CORS 配置（高风险，已修复）

**[2026-05-13 P0#3 已落地]** 当前配置：

```yaml
globalcors:
  cors-configurations:
    '[/**]':
      allowed-origin-patterns: "${CLOUDFLOW_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,https://localhost,http://localhost}"
      allowed-methods: [GET, POST, PUT, DELETE, OPTIONS]
      allowed-headers: "*"
      allow-credentials: true
      max-age: 3600
```

变更内容：
- `config/cloudflow-gateway.yaml`、`cloudflow-backend/cloudflow-gateway/src/main/resources/application.yml` 同步改为环境变量驱动的白名单。
- `docker-compose.yml` 在 `gateway` 服务的 `environment` 增加 `CLOUDFLOW_CORS_ALLOWED_ORIGINS`，与 `.env` 中同名变量联动。
- 默认白名单覆盖：前端 Vite dev（5173）、前端 React dev（3000）、Docker Nginx 的 https/http。

仍需注意：
- 生产环境必须在 `.env` 或部署平台显式覆盖 `CLOUDFLOW_CORS_ALLOWED_ORIGINS` 为真实域名，**默认值仍包含 localhost 仅适用于开发环境**。
- `allow-credentials: true` 保留，因前端使用 `Authorization` Cookie 兜底登录态，仍需配合 SameSite=Lax 与白名单使用。
- Spring `allowed-origin-patterns` 支持通配（如 `https://*.cloudflow.example.com`），生产可启用通配减少配置项。

原始风险描述（已不复存在）：~~`allowed-origin-patterns: "*"` + `allow-credentials: true` 等同于"任意网站都能携带 Cookie/Authorization 请求 CloudFlow API"。~~

### 4.8 配置双源

- `cloudflow-gateway/src/main/resources/application.yml` 中已完整定义路由表、CORS、Sa-Token、Redis 配置。
- `config/cloudflow-gateway.yaml`（Nacos）又重复定义了路由表与 CORS。
- 两份配置存在小差异（`is-read-cookie: true`（本地）vs `is-read-cookie: false`（Nacos common））。
- Nacos `password: nacos` 在本地 yml 硬编码，无法被环境变量覆盖；Redis 密码 `Juwangkeji@2025` 同样硬编码。

### 4.9 Actuator 暴露

`management.endpoints.web.exposure.include = health,info,metrics,prometheus`，且 9000 端口对外暴露，意味着：
- `http://gateway:9000/actuator/prometheus` 公网可达 → 指标完全公开
- `/actuator/health` 暴露依赖项（when-authorized 可缓解）
- Prometheus 抓取若不经过认证则 OK，但风险是任意外部访问者都能拉到 metrics

建议在网关层增加：
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: actuator
          uri: lb://cloudflow-gateway
          predicates:
            - Path=/actuator/**
          filters:
            - SetStatus=403   # 或仅内部子网访问
```

### 4.10 网关推荐改进（按优先级）

| 级别 | 项 | 状态 |
|---|---|---|
| **P0** | ~~CORS 白名单化，移除 `*` + `credentials` 组合~~ | ✅ 2026-05-13 已落地 |
| **P0** | Nacos 密码、Redis 密码移出 application.yml，统一走环境变量 | 待处理 |
| **P1** | 删除 application.yml 中重复的路由表与 CORS，单一以 Nacos 配置为准；或反之 | 待处理 |
| **P1** | Actuator 端口或路径访问控制（仅内网/仅 Prometheus 抓取） | 待处理 |
| **P2** | 给关键路由（`/auth/login`、`/auth/register`、`/auth/captcha/**`）增加 `RequestRateLimiter` 真正启用限流 | 待处理 |
| **P2** | `cloudflow-oa` 服务名补齐 `cloudflow-service-oa` 前缀，统一命名 | 待处理 |
| **P3** | 删除 `TenantWebFilter` 中的 Reactor Context 写入（无消费方），或改为同步刷 Header 让下游可用 | 待处理 |
| **P3** | `AuthFilter.resolveCookieToken` 未使用的死代码清理 | 待处理 |

---

## 五、认证服务（cloudflow-auth）

### 5.1 基本面

| 项 | 值 |
|---|---|
| 端口 | 9001 |
| 启动类 | `AuthApplication`（exclude `DataSourceAutoConfiguration`，启用 dynamic-datasource） |
| Java 文件 | ≈100 |
| Controller | 19 |
| 数据库表（来自 `01.cloudflow-common.sql`） | 15 + `BusinessRule*` 3 = 18 张 |

### 5.2 Controller 清单与职责

| 分组 | Controller | 主要职责 |
|---|---|---|
| 认证 | `AuthController` | 登录、注册、`/info`、`/profile`、改密、`/getRouters`、登出、`/switchTenant`、`/tenant/options` |
| 用户/角色/部门 | `SysUserController`、`SysRoleController`、`SysDeptController`、`SysPostController` | RBAC 基础 |
| 菜单 | `SysMenuController` | 菜单树 + 按钮权限码 |
| 字典 | `system/SysDictTypeController`、`system/SysDictDataController` | 全局字典 |
| 租户 | `SysTenantController` | 租户 CRUD、存储统计、用户配额 |
| 系统配置 | `SysConfigController` | `sys_config` 表运行时配置 |
| 文件 | `system/SysFileController` | 上传、列表、存储统计、删除 |
| 验证码 | `CaptchaController` | 滑块验证码生成与校验 |
| 日志 | `system/SysAuditLogController`、`SysLogController`、`SysLoginLogController` | 操作日志、审计日志、登录日志 |
| 在线用户 | `system/SysOnlineUserController` | Sa-Token Token Session 查询、踢人 |
| 缓存监控 | `CacheMonitorController` | Redis 命名空间统计 |
| 业务规则 | `BusinessRuleController` | 规则 V2：草稿→发布→回滚→命中记录 |
| 内部调用 | `RemoteAuthController` | Feign 接口，被 workflow/oa/hr 调用 |

### 5.3 登录链路（`POST /auth/login`）

1. **滑块验证**：必须先 `GET /captcha/get` 拿背景图+缺口图，前端拖到正确 x → `POST /captcha/check` 拿 passToken；passToken **一次性消费**（验证后立即删除 Redis key）。
2. **租户校验**：按 `tenantCode` 查 `sys_tenant`，校验存在 / 状态 / 过期。
3. **登录锁**：`LoginLockService` 用 `LOGIN:FAIL:{tenantId}:{username}` 计数，达 5 次写入 `LOGIN:LOCK:{tenantId}:{username}` TTL 15 分钟。
4. **用户校验**：`del_flag != 2` && `status == 0` && BCrypt 比对。
5. **密码兼容**：`PasswordService` 同时校验"SHA-256(明文)→BCrypt"和"明文→BCrypt"两种历史口令，后者命中后自动升级到前者；这意味着**老明文 BCrypt 账号会在首次登录后被静默改写**——审计上可接受但缺操作日志。
6. **数据权限预计算**：`calcDataScopeInfo` 把 `dsType` 和 `dsDeptIds` 算好塞进 Sa-Token Session，避免下游服务反查 auth 库（关键性能优化）。
7. **写 Cookie**：`HttpOnly + SameSite=Lax + (Secure 取决于 X-Forwarded-Proto)`，有效期 7 天；同时返回 token 到 body。
8. **登录日志**：成功/失败均落 `sys_login_log`。

### 5.4 Sa-Token 配置

```yaml
sa-token:
  token-name: Authorization
  token-prefix: Bearer
  is-concurrent: false        # 后登录顶下前登录
  is-share: false             # 不同设备不共享同一 token
  timeout: 86400              # 24h
  active-timeout: -1          # 不基于活跃度过期
  is-log: false
cloudflow.security.token:
  expiration: 1440            # 分钟（24h，与 sa-token.timeout 等价）
  refresh-time: 20            # 剩余 ≤20min 时自动续期
```

`is-concurrent: false` 即"踢人下线"，配合 `is-share: false`，**同一账号在新设备登录后旧设备会立刻失效**。这是企业 OA 系统的常见安全策略，但移动端 + 桌面端混用场景会反复互踢，需告知用户。

### 5.5 RBAC 与权限校验

- 权限码：`module:resource:action`（如 `system:user:add`），存于 `sys_menu.perms`。
- 用户→角色（`sys_user_role`）→ 菜单（`sys_role_menu`）→ 权限码（`sys_menu.perms`）。
- 用户登录时把 `roles`/`permissions` 全集塞入 Sa-Token Session，下游通过 `UserContext` 读取。
- **两套并存的权限注解**：
  - `cloudflow-auth` 自有的 `@HasPermission`（`HasPermissionAspect`），通配匹配 `*:*:*`、`PatternMatchUtils.simpleMatch`。
  - Sa-Token 自带的 `@SaCheckPermission`、`@SaCheckRole`（由 `CloudFlowStpInterface` 提供权限源）。
  - 业务侧实际使用情况未做强制收敛，开发可能混用导致行为不一致。
- **`HasPermissionAspect` 抛 `RuntimeException("没有访问权限")`**，未走 `ServiceException` 或 `SaTokenException`，全局异常处理需匹配字符串识别 → 不规范。

### 5.6 多租户

- 字段：`tenant_id BIGINT`，所有租户业务表统一。
- 默认租户：`100000`（`sys_tenant.AUTO_INCREMENT=100000`）。
- 拦截器：MyBatis-Plus `TenantLineInnerInterceptor`（注册在 `common-data` 的 `MybatisPlusConfig`）。
- ignoreTables：`sys_tenant / sys_menu / sys_dict_type / sys_dict_data / sys_config / workflow_template / template_category`。
- 超管 `ADMIN` 通过 `POST /switchTenant` 切换租户上下文，**会重新发 Token**（旧 Token 立即失效）。
- 注册流程允许 `RegisterBody.tenantCode` 指定租户，**会校验用户配额（`isUserLimitReached`）**，但未校验 emailDomain 与租户绑定关系。

### 5.7 文件管理

- `FileStorageRegistry` 根据配置路由到 `LocalFileStorageService` 或 `OssFileStorageService`。
- 本地存储：`FileUploadUtils.upload(CloudFlowConfig.getUploadPath(), file)`（实现在 `common-core/utils/file`）—— 需复查上传文件名是否做 MIME / 后缀白名单 / 文件大小 / 路径穿越校验。
- OSS：AWS SDK v2 S3 协议，支持 MinIO / 阿里云 / 腾讯云 / 七牛云。
- 租户存储配额：`SysTenantService.refreshTenantStorageSummary` 异步统计每个租户 `sys_file` 表的累计大小，与 `sys_tenant.storage_limit` 对比。

### 5.8 业务规则中心 V2

- 表：`business_rule`（最新生效）、`business_rule_version`（版本快照）、`business_rule_hit_record`（命中记录）。
- 流程：起草 → 发布（版本表 +1） → 回滚（把指定历史版本 copy 回 business_rule 表，写入新版本）→ HR/OA 业务运行时调用 `/system/rules/effective/{ruleCode}` 拿规则、执行 → `POST /system/rules/hit-records` 落库。
- 当前固定支持的 3 个 ruleCode：`hr.leave.quota.limit`、`oa.expense.amount.limit`、`oa.contract.risk.threshold`。
- HR/OA 服务通过 `BusinessRuleClient`（Feign）查询，**`BusinessRuleClientFallback` 默认行为是 PASS** —— 规则中心宕机时业务"放行"而非"阻断"，需评估合规要求。

### 5.9 验证码（滑块拼图）

- `SliderPuzzleUtil` 自研 BufferedImage 渲染：从配置 `sys_config` 读宽 300 / 高 150 / 拼图 44 / 圆弧 8。
- 容差从 `sys_config` 读，默认值未在 Properties 中可见。
- 校验通过即删 Redis key，passToken 30s 内一次性使用。
- 单 IP 24 小时验证码生成次数限制（默认未硬编码，从 sys_config 读）。
- 防爆破链路：滑块 → passToken 一次性消费 → 失败 5 次锁 15min，链路完整。
- **缺失**：滑块校验**未与 username/IP 绑定**，理论上 A 用户拿到的 passToken 可被 B 用户用于登录请求。建议在 verifyCaptcha 时把 passToken 与请求 IP 绑定，登录时校验同 IP。

### 5.10 主要风险与改进（按优先级）

| 级别 | 项 |
|---|---|
| **P0** | `cloudflow-auth.yaml` 中明文 DB/Redis 密码 `Juwangkeji@2025` 必须移除（密码已进入 git 历史，应轮换） |
| **P0** | **`useSSL=false`** 在 cloudflow-auth.yaml 中显式关闭 MySQL 连接 TLS，与 cloudflow-common.yaml 中 `useSSL=true` 矛盾，生产环境若选用 `useSSL=false` 将使内网链路明文 |
| **P0** | `logging.level.com.cloudflow.auth: DEBUG` 与 `logging.level.com.cloudflow.auth.mapper: DEBUG` 在 cloudflow-auth.yaml 默认开启 → 生产环境会打印完整 SQL 和敏感参数 |
| **P1** | `BusinessRuleClientFallback` 默认 PASS 不符合合规风控的"宁可错杀"原则，需根据 ruleCode 区分（如 `oa.contract.risk.threshold` 应 WARN，`hr.leave.quota.limit` 应 PASS） |
| **P1** | 滑块 passToken 与 IP/请求来源绑定 |
| **P1** | 收敛 `@HasPermission` vs `@SaCheckPermission`，统一一套权限注解 |
| **P2** | 文件上传：MIME / 后缀 / 大小 / 路径穿越的兜底校验，附件存储隔离租户子目录（避免下载越权） |
| **P2** | 密码静默自动升级时记入审计日志，避免合规上"不可解释" |
| **P2** | 注册流程加 emailDomain → 租户绑定校验 |
| **P3** | `HasPermissionAspect` 抛业务异常而非 RuntimeException |

---

## 六、工作流服务（cloudflow-service-workflow）

### 6.1 基本面

| 项 | 值 |
|---|---|
| 端口 | 9002 |
| 启动类 | `WorkflowApplication`（`@EnableAsync` + `@EnableScheduling` + `@EnableFeignClients`） |
| Java 文件 | ≈230 |
| Controller | 14 |
| Service 接口 + 实现 | 77 个文件，最大复杂度模块 |
| 数据库表（`02.cloudflow-workflow.sql`） | 41 张 |
| 单大 Controller 端点数 | `WorkflowController` 29 个 |

### 6.2 Controller 清单

| Controller | 职责 |
|---|---|
| `WorkflowController` | 流程发起、任务完成、待办/已办/我的、催办、流程追踪、流程定义增改删、表单定义、统计 |
| `WorkflowEnhanceController` | 流程增强：撤回、加签、减签、转办、委派、回退 |
| `WorkflowMonitorController` | 监控大屏：实例总览、节点监控、任务监控、性能、异常告警 |
| `BatchOperationController` | 批量审批、批量归档、批量删除 |
| `TemplateController` | 模板库 CRUD、模板分类树 |
| `ProcessCategoryController` | 流程分类管理 |
| `VersionController` | 版本管理、版本对比、回滚 |
| `DeployEnhancementController` | 部署窗口、部署审批、影响分析、回滚历史 |
| `ImportExportController` | 流程定义/实例/模板的导入导出 |
| `ProcessCopyController` | 抄送任务管理 |
| `AlertController` | 流程告警查询与确认 |
| `AuditLogController` | 工作流操作审计日志 |
| `SimulationController` | 流程仿真（不真跑） |
| `HotUpdateController` | **运行中流程的热更新**（追加/删除/替换节点） |

### 6.3 数据模型（节选）

41 张表分组：

- **定义层**：`wf_process_definition`、`wf_process_category`、`wf_form_definition`、`wf_node_config`、`wf_notification_config`
- **运行层**：`wf_process_instance`、`wf_task`、`wf_node_record`、`wf_task_history`、`wf_task_candidate`、`wf_task_attachment`、`wf_task_read`
- **高级**：`wf_countersign_task`/`wf_countersign_vote`（会签）、`wf_task_add_sign`（加签）、`wf_task_delegation`（委派）、`wf_task_urge`/`wf_urge_effect`（催办与效果）、`wf_process_copy`（抄送）
- **版本与发布**：`wf_process_version_snapshot`、`wf_deploy_record`、`wf_deploy_approval`、`wf_deploy_approval_step`、`wf_deploy_window`、`wf_deploy_impact`、`wf_deploy_notification`、`wf_deploy_rollback_history`
- **归档**：`wf_process_archive`、`wf_process_snapshot`
- **审计与监控**：`wf_audit_log`、`wf_anomaly_alert`、`wf_notification_log`
- **事务**：`wf_transaction_message`（基于消息表的最终一致性，配合 RedisStream）

`wf_process_definition` 索引覆盖完整（`process_key`、`status`、`is_latest`、`dept_id`、`template_id`、复合 `template_archived`、`category_status`），租户隔离索引：`uk_proc_def_key_ver_tenant(process_key, version, tenant_id)`。

### 6.4 权限模型

`WorkflowController` 用 **Sa-Token 注解**：`@SaCheckLogin`、`@SaCheckRole("admin")`、`@SaCheckRole(value = {"admin","manager"}, mode = SaMode.OR)`。这与 `cloudflow-auth` 中的 `@HasPermission`（PatternMatchUtils）形成两套并存的权限注解体系——风格不统一会让安全评审困难。

### 6.5 脚本节点执行（高风险）

`ScriptExecutionService.executeGroovyScript`：

```java
GroovyShell shell = new GroovyShell(binding);
Object result = shell.evaluate(scriptContent);
```

- **完全裸跑 GroovyShell，无 SecureASTCustomizer、无 ImportCustomizer 黑白名单**。
- 可任意访问 `System.exit`、`Runtime.exec`、文件系统、反射、网络、JNI。
- 配置项 `workflow.script.enabled` 默认未在 `cloudflow-service-workflow.yaml` 中显式出现 → 取决于 `WorkflowProperties` 的默认值（需进一步看 properties 类，但默认行为很关键）。
- **门控只有"全局开关"**：要么禁用脚本节点，要么完全放行任意 Groovy。生产环境若误开启 + 流程模板权限管控不严，**任何能保存流程模型的用户都可获得宿主进程的代码执行能力**。
- `ScriptExecutionPolicy.assertModelDeployable` 在 deploy 时扫描 `model_json` 中 `nodes[].type == "SCRIPT"` 且 `props.scriptType ∈ {GROOVY, JAVASCRIPT}` 阻止发布——但 `runtime` 直接调用 `executeGroovyScript` 的入口未被该策略全覆盖（例如表单字段计算、规则求值，需要单独审计调用点）。
- JavaScript 分支抛 `UnsupportedOperationException`，未实现，安全。

**强烈建议**：
1. 默认 `workflow.script.enabled=false`（注释中确实写"默认禁用"，但 properties 类未审计），日志中明确告警。
2. 若必须支持脚本节点，引入 Groovy SecureASTCustomizer 限制语句白名单，或迁移到 GraalVM Polyglot Sandbox。
3. Runtime 直接调用 `executeGroovyScript` 的所有点都要走 `assertInProcessScriptAllowed`。

### 6.6 异步与定时

- `@EnableAsync` + `AsyncConfig`：异步线程池，配合 `common-core` 的 `ContextTaskDecorator` 传递 `UserContext` 与 `TenantContext`。
- `@EnableScheduling`：用于 `TimerSchedulerService`、`AnomalyDetectionServiceImpl`（异常检测）、`TimeoutDetectionServiceImpl`（超时检测）、`ProcessMonitorServiceImpl`。
- **多副本部署时**：未见任何 `@DistributedJob` 注解，多个 workflow 实例同时跑定时任务会**重复扫描数据库与重复告警**。`common-job` 模块已就绪但未被工作流引用。

### 6.7 跨服务集成

- Feign 客户端（`workflow.service.remote`）：
  - `RemoteUserService` → 调 auth 服务取用户、部门、角色
  - `RemoteOaService` + `RemoteOaFallbackFactory` → 调 OA 服务（具体场景不详，疑似获取业务表单数据）
- Redis Stream（`WorkflowCallbackStreamConstants`）：作为 **工作流→OA/CRM/HR** 的回调消息通道，避免同步 Feign 阻塞。
- WebSocket：`NotificationWebSocketHandler` + `WebSocketConfig` + `AuthHandshakeInterceptor`，通过 `/ws/**` 推送任务通知；网关 `lb:ws://` 路由。

### 6.8 监控与告警

- `WorkflowMonitorService` / `ProcessMonitorServiceImpl`：实例/任务/节点维度聚合
- `AnomalyDetectionServiceImpl`：定时扫描异常实例（如长时间未流转）
- `TimeoutDetectionServiceImpl`：基于 `task.default-timeout-hours: 24` 与 `approval.default-timeout-hours: 48` 检测超时
- 告警表：`wf_anomaly_alert`、`alerts.thresholds`（process-timeout-rate / task-timeout-rate / approval-timeout-rate / error-rate）

### 6.9 性能保护

- `RateLimiterService`、`ReplayAttackPreventionServiceImpl`、`DeadlockDetectionService`、`TransactionConsistencyService` 是基础设施级保护服务，名字看起来都有针对工作流引擎特定问题的实现。
- `WorkflowSagaServiceImpl` 实现 SAGA 模式协调跨服务事务（非 Seata）。
- `WfTransactionMessage` 表 + Redis Stream 实现**本地消息表 → MQ 投递**的最终一致性，是工作流回调到 OA/CRM/HR 的核心保障。

### 6.10 配置文件问题

`config/cloudflow-service-workflow.yaml` 中：
- DB/Redis 密码同样硬编码 `Juwangkeji@2025`（4 处，含注释中的 flowhistory 数据源）
- `useSSL=false` 关闭 MySQL TLS
- `logging.level.com.cloudflow.workflow: DEBUG` 默认开启
- `feign.hystrix.enabled: true` 与 Spring Cloud 2023 不兼容（Hystrix 已停止维护，Spring Cloud Alibaba 应用 Sentinel）— 死配置

### 6.11 主要风险与改进（按优先级）

| 级别 | 项 |
|---|---|
| **P0** | 审计 `workflow.script.enabled` 默认值；如默认开启则改为默认 `false`；为 Groovy 加沙箱（SecureASTCustomizer 或 GraalVM Polyglot） |
| **P0** | 移除配置中明文凭据 |
| **P1** | 为所有定时任务加 `@DistributedJob`，多副本环境必须 |
| **P1** | 删除 `feign.hystrix.enabled` 死配置，明确 Sentinel 或 Resilience4j 路线 |
| **P2** | 工作流权限注解收敛（Sa-Token 标准注解 vs auth 自研 `@HasPermission`） |
| **P2** | `WorkflowController.startProcess` 仅 `@SaCheckLogin`，**所有登录用户都能用任意 processDefKey 发起流程** — 应加 `processDefKey` → `start_permission_type/value` 的权限校验（表中字段已存在但 controller 未消费） |
| **P3** | `HotUpdateController.execute` 是高危接口，建议加二次确认 token 或独立的"高危操作"权限码 |

---

## 七、OA 服务（cloudflow-service-oa）

### 7.1 基本面

| 项 | 值 |
|---|---|
| 端口 | 9003 |
| 启动类 | `OaApplication`（`@EnableDiscoveryClient` + `@EnableFeignClients` + `@EnableScheduling`） |
| Java 文件 | ≈260 |
| Controller | 31 |
| 数据库表（`04.cloudflow-oa.sql`） | 36 张 |
| Compose 命名 | `cloudflow-oa`（缺 `service-` 前缀） |

### 7.2 Controller 按业务域分组

| 业务域 | Controller | 备注 |
|---|---|---|
| 协同 | `SysNoticeController`、`SysAnnouncementController`、`SysScheduleController`、`InnerScheduleController`、`MeetingRoomController`、`ContactController`、`VisitorController` | 通知/公告/日程/会议室/通讯录/访客 |
| 行政 | `AssetController`、`ConsumableController`、`VehicleController`、`DutyScheduleController`、`SupplierController` | 资产/耗材/车辆/值班/供应商 |
| 审批业务 | `BusinessTripController`、`ExpenseClaimController`、`PaymentRequestController`、`PurchaseRequestController`、`ContractController`、`SealController`、`LicenseController`、`BorrowManagementController` | 出差/报销/付款/采购/合同/印章/证照/借用 |
| 财务 | `BudgetController`、`InvoiceController` | 预算/发票 |
| 项目/知识 | `ProjectController`、`KnowledgeController` | 项目/知识库 |
| 风险 | `RiskAlertController` | 合同到期、证照到期、车辆审保到期等聚合告警 |
| 平台聚合 | `WorkplaceController`、`TimelineController`、`AuditEventController`、`WorkTaskController` | 工作台 / 业务时间线 / 审计台账 / 工作任务 |
| 集成 | `SyncController`、`ErrorReportController` | 离线同步 / 前端错误上报 |

### 7.3 工作台聚合接口

`WorkplaceController` 提供 3 个聚合 GET：
- `/workplace/summary` — 待办、今日日程、未读消息汇总
- `/workplace/recent-tasks?limit=10` — 最近任务
- `/workplace/timeline?limit=20` — 个人动态

README 描述的 `/oa/workplace/summary` 包含"待办、抄送、公告、日程、会议室、HR 提醒、合同/车辆/证照/资产风险、最近动态和服务健康状态"——是个**重型聚合接口**。

潜在问题：
- 接口可能同时查 10+ 张表，未见缓存（应至少加 30s 短期缓存）
- 接口对**所有登录用户开放**（仅 `@SaCheckLogin` 隐式），没有按用户类型差异化数据量
- 一次性返回大对象，移动端流量与首屏延迟敏感

### 7.4 审计时间线（`oa_trace_event`）

`TimelineController` + `IOaTraceEventService`：
- `GET /timeline?businessType=&businessId=&relatedType=&relatedId=&limit=20` — 按业务键过滤事件
- `GET /timeline/{id}/diff` — 拉取快照差异

`oa_trace_event` 表写入路径覆盖合同、用印、证照、车辆、资产、报销、采购、知识库等业务事件。配合 `cloudflow-common-audit` 的 Javers diff 实现"事件 + 字段级 diff"双轨审计。

### 7.5 审批业务与工作流集成

OA 大量审批业务（合同、出差、报销、付款、采购、用印、证照借用、知识库发布）通过两个机制与工作流交互：
1. **Feign 同步发起**：`WorkflowServiceClient`（在 `RemoteWorkflowService` 等 client 里）调用 workflow 服务的 `/start`、`/complete`。
2. **Redis Stream 异步回调**：`WorkflowApprovalCallbackStreamConfig` + `WorkflowApprovalCallbackStreamConsumer` 消费 workflow 服务发布的审批结果，路由到对应业务的 `ApprovalResultHandler`（如 `BusinessTripApprovalHandler`、`KnowledgeApprovalHandler` 等）。

回调消费侧的"业务键→Handler"分发是约束式的——加新业务必须新建 Handler 类，缺乏注册表机制，扩展性中等。

### 7.6 离线同步（`SyncController`）

3 个端点：
- `POST /sync/upload` — 设备上传离线累积的操作
- `GET /sync/download?lastSyncTime=&deviceId=` — 拉取增量数据
- `POST /sync/resolve-conflicts` — 提交冲突解决方案

潜在问题：
- `lastSyncTime` 是字符串，未见时区与格式校验
- `deviceId` 完全由客户端提供，无注册/绑定，**多人共用同一 deviceId 会导致数据交叉**
- 同步策略未审计冲突检测算法（最后写入胜出？版本号？CRDT？）—— 商业场景需要明确

### 7.7 前端错误上报（`ErrorReportController`）

设计为"尽量不失败"：异常不抛、永远 ok。

但仍需要走网关 AuthFilter，**未登录用户上报失败**——而前端 errorReporter 服务最需要捕获的恰恰是登录前的错误（如登录页 JS 异常）。建议把 `/oa/error-report` 加入网关白名单，配合限流（防垃圾上报）。

### 7.8 权限粒度

抽查 `ContractController`：
- 只有 `@SysLog("新增合同")` 等审计注解，**没有 `@SaCheckLogin` / `@SaCheckPermission` / `@HasPermission`**。
- 依赖网关 AuthFilter 保证已登录，但**未对操作做权限码控制**——任何登录用户都能：增/改/删/提交合同。
- 多租户拦截器只能保证看不到别租户合同，但租户内**任何员工都能删除任何合同**。

这是 31 个 Controller 普遍存在的问题——**业务接口缺乏细粒度权限校验**，依赖前端菜单隐藏。

### 7.9 配置文件问题

- 同样含 `Juwangkeji@2025` 明文密码
- `useSSL=false`
- `feign.hystrix.enabled: true` 死配置
- `cloudflow.oa.announcement.allow-anonymous-read: false` — 公告默认不允许匿名读，但**未验证 SysAnnouncementController 是否真的执行了此配置**

### 7.10 业务表概览（`04.cloudflow-oa.sql` 36 张）

主要表族：
- 公告：`sys_announcement`、`sys_announcement_read`、`sys_notice`
- 日程：`sys_schedule_event`、`sys_schedule_attendee`
- 会议室：`meeting_room`、`meeting_room_booking`
- 通讯录/访客：`oa_contact`、`oa_visitor`、`oa_visitor_log`
- 资产/耗材：`sys_asset`、`sys_asset_log`、`sys_consumable`
- 车辆：`vehicle_*`（车辆、租赁、用车申请、加油、维护、费用）
- 审批主键表：`biz_business_trip`、`biz_expense_claim`、`biz_expense_item`、`biz_payment_request`、`biz_purchase_request`、`biz_purchase_item`、`biz_purchase_receipt`
- 合同/印章/证照：`oa_contract`、`oa_seal`、`oa_seal_use`、`oa_license`、`oa_license_borrow`、`oa_license_renewal`、`oa_license_handover_log`、`oa_license_expiry_reminder_log`、`oa_borrow_reminder_log`
- 知识库：`knowledge_document`、`knowledge_read`
- 风险：`oa_risk_alert`
- 时间线 / 错误：`oa_trace_event`、`frontend_error_log`
- 项目/预算/发票：`oa_project`、`oa_budget`、`oa_invoice`
- 值班/供应商：`oa_duty_schedule`、`sys_supplier`

### 7.11 主要风险与改进（按优先级）

| 级别 | 项 |
|---|---|
| **P0** | 合同、印章、证照、付款这类高敏审批业务**必须加权限码控制**，至少 `@SaCheckPermission("oa:contract:add")` 级别 |
| **P0** | 配置文件移除明文密码 |
| **P1** | `/oa/workplace/summary` 加 30-60s 短期缓存，配合 `@CacheEvict` 在用户行为后失效 |
| **P1** | `ErrorReportController` 加入网关白名单 + IP 限流 |
| **P1** | `SyncController` 的 deviceId 强制绑定用户，冲突算法显式化 |
| **P2** | 服务名 `cloudflow-oa` 改为 `cloudflow-service-oa`，与其他业务服务一致 |
| **P2** | 审批回调 Handler 用 `Map<businessType, Handler>` 注册表替代 if-else 分发 |
| **P3** | 评估 `allow-anonymous-read` 配置是否真的生效 |

---

## 八、CRM 服务（cloudflow-service-crm）

### 8.1 基本面

| 项 | 值 |
|---|---|
| 端口 | 9004 |
| 启动类 | `CrmApplication`（`@EnableFeignClients`，未显式 `@EnableDiscoveryClient`） |
| Java 文件 | ≈75 |
| Controller | 10 |
| 数据库表（`05.cloudflow-crm.sql`） | 8 张 |

### 8.2 Controller 清单

| Controller | 路径 | 职责 |
|---|---|---|
| `CrmCustomerController` | `/customer` | 客户 CRUD + 工作区（生成合同/项目/预算/发票草稿） + Dashboard |
| `CrmContactController` | `/contact` | 联系人 |
| `CrmFollowUpController` | `/follow-up` | 跟进记录 |
| `CrmOpportunityController` | `/opportunity` | 商机看板（Kanban） |
| `CrmQuoteController` | `/quote` | 报价 + 报价审批 |
| `CrmReceivableController` | `/receivable` | 回款 + 账龄分析 |
| `CrmReceivableInnerController` | `/inner/crm/receivable/...` | **内部接口**（OA 调用） |
| `CrmRenewalController` | `/renewal` | 续约管理 + 续约审批 |
| `CrmServiceTicketController` | `/service-ticket` | 工单 |
| `CrmDashboardController` | `/dashboard` | 跨实体仪表盘 |

### 8.3 数据模型（8 张表）

- `oa_crm_customer` — 客户（含 `credit_code` 统一社会信用代码、`health_level`、`owner_id`）
- `oa_crm_contact` — 联系人（含 `mobile`、`email`）
- `oa_crm_follow_up` — 跟进记录
- `oa_crm_opportunity` — 商机
- `oa_crm_quote` — 报价
- `oa_crm_receivable` — 回款
- `oa_crm_renewal` — 续约
- `oa_crm_service_ticket` — 工单

**表前缀异常**：CRM 业务表用 `oa_crm_*` 前缀（不是 `crm_*`），系历史上 CRM 在 OA 模块中孵化的遗留命名。短期影响不大，长期建议在新建表时切到 `crm_*` 并对老表加视图层别名。

### 8.4 与工作流集成

CRM 接入了工作流，但比 OA 多了两个增强：

1. **流程键可配置**：`cloudflow.crm.workflow.process-keys.quote=quote_approval` / `renewal=customer_renewal_review`——比 OA 中硬编码流程键更灵活。
2. **审批回调 Handler**：`QuoteApprovalHandler`、`RenewalApprovalHandler`——结构与 OA 一致（Redis Stream 异步消费）。

### 8.5 内部接口鉴权（`@Inner`）

`CrmReceivableInnerController.syncInvoiceStatus` 是**所有业务服务中唯一显式使用 `@Inner` 注解**的内部接口示例：

```java
@Inner(allowedServices = {"cloudflow-service-oa", "cloudflow-oa"})
@PostMapping("/{id}/invoice-status")
```

由 `cloudflow-common-security` 的 `InnerAspect` 校验 Feign 调用方的 ServiceName。这是 OA/HR 都应该但未实施的安全模式——其他服务的内部接口直接暴露，依赖网关白名单，存在伪造可能。

注意此处同时允许 `cloudflow-service-oa` 和 `cloudflow-oa` 两个 ServiceName，是为了兼容 OA 服务命名不统一的问题。

### 8.6 远程集成

- `RemoteWorkflowService` + Fallback：调工作流发起 / 完成审批
- `RemoteOaService` + Fallback：调 OA 拉合同 / 项目 / 预算 / 发票链路数据（CRM Customer 工作区聚合视图）

### 8.7 健康度算法

`cloudflow.crm.health` 配置：
- `renewal-red-days: 30` — 距续约 30 天内 RED
- `renewal-yellow-days: 90` — 90 天内 YELLOW
- `inactive-yellow-days: 30` — 30 天未跟进 YELLOW
- `overdue-red-days: 30` — 回款逾期 30 天 RED

阈值配置化，便于运营调整，设计良好。

### 8.8 配置文件问题

- 同样 `Juwangkeji@2025` 明文凭据 2 处
- `useSSL=false`
- `feign.hystrix.enabled: true` 死配置
- `logging.level.com.cloudflow.crm: DEBUG` / `mapper: DEBUG` 默认开启 → 生产环境会打印 SQL + 客户数据 + 信用代码

### 8.9 主要风险与改进（按优先级）

| 级别 | 项 |
|---|---|
| **P0** | 配置文件移除明文凭据 |
| **P0** | 关闭生产 `DEBUG` 日志，避免客户敏感信息（`credit_code`、`mobile`、`email`）打印到日志 |
| **P1** | `credit_code` / `mobile` / `email` 字段应加 `@EncryptField` + 接口返回 `@Sensitive` 脱敏 |
| **P1** | 客户、商机、回款的**数据权限**仅靠 `owner_id` 字段+SQL，应配合 `RedisDataScopeHandle` 的 `dsType` 校验，避免销售看其他销售的私单 |
| **P2** | 业务接口加 `@SaCheckPermission` 显式权限码 |
| **P3** | 长期把 `oa_crm_*` 改为 `crm_*` 表前缀 |
| **P3** | 删除 `feign.hystrix.enabled` 死配置 |

---

## 九、HR 服务（cloudflow-service-hr）—— 重大架构警告

### 9.1 基本面

| 项 | 值 |
|---|---|
| 端口 | 9005 |
| 启动类 | `HrServiceApplication`（`@EnableDiscoveryClient` + `@EnableFeignClients`） |
| Java 文件 | ≈45 |
| Controller | 3（`HealthController` / `WorkflowCallbackController` / **`HrDomainController` 单文件 132 个端点**） |
| 数据库表（`03.cloudflow-hr.sql`） | 43 张 |
| Feign Client | `AuthServiceClient` / `WorkflowServiceClient` / `BusinessRuleClient` / `OaScheduleClient` |

### 9.2 架构严重问题：上帝控制器 + 通用 CRUD over Map

HR 业务的核心实现是 `HrDomainController` + `HrDomainCrudService`：

- `HrDomainController` 单个 Java 文件含 **132 个 HTTP 端点**，覆盖员工档案、合同、文档、紧急联系人、岗位族、职级、岗位、编制、薪酬、个税档案、个税专项扣除、绩效目标、绩效结果、招聘需求、候选人、面试、Offer、生命周期（入职/转正/调岗/离职）、薪酬调整等 30+ 业务表。
- 几乎所有端点都是固定模板：
  ```java
  @PostMapping("/employees")
  public R<Long> createEmployee(@RequestBody Map<String, Object> payload) {
      return R.ok(crudService.create("hr_employee", payload));
  }
  ```
- `HrDomainCrudService` 是基于 `JdbcTemplate` + `Map<String,Object>` 的"反射式 ORM"：
  1. `getColumns(tableName)` 用 `SELECT * FROM tbl WHERE 1=0` 反查元数据缓存列名集合
  2. 通过 `toColumnName(camelCaseKey)` 把请求体的驼峰键转下划线
  3. 校验列名 ∈ getColumns，命中即拼 SQL
  4. 自动追加 `tenant_id` 过滤、`deleted=0` 过滤、`update_time = NOW()` 更新

### 9.3 由此带来的具体风险

| 风险 | 评估 |
|---|---|
| **SQL 注入** | 部分缓解。表名经 `sanitizeIdentifier`（白名单正则 `[a-zA-Z][a-zA-Z0-9_]*`）校验，且表名在 Controller 中是字符串字面量。但 `lookupSingleValue` 等少量内部方法**直接字符串拼接 idColumn / valueColumn**，若未来被业务侧动态传入将形成 SQL 注入。 |
| **字段级权限缺失** | **严重**。`hr_employee` 包含身份证号、银行账号、薪资；`hr_payroll`/`hr_salary_adjustment`/`hr_tax_profile`/`hr_tax_deduction` 全部敏感薪税数据。当前实现**任何登录用户都能 GET/POST 任意字段**。 |
| **敏感数据加密缺失** | 表结构里没有 `@EncryptField`（Map 模式根本不走 MyBatis-Plus 实体路径），意味着 `cloudflow-common-encrypt` 的拦截器对 HR 服务**完全无效**。 |
| **数据脱敏缺失** | 返回 `Map<String,Object>` 序列化为 JSON，没有 Jackson Serializer 注入点，`SensitiveUtils` 也未被调用。**身份证号、银行卡号、薪资明文返回**。 |
| **数据权限缺失** | `RedisDataScopeHandle` 依赖 MyBatis-Plus 的 `DataScopeInnerInterceptor`，但 HR 服务走 JdbcTemplate 完全绕过 MyBatis 拦截器链——`dsType` 和 `dsDeptIds` 全不生效，**HR 员工 A 能查到员工 B 的薪资**（只要走 HrDomainController）。 |
| **审计日志缺失** | 没有 `@SysLog` / `@Audit` 注解，所有 HR 操作不进 audit 表，违反企业 HR 合规审计要求。 |
| **N+1 查询泛滥** | `enrichCandidate` / `enrichOffer` / `enrichLifecycleApplication` / `enrichRecruitmentRequisition` 每条结果调一次 `get("hr_xxx", id)` 拉关联信息；分页查 50 条 = 至少 50× 关联查询；首页 Dashboard 加载肉眼可见的慢。 |
| **接口契约模糊** | 所有端点入参 `Map<String,Object>`，没有 DTO/VO，OpenAPI 文档无字段说明，前后端联调全靠口头/示例。 |
| **业务规则散落** | `HrDomainCrudService` 内部硬编码了 `STATUS_LABELS`、`SOURCE_LABELS`、`INTERVIEW_ROUND_LABELS`、`INTERVIEW_TYPE_LABELS`、状态机映射等业务字典——应抽到字典表或 enum。 |

**判断**：当前 HR 服务是"原型/PoC 级实现"，不适合上线承载真实薪税与员工档案数据。

### 9.4 例外：`HrPerformanceService` 与 `WorkflowCallbackController` 是规范实现

| Service | 状态 | 备注 |
|---|---|---|
| `HrPerformanceService` / `HrPerformanceServiceImpl` | 规范 | 仍用 Map 但接口签名收敛，有专门 service 类层 |
| `WorkflowCallbackController` + `WorkflowCallbackServiceImpl` | 规范 | 与 OA/CRM 共享模板，Redis Stream 异步回调 |
| `HealthController` | 仅 actuator 探活 | |
| `BusinessRuleClient` + Fallback | 规范 | 调 auth 服务的规则中心，fallback 默认 PASS（合规问题已在 5.8 提及） |

### 9.5 工作流接入

`cloudflow.hr.workflow.process-keys` 配置 10 个映射：

| 业务 | 流程 Key |
|---|---|
| 请假 | `leave_request` |
| 加班 | `overtime_request` |
| 考勤补卡 | `attendance_appeal` |
| Offer | `offer_approval` |
| 入职 | `onboarding_approval` |
| 转正 | `probation_confirmation_approval` |
| 招聘需求 | `biz_recruit` |
| 离职 | `resignation_approval` |
| 调薪 | `salary_adjustment_approval` |
| 调岗 | `transfer_approval` |

设计良好，配置驱动，新增审批业务不用改代码。

### 9.6 业务参数配置（亮点）

`cloudflow-service-hr.yaml` 中：
- 员工编号 `EMP + 8位`
- 试用期 90 天、合同到期提醒 30 天
- 薪资发放日 15 号
- 个税起征点 5000
- 五险企业/个人比例完整定义（养老 0.16/0.08、医疗 0.08/0.02、失业 0.005/0.005、工伤 0.002、生育 0.007）
- 公积金 0.12

这种"业务字典 + 计算系数"集中到 yaml 的做法对运维友好，但**社保税率应进 sys_config 或专门的 hr_rule 表**，而非 yaml——比例每年都可能变，每次都改 Nacos 配置需要操作权限。

### 9.7 数据库表（43 张）

涉及隐私敏感字段的表（一定要加密 / 脱敏）：
- `hr_employee` — 姓名、性别、出生日期、身份证号、户籍、婚姻、紧急联系人
- `hr_emergency_contact` — 紧急联系人手机号
- `hr_employee_document` — 学历、证书扫描件 URL
- `hr_employee_contract` — 合同附件、薪资条款
- `hr_payroll` — 工资明细
- `hr_salary_adjustment` — 调薪历史
- `hr_tax_profile` — 个税基础信息（家庭成员等）
- `hr_tax_deduction` — 专项附加扣除（房贷、租房、赡养、子女教育、继续教育、大病医疗）
- `hr_social_security` — 社保账户、参保城市
- `hr_offer` — 薪资 Offer

**全部以明文 + JdbcTemplate 直读写**。

### 9.8 主要风险与改进（按优先级）

| 级别 | 项 | 状态 |
|---|---|---|
| **P0** | **HR 服务在当前形态下不可用于承载真实员工数据**——需立即引入字段级权限 + 数据脱敏 + 数据权限 + 字段加密 | ⚠️ 部分修复：脱敏 + 数据权限 + 高敏写权限已落地（见 §9.10），字段级加密仍待办 |
| **P0** | `HrDomainController` 拆分为按业务子域的多个 Controller（员工、合同、薪酬、绩效、招聘、生命周期），用 DTO/VO 替换 Map | 待处理（第二波重构） |
| **P0** | 切回 MyBatis-Plus 路径以启用 `TenantLineInnerInterceptor` / `DataScopeInnerInterceptor` / `MybatisEncryptInterceptor` | 待处理（第二波重构） |
| **P0** | 配置文件移除明文凭据 | 待处理 |
| **P1** | 关闭 HR 服务的生产 DEBUG 日志（**员工身份证号会进 mapper 日志**） | 待处理 |
| **P1** | 对 `hr_employee.id_card` / `bank_account` / `hr_payroll.*` 等敏感字段强制 `@EncryptField` | 待处理（依赖切回 MyBatis-Plus） |
| **P1** | 接口返回路径加 `@Sensitive` 注解，配合补齐的 Jackson Serializer 自动脱敏 | ⚠️ 当前以 `HrDomainCrudService` 内置 MASK_COLUMNS 列名匹配实现，等同效果但更脆弱 |
| **P1** | 所有 HR 写操作加 `@SysLog` + `@Audit` | 待处理 |
| **P2** | 修复 N+1：聚合查询用 JOIN 或一次批量 IN 查询 | 待处理 |
| **P2** | 社保比例迁出 yaml 到 `hr_payroll_rate` 表（按地区 + 生效日期） | 待处理 |
| **P3** | `lookupSingleValue` 等少数拼字符串方法改为 PreparedStatement 占位 | 待处理 |

### 9.9 第一波加固前的对比（原始风险描述）

以下评估反映**首版审计时**的代码状态，已被 §9.10 描述的变更部分覆盖：

| 风险 | 评估（修复前） |
|---|---|
| **字段级权限缺失** | 严重：任何登录用户都能 GET/POST 任意字段 |
| **数据脱敏缺失** | 严重：身份证号、银行卡号、薪资明文返回 |
| **数据权限缺失** | 严重：HR 员工 A 能查到员工 B 的薪资（HrDomainController 走 JdbcTemplate 绕过 MyBatis 拦截器） |

### 9.10 已完成的第一波加固（2026-05-13）

为在不重写 132 个端点的前提下尽快收敛风险，本次落地了 4 项 service 层加固。所有改动集中在 `HrDomainController.java` 与 `HrDomainCrudService.java` 两个文件，外加 HR `pom.xml` 增加 `cloudflow-common-sensitive` 依赖。

| 加固项 | 实现位置 | 行为 |
|---|---|---|
| 全端点强制登录 | `HrDomainController` class-level `@SaCheckLogin` | 132 个端点统一拒绝匿名请求；与网关 AuthFilter 形成纵深防御 |
| 高敏薪税表写权限 | `HrDomainCrudService.assertWriteAllowed` 在 `create/update/delete/setHeadcountActualCount` 入口调用 | 13 张高敏表（`hr_payroll`、`hr_employee_salary`、`hr_salary_adjustment`、`hr_performance_salary_adjustment`、`hr_employee_comp`、`hr_employee_benefit`、`hr_tax_profile`、`hr_tax_deduction`、`hr_social_security`、`hr_salary_grade`、`hr_salary_structure`、`hr_salary_structure_item`、`hr_salary_item`）只接受持有 `admin` / `hr_admin` / `hr_manager` / `hr_specialist` 角色的写入；其他角色抛 `HrBusinessException("FORBIDDEN_SENSITIVE_WRITE")` |
| 数据权限过滤 | `HrDomainCrudService.appendDataScope` 在 `appendFilters` 与 `get` 中调用 | 基于 `UserContext.getDsType()` / `getDsDeptIds()` / `getUserId()` / `getUserName()` 收紧 SQL：dsType=0 全部、dsType=1/2/3 按 `dept_id IN (...)`、dsType=4 按 `user_id` 或 `create_by`；表无对应列则不附加条件（保护字典 / 共享表）；特权角色不受限 |
| 行级敏感字段脱敏 | `HrDomainCrudService.maskRow` 在 `list / page / get` 返回前调用 | 30 个敏感列名（`id_card` / `bank_account` / `salary*` / `phone` / `email` / `address` / 税额 / 社保金额等）按 `SensitiveUtils.maskByFieldName` 脱敏；特权角色跳过 |

#### 9.10.1 改动文件清单

```
cloudflow-backend/cloudflow-service-hr/pom.xml
  + cloudflow-common-sensitive 依赖

cloudflow-backend/cloudflow-service-hr/src/main/java/com/cloudflow/hr/
  controller/HrDomainController.java
    + import cn.dev33.satoken.annotation.SaCheckLogin;
    + class-level @SaCheckLogin

  service/HrDomainCrudService.java
    + import UserContext / SensitiveUtils / HrBusinessException
    + HIGH_SENSITIVE_WRITE_TABLES / HIGH_SENSITIVE_ROLES / MASK_COLUMNS 常量
    + isPrivilegedUser() / assertWriteAllowed() / appendDataScope() / maskRow() / camelToSnake() 私有方法
    + create/update/delete/setHeadcountActualCount 入口 assertWriteAllowed
    + list/page/get 出口 maskRow
    + get/appendFilters 中 appendDataScope
```

#### 9.10.2 仍存在的限制

1. **未实现字段级加密**：薪税字段在 DB 中仍以明文存储，本次加固只是阻止"未授权用户读到原文 / 未授权用户写入"，不能防 DBA / 备份泄漏。完整修复需要切回 MyBatis-Plus 路径 + `@EncryptField`。
2. **角色映射依赖运维约定**：当前默认特权角色码为 `admin` / `hr_admin` / `hr_manager` / `hr_specialist`，与 `06.cloudflow-business-seed.sql` 中的角色码需要对齐才能生效；建议把这组列表抽到 `cloudflow.hr.privileged-roles` 配置项。
3. **脱敏覆盖按列名匹配**：依赖 `MASK_COLUMNS` 白名单与下划线列名命名一致；新增敏感列时必须同步更新 `MASK_COLUMNS`，遗漏即漏脱敏。
4. **审计日志仍缺失**：未加 `@SysLog` / `@Audit`，HR 写操作不会进 audit 表。
5. **N+1 与拆 Controller 未做**：性能问题与代码可维护性问题保留。

> §9.2 - §9.7 关于"上帝控制器 + 通用 CRUD over Map"的整体架构判断仍然成立，本次加固只是把"敏感数据立即裸奔"降级为"敏感数据需通过角色+数据权限才能访问"。后续仍需推进第二波重构。

---

## 十、数据库脚本审计

### 10.1 脚本组成与规模

| 文件 | 用途 | CREATE TABLE 实测 | README 描述 | 行数 |
|---|---|---|---|---|
| `01.cloudflow-common.sql` | 系统底座（租户/部门/用户/角色/菜单/字典/配置/文件/日志 + 业务规则中心 3 表） | 18 | 15 | 408 |
| `02.cloudflow-workflow.sql` | 工作流引擎全套 | 42 | 41 | 1021 |
| `03.cloudflow-hr.sql` | HR 全量 | 37 | 43 | 784 |
| `04.cloudflow-oa.sql` | OA 全量 | 52 | 36 | 1519 |
| `05.cloudflow-crm.sql` | CRM | 8 | 8 | 267 |
| `06.cloudflow-business-seed.sql` | 初始账号、菜单、流程模板、演示数据 | 0 | — | 8526 |
| `99.cloudflow-clear-all.sql` | 数据清空 | 0 | — | 87 |
| **结构表合计** | | **157** | **143** | |

**README 的表数量描述与实测不符**：尤其 OA 实际 52 张（README 写 36），HR 实际 37 张（README 写 43）。文档过时。

### 10.2 字符集与引擎

- 全部 `ENGINE=InnoDB`，无 MyISAM。
- 全部 `DEFAULT CHARSET=utf8mb4`，无 utf8（latin1）混用。
- 排序规则：
  - 数据库默认 `utf8mb4_unicode_ci`（建库脚本 `01.cloudflow-common.sql:9`）
  - CRM/HR 服务连接串显式用 `connectionCollation=utf8mb4_0900_ai_ci`（MySQL 8 默认） → 与建库时的 `utf8mb4_unicode_ci` 不一致，**JOIN 查询时可能触发隐式排序规则转换，性能下降**

### 10.3 主键与 ID 策略

- 一律 `BIGINT AUTO_INCREMENT`（含极少数 `VARCHAR(64)` 流程定义 ID 由业务层生成）。
- `sys_tenant.AUTO_INCREMENT=100000` 把租户 ID 从 6 位起步，预留低位号段。
- 未见 UUID / 雪花 ID 字段（`IdUtils.simpleUUID` 仅用于 captcha/passToken/file 等运行时键）。

### 10.4 多租户字段覆盖

- `tenant_id` 字段在结构表中累计出现 **158 处声明**，覆盖率：
  - 业务表绝大多数携带 `tenant_id BIGINT DEFAULT 100000`
  - 部分**全局表显式不带 tenant_id**：`sys_menu`、`sys_dict_type`、`sys_dict_data`、`sys_config`、`workflow_template`、`template_category`（与 `cloudflow.tenant.ignore-tables` 配置一致）

### 10.5 索引覆盖

- 索引声明累计 **445 处**（`KEY` + `INDEX` + `UNIQUE KEY`）。
- 抽样检查几张高频表，索引设计合理：
  - `wf_process_definition`：10 个索引，含复合 `(category, status, is_archived)`、`(template_id, is_archived)`
  - `hr_employee`：`(tenant_id, employee_no)` UK、`(tenant_id, employee_status)`、`(tenant_id, dept_id)`、`(tenant_id, user_id)` — **全部以 tenant_id 开头**，配合 `tenant_id = ?` 拦截器追加条件能命中
  - `oa_crm_customer`：`(tenant_id, customer_code)` UK、`(owner_id)`、`(health_level)`、`(customer_name)`

整体索引规范度高，未见明显缺索引的高频表。

### 10.6 外键策略

- **结构表中无任何 `FOREIGN KEY` 约束**（grep 0 处）。
- 设置 `SET FOREIGN_KEY_CHECKS = 0` 表明刻意关闭外键校验。
- 这是微服务架构的常规选择（关系完全由代码层维护），但意味着**孤儿数据需要应用层兜底**——抽查 HR 服务的 `HrDomainCrudService` 删除是逻辑删除 `deleted=1`，未级联删除子表 → 子表（如 `hr_employee_contract`）可能成为孤儿。

### 10.7 软删除约定

- `deleted` 标志累计 **141 处**（含 `deleted` 与 `del_flag`）。
- **两套软删除规范并存**：
  - `del_flag CHAR(1) DEFAULT '0'`（取值 `'0'`/`'2'`）— common / oa 老表
  - `deleted TINYINT(1) NOT NULL DEFAULT 0`（取值 0/1）— hr / 部分新表
- MyBatis-Plus 实体上的 `@TableLogic` 注解需要分别配置 `value` 和 `delval`，给代码维护带来负担。建议长期统一为 `deleted TINYINT`。

### 10.8 审计字段

`create_by` / `create_time` / `update_by` / `update_time` 在结构表中基本全覆盖；少量纯关联表（如 `sys_user_role`、`sys_role_menu`）未带。`MyMetaObjectHandler`（`common-data` 模块）自动填充。

### 10.9 金额字段

- 全部使用 `DECIMAL(12,2)` 或 `DECIMAL(14,2)`，**未使用 `DOUBLE` / `FLOAT`**（grep 0 处）—— 财务级金额精度合规。
- 抽样：`hr_salary_*`、`oa_crm_quote`、`oa_crm_receivable`、`biz_expense_*` 等表均符合。

### 10.10 敏感字段

- HR `hr_employee` 表**实际未存储身份证号、银行卡号**——只有 `name / gender / birth_date / phone / email`；身份证号与银行账号疑似通过 `hr_employee_document.attachment_urls`（JSON）以 OSS URL 形式承载。
- 仍属敏感的字段：
  - `hr_employee.phone`、`hr_employee.email`
  - `hr_emergency_contact.phone`、`hr_emergency_contact.address`
  - `hr_employee_salary` / `hr_payroll` / `hr_employee_comp` / `hr_tax_*` 全部金额与税务字段
  - `oa_crm_customer.credit_code`、`mobile`、`email`
  - `sys_user.phonenumber`、`sys_user.email`
- **当前没有任何字段标注 `@EncryptField` 强制加密**（代码侧抽样未见使用，需要全工程扫描确认）；`hr_employee_document.attachment_urls` 中的附件 URL 应配合 OSS 私有 Bucket + 临时签名 URL。

### 10.11 表前缀命名

- `sys_*`：系统基础（用户、菜单、字典、配置、日志、文件、租户、部门、岗位、角色）
- `biz_*`：业务流转（预算执行流水、采购流水、报销项、出差等）
- `oa_*`：OA 业务（合同、印章、证照、知识库、风险等）
- `oa_crm_*`：**CRM（命名遗留，建议长期改 `crm_*`）**
- `wf_*` + `workflow_*`：工作流（混用，**部分用 `wf_` 前缀，部分用 `workflow_` 前缀**，建议统一）
- `hr_*`：HR 全量
- `template_*`：模板库
- `meeting_room` / `vehicle_*` / `frontend_error_log`：单独前缀

命名规范度中等，存在 3 处不一致：CRM `oa_crm_*`、Workflow `wf_*` vs `workflow_*`、单数表名（如 `meeting_room` 而非 `oa_meeting_room`）。

### 10.12 文件演进风险

`06.cloudflow-business-seed.sql` 单文件 **8526 行**，包含初始菜单、账号、流程模板、HR 复杂场景演示数据。Docker Compose 中**未挂载此文件到 `docker-entrypoint-initdb.d/`**——意味着首次启动容器不会自动建演示账号 `admin`/`li`/`wang`，README 描述与 Compose 实际不一致。

### 10.13 主要风险与改进（按优先级）

| 级别 | 项 |
|---|---|
| **P1** | README 表数量与实际偏差较大（多 14 张），更新文档或核对漏掉的脚本 |
| **P1** | 排序规则不一致（建库 `utf8mb4_unicode_ci` vs 连接串 `utf8mb4_0900_ai_ci`）—— 统一到 8.0 默认的 `utf8mb4_0900_ai_ci` 或保持 `unicode_ci` |
| **P2** | 软删除标志两套并存（`del_flag CHAR(1)` 与 `deleted TINYINT`），长期统一 |
| **P2** | 表前缀统一：CRM 改 `crm_*`、Workflow 统一 `wf_*` |
| **P2** | Docker Compose 增加 `06.cloudflow-business-seed.sql` 的初始化挂载（或 README 明确"演示数据需手动导入"） |
| **P3** | 评估 JSON 字段（`attachment_urls`）的 OSS URL 是否签名、过期、按租户 Bucket 隔离 |

---

## 十一、配置中心与部署审计

### 11.1 Nacos 配置文件

`config/` 下 7 份 YAML：

| 文件 | 体量 | 与服务对应 |
|---|---|---|
| `cloudflow-common.yaml` | 公共 datasource/redis/sa-token | 所有业务服务 |
| `cloudflow-gateway.yaml` | 路由 + CORS + 默认租户 | gateway |
| `cloudflow-auth.yaml` | DB/Redis/Sa-Token + 密码策略 + 验证码 + session | auth |
| `cloudflow-service-workflow.yaml` | engine/task/approval/notification/deployment 配置 + Hystrix | workflow |
| `cloudflow-oa.yaml` | 各业务模块开关阈值 | oa |
| `cloudflow-service-crm.yaml` | 工作流键 + 健康度阈值 | crm |
| `cloudflow-service-hr.yaml` | 员工/招聘/培训/绩效/薪资/社保全套参数 + 10 个流程键 | hr |

### 11.2 明文凭据扫描（已进入 git 历史）

```
config/cloudflow-auth.yaml:19          MySQL root密码
config/cloudflow-auth.yaml:39          Redis密码
config/cloudflow-common.yaml:35,52     MySQL/Redis密码
config/cloudflow-oa.yaml:19,38         MySQL/Redis密码
config/cloudflow-service-crm.yaml:14,28
config/cloudflow-service-hr.yaml:19,33
config/cloudflow-service-workflow.yaml:19,39

cloudflow-gateway/src/main/resources/application.yml:86      ${REDIS_PASSWORD:Juwangkeji@2025}（默认值仍是明文）
cloudflow-service-workflow/src/main/resources/application.yml:34,52   ${VAR:Juwangkeji@2025}
cloudflow-service-oa/src/main/resources/application.yml:31,38         直接硬编码，无环境变量包装
```

明文密码 `Juwangkeji@2025` 共出现 **17 处**，且已通过 git commit `ed8bf45e`、`b391fc91`、`07b724ce`、`e9bbe8c6` 等被推送（按 git status 显示）。

更严重的：
1. `cloudflow-service-oa/application.yml` **没有任何环境变量包装**，密码完全硬编码到 jar 内。
2. `cloudflow-gateway` / `cloudflow-service-workflow` 等服务的 application.yml 使用 `${VAR:Juwangkeji@2025}` —— 即使 docker compose 设置了环境变量，**默认值仍是明文**，git diff 也会暴露。
3. **Nacos 凭据** `username: nacos / password: nacos` 在每个服务的 application.yml 中硬编码，**Nacos 自身的认证强度=零**。

### 11.3 多源配置冲突

每个服务都同时加载：
- Nacos `cloudflow-{service}.yaml`
- Nacos `cloudflow-common.yaml`
- 本地 `application.yml` 兜底
- 部分服务还会加载 `optional:file:./config/*.yaml`

观察到的冲突：
- gateway 本地 `is-read-cookie: true` 与 Nacos common `is-read-cookie: false`
- DB URL 在 common.yaml 用 `useSSL=true`，在 auth/oa/workflow/crm/hr 各自 yaml 中 `useSSL=false`，覆盖关系取决于配置加载顺序

实际行为取决于 Spring 的 `spring.config.import` 顺序，文档未明确说明，会让运维定位"为啥这个配置不生效"很痛苦。

### 11.4 多环境隔离

- 全部服务的 `spring.profiles.active=dev`（gateway application.yml）默认。
- Docker Compose 用 `SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}` 覆盖到 prod。
- 但 `config/` 下**只有 prod 风格的 yaml**（地址都是 192.168.1.173），没有 `cloudflow-common-dev.yaml` / `cloudflow-common-prod.yaml` 分环境。
- Nacos 通过 namespace `0ccb9313-39d8-4a58-9fa5-ce834b77e60d` 单一空间 —— 实际 prod/dev 共用一套 Data ID 与命名空间。

### 11.5 日志级别（生产风险）

| 服务 | 默认日志级别 |
|---|---|
| auth | `com.cloudflow.auth: DEBUG`、`com.cloudflow.auth.mapper: DEBUG` |
| workflow | `com.cloudflow.workflow: DEBUG`、`com.cloudflow.workflow.mapper: DEBUG` |
| oa | application.yml 显示 `log-impl: StdOutImpl`（**MyBatis SQL 直接打 stdout**） |
| crm | `com.cloudflow.crm: DEBUG`、`com.cloudflow.crm.mapper: DEBUG` |
| hr | `com.cloudflow.hr: DEBUG`、`com.cloudflow.hr.mapper: DEBUG`、`com.cloudflow.common.datascope: DEBUG` |

`com.cloudflow.common.yaml` 中虽然把根 logger 设为 `INFO`，但**子配置文件再次覆盖为 DEBUG**——实际运行时业务服务的 mapper 日志全开，所有 SQL+参数+结果集进日志：身份证、手机号、薪资、密码哈希都会落盘。

### 11.6 Docker Compose 编排

`docker-compose.yml` 共编排 13 个容器：

| 容器 | 资源限制 | 健康检查 | 依赖 |
|---|---|---|---|
| mysql | 默认 | mysqladmin ping | 无 |
| redis | maxmemory 256mb | redis-cli ping | 无 |
| nacos | 0.5-1.0 cpu / 0.5-1G | curl actuator/health | mysql |
| gateway | 0.25-0.5 cpu / 0.25-0.5G | curl actuator/health | nacos, redis |
| auth | 同上 | 同上 | mysql, redis, nacos |
| workflow | 0.5-1.0 cpu / 0.5-1G | 同上 | mysql, redis, nacos |
| oa | 0.25-0.5 cpu / 0.25-0.5G | 同上 | mysql, redis, nacos |
| crm | 同上 | 同上 | 同上 |
| hr | 同上 | 同上 | 同上 |
| prometheus | 0.1-0.5 cpu / 0.128-0.5G | 无 | gateway/auth/wf/oa/crm/hr |
| grafana | 同上 | 无 | prometheus |
| frontend | 0.1-0.25 cpu / 0.064-0.128G | wget https://localhost | gateway |

**资源限制偏紧**：
- workflow 限制 1G 内存承载 41 张表+多线程异步+定时任务，配合 `MaxRAMPercentage=75.0`（≈768MB 堆），中大型流程场景下可能 OOM。
- HR 服务 512MB 内存承载 132 个端点 + Map 序列化频繁 → GC 压力大。

### 11.7 凭据与默认密码

| 凭据 | 默认值（docker-compose.yml） | 风险 |
|---|---|---|
| `MYSQL_ROOT_PASSWORD` | `cloudflow_2026` | 弱密码 |
| `REDIS_PASSWORD` | `cloudflow_redis_2026` | 弱密码 |
| `NACOS_AUTH_TOKEN` | `SecretKey0123456789...` | **示例值原样使用，已被各种公开文档收录** |
| `GRAFANA_ADMIN_PASSWORD` | `cloudflow_grafana_2026` | 弱密码 |

**Docker Compose 默认密码 ≠ Nacos 配置中的密码**：
- Compose 启动 MySQL 用 `cloudflow_2026`
- Nacos 配置文件用 `Juwangkeji@2025`
- 业务服务用 Nacos 配置 → **数据库连接会全部失败**

这强烈暗示 docker-compose 流水线**从未端到端跑通过**，README 中"`docker compose up -d` 启动完整环境"是不可行的。

### 11.8 Dockerfile 评估

抽样 `docker/gateway/Dockerfile`：

```dockerfile
FROM maven:3.8-openjdk-17 as builder
COPY cloudflow-backend/ .
RUN mvn clean package -DskipTests -pl cloudflow-gateway -am

FROM eclipse-temurin:17-jre-alpine
RUN addgroup -S appuser && adduser -S appuser -G appuser
USER appuser
COPY --from=builder --chown=appuser:appuser /app/cloudflow-gateway/target/*.jar app.jar
ENTRYPOINT ["java", "-XX:+UseContainerSupport", "-XX:MaxRAMPercentage=75.0",
            "-Djava.security.egd=file:/dev/./urandom", "-jar", "app.jar"]
```

**优点**：
- 多阶段构建，最终镜像基于 alpine + 17-jre，体积小
- 非 root 用户 `appuser`
- 容器化 JVM 参数齐全
- `java.security.egd` 优化随机数源

**问题**：
- `-DskipTests` 跳过测试构建（已在 enforcer 中通过 `analyze-only` 验证，可接受）
- 没有 `HEALTHCHECK` 指令（依赖 docker-compose 的 healthcheck）
- 没有限制堆外内存 / Metaspace
- 没有 Layered JAR 优化，热重建时无法利用 Docker 层缓存

### 11.9 监控

- Prometheus `/actuator/prometheus` 抓取，**无认证**（容器内网信任）
- 7 个 scrape job 覆盖全后端
- `alerts.yml` 已就绪（未审计具体规则）
- Grafana 配 provisioning（dashboard.yml + datasource cloudflow-prometheus.yml）
- Grafana 默认端口 3000 与前端开发端口冲突——已通过 `GRAFANA_PORT` 环境变量提供切换能力

### 11.10 主要风险与改进（按优先级）

| 级别 | 项 |
|---|---|
| **P0** | **轮换 `Juwangkeji@2025`**，从 git 历史中已无法擦除，必须修改生产 DB/Redis 密码 |
| **P0** | 配置文件中所有密码改为 `${ENV_VAR}`（无默认值），并把默认 fallback 从源码与 git 历史中清除 |
| **P0** | Docker Compose 与 Nacos 配置的密码必须一致，否则一键启动不可达——`docker compose up -d` 端到端流水线必须能跑通 |
| **P0** | Nacos `nacos/nacos` 默认凭据修改并启用 RBAC，否则任意可达 8848 端口的人能改所有应用配置 |
| **P0** | 所有 `application.yml` 中的 `useSSL=false` 改为 `useSSL=true` + `verifyServerCertificate=false`，至少做到链路加密 |
| **P0** | 关闭生产环境业务服务 mapper DEBUG 日志 |
| **P1** | 多环境隔离：拆 `cloudflow-common-dev.yaml` / `-prod.yaml`，Nacos namespace 区分 |
| **P1** | 收敛配置双源：本地 application.yml 只保留 fallback 极简配置，业务以 Nacos 为准 |
| **P1** | 提升 workflow / HR 服务容器资源限制 |
| **P2** | Dockerfile 加 `HEALTHCHECK` 与 Layered JAR 提升构建效率 |
| **P3** | Prometheus 抓取增加 token 或 basic auth |

---

## 十二、跨模块代码重复 / 共性问题

### 12.1 三服务镜像的工作流回调三件套

OA / CRM / HR 三个业务服务都各自实现了一份完全相同结构的工作流回调链路：

```
WorkflowApprovalCallbackStreamConfig   (Redis Stream 消费者注册)
WorkflowCallbackStreamConstants        (Stream Key + Group 常量)
WorkflowApprovalCallbackStreamConsumer (消费者实现)
WorkflowCallbackService / Impl         (业务分发入口)
ApprovalResultHandler                  (业务子域 Handler 接口)
{Business}ApprovalHandler              (具体业务 Handler 实现)
```

三套代码 80% 雷同，仅 Handler 实现不同。建议抽出 `cloudflow-common-workflow-callback`（或类似）模块统一抽象，业务服务只实现 Handler。

### 12.2 三服务的 Feign 配置类

OA / CRM / HR / Workflow 都有 `config/FeignRequestInterceptor.java`，作用一致：把 `UserContext` 中的 token/userId/tenantId 透传到下游 Feign 调用。代码可下沉到 `cloudflow-common-security`。

### 12.3 双权限注解体系

- `cloudflow-auth` 使用 `@HasPermission`（自研，AOP 抛 `RuntimeException`）
- `cloudflow-service-workflow` 使用 `@SaCheckLogin` / `@SaCheckRole` / `@SaCheckPermission`（Sa-Token 标准）
- `cloudflow-service-oa/crm/hr` 大量端点**无任何权限注解**（仅靠网关已登录校验）

应统一到 Sa-Token 标准注解 + 显式权限码。

### 12.4 配置文件 boilerplate

- `feign.hystrix.enabled: true` 死配置在所有业务服务的 yaml 中存在
- `hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds: 10000` 同上
- Hystrix 已停止维护，Spring Cloud 2023 + Spring Cloud Alibaba 2023 路线应用 **Sentinel** 或 **Resilience4j**

### 12.5 重复的 `getClientIp` 实现

- `AuthController.getClientIp`（auth）
- `ErrorReportController.getClientIp`（oa）
- `RateLimiterAspect.getIpAddr`（common-ratelimiter）
- `RateLimitConfig.resolveClientIp`（gateway）

四套实现大同小异。应抽到 `cloudflow-common-core/IpUtils`。

### 12.6 包名错位

- `com.cloudflow.common.core.utils.TokenService` 实际在 `cloudflow-common-security` 模块
- `com.cloudflow.common.core.utils.RedisCache` 实际在 `cloudflow-common-redis` 模块
- `com.cloudflow.common.core.utils.RedisStreamUtil` 实际在 `cloudflow-common-redis` 模块
- `com.cloudflow.common.core.utils.SysConfigHelper` 实际在 `cloudflow-common-redis` 模块
- `com.cloudflow.common.handler.MyMetaObjectHandler` 实际在 `cloudflow-common-data` 模块
- `com.cloudflow.common.cache.TenantRedisCacheManager` 实际在 `cloudflow-common-redis` 模块
- `com.cloudflow.common.config.MybatisPlusConfig` 实际在 `cloudflow-common-data` 模块
- `com.cloudflow.common.config.RedisConfig` 实际在 `cloudflow-common-redis` 模块

历史重构遗留。Maven 依赖关系正确，但 IDE 跳转与代码导航很迷惑。

---

## 十三、综合风险摘要与改进路线图

### 13.1 P0（必须立即处理）

1. **凭据轮换与密钥治理**（**未处理**）
   - 立即修改生产环境 MySQL/Redis/Nacos/Grafana 默认密码
   - 从所有 yaml 和 application.yml 中清除明文密码（含环境变量默认值）
   - 把这些配置移到 K8s Secret / Vault / Nacos 配置加密功能

2. **HR 服务架构重构**（**第一波 ✅ 已完成 2026-05-13；后续 ⏳ 待办**）
   - ✅ 已落地：全端点 `@SaCheckLogin`、高敏薪税表写权限校验、数据权限过滤、行级脱敏返回
   - ⏳ 后续：拆 `HrDomainController` 为多子域 Controller、用 DTO/VO 替换 Map、切回 MyBatis-Plus 路径恢复 `@EncryptField` / 字段级审计；详见 §9.10

3. **CORS 收敛**（**✅ 已完成 2026-05-13**）
   - ~~网关 `allowed-origin-patterns: "*"` + `allow-credentials: true` 风险面过宽~~
   - ~~改为前端域名白名单~~
   - 改为环境变量 `CLOUDFLOW_CORS_ALLOWED_ORIGINS` 驱动的白名单，默认 localhost 开发域；docker-compose 已注入环境变量；详见 §4.7

4. **Groovy 脚本沙箱**（**未处理**）
   - 确认 `workflow.script.enabled` 默认 `false`
   - 加 `SecureASTCustomizer` 限制可执行语句

5. **生产 DEBUG 日志**（**未处理**）
   - 关闭所有业务服务的 `mapper: DEBUG` 与 `MyBatis log-impl: StdOutImpl`

6. **MySQL TLS**（**未处理**）
   - 应用层连接串 `useSSL=true`

7. **业务接口权限校验**（**未处理**）
   - OA/CRM/HR 大量端点缺权限码，依赖前端隐藏菜单 — 必须加 `@SaCheckPermission`
   - 注：HR 在本次加固后所有端点已强制 `@SaCheckLogin` + service 层数据权限/脱敏，但仍缺细粒度权限码

### 13.2 P1（短期内必做）

1. `EncryptUtils` 异常时不应回退明文
2. `common-sensitive` 补齐 Jackson Serializer 与注解
3. 业务规则 fallback 策略按 ruleCode 区分（不应统一 PASS）
4. 工作流定时任务加 `@DistributedJob` 保证多副本一致
5. 删除 Hystrix 死配置，明确 Sentinel/Resilience4j 路线
6. Docker Compose 与 Nacos 配置密码同步，让 `docker compose up -d` 端到端跑通
7. CRM 敏感字段（credit_code/mobile/email）加密+脱敏
8. 多环境隔离：Nacos namespace 拆分 dev/test/prod，配置文件 profile 化

### 13.3 P2（中期优化）

1. 收敛三处镜像的工作流回调代码到公共模块
2. 软删除 `del_flag` vs `deleted` 统一
3. 表前缀统一（`oa_crm_*` → `crm_*`、`workflow_*` 与 `wf_*` 二选一）
4. 工作台聚合接口加缓存
5. 数据权限 `RedisDataScopeHandle` 与 HR 服务对齐
6. 限流 IP 解析校验来源是否网关
7. `@RepeatSubmit` args 摘要算法改稳定
8. 业务规则中心 fallback、滑块 passToken 与 IP/请求绑定

### 13.4 P3（长期 / 清理）

1. `TokenService` 等错位包名物理修正
2. `cloudflow-common-seata` 落地或删除
3. README 中表数量、模块描述与实际对齐
4. Dockerfile 加 Layered JAR 优化
5. Hystrix → Sentinel 实质迁移
6. 工作流 `HotUpdateController` 加高危确认 token

### 13.5 风险分布全景

| 维度 | 严重 | 中等 | 轻微 |
|---|---|---|---|
| 凭据/密钥 | 17 处明文 + 4 处弱默认 | Nacos 默认凭据 | 配置双源 |
| 数据保护 | ~~HR 整体未保护~~ HR 已落地 service 层数据权限+脱敏（仍缺字段级加密）、CRM/Auth 部分字段未加密 | `common-encrypt` 异常回退明文 | `common-sensitive` 未自动化（HR 已就地引用） |
| 权限模型 | OA/CRM 业务端点缺权限码（HR 已强制 `@SaCheckLogin`） | 两套权限注解并存 | `@HasPermission` 异常类型 |
| 注入与 RCE | Groovy 脚本无沙箱 | ~~CORS 过宽 + credentials~~ ✅ CORS 已白名单化 | — |
| 代码质量 | HR 上帝控制器 + 通用 Map（仍待拆分） | 三服务镜像回调三件套 | 包名物理错位、Hystrix 死配置 |
| 可观测 | 生产 DEBUG 日志 | Prometheus 无认证 | actuator 暴露范围 |
| 部署 | docker-compose 与 Nacos 密码不同步 | 资源限制偏紧 | Dockerfile 可优化 |

### 13.6 修复记录（按时间）

| 日期 | 范围 | 改动 | 文件 |
|---|---|---|---|
| 2026-05-13 | P0#3 | 网关 CORS 白名单化（环境变量驱动 + 默认 localhost 开发域） | `config/cloudflow-gateway.yaml`、`cloudflow-backend/cloudflow-gateway/src/main/resources/application.yml`、`docker-compose.yml` |
| 2026-05-13 | P0#2 第一波 | HR class-level `@SaCheckLogin`；service 层加敏感薪税表写白名单 + 角色校验、数据权限过滤、行级脱敏 | `cloudflow-backend/cloudflow-service-hr/pom.xml`、`cloudflow-backend/cloudflow-service-hr/src/main/java/com/cloudflow/hr/controller/HrDomainController.java`、`cloudflow-backend/cloudflow-service-hr/src/main/java/com/cloudflow/hr/service/HrDomainCrudService.java` |

---

## 十四、附录

### 14.1 审计文件覆盖度

- 已读源码：网关 5/5、auth 关键类、common 16 个子模块各 1-3 个核心文件、workflow 入口与核心策略、OA 代表 Controller、CRM 代表 Controller、HR 全核心、所有 7 个 yaml、docker-compose、docker/gateway/Dockerfile、Prometheus 配置
- 通过 Grep 统计：CREATE TABLE / @RestController / @Mapping / tenant_id / DECIMAL / 明文凭据出现位置等
- 未深入：每个 Controller 全量 method 实现、所有 ServiceImpl、mybatis xml、前端代码、流程模板示例数据

### 14.2 文档结构

| 章节 | 主题 |
|---|---|
| 一 | 执行摘要 |
| 二 | 整体架构 |
| 三 | 公共模块（16） |
| 四 | 网关 |
| 五 | 认证 |
| 六 | 工作流 |
| 七 | OA |
| 八 | CRM |
| 九 | HR |
| 十 | 数据库 |
| 十一 | 配置与部署 |
| 十二 | 跨模块共性问题 |
| 十三 | 综合风险摘要与改进路线图 |
| 十四 | 附录 |

---

*报告完。如需对其中任意章节深入调研或落地某项 P0 整改，请单独发起任务。*
