# CloudFlow Pro P0 风险修复方案

- 编写日期：2026-05-13
- 更新日期：2026-05-14
- 适用基线：`dev` 分支（commit `e9bbe8c6` 之后，含 2026-05-14 当日增量）
- 关联文档：[后端审计报告.md](./后端审计报告.md) §13.1
- 修复范围：P0 风险剩余项的代码侧闭环与状态同步

当前状态（2026-05-14）：

| 条目 | 状态 | 说明 |
|---|---|---|
| P0-4 关闭生产 DEBUG 日志 | ✅ 已完成 | `config/*.yaml`、业务服务 `application.yml`、`docker-compose.yml` 全部改为 `LOG_LEVEL_APP` / `LOG_LEVEL_MAPPER` / `MYBATIS_LOG_IMPL` 环境变量驱动；`StdOutImpl` 与默认 DEBUG 已清除 |
| P0-5 MySQL TLS 启用 | ✅ 已完成 | `useSSL=false` 已从配置主路径移除，统一改为 `MYSQL_SSL_PARAMS` 占位符；本地启动脚本、Compose、Nacos 配置已统一 |
| P0-7 EncryptUtils 异常策略修复 | ✅ 已完成 | `encrypt()` 失败抛 `EncryptException`，`decrypt()` 失败返回 `"***"`；`cloudflow.encrypt.enabled` 默认 `false`，启用且无密钥时启动失败 |
| P0-1 凭据轮换与密钥治理 | ⚠️ 代码侧已完成 | 主仓库配置已占位符化，新增 `.env.example`、`deploy/cloudflow.env.example`、Compose 注入链、CI 明文扫描、Nacos 同步；生产实密轮换与 Nacos 默认口令切换需在线下变更窗口执行 |
| P0-3 Groovy 脚本沙箱 | ✅ 已完成 | 新增 `GroovySandboxConfig`，`cloudflow.workflow.script.enabled=false` 默认关闭，发布与运行时均经 `ScriptExecutionPolicy` 校验 |
| P0-6 OA/CRM 接口权限码补齐 | ✅ 已完成 | OA 32 个 Controller、CRM 20 个 Controller 的对外接口已补 `@SaCheckLogin + @SaCheckPermission`；`/inner/**` 保持 `@Inner`；`06.cloudflow-business-seed.sql`、前端显隐已同步 |
| P0-2 HR 服务第二波重构 | ✅ 已完成（当前仓库范围） | `HrDomainController` / `HrDomainCrudService` 已下线，改为分子域资源控制器 + Typed CRUD；`@SysLog` / `@Audit` / `@EncryptField` 已落地；按当前范围不新增 `07` 迁移脚本，不处理旧数据迁移 |

---

## 一、修复优先级与建议顺序

按 **改动复杂度 × 安全收益 × 阻塞关系** 排序，推荐分 3 波推进：

| 波次 | 项目 | 工期估算 | 改动范围 | 阻塞关系 |
|---|---|---|---|---|
| **第一波（快速胜利）** | P0-4 关闭生产 DEBUG 日志 | 0.5 天 | 7 份 yaml | ✅ 已完成 |
| | P0-5 MySQL TLS 启用 | 0.5 天 | 6 份 yaml + 启动链 | ✅ 已完成 |
| | P0-7 EncryptUtils 异常策略修复 | 0.5 天 | 单类 + 单元测试 | ✅ 已完成 |
| **第二波（治理改造）** | P0-1 凭据轮换与密钥治理 | 2-3 天 | 全部 yaml + Compose + CI | ⚠️ 代码侧已完成；生产切换待窗口 |
| | P0-3 Groovy 脚本沙箱 | 1-2 天 | workflow 服务 1 个类 + 配置 | ✅ 已完成 |
| **第三波（业务改造）** | P0-6 OA/CRM 接口权限码补齐 | 5-7 天 | OA 32 + CRM 20 Controller | ✅ 已完成 |
| | P0-2 HR 服务第二波重构 | 10-15 天 | HR 服务整体重写 | ✅ 已完成（不含旧数据迁移） |

---

## 二、P0-4 关闭生产 DEBUG 日志（最优先，影响最小）

状态同步（2026-05-14）：✅ 已完成。`config/*.yaml`、业务服务本地配置、Compose 注入链均已改为环境变量驱动；主路径已无 `DEBUG` / `StdOutImpl` 默认值。

### 2.1 问题描述

业务服务的 Nacos 配置默认开启 mapper DEBUG，导致生产环境会将 **完整 SQL + 参数（含身份证号、手机号、薪资、密码哈希）落盘到日志文件**。

### 2.2 影响范围

| 服务 | 配置项 | 文件 |
|---|---|---|
| auth | `com.cloudflow.auth: DEBUG` / `mapper: DEBUG` | `config/cloudflow-auth.yaml` |
| workflow | `com.cloudflow.workflow: DEBUG` / `mapper: DEBUG` | `config/cloudflow-service-workflow.yaml` |
| oa | `mybatis-plus.configuration.log-impl: StdOutImpl` | `config/cloudflow-oa.yaml` |
| crm | `com.cloudflow.crm: DEBUG` / `mapper: DEBUG` | `config/cloudflow-service-crm.yaml` |
| hr | `com.cloudflow.hr: DEBUG` / `mapper: DEBUG` / `datascope: DEBUG` | `config/cloudflow-service-hr.yaml` |

### 2.3 修复思路

引入 **profile 化日志级别**，dev/test 保留 DEBUG，prod 收敛到 INFO：

```yaml
logging:
  level:
    com.cloudflow.hr: ${LOG_LEVEL_APP:INFO}
    com.cloudflow.hr.mapper: ${LOG_LEVEL_MAPPER:WARN}
    com.cloudflow.common.datascope: ${LOG_LEVEL_APP:INFO}

mybatis-plus:
  configuration:
    log-impl: ${MYBATIS_LOG_IMPL:org.apache.ibatis.logging.nologging.NoLoggingImpl}
```

### 2.4 修复步骤

1. 修改 5 份 Nacos yaml，所有 DEBUG 改为 `${LOG_LEVEL_*:INFO}` 占位
2. 修改 OA 的 `log-impl: StdOutImpl` 为 `${MYBATIS_LOG_IMPL:NoLoggingImpl}`
3. `docker-compose.yml` 增加默认环境变量：
   ```yaml
   environment:
     LOG_LEVEL_APP: INFO
     LOG_LEVEL_MAPPER: WARN
     MYBATIS_LOG_IMPL: org.apache.ibatis.logging.nologging.NoLoggingImpl
   ```
4. 本地开发通过 `.env` 或 IDE 启动配置覆盖为 DEBUG

### 2.5 验证方式

- `docker compose up -d` 后查看任意业务服务日志，**不再出现 `Preparing: SELECT ...` / `Parameters: ...`**
- 触发一次登录，确认 SQL 不在日志中
- 本地启动覆盖环境变量后，DEBUG 日志正常输出

### 2.6 回滚预案

每个 yaml 改动小（< 10 行），通过 Nacos 配置历史一键回滚。

---

## 三、P0-5 MySQL 链路加密（TLS）

状态同步（2026-05-14）：✅ 已完成。当前仓库主路径已无 `useSSL=false`，统一通过 `MYSQL_SSL_PARAMS` 注入 `useSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true`。

### 3.1 问题描述

所有业务服务 `application.yml` 中 `useSSL=false`，与 `cloudflow-common.yaml` 中 `useSSL=true` 矛盾。实际加载顺序导致 **生产连接走明文**，内网抓包可还原全部 SQL 与敏感字段。

### 3.2 影响范围

- `cloudflow-backend/*/src/main/resources/application.yml` × 6（auth/workflow/oa/crm/hr/gateway）
- `config/*.yaml` × 6（同上）
- MySQL 容器需启用 TLS 证书

### 3.3 修复思路

**两步走**：

1. 应用侧统一连接串参数为 `useSSL=true&requireSSL=false&verifyServerCertificate=false`（内网信任，仅加密链路）
2. MySQL 容器侧默认开启 TLS（MySQL 8 默认即启用 caching_sha2_password + TLS，只需确认未禁用）

### 3.4 修复步骤

1. **统一连接串**：在 `cloudflow-common.yaml` 定义 `${MYSQL_SSL_PARAMS:useSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true}`，各服务连接串引用此变量
2. **删除子配置中的覆盖**：6 份业务 yaml 中的 `useSSL=false` 字段移除
3. **MySQL 容器配置**：
   - `docker-compose.yml` 的 `mysql` 服务确认未传入 `--skip-ssl`
   - 可选：挂载自签证书 `/etc/mysql/certs/`，启动参数 `--ssl-ca=...`
4. **滚动重启业务服务**，观察连接是否成功

### 3.5 验证方式

```sql
-- 在任一业务服务的连接上执行
SHOW SESSION STATUS LIKE 'Ssl_cipher';
-- 期望返回非空 cipher 名（如 TLS_AES_256_GCM_SHA384）
```

或通过 Wireshark/tcpdump 抓 3306 端口流量，确认非明文。

### 3.6 风险

- 自签证书场景需 `verifyServerCertificate=false`，否则连接失败
- 老版本 MySQL Connector/J 与 TLSv1.3 兼容性问题（当前 8.0.33 已支持）

---

## 四、P0-7 EncryptUtils 异常回退明文

状态同步（2026-05-14）：✅ 已完成。`EncryptUtils`、`EncryptorProperties`、`EncryptorAutoConfiguration` 与单元测试均已同步落地。

### 4.1 问题描述

`cloudflow-backend/cloudflow-common/cloudflow-common-encrypt/src/main/java/com/cloudflow/common/encrypt/core/EncryptUtils.java`

```java
public static String encrypt(String plainText) {
    try {
        return doEncrypt(plainText);
    } catch (Exception e) {
        return plainText;  // ⚠️ 异常时静默回退明文
    }
}
```

后果：
- 配错密钥（默认密钥即空串）→ `@EncryptField` 标注的字段**以明文落库**
- 无任何日志告警，DBA 与开发完全无感知
- 加密功能形同虚设

### 4.2 修复思路

区分 **加密路径** 与 **解密路径** 的容错策略：

| 方向 | 异常处理（当前） | 异常处理（修复后） |
|---|---|---|
| **encrypt（写入）** | 返回明文 | 抛 `EncryptException` + ERROR 日志 + 监控告警 |
| **decrypt（读取）** | 返回密文 | 返回 `"***"` + WARN 日志（保证业务可降级展示） |

理由：写入失败必须中断，否则数据永久污染；读取失败可降级展示避免拖垮业务，但需明确指示"该字段无法解密"。

### 4.3 修复步骤

1. 修改 `EncryptUtils.encrypt`：
   ```java
   public static String encrypt(String plainText) {
       if (StrUtil.isBlank(plainText)) return plainText;
       try {
           return doEncrypt(plainText);
       } catch (Exception e) {
           log.error("[EncryptUtils] 加密失败，拒绝写入明文 | key={}, len={}",
               maskKey(currentKey()), plainText.length(), e);
           throw new EncryptException("FIELD_ENCRYPT_FAILED", e);
       }
   }
   ```
2. 修改 `EncryptUtils.decrypt`：
   ```java
   public static String decrypt(String cipherText) {
       if (StrUtil.isBlank(cipherText)) return cipherText;
       try {
           return doDecrypt(cipherText);
       } catch (Exception e) {
           log.warn("[EncryptUtils] 解密失败，返回占位符 | len={}", cipherText.length());
           return "***";
       }
   }
   ```
3. 新增 `EncryptException extends ServiceException`，由 `GlobalExceptionHandler` 转换为 500 + 友好提示
4. 增加单元测试：
   - 空密钥 → encrypt 抛异常
   - 错误密文 → decrypt 返回 `***`
   - 正常路径 → 加密解密对称
5. 启动时增加 **配置自检**：`EncryptorAutoConfiguration` 启动时若密钥为空且 `enabled=true`，抛出 BeanCreationException 让应用启动失败（fail-fast）

### 4.4 验证方式

- 单元测试：`mvn test -pl cloudflow-common/cloudflow-common-encrypt`
- 集成测试：故意配错密钥启动 auth 服务，期望启动失败而非静默运行
- 监控告警：Sentry/ELK 中 `FIELD_ENCRYPT_FAILED` 关键字应触发告警

### 4.5 注意事项

修复前需先扫描存量数据：当前 DB 中是否存在 **本该加密但实际明文** 的脏数据。若存在，需先编写迁移脚本批量加密后再上线新代码。

---

## 五、P0-1 凭据轮换与密钥治理

状态同步（2026-05-14）：⚠️ 代码侧已完成，运维侧待窗口。仓库内 `config/*.yaml`、`docker-compose.yml`、`.env.example`、`deploy/cloudflow.env.example`、`README.md`、Nacos 7 个标准 Data ID、CI 明文扫描规则均已同步；当前未在仓库内直接执行生产 MySQL/Redis/Nacos/Grafana 实密轮换。

### 5.1 问题描述

明文密码 `Juwangkeji@2025` 共出现 **17 处**，已通过 git 提交进入历史。Docker Compose 与 Nacos 密码不一致导致 `docker compose up -d` 端到端跑不通。

### 5.2 影响范围

| 文件 | 行号 | 内容 |
|---|---|---|
| `config/cloudflow-auth.yaml` | 19, 39 | MySQL + Redis 密码 |
| `config/cloudflow-common.yaml` | 35, 52 | MySQL + Redis 密码 |
| `config/cloudflow-oa.yaml` | 19, 38 | MySQL + Redis 密码 |
| `config/cloudflow-service-crm.yaml` | 14, 28 | 同上 |
| `config/cloudflow-service-hr.yaml` | 19, 33 | 同上 |
| `config/cloudflow-service-workflow.yaml` | 19, 39 | 同上 |
| `cloudflow-gateway/.../application.yml` | 86 | `${REDIS_PASSWORD:Juwangkeji@2025}` |
| `cloudflow-service-workflow/.../application.yml` | 34, 52 | 同上 |
| `cloudflow-service-oa/.../application.yml` | 31, 38 | **直接硬编码，无占位符** |
| `docker-compose.yml` | - | `MYSQL_ROOT_PASSWORD: cloudflow_2026`（与 Nacos 不一致） |

### 5.3 修复思路

四步走：

1. **轮换生产密码**：旧密码已泄露到 git，必须立即修改 MySQL/Redis/Nacos/Grafana 实际密码
2. **去硬编码**：所有 yaml 改为 `${ENV_VAR}`（**无默认值**），强制运维注入
3. **凭据统一**：Compose `.env` 文件作为唯一真相源，Nacos 配置 placeholder 引用同名变量
4. **git 历史清理**（可选但建议）：用 `git filter-repo` 重写历史，删除明文密码痕迹

### 5.4 修复步骤

#### 5.4.1 准备阶段

1. 生成新密码（建议 20+ 字符高熵）：
   ```bash
   openssl rand -base64 32  # 数据库
   openssl rand -base64 24  # Redis
   openssl rand -hex 32     # Nacos auth token
   ```
2. 创建 `.env.example`（提交到 git）：
   ```env
   MYSQL_ROOT_PASSWORD=请替换为高强度密码
   MYSQL_APP_PASSWORD=请替换为高强度密码
   REDIS_PASSWORD=请替换为高强度密码
   NACOS_AUTH_TOKEN=请替换为32位以上Base64串
   GRAFANA_ADMIN_PASSWORD=请替换为高强度密码
   ```
3. `.gitignore` 加入 `.env`

#### 5.4.2 应用侧改造

1. **所有 application.yml 占位符化**（重点修复 oa 服务硬编码）：
   ```yaml
   spring:
     datasource:
       dynamic:
         datasource:
           master:
             password: ${MYSQL_PASSWORD}   # 不留默认值
   ```
2. **Nacos yaml 占位符化**：保持与上面一致
3. **新增应用启动自检**：在 `CloudFlowConfig` 中校验 `MYSQL_PASSWORD` / `REDIS_PASSWORD` 非空，否则 `System.exit(1)`

#### 5.4.3 Docker Compose 同步

1. `docker-compose.yml` 所有服务的 `environment` 改为引用 `.env`：
   ```yaml
   environment:
     MYSQL_PASSWORD: ${MYSQL_APP_PASSWORD}
     REDIS_PASSWORD: ${REDIS_PASSWORD}
   ```
2. MySQL 容器初始化时通过 `MYSQL_ROOT_PASSWORD` 应用同一密码
3. 文档说明：首次启动前必须 `cp .env.example .env` 并填写

#### 5.4.4 Nacos 加固

1. 修改 Nacos 默认 `nacos/nacos` 凭据
2. 启用 Nacos 认证：`NACOS_AUTH_ENABLE=true`，`NACOS_AUTH_TOKEN` 改为高熵 Base64
3. 业务服务连接 Nacos 的 username/password 也走 `${NACOS_USERNAME}` / `${NACOS_PASSWORD}`

#### 5.4.5 历史清理（可选）

```bash
# 使用 git-filter-repo（推荐，比 filter-branch 安全）
pip install git-filter-repo
git filter-repo --replace-text <(echo "Juwangkeji@2025==>REDACTED")
git push --force-with-lease origin --all
```

⚠️ **此操作改写历史**，需团队协调强制重新 clone，且 GitHub PR 评论中的引用无法清除。

### 5.5 验证方式

1. **端到端启动**：`cp .env.example .env && 填写 && docker compose up -d`，所有容器健康
2. **凭据扫描**：
   ```bash
   git grep -E "(Juwangkeji|cloudflow_2026|cloudflow_redis_2026)" -- ':!后端审计报告.md' ':!P0修复方案.md'
   ```
   期望 0 匹配
3. **缺失环境变量启动**：故意删除 `.env` 中某项，期望应用启动失败而非用兜底值

### 5.6 风险与回滚

- 改造期间存在"老服务连旧密码、新服务连新密码"的过渡，建议在低峰期窗口集中切换
- 回滚：保留旧密码备份，配置回滚后即可恢复

---

## 六、P0-3 Groovy 脚本沙箱

状态同步（2026-05-14）：✅ 已完成。当前实现采用 `cloudflow.workflow.script.enabled=false` 默认关闭；`ScriptExecutionPolicy` 同时覆盖发布期与运行期，`GroovySandboxConfig` 提供 `SecureASTCustomizer + TimedInterrupt(5000ms)`。

### 6.1 问题描述

`cloudflow-backend/cloudflow-service-workflow/src/main/java/com/cloudflow/workflow/service/ScriptExecutionService.java`

```java
GroovyShell shell = new GroovyShell(binding);
Object result = shell.evaluate(scriptContent);  // ⚠️ 裸跑
```

无任何沙箱限制，可访问 `System.exit` / `Runtime.exec` / 反射 / 文件系统 / JNI。任何能保存流程模型的用户即可获得 **宿主进程 RCE**。

### 6.2 影响范围

- `ScriptExecutionService.java`（核心）
- `ScriptExecutionPolicy.java`（已存在的 deploy 时白名单，需扩展到 runtime）
- `WorkflowProperties`（配置 `workflow.script.enabled` 默认值）
- 所有调用 `executeGroovyScript` 的运行时入口（表单字段计算、规则求值等）

### 6.3 修复思路

**纵深防御 4 层**：

1. **配置门控**：`workflow.script.enabled` 默认 `false`，仅显式启用时生效
2. **AST 沙箱**：用 Groovy `SecureASTCustomizer` 限制可用语句/类/包
3. **CodeSource 隔离**：脚本以独立 ClassLoader 加载，限制权限
4. **超时与资源限制**：`@TimedInterrupt` 注解 + 单独线程池 + CPU/内存配额

### 6.4 修复步骤

1. **新增 `GroovySandboxConfig`**：
   ```java
   @Component
   public class GroovySandboxConfig {
       public CompilerConfiguration buildSecureConfig() {
           SecureASTCustomizer sec = new SecureASTCustomizer();
           sec.setIndirectImportCheckEnabled(true);
           sec.setImportsWhitelist(List.of(
               "java.lang.Math", "java.util.Date",
               "java.math.BigDecimal", "java.time.*"));
           sec.setStarImportsWhitelist(List.of("java.util"));
           sec.setStaticImportsWhitelist(Collections.emptyList());
           sec.setReceiversBlackList(List.of(
               System.class.getName(), Runtime.class.getName(),
               ProcessBuilder.class.getName(), Thread.class.getName(),
               ClassLoader.class.getName(), File.class.getName()));
           sec.setStatementsBlacklist(List.of(
               "while", "for"));  // 防死循环（可选）

           ImportCustomizer imp = new ImportCustomizer();
           // 仅引入业务需要的工具类

           CompilerConfiguration cc = new CompilerConfiguration();
           cc.addCompilationCustomizers(sec, imp);
           cc.addCompilationCustomizers(new ASTTransformationCustomizer(
               Map.of("value", 5000L, "thrown", TimeoutException.class),
               TimedInterrupt.class));  // 5 秒超时
           return cc;
       }
   }
   ```

2. **改造 `ScriptExecutionService`**：
   ```java
   public Object executeGroovyScript(String script, Map<String, Object> ctx) {
       if (!properties.getScript().isEnabled()) {
           throw new ServiceException("SCRIPT_DISABLED");
       }
       CompilerConfiguration cc = sandboxConfig.buildSecureConfig();
       GroovyShell shell = new GroovyShell(new Binding(ctx), cc);
       return shell.evaluate(script);
   }
   ```

3. **配置默认禁用**：`cloudflow-service-workflow.yaml`
   ```yaml
   workflow:
     script:
       enabled: false              # 默认禁用
       timeout-ms: 5000
       max-memory-mb: 64
   ```

4. **所有 runtime 入口加策略校验**：搜索 `executeGroovyScript` 全部调用点，前置 `ScriptExecutionPolicy.assertRuntimeAllowed`

5. **单元测试**：
   - `System.exit(0)` 脚本 → 期望抛 SecurityException
   - `Runtime.getRuntime().exec("ls")` → 同上
   - `new File("/etc/passwd").text` → 同上
   - 死循环脚本 → 5 秒后抛 TimeoutException
   - 正常算术脚本 → 正常返回

### 6.5 验证方式

```java
// 渗透测试用例
String[] maliciousScripts = {
    "System.exit(0)",
    "Runtime.getRuntime().exec('whoami')",
    "new File('/etc/passwd').text",
    "Thread.currentThread().contextClassLoader",
    "(0..1_000_000_000).each{}"  // CPU 耗尽
};
for (String s : maliciousScripts) {
    assertThrows(SecurityException.class, () -> service.executeGroovyScript(s, Map.of()));
}
```

### 6.6 注意事项

- Groovy SecureASTCustomizer 仍存在已知绕过（如反射调用），建议结合 JVM SecurityManager（JDK17 中已 deprecated，迁移到 Class Data Sharing）
- 长期方案：迁移到 **GraalVM Polyglot** + Sandbox Resource Limits

---

## 七、P0-6 OA/CRM 接口权限码补齐

状态同步（2026-05-14）：✅ 已完成。OA 32 个 Controller、CRM 20 个 Controller 已完成外部接口权限注解补齐；内部接口保留 `@Inner`；`06.cloudflow-business-seed.sql` 与前端权限显隐已同步。

### 7.1 问题描述

OA 31 个 Controller（合同/印章/证照/付款/采购）+ CRM 10 个 Controller 几乎全部缺少 `@SaCheckPermission`，依赖前端菜单隐藏。**租户内任何登录用户都能删合同 / 盖印章 / 改商机所有者**。

### 7.2 影响范围

| 服务 | Controller 数 | 高敏 Controller |
|---|---|---|
| OA | 31 | `ContractController` / `SealController` / `LicenseController` / `PaymentRequestController` / `ExpenseClaimController` / `PurchaseRequestController` |
| CRM | 10 | `CrmCustomerController` / `CrmOpportunityController` / `CrmReceivableController` / `CrmQuoteController` |

### 7.3 修复思路

**3 步推进**：

1. **梳理权限矩阵**：与产品/业务方一起整理"业务对象 × 操作 × 角色"矩阵，输出 `permission_matrix.xlsx`
2. **建立权限码命名规范**：`{module}:{resource}:{action}`，如 `oa:contract:add` / `crm:opportunity:delete`
3. **批量加注解**：按矩阵给每个端点加 `@SaCheckPermission`，同时在 `sys_menu.perms` 表中录入对应权限码

### 7.4 修复步骤

#### 7.4.1 权限矩阵示例（OA 合同模块）

| 端点 | 权限码 | 默认角色 |
|---|---|---|
| `GET /oa/contract/list` | `oa:contract:list` | hr_specialist, oa_clerk, legal, admin |
| `GET /oa/contract/{id}` | `oa:contract:query` | 同上 + 合同关联人 |
| `POST /oa/contract` | `oa:contract:add` | oa_clerk, legal, admin |
| `PUT /oa/contract` | `oa:contract:edit` | oa_clerk, legal, admin |
| `DELETE /oa/contract/{id}` | `oa:contract:delete` | **admin only** |
| `POST /oa/contract/{id}/submit` | `oa:contract:submit` | oa_clerk, legal |
| `POST /oa/contract/{id}/cancel` | `oa:contract:cancel` | legal, admin |

#### 7.4.2 Controller 改造模板

```java
@RestController
@RequestMapping("/oa/contract")
@SaCheckLogin  // class-level 兜底
public class ContractController {

    @SaCheckPermission("oa:contract:list")
    @GetMapping("/list")
    public R<PageResult<ContractVO>> list(@Valid ContractQuery query) { ... }

    @SaCheckPermission("oa:contract:add")
    @SysLog(value = "新增合同", businessType = BusinessType.INSERT)
    @PostMapping
    public R<Long> add(@Valid @RequestBody ContractDTO dto) { ... }

    @SaCheckPermission("oa:contract:delete")
    @SysLog(value = "删除合同", businessType = BusinessType.DELETE)
    @DeleteMapping("/{id}")
    public R<Void> delete(@PathVariable Long id) { ... }
}
```

#### 7.4.3 数据库初始化

在 `06.cloudflow-business-seed.sql` 增加新权限码：

```sql
INSERT INTO sys_menu (menu_id, menu_name, parent_id, perms, menu_type, ...) VALUES
  (3001, '合同查询', 3000, 'oa:contract:query', 'F', ...),
  (3002, '合同新增', 3000, 'oa:contract:add', 'F', ...),
  ...
```

并在 `sys_role_menu` 中按矩阵分配。

#### 7.4.4 测试

- 单元测试：用 mock 用户（无权限码）调用 → 期望 403
- 集成测试：4 种典型角色（admin/manager/clerk/employee）跑全量端点测试集
- 渗透测试：用普通员工 token 调高敏端点（删合同）→ 期望 403

### 7.5 推进策略

OA 31 + CRM 10 = 41 个 Controller，估算 200+ 端点。建议：

1. **按风险分批**：先做合同/印章/证照/付款（财务+法务相关），后做项目/任务/工作台
2. **每批 PR 独立审计**：业务方 + 安全方双签
3. **灰度发布**：先在 dev/test 环境跑 1 周，再上 prod

### 7.6 验证方式

```bash
# 全量扫描未带权限注解的 Controller 方法
grep -rL "@SaCheckPermission" \
  cloudflow-service-oa/src/main/java/.../controller/ \
  --include="*Controller.java"
```

期望仅剩白名单方法（如 `/workplace/summary` 聚合接口）。

---

## 八、P0-2 HR 服务第二波重构

状态同步（2026-05-14）：✅ 已完成（当前仓库范围）。旧 `HrDomainController` / `HrDomainCrudService` / `HrDomainSupportService` 已移除，现为分子域资源控制器 + Entity/Mapper/DTO/TypedCrud 路径；写操作已补 `@SysLog` / `@Audit`，高敏 DTO 已补 `@EncryptField`。按当前范围不新增 `07` 迁移脚本，不处理旧数据迁移。

### 8.1 问题描述

§9.10 描述的是首轮加固后的中间态；当前仓库已继续完成第二波，`HrDomainController`（单文件 132 端点）与 `HrDomainCrudService`（JdbcTemplate + Map）已从主路径移除。

仍存在的限制：
- 薪税字段 DB 中明文存储（依赖角色限制读取，无法防 DBA / 备份泄漏）
- 脱敏依赖列名硬编码白名单 `MASK_COLUMNS`
- 无 `@SysLog` / `@Audit`，写操作不进审计
- N+1 查询泛滥
- 接口契约靠口头约定

### 8.2 影响范围

整个 HR 服务 ≈45 个 Java 文件 + 43 张 DB 表：

- `HrDomainController.java`（拆分）
- `HrDomainCrudService.java`（废弃或保留作降级）
- 新增 30+ DTO/VO 类
- 新增 30+ Entity + Mapper + XML
- 新增 12+ 业务子域 Service

### 8.3 修复思路

**分子域拆分 + MyBatis-Plus 实体化**，按 6 个业务子域推进：

| 子域 | 主表 | 优先级 |
|---|---|---|
| 员工档案 | hr_employee / hr_employee_document / hr_emergency_contact | P0（先做） |
| 合同 | hr_employee_contract | P0 |
| 薪酬 | hr_payroll / hr_employee_salary / hr_salary_adjustment | P0（最敏感） |
| 税务社保 | hr_tax_profile / hr_tax_deduction / hr_social_security | P0 |
| 招聘 | hr_recruitment_requisition / hr_candidate / hr_interview / hr_offer | P1 |
| 绩效 | hr_performance_* | P1 |
| 生命周期 | hr_lifecycle_application（入职/转正/调岗/离职） | P1 |

### 8.4 修复步骤（以"员工档案"子域为例）

#### 8.4.1 建立分层骨架

```
hr/
├── controller/
│   └── EmployeeController.java       (≈10 个端点)
├── service/
│   ├── EmployeeService.java
│   └── impl/EmployeeServiceImpl.java
├── mapper/
│   └── EmployeeMapper.java
├── domain/
│   ├── entity/Employee.java          (含 @EncryptField 标注)
│   ├── dto/EmployeeCreateDTO.java
│   ├── dto/EmployeeUpdateDTO.java
│   ├── dto/EmployeeQuery.java
│   └── vo/EmployeeVO.java            (含 @Sensitive 标注)
└── resources/mapper/hr/
    └── EmployeeMapper.xml
```

#### 8.4.2 Entity 加加密 + 审计字段

```java
@TableName("hr_employee")
@Data
public class Employee {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String employeeNo;

    @EncryptField  // 自动加密落库
    private String idCard;

    @EncryptField
    private String bankAccount;

    private String phone;
    private String email;
    // ...

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;

    @TableLogic
    private Integer deleted;
}
```

#### 8.4.3 VO 加脱敏

```java
@Data
public class EmployeeVO {
    private Long id;
    private String employeeNo;

    @Sensitive(strategy = SensitiveStrategy.ID_CARD)
    private String idCard;

    @Sensitive(strategy = SensitiveStrategy.BANK_CARD)
    private String bankAccount;

    @Sensitive(strategy = SensitiveStrategy.PHONE)
    private String phone;
    // ...
}
```

#### 8.4.4 Controller 改造

```java
@RestController
@RequestMapping("/hr/employees")
@SaCheckLogin
public class EmployeeController {

    @SaCheckPermission("hr:employee:list")
    @GetMapping
    public R<PageResult<EmployeeVO>> page(@Valid EmployeeQuery query) {
        return R.ok(employeeService.page(query));
    }

    @SaCheckPermission("hr:employee:add")
    @SysLog("新增员工")
    @Audit(businessType = BusinessType.INSERT, businessKey = "#dto.employeeNo")
    @PostMapping
    public R<Long> create(@Valid @RequestBody EmployeeCreateDTO dto) {
        return R.ok(employeeService.create(dto));
    }
    // ...
}
```

#### 8.4.5 旧数据迁移范围说明

本轮仓库范围**不包含旧数据迁移**：

- 不新增 `07.cloudflow-hr-p0-migration.sql`
- 不在仓库内提供批量回填历史明文字段的命令行工具
- 以 `03.cloudflow-hr.sql` + `06.cloudflow-business-seed.sql` 可直接初始化的新环境为验收基线

如后续需要对既有生产数据做加密回填，应单独立项并在 staging 全量演练。

#### 8.4.6 当前落地形态

- 旧 `HrDomainController` 已删除
- 旧 `HrDomainCrudService` 已删除
- 当前由 `HrEmployee/Organization/Recruitment/Lifecycle/Attendance/Compensation/Performance` 七组资源控制器直接承接原有 HR 业务域

### 8.5 验证方式

1. **接口契约**：Knife4j 文档完整展示所有 DTO/VO 字段
2. **加密验证**：DB 中 `id_card` / `bank_account` 字段为 Base64 密文，应用层透明解密
3. **审计验证**：每次写操作在 `sys_audit_log` 产生记录
4. **数据权限**：dsType=4 的员工只能看自己档案
5. **性能**：员工列表 50 条 N+1 → 单 SQL JOIN，P95 < 200ms

### 8.6 风险与回滚

- **数据迁移不可逆**：必须备份 + 在 staging 环境完整演练
- **接口契约破坏**：前端需同步改，建议提前 2 周对接
- 回滚预案：保留 `hr_employee_backup_*` 表 30 天

---

## 九、总修复时间线

```
Week 1 (第一波):
  [done] P0-4 关闭 DEBUG 日志
  [done] P0-5 MySQL TLS
  [done] P0-7 EncryptUtils fail-fast + 默认关闭加密
  [done] P0-3 Groovy 沙箱 + 测试

Week 2 (第二波):
  [done] P0-1 配置占位符化 / .env 模板 / Compose 注入链 / Nacos 同步 / CI 明文扫描
  [todo] 生产实密轮换 + Nacos 默认口令切换（运维窗口）

Week 3-4 (第三波 - OA/CRM):
  [done] P0-6 权限矩阵落库 + `@SaCheckPermission` 铺开 + 前端显隐同步

Week 5-7 (第三波 - HR):
  [done] P0-2 第二波控制器拆分 + Typed CRUD + 审计/加密接入
  [scope-out] 旧数据迁移脚本与历史数据回填
```

## 十、配套机制

1. **PR 模板**：每个 P0 修复 PR 必须勾选"已通过单元测试""已通过安全扫描""已更新审计报告状态"
2. **CI 加固**：
   - 提交时扫描 `Juwangkeji` / 弱密码模式
   - `dev` / `main` 执行 `mvn clean verify`、前端 `npm ci && npm run type-check && npm run build`
   - 提交时检查 Controller 方法是否带 `@SaCheckPermission` 或显式 `@SaCheckLogin`（白名单除外）
3. **审计报告同步**：每完成一项，在 `后端审计报告.md` §13.6 修复记录表追加一行
4. **告警监控**：
   - Sentry 增加 `EncryptException` / `FIELD_ENCRYPT_FAILED` 告警
   - Prometheus 增加"403 拒绝次数"告警，发现权限风暴

---

## 十一、附录：快速命令清单

```bash
# 扫描明文密码残留
git grep -E "(Juwangkeji|cloudflow_2026|cloudflow_redis_2026)" \
  -- ':!后端审计报告.md' ':!P0修复方案.md'

# 扫描无权限注解的 Controller
grep -rL "@SaCheckPermission" \
  cloudflow-backend/cloudflow-service-*/src/main/java/**/controller/ \
  --include="*Controller.java"

# 扫描 DEBUG 日志配置
grep -rn "level.*DEBUG\|log-impl.*StdOutImpl" config/

# 扫描 useSSL=false
grep -rn "useSSL=false" cloudflow-backend/ config/

# 扫描裸跑的 GroovyShell
grep -rn "new GroovyShell" cloudflow-backend/

# 生成强密码
openssl rand -base64 32

# 验证 MySQL TLS（在 mysql client 中执行）
# SHOW SESSION STATUS LIKE 'Ssl_cipher';
```

---

## 十二、CRM 模块整改阶段 1–3 联动（2026-05-13 已落地）

CRM 模块单独启动了 4 阶段整改计划（见 `docs/CRM-模块评估与整改方案.md`），阶段 1–3 已落地。与本 P0 文档的联动关系：

### 12.1 直接关系

| P0 条目 | 联动情况 |
|---|---|
| P0-1 凭据治理 | 已切到 `DB_USERNAME/DB_PASSWORD/REDIS_PASSWORD/NACOS_PASSWORD` 占位符注入链；CRM 新增 Feign / Stream 无需额外改造 |
| P0-2 HR 架构重构（当前仓库范围已完成） | CRM 阶段 2 增加 `/inner/hr/*` 只读接口（`@Inner` 白名单），未扩大 HR 攻击面；HR 第二波落地后这些接口继续保留为只读集成面 |
| P0-3 CORS 收敛 | 无需改动；CRM 新增的前端接口均走网关白名单 |
| P0-4 生产 DEBUG 日志 | 已随本轮统一收敛到 `LOG_LEVEL_APP/LOG_LEVEL_MAPPER` |
| P0-5 MySQL TLS | CRM 新增表（`oa_crm_handover_task`、`oa_crm_approval`、`oa_contract.source_*`、`oa_crm_receivable.invoice_id`）随主库启用 TLS 即可 |
| P0-6 业务接口权限码 | 已完成：`/approval/*`、`/handover-task/*` 以及其余对外 CRM/OA 接口均已补 `@SaCheckPermission`；`/inner/*` 继续使用 `@Inner` |
| P0-7 EncryptUtils | 无直接关系；CRM 阶段 1 只抽常量，未触碰加密链路 |

### 12.2 新增接口清单（方便 P0-6 统一补权限码）

| 路径 | 说明 |
|---|---|
| `POST /crm/approval/customer-claim` | 客户领取/公海释放审批发起 |
| `POST /crm/approval/customer-level` | 客户分级变更审批发起 |
| `POST /crm/approval/opportunity-downgrade` | 商机降级关闭审批发起 |
| `POST /crm/approval/refund` | 退款审批发起 |
| `GET  /crm/approval/{id}` | 审批流水查询 |
| `GET  /crm/handover-task/pending` | 离职交接待办列表 |
| `POST /crm/handover-task/{id}/reassign` | 重新指派 owner |
| `POST /crm/handover-task/{id}/close` | 关闭交接任务 |
| `GET  /hr/performance/crm/{owners,depts,top-owners,top-depts}` | HR 绩效看板代理到 CRM 的聚合接口（已 `@SaCheckLogin`） |
| `GET  /crm/inner/crm/performance/*` | 供 HR 调用的内部聚合（已 `@Inner(allowedServices=cloudflow-service-hr)`） |
| `GET  /hr/inner/hr/*` | 供 CRM/OA/Workflow 调用的员工/部门查询（已 `@Inner` 白名单） |

### 12.3 对本文档时间线的影响

已并入本文档的实际完成状态；阶段 1–3 的 CRM 改动已成为 P0-6 与 P0-2 的组成部分之一。

> 本节仅作进度同步。详细内容见 `后端审计报告.md` §8.10、§9.11 与 §13.6。

---

*文档版本：v1.1 — 2026-05-14。本文已同步 2026-05-14 前完成的代码侧工作；生产实密轮换与 Nacos 默认口令变更仍需在线下变更窗口执行。*
