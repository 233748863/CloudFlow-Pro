# CloudFlow 桌面前端设计语言重构台账

## 摘要
- 目标：让 `cloudflow-frontend` 桌面端整体设计语言尽量以 99% 相似度向 `.references/sub2api/frontend` 靠拢，优先统一布局骨架、信息架构、原子组件、主题模式、比例、状态反馈与页面密度。
- 硬要求：后续所有页面回炉都必须满足“代码和设计语言都往源码 `.references/sub2api` 上靠”，不能再出现“不是往源码靠，而是改成了另外一种设计语言”的情况；一旦出现，直接视为不符合要求，必须继续回炉。
- 范围：首期只覆盖桌面端路由页面与共享特性面；移动端本轮不实施，但保留 Phase 2 占位。
- 当前事实：参考项目为 Vue 3 + Tailwind，CloudFlow 为 React + Tailwind 4，因此本次采用“提炼设计合同 + 重建 React 设计系统”的方式，不复制参考项目实现。
- 台账纠偏：截至 2026-04-20，此前所有“已完成”均只代表第一轮收口，不代表最终达标；现统一回退到 `实施中`，当前无桌面页面和共享层达到最终完成标准。
- 当前基线：`ThemeProvider/useTheme`、`html.dark`、新 `MainLayout`、`TablePageLayout`、`components/ui`、`/login`、`/register`、`/dashboard`、`/workplace` 等已完成第一轮回炉，但仍未达到 99% 源码对齐标准。
- 执行纪律：从本条开始，后续每次进入任一页面或共享层改造前，必须重新读取本台账的强制规则与对应页面行，再开始动手。

## 参考设计合同
- 色板：主色统一为 `teal/cyan` 轴，主色使用 `#14b8a6 / #0d9488 / #0891b2`；禁用页面私有 `purple/pink/indigo` 作为主视觉。
- 字体：统一使用 system UI 中文优先栈，不额外引入与参考项目不一致的品牌字体。
- 间距：基于 4px 栅格，页面外边距默认 `16 / 24 / 32`，卡片内边距默认 `20 / 24`。
- 圆角：输入与按钮以 `12px` 为基础，卡片与弹窗以 `16px` 为基础，大模块保留 `24px`。
- 阴影：列表与卡片默认轻阴影，弹层与全局通知使用中高层级阴影；不再使用厚重拟物阴影。
- 表单密度：参考项目的紧凑 SaaS 密度，输入高度 `40-44px`，按钮高度 `36-48px`。
- 卡片比例：统计卡与工作台卡片优先采用“标题小、数值强、辅助说明弱”的信息层级。
- 表格样式：表头弱底色、轻边框、浅 hover；分页与筛选区与内容区明确分层。
- 玻璃态边界：只在主布局、顶部 Header、全局弹层等壳层使用，业务页内部不滥用。
- 暗色规则：统一由 `html.dark` 控制；所有共享层必须同时支持 `light | dark | system` 三种模式。

## 最终完成门槛（纠偏后）
- 只有布局骨架、信息架构、卡片比例、字体层级、间距节奏、组件状态、空态/加载态、Light/Dark 两套主题都与 `.references/sub2api/frontend` 基本 1:1 对齐，才允许标记为 `已完成`。
- “已接入 ThemeProvider / MainLayout / components/ui”、“已完成第一轮收口”、“构建通过”都不再视为完成，只能视为具备继续重做的基础。
- 任何页面只要仍保留明显的 CloudFlow 私有 Hero、工作台拼装语法、表格密度偏差、卡片比例偏差或独立视觉语言，就必须维持在 `设计中`、`实施中` 或 `联调中`。
- 完成日期只允许填写真正达到最终门槛的页面；非 `已完成` 条目统一使用 `-`。

## 强制执行规则
- 每次开始任何页面或共享层改造前，必须先重新阅读本台账的 `参考设计合同`、`最终完成门槛`、目标页面所在矩阵行，以及 `完成记录` 中该页最近一次回炉记录；不允许凭上一轮记忆继续改。
- 每次开始动手前，必须同时打开 `.references/sub2api/frontend` 中最接近的参考页面；如果没有直接对应页面，至少要回到后台标准页语法参考，如 `src/views/admin/UsersView.vue`、`ChannelsView.vue` 或其他最接近的后台列表页，禁止自行发明新设计语言。
- 本项目的硬约束不是“参考源码做一个更轻的 CloudFlow 风格”，而是“代码和设计语言都往源码 `.references/sub2api` 上靠”；如果改出来仍然是另外一种设计语言，即使比旧版更轻、更整洁，也一律判定为未达标。
- 台账中的“参考源码靠拢”定义，默认指向布局骨架、信息密度、筛选条形式、容器数量、卡片比例、列表/表格语法、弹层结构、字体层级、留白节奏和 Light/Dark 规则同时靠拢；不是只保留业务逻辑、再换一套“更轻”的 CloudFlow 风格。
- “代码往源码靠”包含：页面容器拆分方式、筛选条组织、主内容骨架、侧栏/弹层层级、组件组合关系和第三方控件皮肤策略都要优先向参考页看齐；不是只在业务逻辑不变的前提下重画一层皮。
- 如果用户明确指出“不像源码”“又起了一套设计语言”“页面太重”“卡片没有意义”“还在用原来的设计语言”等问题，必须视为该页未达标；该页不得标记为 `已完成`，并应继续维持在 `实施中` 或 `联调中`。
- 每次页面改造结束前，必须完成 4 项检查：`源码对照检查`、`Light/Dark 检查`、`构建检查`、`台账同步`。任一项未完成，只能算进行中，不能写完成结论。
- 每次页面回炉时，必须优先删除与源码语法冲突的旧壳层，包括但不限于私有 Hero、无意义统计卡、工作台拼装区、装饰性说明卡、过度皮肤化的第三方控件外观；原则固定为：`能删就删，能并就并，能扁平就扁平`。
- 后续每一轮实际动手前，都以本节为第一读取入口；若本节与旧完成记录、旧实现或口头默认习惯冲突，一律以本节和参考源码为准。

## 共享层清单
| 共享特性面 | 当前状态 | 目标模板/参考 | 依赖文件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 主题模式 | 已完成主题基础层第一轮落地，但 token、状态色和主题切换反馈仍未 99% 贴齐源码 | `AppLayout` + 全局 token | `src/context/ThemeContext.tsx` `src/index.tsx` `src/index.css` | 已接入 `light/dark/system` | 实施中 | - | 第一轮收口已完成，未达到最终验收 | `html.dark` 仍为唯一暗色入口，但主题细节需继续回炉 |
| 主布局 | 已完成桌面壳层第一轮重构，并已把桌面壳层收口为固定视口 + `main` 唯一滚动容器，修复仪表盘、日程等页面的滚轮落不到主内容区的问题；本轮继续把侧栏品牌副标题、Header 小字说明、底部重边框按钮与底部退出入口收回到更接近 `.references/sub2api` 的源码语法，并把活动路由所在分组改成可手动收起；但侧栏比例和少量 Header 细节仍未 99% 贴齐源码 | `AppLayout / AppSidebar / AppHeader` | `src/layouts/MainLayout.tsx` | 已接入 | 实施中 | - | 主布局已继续向源码侧栏/页头语法收口，滚动容器契约保持稳定，未达到最终验收 | 菜单分组、折叠、主题切换和主内容滚动已可用，但不计最终完成 |
| 认证壳层 | 已按认证页参考重做一轮，但布局比例、表单密度和状态反馈仍需继续贴近源码 | `AuthLayout` | `src/pages/AuthPage.tsx` `src/pages/auth-page.css` | 页面、主题切换、输入、按钮与反馈基础暗色已接入 | 实施中 | - | 第一轮回炉已完成，未达到 99% 源码对齐 | 登录与注册共享实现 |
| 表格布局 | 已接入统一列表容器，但筛选区、表头、分页和滚动区分层仍未完全贴齐源码 | `TablePageLayout` | `src/components/layout/TablePageLayout.tsx` | 已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 后续按源码列表页统一重做 |
| 原子组件入口 | `components/ui` 已成为统一导出口，但按钮、输入、下拉、卡片和空态仍需继续向源码对齐 | `Button/Input/Select/...` | `src/components/ui/*` `src/components/ui/index.ts` | 已接入基础暗色 | 实施中 | - | 基础原子层已可用，未达到最终验收 | `common` 仍有历史组件待进一步迁移 |
| Toast | 已统一到同一套主题变量，但视觉比例和动效细节仍未 99% 贴齐源码 | `Toast` | `src/components/common/Toast.tsx` `src/components/common/toast.css` | 已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 跟随全局主题 |
| 弹窗体系 | 基础弹窗已切回参考源码的预设宽度档位、视口内滚动策略与 `body.modal-open` 锁滚契约，标题区、内容区和危险操作语法仍需继续回炉 | `BaseDialog / ConfirmDialog` | `src/components/common/BaseDialog.tsx` `src/components/common/ConfirmDialog.tsx` `src/components/ui/dialog.tsx` | 已接入基础暗色 | 实施中 | - | 共享层已收口到“预设宽度 + 面板限高 + 内容区滚动 + class 锁滚”契约，未达到最终验收 | 仍需继续回收存量业务弹窗里的超宽自定义比例，并逐步统一历史弹层的锁滚实现 |
| Workspace 原语 | 已完成第一轮暗色与文案清洗，但仍保留大量 CloudFlow 私有工作台语法 | `Workspace` 系列 | `src/components/workspace/*` | 大部分已接入 | 实施中 | - | 仅完成第一轮收口，未达到最终验收 | 仍需继续覆盖业务页边角样式 |
| 公告体系 | 已推进第二轮回炉，用户视图、管理视图、详情弹层与已读明细已统一压成 `轻量页头 + 工具条 + 单结果容器 + 标准弹窗/表格` 语法，并移除公告页主链路中的 `Workspace` 壳层；但顶部入口与细部比例仍需最终对照 `.references/sub2api` 走查 | `Announcement` 系列 | `src/components/common/Announcement*` `src/pages/AnnouncementPage.tsx` | 用户视图、管理视图、详情弹层、顶部入口与已读明细已统一接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近源码后台页语法，待最终源码对照验收 | 共享特性面进入最终源码对照阶段 |
| 任务处理弹层 | 已完成第一轮结构重构，但详情区、操作区和加减签交互仍需继续贴齐源码节奏 | `TaskHandleModal` | `src/components/TaskHandleModal.tsx` `src/components/SignatureModal.tsx` | 基础暗色已接入 | 实施中 | - | 第一轮收口中，未达最终验收 | 与 `/tasks`、`/my-apps` 联动收口 |
| 流程轨迹 | 已完成第一轮视觉收口，但时间线比例、节点表达和容器留白仍需继续回炉 | `ProcessTrace` | `src/components/ProcessTrace.tsx` | 基础暗色已接入 | 实施中 | - | 第一轮收口中，未达最终验收 | 被任务详情与流程类页面复用 |
| 流程发起目录 | 已完成第一轮目录页收口，但目录信息层级、卡片比例和发起弹层仍未 99% 贴齐源码 | 目录/卡片页 | `src/pages/Workplace.tsx` `src/components/workflow/catalog/*` | 已接入基础暗色 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 后续按源码目录页二次重做 |
| 流程构建器 | 已推进第三轮纠偏第八批，流程设计页头与节点卡最后一层继续减重，节点信息已从多枚标签/提示块收敛为标题 + 元信息 + 摘要文本语法，审批与条件表达也进一步压平，整体更贴近参考后台页的轻量密度；当前仅剩最终源码对照走查 | 流程设计画布页 | `src/components/WorkflowBuilder.tsx` `src/components/WorkflowSettingsModal.tsx` | 页面与构建器基础暗色已接入 | 联调中 | - | 第三轮纠偏第八批已推进，页头与节点卡收口完成并构建通过，待最终源码对照验收 | 属于高复杂批次 |
| 表单设计器 | 已推进第二轮回炉第二批，页面壳层、组件库列、画布头部、字段卡与预览卡已进一步统一到轻量设计工作台语法，但字段编辑密度、预览细节和更深层交互仍未贴齐源码 | 流程设计画布页 | `src/components/FormBuilder.tsx` | 页面与设计器基础暗色已接入 | 实施中 | - | 第二轮回炉已推进，未达最终验收 | 与 `/forms` 页面联动推进 |
| 图表与日历适配 | 已完成日历页与性能统计页第一轮暗色适配，但图表表达和日历皮肤仍未达到源码相似度 | 图表/日历适配层 | `SchedulePage` `PerformanceStats` 等 | 日历页与性能统计页基础暗色已接入 | 实施中 | - | 第一轮收口中，未达最终验收 | 继续统一 FullCalendar、图表和监控类页面 |

## 页面矩阵

### 认证
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 认证 | `/login` | `src/pages/AuthPage.tsx` | 认证页 | 已按参考认证页重做一轮，但卡片比例、输入密度、辅助文案和状态反馈仍未达到 99% 源码对齐 | 参考 `AuthLayout` 的认证页 | ThemeProvider、认证壳层、components/ui、AuthCaptchaDialog | 页面、登录表单与验证码基础暗色已接入 | 实施中 | - | 第一轮回炉已完成，未达到最终验收 | 与 `/register` 共享实现 |
| 认证 | `/register` | `src/pages/AuthPage.tsx` | 认证页 | 已按参考认证页重做一轮，但卡片比例、输入密度、辅助文案和状态反馈仍未达到 99% 源码对齐 | 参考 `AuthLayout` 的认证页 | ThemeProvider、认证壳层、components/ui、AuthCaptchaDialog | 页面、注册表单与验证码基础暗色已接入 | 实施中 | - | 第一轮回炉已完成，未达到最终验收 | 与 `/login` 共享实现 |

### 首页/工作台
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 首页/工作台 | `/, /dashboard` | `src/pages/Dashboard.tsx` | 门户仪表盘页 | 已按参考 `DashboardView` 改成四段式仪表盘，但统计卡比例、图表表达、最近活动细节和局部信息密度仍未达到 99% 源码对齐 | 参考 `DashboardView` 门户仪表盘页 | ThemeProvider、MainLayout、components/ui、`components/user/dashboard/*` | 已接入 | 实施中 | - | 第二轮回炉中，未达到最终验收 | `/` 与 `/dashboard` 合并记录 |
| 首页/工作台 | `/workplace` | `src/pages/Workplace.tsx` | 目录/卡片页 | 已完成目录页第一轮重构，但分类信息层级、卡片比例、筛选节奏和发起弹层仍未达到 99% 源码对齐 | 参考目录/卡片页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、流程发起目录 | 已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 高流量入口页 |
| 首页/工作台 | `/tasks` | `src/pages/TaskListPage.tsx` | 标准列表页 | 已完成任务工作台与任务详情弹层第一轮重构，统一 Hero、筛选区、列表/看板与弹层结构 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、任务处理弹层 | 页面与弹层基础暗色已接入 | 实施中 | - | 构建通过，待联调验收 | 高流量入口页 |
| 首页/工作台 | `/my-apps` | `src/pages/TaskListPage.tsx` | 标准列表页 | 已完成申请工作台与详情弹层第一轮重构，统一筛选区、分页区、流程卡片与只读详情结构 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、任务处理弹层 | 页面与弹层基础暗色已接入 | 实施中 | - | 构建通过，待联调验收 | 与 `/tasks` 共享实现 |
| 首页/工作台 | `/my-copies` | `src/pages/CopyListPage.tsx` | 标准列表页 | 已完成第一轮抄送列表页收口，但列表骨架、结果概况卡、详情弹窗比例和内容密度仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、ProcessTrace | 页面与详情弹窗基础暗色已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 高频列表页批次 |

### 流程与表单
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 流程与表单 | `/workflow` | `src/pages/WorkflowCreate.tsx` | 目录/卡片页 | 已按源码后台页语法推进第三轮回炉第一批，旧 `WorkspaceBackdrop / WorkspaceHeroCard / WorkspaceSectionCard` 已移除，页面改为轻量页头 + 单容器分栏入口结构，创建方式收敛为两条主入口行与右侧规则栏，但按钮细节和信息密度仍需继续贴齐源码 | 参考目录/卡片页 | ThemeProvider、MainLayout、TablePageLayout、components/ui | 页面基础暗色已接入 | 联调中 | - | 第三轮回炉第一批已推进，旧 Workspace 入口壳层已拆除并构建通过，待最终源码对照验收 | 与 `/workflow/create` 共享实现 |
| 流程与表单 | `/workflow/create` | `src/pages/WorkflowCreate.tsx` | 目录/卡片页 | 已按源码后台页语法推进第三轮回炉第一批，旧 `WorkspaceBackdrop / WorkspaceHeroCard / WorkspaceSectionCard` 已移除，页面改为轻量页头 + 单容器分栏入口结构，创建方式收敛为两条主入口行与右侧规则栏，但按钮细节和信息密度仍需继续贴齐源码 | 参考目录/卡片页 | ThemeProvider、MainLayout、TablePageLayout、components/ui | 页面基础暗色已接入 | 联调中 | - | 第三轮回炉第一批已推进，旧 Workspace 入口壳层已拆除并构建通过，待最终源码对照验收 | 与 `/workflow` 共享实现 |
| 流程与表单 | `/workflow/design` | `src/pages/WorkflowDesign.tsx` | 流程设计画布页 | 已继续按参考后台页语法推进第三轮纠偏第八批，页头已撤掉状态胶囊并改为更轻的标题区，节点卡也从彩色标签/提示块收敛为标题 + 元信息 + 摘要文本语法，审批与条件信息继续去标签化，当前页面已完成最后一层减重并进入最终源码对照走查 | 参考流程设计画布页 | ThemeProvider、MainLayout、components/ui、WorkflowBuilder、WorkflowSettingsModal | 页面与流程设计器基础暗色已接入 | 联调中 | - | 第三轮纠偏第八批已推进，页头与节点卡最后一层减重完成并构建通过，待最终源码对照验收 | 高复杂批次 |
| 流程与表单 | `/workflow/monitor` | `src/pages/WorkflowMonitor.tsx` | 复杂工作台页 | 已按源码后台页语法推进第三轮纠偏第一批，页头、工具栏和四个统计块已收成紧凑后台卡片，主体改为 `TablePageLayout + 单容器双栏 + 列表行` 结构，趋势与告警从大卡片工作台改成表格式行列表，但趋势比例、告警列宽和监控批次联动仍需继续贴齐源码 | 参考复杂工作台页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、WorkflowMonitor | 页面基础暗色已接入 | 联调中 | - | 第三轮纠偏第一批已推进，自定义监控工作台语法已拆除并构建通过，待最终源码对照验收 | 高复杂批次 |
| 流程与表单 | `/workflow/deploy` | `src/pages/DeployManagement.tsx` | 复杂工作台页 | 已按源码后台页语法推进第三轮纠偏第二批，页面壳层、窗口、审批、回滚、统计四个治理模块的主视图均已收成轻量页头/工具栏/单容器/列表行语法，窗口编辑、审批详情和回滚弹窗也继续压平，当前这页已基本脱离旧的 Deploy Workspace 设计语言，仅剩少量细节走查 | 参考复杂工作台页 | ThemeProvider、MainLayout、components/ui、DeployWindowManagement、DeployApprovalManagement、VersionRollbackManagement、DeployStatistics | 页面与四个治理子模块基础暗色已接入 | 联调中 | - | 第三轮纠偏第二批已推进，主视图与关键弹窗均构建通过并进入最终源码对照走查 | 高复杂批次 |
| 流程与表单 | `/workflow/alerts` | `src/pages/AlertList.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面已收成轻量页头 + `TablePageLayout` + 单容器结果区 + slim 侧栏结构，筛选条、告警结果和焦点区都改为标准列表页语法，异常解决弹窗也继续压轻，当前已基本脱离旧监控工作台式结构 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、AlertList | 页面与解决弹窗基础暗色已接入 | 联调中 | - | 第三轮纠偏第一批已推进，主视图与解决弹窗构建通过并进入最终源码对照走查 | 监控批次 |
| 流程与表单 | `/workflow/performance` | `src/pages/PerformanceStats.tsx` | 复杂工作台页 | 已按源码后台页语法推进第三轮纠偏第一批，页面已收成轻量页头 + `TablePageLayout` + 单容器结果区 + slim 侧栏结构，筛选条、流程分布、日期趋势与结果表都改为后台页语法，同时保留日期筛选、流程筛选、聚合统计和导出逻辑，当前已基本脱离旧分析工作台式结构 | 参考复杂工作台页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、PerformanceStats | 页面基础暗色已接入 | 联调中 | - | 第三轮纠偏第一批已推进，主视图构建通过并进入最终源码对照走查 | 图表适配批次 |
| 流程与表单 | `/forms` | `src/pages/FormDesign.tsx` | 流程设计画布页 | 已按参考流程设计画布页推进第二轮回炉第二批，页面壳层、组件库列、画布头部、字段卡与预览卡都已进一步收口到轻量表单设计工作台语法，但字段编辑密度、预览细节和更深层编辑交互仍未达到 99% 源码对齐 | 参考流程设计画布页 | ThemeProvider、MainLayout、components/ui、FormBuilder | 页面与表单设计器基础暗色已接入 | 实施中 | - | 第二轮回炉已推进，页面壳层、组件库列、画布头部、字段卡与预览卡构建通过，未达到最终验收 | 高复杂批次 |
| 流程与表单 | `/templates` | `src/pages/TemplateLibrary.tsx` | 目录/卡片页 | 已按源码后台页语法推进第三轮纠偏第二批，页面已移除 `WorkspaceBackdrop / HeroCard / Workbench / ResultCard / SectionCard` 整套旧壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 左侧轻筛选 + 右侧单容器结果区结构，默认视图切到更接近源码后台页的列表语法，预览与创建弹窗也同步压平，但卡片视图比例、预览弹窗细节和模板管理链路仍需最终走查 | 参考目录/卡片页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、Pagination、TemplateLibrary | 页面与弹窗基础暗色已接入 | 联调中 | - | 第三轮纠偏第二批已推进，主视图与弹窗构建通过并进入最终源码对照走查 | 模板库 |
| 流程与表单 | `/templates/manage` | `src/pages/admin/TemplateManagement.tsx` | 标准列表页 | 已完成第一轮模板管理页收口，但分类表、列表密度和治理弹窗仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、ConfirmDialog、TemplateManagement | 页面与弹窗基础暗色已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 管理端模板 |
| 流程与表单 | `/workflow/category` | `src/pages/admin/ProcessCategoryPage.tsx` | 标准列表页 | 已完成第一轮流程分类管理页收口，但分类树、详情面板和编辑弹窗仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、ProcessCategoryPage | 页面基础暗色已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 分类管理 |
| 流程与表单 | `/workflow/management` | `src/pages/admin/ProcessManagement.tsx` | 标准列表页 | 已完成第一轮流程管理页收口，但治理条、结果表格和治理弹窗仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、TableRowActions | 页面与弹窗基础暗色已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 批量治理标准页批次 |
| 流程与表单 | `/workflow/import` | `src/pages/admin/WorkflowImport.tsx` | 标准列表页 | 已完成第一轮流程导入页收口，但上传区、导入队列和结果摘要仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、WorkflowImport | 页面与队列项基础暗色已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 导入治理页批次 |
| 流程与表单 | `/workflow/archived` | `src/pages/admin/ArchivedWorkflows.tsx` | 标准列表页 | 已完成第一轮归档流程管理页收口，但批量治理条、归档卡片列表和危险操作弹窗仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、ArchivedWorkflows | 页面与弹窗基础暗色已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 归档治理页批次 |
| 流程与表单 | `/workflow/versions/:workflowId` | `src/pages/VersionHistoryPage.tsx` | 详情/时间线页 | 已完成第一轮版本历史页收口，但版本操作台、时间线比例和差异/回滚弹窗仍未达到 99% 源码对齐 | 参考详情/时间线页 | ThemeProvider、MainLayout、Workspace 原语、components/ui、VersionHistory | 页面与弹窗基础暗色已接入 | 实施中 | - | 第一轮收口已完成，未达到最终验收 | 版本历史 |

### OA / 办公
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| OA/办公 | `/announcement` | `src/pages/AnnouncementPage.tsx` | 标准列表页 | 已推进第二轮回炉，用户视图、管理工作台、已读明细与详情弹层已统一改为参考后台页的 `轻量页头 + 工具条 + 单结果容器` 语法，并去除页面主链路中的 `Workspace` 壳层；但顶部铃铛入口和最终比例细节仍需 99% 源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、Pagination、components/ui、公告体系 | 用户视图、管理工作台、详情弹层、顶部入口与已读明细已统一接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近源码后台列表页，待最终源码对照验收 | 高流量入口页 |
| OA/办公 | `/office/announcement` | `src/pages/AnnouncementPage.tsx` | 标准列表页 | 已推进第二轮回炉，用户视图、管理工作台、已读明细与详情弹层已统一改为参考后台页的 `轻量页头 + 工具条 + 单结果容器` 语法，并去除页面主链路中的 `Workspace` 壳层；但顶部铃铛入口和最终比例细节仍需 99% 源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、Pagination、components/ui、公告体系 | 用户视图、管理工作台、详情弹层、顶部入口与已读明细已统一接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近源码后台列表页，待最终源码对照验收 | 与 `/announcement` 共享实现 |
| OA/办公 | `/schedule` | `src/pages/SchedulePage.tsx` | 日历调度页 | 已按源码后台页语法推进第三轮纠偏第二批，页面继续删除自定义统计胶囊、重皮肤日历和多段侧栏摘要，收敛为单筛选/操作条 + 单主容器；主体改成左侧中性日历画布 + 下方标准列表 + 右侧单 `Agenda` 窄栏，新建/详情层改回常规 `BaseDialog` 语法，但 FullCalendar 最终细节与信息密度仍需最后走查 | 参考日历调度页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、Pagination、BaseDialog、日历适配 | 页面与关键弹层基础暗色已接入 | 联调中 | - | 第三轮纠偏第二批已推进，主视图、日历皮肤和关键弹层构建通过并继续等待最终源码对照验收 | 高复杂批次 |
| OA/办公 | `/meeting-room` | `src/pages/MeetingRoomPage.tsx` | 复杂工作台页 | 已按源码后台页语法推进第三轮纠偏第一批，页面已移除 `WorkspaceBackdrop / HeroMetrics / SectionCard / SegmentedControl / StatusPanel / EmptyPanel` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果区结构；会议室列表进一步从单列长结果行收敛为单容器内的紧凑三列密排，我的预订与统计区也统一收敛到同一套后台页语法，周排期与预订/管理弹窗同步去 Workspace 化，但统计表细节、周排期比例和弹层节奏仍需最终走查 | 参考复杂工作台页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、MeetingRoomPage | 页面与关键弹层基础暗色已接入 | 联调中 | - | 第三轮纠偏第一批已推进，主视图与关键弹层构建通过并进入最终源码对照走查 | 高复杂批次 |
| OA/办公 | `/expense/claim` | `src/pages/ExpenseClaimPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第二批，顶部摘要已压成单行汇总，结果头改为 `标题 + 当前筛选 + 共 N 条`，列表主列改成报销单号、申请人/部门、类别/明细的双行信息列，行内动作也收成 icon-only 轻动作；删除/提交链路统一改回共享 `ConfirmDialog`，新建与详情弹层删除说明性文案并继续压薄字段卡和明细区，但最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination、ExpenseClaimPage | 页面与关键弹层基础暗色已接入 | 联调中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 中复杂业务页 |
| OA/办公 | `/payment/request` | `src/pages/PaymentRequestPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第二批，顶部摘要已压成单行汇总，结果头改为 `标题 + 当前筛选 + 共 N 条`，列表主列改成付款单号、收款方/账户、申请人/部门、类型/日期的双行信息列，行内动作也收成 icon-only 轻动作；删除/提交链路统一改回共享 `ConfirmDialog`，新建与详情弹层删除说明性文案并继续压薄字段卡和附件区，但最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination、PaymentRequestPage | 页面与关键弹层基础暗色已接入 | 联调中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 中复杂业务页 |
| OA/办公 | `/hr/attendance/supplement` | `src/pages/AttendanceSupplementPage.tsx` | 表单页 | 已按源码后台列表页语法推进第三轮纠偏第二批，页面继续把顶部四枚统计胶囊压成单行摘要，结果头改为更克制的 `标题 + 当前筛选 + 共 N 条` 语法，列表行操作也收成更接近源码后台页的 icon-only 轻动作；新建/编辑弹层移除说明句并放宽为 `wide`，详情弹层的字段卡、原因块和流程轨迹块也继续压薄，但最终源码细节仍需继续收口 | 参考表单页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、Pagination、ProcessTrace、components/ui | 页面、表格与关键弹层基础暗色已接入 | 实施中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 中复杂业务页 |
| OA/办公 | `/hr/overtime/applications` | `src/pages/OvertimeApplicationPage.tsx` | 表单页 | 已按源码后台列表页语法推进第三轮纠偏第二批，页面继续把顶部统计胶囊压成单行摘要，结果头改为更克制的 `标题 + 当前筛选 + 共 N 条` 语法，列表行操作也收成 icon-only 轻动作；新建/编辑弹层移除说明句并删除右侧“当前配置”侧卡，改成行内摘要，详情弹层的字段卡、事由块和流程轨迹块也继续压薄，但最终源码细节仍需继续收口 | 参考表单页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、Pagination、ProcessTrace、components/ui | 页面、表格与关键弹层基础暗色已接入 | 实施中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 中复杂业务页 |
| OA/办公 | `/hr/leave/application` | `src/pages/LeaveApplicationPage.tsx` | 表单页 | 已按源码后台列表页语法推进第三轮纠偏第二批，顶部统计已压成单行摘要，结果头改为更克制的 `标题 + 当前筛选 + 共 N 条` 语法，列表行动作收成 icon-only 轻动作；新建弹层移除说明句和右侧“当前配置”侧卡，改成行内摘要，详情弹层的字段卡、原因块和流程轨迹块也继续压薄，但最终源码细节仍需继续收口 | 参考表单页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、Pagination、ProcessTrace、components/ui | 页面、表格与关键弹层基础暗色已接入 | 实施中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 中复杂业务页 |
| OA/办公 | `/office/business-trip` | `src/pages/BusinessTripPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第二批，顶部摘要已压成单行汇总，结果头改为 `标题 + 当前筛选 + 共 N 条`，列表行动作收成 icon-only 轻动作；删除/提交/取消链路统一改回共享 `ConfirmDialog`，新建弹层移除说明句与分段说明文案，详情弹层的信息卡、附件块和流程轨迹块也继续压薄；本轮列表已补回出差人、部门和项目等基础信息，但最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination、BusinessTripPage、ProcessTrace | 页面与关键弹层基础暗色已接入 | 联调中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 中复杂业务页 |
| OA/办公 | `/office/contact` | `src/pages/ContactPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第二批，页头说明已继续减法，筛选条收成单搜索 + 轻操作，左侧部门导航也从卡片按钮压回更轻的后台侧列语法；右侧结果表重组为联系人、组织、联系方式三段双行信息列，行内动作收成 icon-only，详情名片也继续压薄为更克制的字段块，但最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、Pagination、ContactPage | 页面与详情弹层基础暗色已接入 | 联调中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台列表页与轻详情语法收口，待继续对照源码收口 | 联系人页面 |
| OA/办公 | `/admin/visitor` | `src/pages/VisitorPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续删除英文眉题、顶部说明段和胶囊统计，把状态摘要压回单行结果条，筛选区改为单条轻筛选栏，行内动作收成 icon-only 中性操作，新增预约与取消确认弹层也同步去掉冗余说明并收窄到更接近 `.references/sub2api` 的轻弹层比例；但列表列宽与少量最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、Pagination、BaseDialog、ConfirmDialog、components/ui | 页面与关键弹层已接入 Light / Dark | 联调中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 访客页面 |
| OA/办公 | `/admin/duty-schedule` | `src/pages/DutySchedulePage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续删除英文眉题、顶部说明段和胶囊统计，把状态摘要压回单行结果条，筛选区改为单条轻筛选栏，签到/换班/签退动作收成 icon-only 中性操作，新增排班与换班弹层也同步去掉冗余说明并收窄到更接近 `.references/sub2api` 的轻弹层比例；但列表列宽、排班明细密度和最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、Pagination、BaseDialog、components/ui | 页面与关键弹层已接入 Light / Dark | 联调中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 排班页 |

### Admin
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Admin | `/admin/asset` | `src/pages/admin/asset/AssetList.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第五批，页面继续把二维码弹层压回轻摘要 + 单二维码主体结构，日志弹层改成单容器分隔行列表，新增/编辑资产表单彻底收成 `BaseDialog footer + form body` 并把宽度收窄到 `max-w-2xl`；整体更贴近 `.references/sub2api` 的后台列表页与轻弹层节奏，但日志字段密度和少量最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、Pagination、components/ui、AssetForm | 页面、表格与关键弹层基础暗色已接入 | 实施中 | - | 第三轮纠偏第五批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 资产台账 |
| Admin | `/admin/vehicle` | `src/pages/admin/vehicle/VehicleList.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第四批，页面继续去掉装饰性英文眉题与顶部阴影，批量选择条压成更轻的页内操作，并把车辆详情从字段卡墙收成 `DetailSection + 明细行` 语法；整体更贴近 `.references/sub2api` 的后台列表页与轻弹层节奏，但表单比例和最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、Pagination、components/ui | 页面、表格与关键弹层基础暗色已接入 | 实施中 | - | 第三轮纠偏第四批已推进，构建通过并继续向源码后台列表页与轻弹层语法靠拢，待继续对照源码收口 | 与 `/admin/vehicle/list` 共享实现 |
| Admin | `/admin/vehicle/list` | `src/pages/admin/vehicle/VehicleList.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第四批，页面继续去掉装饰性英文眉题与顶部阴影，批量选择条压成更轻的页内操作，并把车辆详情从字段卡墙收成 `DetailSection + 明细行` 语法；整体更贴近 `.references/sub2api` 的后台列表页与轻弹层节奏，但表单比例和最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、Pagination、components/ui | 页面、表格与关键弹层基础暗色已接入 | 实施中 | - | 第三轮纠偏第四批已推进，构建通过并继续向源码后台列表页与轻弹层语法靠拢，待继续对照源码收口 | 与 `/admin/vehicle` 共享实现 |
| Admin | `/admin/vehicle/booking` | `src/pages/admin/vehicle/VehicleBooking.tsx` | 表单页 | 已按源码后台表单页语法推进第三轮纠偏第三批，页面继续去掉装饰性英文眉题、重色步骤胶囊与确认区摘要卡墙；车辆选择态压回更中性的选中反馈，确认区收成 `分组标题 + 明细行` 语法，显而易见的 placeholder 继续删除；整体更贴近 `.references/sub2api` 的后台表单页节奏，但车辆卡密度、字段留白和最终源码细节仍需继续收口 | 参考表单页 | ThemeProvider、MainLayout、components/ui、FileUpload | 页面、表单与状态页基础暗色已接入 | 实施中 | - | 第三轮纠偏第三批已推进，构建通过并继续向源码后台表单页与轻确认区语法靠拢，待继续对照源码收口 | 用车申请 |
| Admin | `/admin/vehicle/usage` | `src/pages/admin/vehicle/VehicleUsageList.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第五批，页面继续把费用视图摘要压短为汇总 + 条数，移除结果头下方的类型分布说明，并把详情/审批/费用录入里的说明块统一到 `DetailSection` 语法；整体更贴近 `.references/sub2api` 的后台列表页与轻弹层节奏，但最终源码细节仍需继续向源码收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、Pagination、components/ui | 页面、表格与关键弹层基础暗色已接入 | 实施中 | - | 第三轮纠偏第五批已推进，构建通过并继续向源码后台列表页语法靠拢，待继续对照源码收口 | 用车记录 |

### HR
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| HR | `/hr/dashboard` | `src/pages/hr/HrDashboardPage.tsx` | 门户仪表盘页 | 已推进第二轮回炉，HR 仪表盘已移除 `WorkspaceHeroCard / MetricCard / SectionCard / Backdrop / PageContent` 等旧工作台壳层，改为参考后台仪表盘的 `轻量页头 + 顶部状态/操作条 + 紧凑统计卡 + 快捷入口网格 + 双列表数据区` 结构；但统计卡比例、快捷入口密度和双列表细节仍需最终源码对照 | 参考门户仪表盘页 | ThemeProvider、MainLayout、components/ui | 页面已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近源码后台仪表盘页，待最终源码对照验收 | HR 批次起点 |
| HR | `/hr/attendance/checkin` | `src/pages/admin/attendance/AttendanceCheckIn.tsx` | 表单页 | 已按源码后台表单页语法推进第三轮纠偏第二批，顶部状态条已压成日期 / 班次 / 定位三项，主打卡区进一步收成轻量时间主块 + 双主动作，定位状态与结果区说明继续减法，右侧规则栏也删掉了 Wi-Fi / 规则状态等冗余条件；本轮继续移除页面私有 `max-width` 收窄壳层，使外层宽度、页边距与 `/hr/attendance/supplement` 重新对齐；整体更贴近 `.references/sub2api` 的后台表单页节奏，但限制提示、时间块比例和最终源码细节仍需继续对照 | 参考表单页 | ThemeProvider、MainLayout、components/ui | 页面已接入 Light / Dark | 联调中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台表单页语法收口，待最终源码对照验收 | 考勤打卡 |
| HR | `/hr/attendance/rule` | `src/pages/admin/attendance/AttendanceRule.tsx` | 表单页 | 已按源码后台设置页语法推进第三轮纠偏第二批，顶部状态胶囊已压到规则 / 状态 / 工作日三项，空态说明句与显而易见的 placeholder 继续删除，启用 / 加班 / 拍照统一成紧凑开关行，`打卡设置` 已从双列改回源码更接近的单列设置节奏，消除右侧参数悬空；本轮继续移除页面私有 `max-width` 收窄壳层，使外层宽度、页边距与 `/hr/attendance/supplement` 重新对齐；整体更贴近 `.references/sub2api` 的后台设置页节奏，但字段密度和只读态细节仍需最终源码对照 | 参考表单页 | ThemeProvider、MainLayout、components/ui | 页面已接入 Light / Dark | 联调中 | - | 第三轮纠偏第二批已推进，构建通过并继续向源码后台设置页语法收口，待最终源码对照验收 | 考勤规则 |
| HR | `/hr/employees` | `src/pages/hr/HrEmployeePage.tsx` | 标准列表页 | 已推进第二轮回炉，员工页主列表已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceTableStateRow` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 单主结果表 + BaseDialog` 结构；员工子工作区 `HrEmployeeWorkspace` 也同步改为 `轻量头部 + 信息胶囊 + Tabs + 单表格容器 + BaseDialog + ConfirmDialog` 语法，但顶部信息条比例、Tabs 密度和三类档案弹层节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、ConfirmDialog、components/ui、`src/pages/hr/HrEmployeeWorkspace.tsx` | 页面、子工作区、弹层与确认框已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 员工台账 |
| HR | `/hr/recruitment` | `src/pages/hr/HrRecruitmentPage.tsx` | 标准列表页 | 已推进第二轮回炉，招聘页已移除 `WorkspaceHeroCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceTableStateRow` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + 单搜索条 + Tabs + 单表格容器 + BaseDialog` 结构；但顶部状态条比例、三组 Tabs 表格密度和四类弹层节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、BaseDialog、components/ui | 页面、Tabs 与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 招聘页 |
| HR | `/hr/headcount` | `src/pages/hr/HrHeadcountPage.tsx` | 标准列表页 | 已推进第二轮回炉，编制页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 单主结果容器 + 右侧紧凑详情栏 + BaseDialog` 结构；但筛选条比例、右侧详情密度和表格行节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、components/ui | 页面、详情栏与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 编制页 |
| HR | `/hr/salary` | `src/pages/hr/HrSalaryPage.tsx` | 复杂工作台页 | 已推进第二轮回炉第四十五批，薪酬页已移除 `WorkspaceBackdrop / WorkspacePageContent / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，统一替换为页内轻量后台壳层，并把顶部 Hero 与四张概览卡压成 `轻量页头 + 状态/操作条 + 路由级分段导航` 语法；此前已把“员工薪资”页签第一屏的在岗薪资档案、员工薪资详情、薪资档案历史、到手收入测算四块主容器统一成同一套轻量 Section 语法，把“调薪申请”页签左侧统计卡压成紧凑状态条、右侧详情头部八宫格压成单结果信息容器，并把“联调提示”压成单说明容器；最近数批继续把“薪资结构”详情里的项目卡片压成紧凑表格，把“薪资项目 / 分配员工薪资”弹层三卡摘要压成单摘要容器，把“薪资结构”项目选择改成高密度行式列表，并进一步删除页头总说明、员工/调薪页签和基础配置区块解释句、独立联调提示块、列表行里的排序/重复状态标签、薪级与专项扣除表格中的删除辅助文案，以及各弹层里显而易见的 `no-changes` 黄条提示；最近二十五批又继续回炉信息架构与交付链路，把薪酬管理顶层从单页 Tabs 切换改成 `/hr/salary/*` 路由级分段后，先补齐侧栏独立菜单、菜单权限与 SQL 种子，再把 `薪资项目 / 薪资结构 / 薪资等级 / 社保方案` 四个基础配置子页拆到 `src/pages/hr/salary/SalaryFoundationSections.tsx`，把 `员工薪资 / 调薪申请` 两条主链路拆到 `src/pages/hr/salary/SalaryPrimarySections.tsx`，并继续把 `分配员工薪资 / 发起调薪 / 薪资项目 / 薪资结构 / 薪资等级 / 社保方案 / 分配社保公积金方案 / 个税配置 / 专项扣除` 九个深层弹层拆到 `src/pages/hr/salary/SalaryFormDialogs.tsx`、`src/pages/hr/salary/SalaryFoundationDialogs.tsx` 与 `src/pages/hr/salary/SalaryTaxDialogs.tsx`，同时把页内 `window.confirm` 删除/动作确认链路统一替换成共享 `ConfirmDialog`；最近十四批又继续删除最外层空 `Workspace` 容器、去掉顶部状态胶囊墙、把路由分段导航从 `Tabs` 组件改成普通分段按钮条，并把 `WorkspaceSectionCard / WorkspaceMetricStrip / WorkspaceDiagnosticSummary / WorkspaceInlineRiskList / DetailRow` 继续压成更接近源码后台页的轻边框、轻分隔和薄层容器表达；本轮继续修正 `个税配置` 弹窗下半区，让外层父容器重新回归两列 grid，从而使 `xl:col-span-2` 真正生效，彻底修复“块移位后仍被左列宽度约束”的问题 | 参考复杂工作台页 | ThemeProvider、MainLayout、BaseDialog、components/ui、ConfirmDialog | 页面壳层、分段导航、员工薪资主链路、调薪申请主链路、基础配置主视图、弹层与确认框已接入 Light / Dark | 实施中 | - | 第二轮回炉第四十五批已推进，个税配置弹窗下半区的父容器与跨列关系已修复，但少量深层弹层与局部状态表达仍需继续对照源码 | 当前最大页面之一 |
| HR | `/hr/leave/quota` | `src/pages/hr/HrLeaveQuotaPage.tsx` | 标准列表页 | 已推进第二轮回炉，假期额度页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 左侧员工列表 + 右侧额度结果区 + BaseDialog` 结构；但左右栏比例、结果区表格密度和调整弹层节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、components/ui | 页面、结果区、表格与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 假期额度 |
| HR | `/hr/onboarding` | `src/pages/hr/HrOnboardingPage.tsx` | 标准列表页 | 已推进第二轮回炉，入职页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 左侧申请列表 + 右侧详情与任务表 + BaseDialog` 结构；但左侧列表密度、右侧详情比例和创建弹层节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、components/ui | 页面、详情区、任务表与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 入职页 |
| HR | `/hr/probation` | `src/pages/hr/HrProbationPage.tsx` | 标准列表页 | 已推进第二轮回炉，转正页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 三栏主区（员工列表 / 申请列表 / 申请详情）+ BaseDialog` 结构；但三栏比例、申请列表密度和创建弹层节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、components/ui | 页面、三栏主区与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 转正页 |
| HR | `/hr/offer` | `src/pages/hr/HrOfferPage.tsx` | 标准列表页 | 已推进第二轮回炉，Offer 页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 左侧 Offer 列表 + 右侧详情区 + BaseDialog` 结构；但左侧列表密度、右侧详情比例和创建弹层节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、components/ui | 页面、详情区与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 录用页 |
| HR | `/hr/transfer` | `src/pages/hr/HrTransferPage.tsx` | 标准列表页 | 已推进第二轮回炉，调动页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 三栏主区（员工列表 / 申请列表 / 申请详情）+ BaseDialog` 结构；但三栏比例、申请列表密度和详情区节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、components/ui | 页面、三栏主区与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 调动页 |
| HR | `/hr/resignation` | `src/pages/hr/HrResignationPage.tsx` | 标准列表页 | 已推进第二轮回炉，离职页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为参考后台列表页的 `轻量页头 + 状态/操作条 + TablePageLayout + 三栏主区（员工列表 / 申请列表 / 申请详情）+ 单交接表 + BaseDialog` 结构；但三栏比例、交接表密度和详情区节奏仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、BaseDialog、components/ui | 页面、三栏主区、交接表与弹层已接入 Light / Dark | 联调中 | - | 第二轮回炉已推进，构建通过并明显贴近 `.references/sub2api` 后台列表页，待最终源码对照验收 | 离职页 |

### System
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| System | `/users` | `src/pages/OrgStructurePage.tsx` | 复杂工作台页 | 已按参考后台设计语言完成第二轮回炉，移除大 Hero / Workbench / ResultCard / SectionCard 结构，改为轻量头部摘要 + 双栏组织工作区语法，并同步回炉 `OrgStructure` 组件内部的部门树、成员表格和详情/调岗弹层，但双栏比例、组织树密度和成员动作细节仍未达到 99% 源码对齐 | 参考复杂工作台页 | ThemeProvider、MainLayout、components/ui、BaseDialog、ConfirmDialog | 页面、部门树、成员表格与弹层基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 组织结构 |
| System | `/code` | `src/pages/CodeGeneration.tsx` | 复杂工作台页 | 已按参考后台设计语言完成第二轮回炉，移除大 Hero / Workbench / ResultCard 结构，改为轻量头部摘要 + 流程选择区 + 代码预览工作区语法，并同步收窄 `SourceCodeViewer` 的结果说明、标签页和 AI 动作壳层，但流程选择区比例、代码预览头部细节和生成反馈节奏仍未达到 99% 源码对齐 | 参考复杂工作台页 | ThemeProvider、MainLayout、components/ui、Tabs | 页面、标签页、代码预览壳层与生成反馈基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 代码生成 |
| System | `/system/users` | `src/pages/system/UserList.tsx` | 标准列表页 | 已按源码后台列表页骨架完成第二轮回炉，移除大 Hero / Workbench 结构，改为 `filters + table + pagination + light dialog` 轻量列表语法，并保留角色分配、部门归属、租户选择和密码哈希能力，但列表密度、表单比例和角色区细节仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、筛选器、分页与编辑弹层基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 标准 CRUD 批次 |
| System | `/system/roles` | `src/pages/system/RoleList.tsx` | 标准列表页 | 已按源码后台列表页骨架完成第二轮回炉，移除大 Hero / Workbench 结构，改为 `filters + table + pagination + light dialog` 轻量列表语法，并保留菜单树、部门树、租户归属和数据范围配置，但列表密度、授权树表达和弹窗细节仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、权限树、部门树、分页与弹层基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 标准 CRUD 批次 |
| System | `/system/menus` | `src/pages/system/MenuList.tsx` | 标准列表页 | 已按源码后台列表页骨架完成第二轮回炉，移除大 Hero / Workbench 结构，改为 `filters + tree table + light dialog` 轻量树表格语法，并保留搜索保留祖先链、层级展开和子节点增量创建，但树表格密度、图标表达和弹窗细节仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog | 页面、树表格与弹层基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 标准 CRUD 批次 |
| System | `/system/files` | `src/pages/system/FileList.tsx` | 标准列表页 | 已按源码后台列表页骨架完成第二轮回炉，移除大 Hero / Workbench / ResultCard 结构，改为 `filters + table + pagination` 轻量列表语法，并将上传、容量校准和存储摘要压缩进轻量头部信息区，但表格密度、文件类型表达和上传反馈细节仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、ConfirmDialog、Pagination | 页面、筛选器、结果表格与确认弹窗基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 标准 CRUD 批次 |
| System | `/system/tenant` | `src/pages/system/TenantList.tsx` | 标准列表页 | 已按源码后台列表页骨架完成第二轮回炉，移除大 Hero / Workbench / ResultCard 结构，改为 `filters + table + pagination + light dialog` 轻量列表语法，并保留租户配额、存储刷新、状态切换和编辑表单能力，但列表密度、风险标签表达和表单比例细节仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、筛选器、分页与编辑弹层基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 标准 CRUD 批次 |
| System | `/system/log` | `src/pages/system/OperationLogPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续撤掉“查看趋势”弹层入口，把趋势图直接放回页内上半区，形成上图下表的单页结构；筛选仍维持轻量工具条，列表留在下半区单结果容器，详情与删除链路继续使用共享弹层/确认框，但列表密度和详情弹层细节仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、页内趋势区、详情弹层与结果表格基础暗色已接入 | 实施中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页与页内趋势语法收口，待继续对照源码收口 | 日志页 |
| System | `/system/audit-log` | `src/pages/system/AuditLogPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续删除说明型副文案和重摘要胶囊，把筛选区、结果头、表格与详情弹层统一收成和 `/system/log` 同一套轻量列表页语法；前后值对比详情保留，但压回更中性的对照面板表达，列表密度和详情编排仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、对比详情弹层、分页与结果表格基础暗色已接入 | 实施中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页与轻详情语法收口，待继续对照源码收口 | 审计日志 |
| System | `/system/login-log` | `src/pages/system/LoginLogPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续删除说明型副文案和重摘要胶囊，把筛选区、结果头、表格与详情弹层统一收成和 `/system/log` 同一套轻量列表页语法；批量删除、服务端分页和详情查看保留，但列表密度和详情编排仍需最终源码对照 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、分页、详情弹层与结果表格基础暗色已接入 | 实施中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页与轻详情语法收口，待继续对照源码收口 | 登录日志 |
| System | `/system/online` | `src/pages/system/OnlineUserPage.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续删除说明型副文案和重摘要胶囊，把顶部状态汇总压成单行结果条，筛选区收成单条轻筛选栏，列表头与行内强退动作也统一收成更克制的后台列表页语法；批量强退、当前会话不可操作约束和服务端分页保留，但列表密度和少量最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、ConfirmDialog、Pagination | 页面、分页、结果表格与强退确认基础暗色已接入 | 实施中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页语法收口，待继续对照源码收口 | 在线用户 |
| System | `/system/post` | `src/pages/system/PostList.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续删除说明型副文案和重摘要胶囊，把顶部状态汇总压成单行结果条，筛选区收成单条轻筛选栏，列表头与编辑/删除弹层统一收成更克制的后台列表页语法；岗位创建、编辑与删除链路保留，但表格密度和少量最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、编辑弹层、分页与结果表格基础暗色已接入 | 实施中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 岗位管理 |
| System | `/system/config` | `src/pages/system/ConfigList.tsx` | 标准列表页 | 已按源码后台列表页语法推进第三轮纠偏第一批，页面继续删除说明型副文案和重摘要胶囊，把顶部状态汇总压成单行结果条，筛选区收成单条轻筛选栏，列表头与编辑/删除弹层统一收成更克制的后台列表页语法；内置参数不可删除等业务约束保留，但表格密度、键值表达和少量最终源码细节仍需继续收口 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog、Pagination | 页面、编辑弹层、分页与结果表格基础暗色已接入 | 实施中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台列表页与轻弹层语法收口，待继续对照源码收口 | 参数配置 |
| System | `/system/cache` | `src/pages/system/CacheMonitor.tsx` | 复杂工作台页 | 已按源码后台页语法推进第三轮纠偏第一批，页面继续去掉大 Hero / Workbench / ResultCard / SectionCard 壳层，改为轻量页头 + `TablePageLayout` + 视图切换 + 单容器双栏浏览器语法；概览信息、命令统计、Key 树、详情区与删除链路保留，但双栏比例、浏览器明细密度和少量最终源码细节仍需继续收口 | 参考复杂工作台页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、ConfirmDialog | 页面、Key 树、详情区与删除确认基础暗色已接入 | 实施中 | - | 第三轮纠偏第一批已推进，构建通过并继续向源码后台复杂页语法收口，待继续对照源码收口 | 监控类页面 |
| System | `/system/dict` | `src/pages/admin/DictPage.tsx` | 标准列表页 | 已按源码后台列表页骨架完成第二轮回炉，移除大 Hero / Workbench / SectionCard 结构，改为 `filters + split list/table + light dialog` 轻量双栏语法，并保留类型筛选、类型与数据双弹层、联动删除和右侧数据表能力，但双栏比例、类型卡密度和数据表细节仍未达到 99% 源码对齐 | 参考标准列表页 | ThemeProvider、MainLayout、TablePageLayout、components/ui、BaseDialog、ConfirmDialog | 页面、双栏结构、编辑弹层与删除确认基础暗色已接入 | 实施中 | - | 第二轮回炉已完成并构建通过，未达到最终验收 | 字典管理 |

## 完成记录
| 日期 | 完成项 | 影响共享组件/页面 | Light / Dark 验收结论 | 遗留事项 |
| --- | --- | --- | --- | --- |
| 2026-04-22 | `/hr/overtime/applications` 第三轮纠偏第二批 | `src/pages/OvertimeApplicationPage.tsx` | 顶部摘要已从胶囊墙压成单行汇总，结果头改为标题 + 当前筛选 + 共 N 条，列表行动作收成 icon-only 轻动作；新建/编辑弹层移除说明句并删除右侧“当前配置”侧卡，改成行内摘要，详情弹层字段卡、事由块和流程轨迹块也继续压薄。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧表单比例、详情密度和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/attendance/supplement` 第三轮纠偏第二批 | `src/pages/AttendanceSupplementPage.tsx` | 顶部摘要已从胶囊墙压成单行汇总，结果头改为标题 + 当前筛选 + 共 N 条，列表行动作收成 icon-only 轻动作；新建/编辑弹层移除说明句并放宽为 `wide`，详情弹层字段卡、原因块和流程轨迹块也继续压薄。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧表单比例、详情密度和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/attendance/checkin` `/hr/attendance/rule` 宽度纠偏 | `src/pages/admin/attendance/AttendanceCheckIn.tsx`、`src/pages/admin/attendance/AttendanceRule.tsx` | 两页已移除私有 `max-width` 收窄壳层，外层宽度、页边距与 `/hr/attendance/supplement` 重新对齐，不再单独形成更窄的一套页面壳。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧打卡/规则页内部的信息密度与最终源码细节；当前状态维持 `联调中` |
| 2026-04-22 | `/hr/attendance/rule` 第三轮纠偏第二批 | `src/pages/admin/attendance/AttendanceRule.tsx` | 顶部状态胶囊已压到规则 / 状态 / 工作日三项，空态说明句与显而易见的 placeholder 继续删除，启用 / 加班 / 拍照已统一成紧凑开关行，Wi-Fi 与备注在只读态也已改成轻量结果块；本轮继续把 `打卡设置` 从双列改回单列，修掉右侧参数悬空。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧字段密度和只读态细节；当前状态维持 `联调中` |
| 2026-04-22 | `/hr/attendance/checkin` 第三轮纠偏第二批 | `src/pages/admin/attendance/AttendanceCheckIn.tsx` | 顶部状态条已压成日期 / 班次 / 定位三项，主打卡区继续压成轻量时间主块 + 双主动作，定位状态与结果区说明继续减法，右侧规则栏删掉了 Wi-Fi / 规则状态等冗余条件。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧限制提示、时间块比例和最终源码细节；当前状态维持 `联调中` |
| 2026-04-22 | `/admin/asset` 第三轮纠偏第二批 | `src/pages/admin/asset/AssetList.tsx`、`src/pages/admin/asset/AssetForm.tsx` | 顶部摘要已从胶囊堆压成单行汇总，筛选条补回单行筛选摘要，结果头改为标题 + 当前筛选 + 共 N 条，列表主列也收成资产编码、资产信息、状态、价格/位置的双行信息列；行内动作改为 icon-only 轻动作，表单与二维码/日志/领用/送修等弹层说明句继续减法。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧借用链路、行内动作排序和少量弹层节奏；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/attendance/rule` 第三轮纠偏第一批 | `src/pages/admin/attendance/AttendanceRule.tsx` | 页头说明句、空态长说明和设置分组解释文案已继续压掉，启用/加班/拍照三处开关块也已压成更自解释的紧凑设置行。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧状态条胶囊数量、设置项密度和少量输入提示；当前状态维持 `联调中` |
| 2026-04-22 | `/hr/attendance/checkin` 第三轮纠偏第一批 | `src/pages/admin/attendance/AttendanceCheckIn.tsx` | 页头解释句、主面板副说明与右侧条件面板的说明型描述已继续压掉，打卡动作改成更自解释的紧凑按钮，右侧条件也收成更克制的信息行。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧主面板时间块比例、条件区密度和少量提示表达；当前状态维持 `联调中` |
| 2026-04-22 | `/admin/asset` 第三轮纠偏第四批 | `src/pages/admin/asset/AssetList.tsx`、`src/pages/admin/asset/AssetForm.tsx` | 新增/编辑资产表单已收回到 `BaseDialog footer + form body` 语法，详情弹层承接次级动作后，列表行仅保留 `详情 + 编辑 + 单主动作`；领用/处置弹层也统一成对象摘要 + 单字段输入节奏。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧日志密度与少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | 主布局侧栏 / 页头第三轮纠偏 | `src/layouts/MainLayout.tsx` | 已对照 `.references/sub2api` 去掉侧栏品牌副标题、Header 左侧小字说明和底部退出入口，把底部主题 / 收起按钮收回到源码侧栏的平直 `sidebar-link` 语法，并将活动路由所在分组改为允许手动收起。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧侧栏宽度比例与 Header 其余细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle` `/admin/vehicle/list` 第三轮纠偏第四批 | `src/pages/admin/vehicle/VehicleList.tsx` | 页面继续去掉装饰性英文眉题与顶部阴影，批量选择条压成更轻的页内操作，详情弹层也从字段卡墙收成 `DetailSection + 明细行` 结构；Light / Dark 下 `npm run build` 通过，页面继续向 `.references/sub2api` 的后台列表页与轻弹层语法收口 | 仍需继续对照 `.references/sub2api` 收紧表单比例和最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle` `/admin/vehicle/list` 第三轮纠偏第三批 | `src/pages/admin/vehicle/VehicleList.tsx` | 顶部摘要已继续压成单行汇总，筛选条收口为搜索 + 状态 + 当前筛选摘要，结果头改为标题 + 当前筛选 + 共 N 条，列表主列也收成车牌号、车辆信息、状态、里程/位置、保险到期的双行信息列；批量删除条与行内动作进一步减重为 icon-only 轻动作。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧详情字段层级和少量弹层节奏；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/usage` 第三轮纠偏第五批 | `src/pages/admin/vehicle/VehicleUsageList.tsx` | 费用视图摘要已压短为汇总 + 条数，结果头下方的类型分布说明已移除，详情/审批/费用录入里的说明块继续统一到 `DetailSection` 语法；Light / Dark 下 `npm run build` 通过，页面继续向 `.references/sub2api` 的后台列表页与轻弹层语法收口 | 仍需继续对照 `.references/sub2api` 收紧最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/usage` 第三轮纠偏第四批 | `src/pages/admin/vehicle/VehicleUsageList.tsx` | 详情、审批与归还弹层里的字段墙已压成分组明细行，去掉大面积卡片式详情展示；Light / Dark 下 `npm run build` 通过，页面继续向 `.references/sub2api` 的后台详情面板语法收口 | 仍需继续对照 `.references/sub2api` 收紧费用筛选后的摘要组织和最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/usage` 第三轮纠偏第三批 | `src/pages/admin/vehicle/VehicleUsageList.tsx` | 用车列表行内动作已压成“详情 + 单主动作”，取消申请与进行中录费补到详情弹层；费用汇总已跟日期筛选同步。Light / Dark 下 `npm run build` 通过，页面继续向 `.references/sub2api` 的后台列表页语法收口 | 仍需继续对照 `.references/sub2api` 收紧费用筛选后的摘要组织、少量详情字段编排和最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/usage` 第三轮纠偏第二批 | `src/pages/admin/vehicle/VehicleUsageList.tsx` | 费用视图已补齐类型与起止日期筛选，结果头改为当前筛选摘要，详情弹层顶部信息也继续压轻；Light / Dark 下 `npm run build` 通过，页面继续向 `.references/sub2api` 的后台列表页语法收口 | 仍需继续对照 `.references/sub2api` 收紧列表动作密度、费用筛选后的摘要组织和少量详情节奏；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/usage` 第三轮纠偏第一批 | `src/pages/admin/vehicle/VehicleUsageList.tsx` | 已移除 `WorkspaceBackdrop / WorkspaceHeroMetricsSection / WorkspaceWorkbenchCard / WorkspaceResultCard / WorkspacePaginationBar / WorkspacePageContent / WorkspaceTableStateRow` 与原生 `Dialog`、原生 `confirm` 等旧语法，改为轻量页头 + `TablePageLayout` + 视图切换/状态筛选单条 + 单容器结果表 + `BaseDialog / ConfirmDialog / Pagination` 结构；用车记录、费用明细与审批/归还/费用录入链路已回收到共享弹层。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表动作密度、费用视图筛选深度和少量详情节奏；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/booking` 第三轮纠偏第三批 | `src/pages/admin/vehicle/VehicleBooking.tsx` | 页面继续去掉装饰性英文眉题、重色步骤胶囊与确认区摘要卡墙；车辆选择态压回更中性的选中反馈，确认区收成 `分组标题 + 明细行` 结构，显而易见的 placeholder 继续删除。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧车辆卡密度、字段留白和最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/booking` 第三轮纠偏第二批 | `src/pages/admin/vehicle/VehicleBooking.tsx` | 已删除右侧摘要栏与整页状态卡，改为页内单主容器；加载/错态/空态统一页内化，步骤条改为头部轻量进度，确认区压成字段摘要 + 文本块，附件说明同步减法。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧车辆卡密度、字段留白和极少量表单细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle` `/admin/vehicle/list` 第三轮纠偏第二批 | `src/pages/admin/vehicle/VehicleList.tsx` | 筛选条已压回单行搜索 / 状态 / 摘要 / 操作按钮，结果头简化为单行统计，批量删除条改为轻量页内操作，新增/详情弹层去掉说明性文案。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧行内动作密度、详情信息层级和少量弹层节奏；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle/booking` 第三轮纠偏第一批 | `src/pages/admin/vehicle/VehicleBooking.tsx` | 已移除 `WorkspaceBackdrop / WorkspaceHeroMetricsSection / WorkspaceSectionCard / WorkspaceStatusPage / WorkspaceEmptyPanel / WorkspacePageContent` 等旧工作台壳层，改为轻量页头 + 单主容器三步表单 + 右侧摘要栏结构；可用车辆状态、空态和提交流程已压回更克制的后台页表达。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧车辆卡密度、摘要栏比例和少量细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/vehicle` `/admin/vehicle/list` 第三轮纠偏第一批 | `src/pages/admin/vehicle/VehicleList.tsx` | 已移除 `WorkspaceBackdrop / WorkspaceHeroCard / WorkspaceWorkbenchCard / WorkspaceResultCard / WorkspaceDialogShell / WorkspacePaginationBar / WorkspaceEmptyPanel / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果表 + `BaseDialog / ConfirmDialog / Pagination` 结构；新增/编辑、详情和单个/批量删除链路已回收到共享弹层。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧行内动作密度、表单节奏和少量细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/asset` 第三轮纠偏第一批 | `src/pages/admin/asset/AssetList.tsx`、`src/pages/admin/asset/AssetForm.tsx` | 已移除旧白卡统计看板、手写分页、原生 `Dialog`、`window.confirm/window.prompt` 与 `WorkspaceTableStateRow` 等历史语法，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果表 + `BaseDialog / ConfirmDialog / Pagination` 结构；资产新增/编辑、二维码、日志、领用、归还、送修、报废等动作已回收到共享弹层，`AssetForm` 也同步压紧为更接近后台弹窗表单的节奏。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧行内动作密度、借用链路和少量弹层细节；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/attendance/supplement` 第三轮纠偏第一批 | `src/pages/AttendanceSupplementPage.tsx` | 已移除 `WorkspaceBackdrop / WorkspaceHeroMetricsSection / WorkspaceWorkbenchCard / WorkspaceResultCard / WorkspacePaginationBar / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果表 + `BaseDialog / ConfirmDialog` 结构；编辑/提交/删除链路已回收到共享弹层与确认框，详情弹层同步压回标准后台页语法。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧表单比例、详情信息密度和少量弹层文案；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/overtime/applications` 第三轮纠偏第一批 | `src/pages/OvertimeApplicationPage.tsx` | 已移除 `WorkspaceBackdrop / WorkspaceHeroMetricsSection / WorkspaceWorkbenchCard / WorkspaceResultCard / WorkspacePaginationBar / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果表 + `BaseDialog / ConfirmDialog` 结构；编辑/提交/删除/撤销链路已回收到共享弹层与确认框，详情弹层同步压回标准后台页语法。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧表单摘要比例、详情信息密度和少量弹层文案；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/leave/application` 第三轮纠偏第二批 | `src/pages/LeaveApplicationPage.tsx` | 顶部摘要已从胶囊统计压成单行汇总，结果头改为标题 + 当前筛选 + 共 N 条，列表行动作收成 icon-only 轻动作；新建弹层移除说明句和右侧“当前配置”侧卡，改成行内摘要，详情弹层字段卡、原因块和流程轨迹块也继续压薄。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧表单比例、详情密度和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/leave/application` 第三轮纠偏第一批 | `src/pages/LeaveApplicationPage.tsx` | 已移除 `WorkspaceBackdrop / WorkspaceHeroMetricsSection / WorkspaceWorkbenchCard / WorkspaceResultCard / WorkspacePaginationBar / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果表 + `BaseDialog / ConfirmDialog` 结构；提交/撤销链路已回收到共享确认框，创建与详情弹层同步压回标准后台页语法。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧表单摘要比例、详情信息密度和少量弹层文案；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第四十五批 | `src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已把“个税配置”弹窗外层父容器改回两列 grid，使下半区 `xl:col-span-2` 真正生效，修复“块移位后仍被左列宽度约束”的问题。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `278.03 kB` | 仍需继续对照源码收紧少量深层弹层与局部状态表达；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第四十四批 | `src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已把“个税配置”弹窗下半区设为真正跨两列的全宽区，并给档位表补 `min-w-[760px]`，修复“看起来移到左侧、实际仍挤在左列容器里”的问题。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `278.08 kB` | 仍需继续对照源码收紧少量深层弹层与局部状态表达；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第四十三批 | `src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已把“个税配置”弹窗下半区的 `税率 JSON + 档位表` 从右列移到全宽区，修复底部内容在右列里被压缩的问题。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `278.04 kB` | 仍需继续对照源码收紧少量深层弹层与局部状态表达；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第四十二批 | `src/pages/hr/salary/SalaryFoundationDialogs.tsx` | 已把“薪资结构 / 薪资等级 / 社保方案”三个基础配置弹窗中的解释性 warning 条移除，只保留真正阻断保存的 danger 校验。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `277.80 kB` | 仍需继续对照源码收紧少量深层弹层与局部状态表达；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第四十一批 | `src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已把“个税配置”弹窗里被压缩的“样本预览 / 参考值”从并排改成纵向展开，避免当前对话框宽度下再次挤压内容列。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `277.75 kB` | 仍需继续对照源码收紧少量深层弹层与局部状态表达；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第四十批 | `src/pages/hr/salary/SalaryPrimarySections.tsx`、`src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已把“到手收入测算”收成左侧主内容 + 右侧固定窄侧栏，并将右侧“个税 / 专项扣除明细”两张卡合成单容器；同时把“个税配置”弹窗改成固定表单列 + 主内容列，并压低样本/参考区的色彩强调。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `277.79 kB` | 仍需继续对照源码收紧少量深层弹层与局部状态表达；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十九批 | `src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已把“专项扣除”弹窗从“上下都挤在右列”的旧结构改成上半双栏 + 下半全宽列表，并把风险提示、状态胶囊、删除按钮和参考状态从旧的高饱和重点色收回到更接近源码后台页的中性细边框语法；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `278.01 kB` | 仍需继续对照源码收紧测算双栏比例与少量深层弹层节奏；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十八批 | `src/pages/hr/salary/SalaryPrimarySections.tsx` | 已把“员工薪资”主链路中的员工详情、薪资历史、到手测算、调薪履历、社保台账五块残留多卡摘要统一改成 `WorkspaceMetricStrip` 条状结果容器，并把个税区三张小卡收成单 `DetailRow` 容器；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `277.10 kB` | 仍需继续对照源码收紧测算双栏比例与少量深层弹层节奏；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十七批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryPrimarySections.tsx` | 已继续压轻 `WorkspaceSectionCard / WorkspaceMetricStrip / WorkspaceDiagnosticSummary / WorkspaceInlineRiskList / DetailRow` 的边框、背景和间距，并把调薪详情里的动作校验、闭环、原因三块白卡收成更薄的后台页容器语法；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `284.49 kB` | 仍需继续对照源码收紧员工详情、薪资历史、到手测算摘要里残留的多卡块；当前状态维持 `实施中` |
| 2026-04-22 | 主布局滚动容器纠偏 | `src/layouts/MainLayout.tsx` | 已把桌面主布局从“外层随内容增长”收口为固定视口壳层，并让 `main` 成为唯一纵向滚动容器，修复 `/dashboard`、`/schedule` 等页面鼠标滚轮落不到主内容区的问题；Light / Dark 下 `npm run build` 通过 | 仍需继续观察少量第三方控件区域在极端长页里的滚轮表现，并保持后续页面不要重新引入依赖 body 的页面滚动 |
| 2026-04-22 | 共享弹窗滚动锁修复 | `src/utils/bodyScrollLock.ts`、`src/components/common/BaseDialog.tsx`、`src/components/common/AnnouncementDetailModal.tsx`、`src/components/common/AnnouncementPopup.tsx`、`src/App.tsx`、`src/index.css` | 已参考 `.references/sub2api` 把弹窗锁滚从直接写 `document.body.style.overflow` 改成 `body.modal-open` + 计数器管理，并补上应用启动时对历史残留 `overflow:hidden` 的兜底清理；页面级鼠标滚动不再会被多弹窗交错关闭永久锁死。Light / Dark 下 `npm run build` 通过 | 仍需继续把其余历史弹层逐步统一到同一套共享锁滚工具，避免再出现并行实现 |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十六批 | `src/components/common/BaseDialog.tsx`、`src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryFormDialogs.tsx`、`src/pages/hr/salary/SalaryFoundationDialogs.tsx`、`src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已按 `.references/sub2api` 的弹窗契约把共享 `BaseDialog` 收口到预设宽度档位、面板限高和内容区滚动，并把薪酬链路里直接写死的 `max-w-5xl / max-w-6xl` 回收到 `normal / wide` 预设，避免弹窗继续放大到接近整页；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `283.96 kB` | 仍需继续对照源码回收其他页面遗留的超宽业务弹窗，并收紧薪酬页最后一层视觉比例；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十五批 | `src/pages/hr/salary/SalaryFoundationDialogs.tsx` | 已把 `社保方案` 弹窗从 `max-w-5xl` 收窄到 `max-w-4xl`，并补上视口内滚动与更紧凑的比例输入网格，避免弹窗视觉上大过页面主体；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `284.16 kB` | 仍需继续对照源码收紧最后一层视觉比例与局部工作台原语命名；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十四批 | `src/pages/hr/HrSalaryPage.tsx` | 已把 `WorkspaceSectionCard / WorkspaceMetricStrip / WorkspaceDiagnosticSummary / WorkspaceInlineRiskList` 继续压成更接近 `.references/sub2api` 后台页的轻边框、轻分隔和行式提示语法，降低卡片套卡片的重量；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `283.93 kB` | 仍需继续对照源码收紧最后一层视觉比例与局部工作台原语命名；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十三批 | `src/pages/hr/HrSalaryPage.tsx` | 已把路由分段导航从 `Tabs` 组件切回更接近 `.references/sub2api` 后台页的普通分段按钮条，页面主体改为按当前路由直接渲染对应分区；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `284.11 kB` | 仍需继续对照源码收紧最后一层视觉比例与局部工作台原语命名；当前状态维持 `实施中` |
| 2026-04-22 | `/hr/salary` 第二轮回炉第三十二批 | `src/pages/hr/HrSalaryPage.tsx` | 已删除最外层空 `Workspace` 容器与顶部四枚概览胶囊，把页头压成更接近 `.references/sub2api` 后台页的单标题 + 单操作条 + 紧凑分段导航；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `284.12 kB` | 仍需继续对照源码收紧最后一层视觉比例与局部工作台原语命名；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第三十一批 | `src/pages/hr/HrSalaryPage.tsx`、`src/components/common/ConfirmDialog.tsx` | 已把 `薪资项目 / 薪资结构 / 薪级 / 专项扣除 / 调薪流转` 的 `window.confirm` 链路统一替换为共享 `ConfirmDialog`，并补齐多行提示展示；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `285.97 kB` | 仍需继续对照源码收紧最后一层视觉比例与少量壳层命名/组织；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第三十批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryTaxDialogs.tsx` | 已把 `个税配置 / 专项扣除` 两个税务重弹层从 `HrSalaryPage` 大文件中继续抽成独立文件，主文件进一步收敛为路由分段、状态和动作编排层；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `285.05 kB` | 仍需继续对照源码收紧删除确认链路和最后一层视觉比例；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十九批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryFormDialogs.tsx` | 已把 `分配社保公积金方案` 弹层从 `HrSalaryPage` 大文件中继续抽成独立文件，主文件进一步收敛为路由分段、状态和动作编排层；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `281.59 kB` | 仍需继续对照源码收紧个税配置、专项扣除两块重弹层、删除确认链路和最后一层视觉比例；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十八批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryFoundationDialogs.tsx` | 已把 `薪资等级 / 社保方案` 两个基础配置弹层从 `HrSalaryPage` 大文件中继续抽成独立文件，主文件进一步收敛为路由分段、状态和动作编排层；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `280.91 kB` | 仍需继续对照源码收紧剩余创建/编辑弹层、删除确认链路和最后一层视觉比例；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十七批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryFoundationDialogs.tsx` | 已把 `薪资项目 / 薪资结构` 两个基础配置弹层从 `HrSalaryPage` 大文件中继续抽成独立文件，主文件进一步收敛为路由分段、状态和动作编排层；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `279.90 kB` | 仍需继续对照源码收紧剩余创建/编辑弹层、删除确认链路和最后一层视觉比例；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十六批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryFormDialogs.tsx` | 已把 `分配员工薪资 / 发起调薪` 两个深层弹层从 `HrSalaryPage` 大文件中继续抽成独立文件，主文件进一步收敛为路由分段、状态和动作编排层；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `278.69 kB` | 仍需继续对照源码收紧剩余创建/编辑弹层、删除确认链路和最后一层视觉比例；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十五批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryPrimarySections.tsx` | 已把 `员工薪资 / 调薪申请` 两条主链路从 `HrSalaryPage` 大文件中继续抽成独立 section 文件，主文件进一步收敛为路由分段、状态和动作编排层；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `277.12 kB` | 仍需继续对照源码收紧深层创建/编辑弹层、删除确认链路和最后一层视觉比例；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十四批 | `src/pages/hr/HrSalaryPage.tsx`、`src/pages/hr/salary/SalaryFoundationSections.tsx` | 已把 `薪资项目 / 薪资结构 / 薪资等级 / 社保方案` 四个基础配置子页从 `HrSalaryPage` 内联 giant switch 中抽成独立文件，主文件只保留路由分段和状态编排；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `268.72 kB` | 仍需继续对照源码收紧删除确认链路、部分创建弹层，并继续把员工薪资和调薪申请两条主链路拆出独立文件；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十三批 | `cloudflow-backend/DB/06.cloudflow-business-seed.sql`、`docs/前端设计语言重构台账.md` | 已补齐侧栏独立菜单、菜单权限与 SQL 种子：保留 `/hr/salary` 作为主入口，同时新增 `薪资项目 / 薪资结构 / 薪资等级 / 社保方案` 四条 HR 子菜单，并把 HR 角色授权与排序一并补进种子，现有两级侧栏会直接吃到新菜单树 | 仍需继续对照源码收紧删除确认链路、部分创建弹层，并把四个基础配置子页继续拆成真正独立文件；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十二批 | `src/pages/hr/HrSalaryPage.tsx` | 已继续把基础配置从单一 `foundation` 入口拆成 `items / structures / grades / insurance` 四个独立子页出口，顶部分段导航同步扩成六段，旧 `/hr/salary/foundation` 已兼容重定向到 `/hr/salary/items`；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `264.25 kB` | 仍需继续对照源码收紧删除确认链路、部分创建弹层，并把四个基础配置子页继续拆成真正独立文件；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十一批 | `src/router.tsx`、`src/pages/hr/HrSalaryPage.tsx` | 已把薪酬管理顶层从单页 Tabs 切换改成 `/hr/salary/*` 路由级分段，先拆出 `employees / adjustments / foundation` 三个子页入口；切换时组件实例保持不卸载，筛选状态和当前上下文仍可保留。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `263.69 kB` | 仍需继续对照源码收紧删除确认链路、部分基础配置创建弹层和更细的子路由拆分；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二十批 | `src/pages/hr/HrSalaryPage.tsx` | 已继续修正基础配置左右比重：把“薪资项目 / 薪资结构”外层比例进一步调回左侧更宽，避免上一轮后左侧项目区再次被压窄；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `263.32 kB` | 仍需继续对照源码收紧删除确认链路与部分基础配置创建弹层；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十九批 | `src/pages/hr/HrSalaryPage.tsx` | 已修正基础配置左侧再次被压缩的问题：把“薪资项目 / 薪资结构”外层比例调回左侧更宽，并给左侧“薪资项目”表补最小列宽、操作列保宽与按钮不换行，避免右侧修复后左侧表格和操作按钮再次被挤压。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `263.31 kB` | 仍需继续对照源码收紧删除确认链路与部分基础配置创建弹层；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十八批 | `src/pages/hr/HrSalaryPage.tsx` | 已修正基础配置双栏与结构表格可用性：把“薪资项目 / 薪资结构”两列改为不互相拉伸，避免右列变长时左列被整块拉高留白；把结构列表、关联现薪样本、关联项目改为局部滚动，并给样本表补最小列宽与操作列宽，修复“查看现薪”按钮被挤压只剩半截的问题。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物为 `262.91 kB` | 仍需继续对照源码收紧删除确认链路与部分基础配置创建弹层；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十七批 | `src/pages/hr/HrSalaryPage.tsx` | 已继续按 `.references/sub2api` 的自解释后台页语法回炉深层风险文案：把“个税配置 / 专项扣除 / 社保分配 / 分配员工薪资 / 发起调薪”里的风险详情、摘要 hint 与诊断长句继续压成短状态、短差值与短区间表达；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物继续降到 `262.01 kB` | 仍需继续对照源码收紧删除确认链路与部分基础配置创建弹层；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十六批 | `src/pages/hr/HrSalaryPage.tsx` | 已继续按 `.references/sub2api` 的自解释后台页语法回炉深层弹层：把“个税配置 / 专项扣除 / 社保分配 / 分配员工薪资 / 发起调薪”中的解释句、长 hint、引导性空态、参考说明与整块说明卡继续压成短状态、短摘要与标准表格语法；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物继续降到 `265.89 kB` | 仍需继续对照源码收紧删除确认、风险诊断文本与少量创建弹层；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十五批 | `src/pages/hr/HrSalaryPage.tsx` | 已继续按 `.references/sub2api` 的自解释后台页语法回炉薪酬页三条主分支：把“员工薪资”里的历史卡片、测算八卡、五险一金/个税摘要、社保台账校验列继续压成数值、短状态与标签；把“调薪申请”列表和详情里的诊断副文案继续压平；把“基础配置”里的薪资项目、薪资结构、薪资等级、社保方案主视图说明句继续删减。Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物继续降到 `268.76 kB` | 仍需继续对照源码收紧深层弹层与个税/专项扣除表单摘要；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十四批 | `src/pages/hr/HrSalaryPage.tsx` | 已按基础配置页的文案减法标准继续回炉“员工薪资 / 调薪申请”两页签：现薪列表、员工详情、薪资档案历史、调薪履历、申请列表和详情中的说明句、长 hint、空态 fallback 与整块风险面板继续压成短状态语义；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物继续降到 `274.69 kB` | 仍需继续对照源码收紧少量深层表单摘要与个别历史诊断表述；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十三批 | `src/pages/hr/HrSalaryPage.tsx` | 已继续把“薪资等级 / 社保方案”中明显解释页面自身的 hint、重复状态句和指导性长句压成短状态语义；`WorkspaceDiagnosticSummary` 在无风险项时也不再重复渲染第二条同义说明；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物继续降到 `282.34 kB` | 仍需继续对照源码收紧少量深层表单摘要与个别历史诊断表述；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十二批 | `src/pages/hr/HrSalaryPage.tsx` | 已继续把“到手收入测算”和“社保台账”中的整块风险面板收成统一白底摘要容器，并把若干显而易见的状态 fallback 压成短句或 `-`；Light / Dark 下 `npm run build` 通过，`HrSalaryPage` 构建产物继续降到 `283.89 kB`，页面继续向 `.references/sub2api` 后台页的低说明密度收口 | 仍需继续对照源码收紧少量深层表单摘要、统计卡 hint 与个别历史诊断表述；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十一批 | `src/pages/hr/HrSalaryPage.tsx` | 已把 `WorkspaceDiagnosticSummary / WorkspaceInlineRiskList` 两类页内风险容器统一改成白底行式语法；员工薪资页签里的未来生效/待分配/离职过滤和最近调薪提示已压成单行状态胶囊；调薪详情已去掉“下一步建议”和额外动作说明层；员工专项扣除右侧“个税参考模板”已改成标准表格，Light / Dark 下 `npm run build` 通过并继续向 `.references/sub2api` 后台页收口 | 仍需继续对照源码收紧少量深层表单摘要与个别统计块；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第十批 | `src/pages/hr/HrSalaryPage.tsx` | 已删除员工薪资、薪资档案历史、到手收入测算、调薪履历、调薪申请列表、调薪详情、社保台账等区块的说明句；两块独立“联调提示”已整体移除；薪级与专项扣除表格里的“删除前需确认/删除口径已对齐”常驻辅助文案也已去掉，仅保留真正影响操作的异常项，Light / Dark 下 `npm run build` 通过并继续向 `.references/sub2api` 后台页的自解释语法收口 | 仍需继续对照源码收紧员工专项扣除、调薪弹层和少量剩余风险摘要层级；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第九批 | `src/pages/hr/HrSalaryPage.tsx` | 已删除页头总说明、`薪资项目/薪资结构/薪资等级/社保方案` 当前视图里的解释句，并移除“薪资项目”列表行中的 `排序`、重复启用标签、结构样本和删除阻塞行内说明；同时把多类表单诊断里的 `本次保存不会带来变化` 黄条提示去掉，Light / Dark 下 `npm run build` 通过，页面继续向 `.references/sub2api` 后台页的自解释语法收口 | 仍需继续对照源码收紧员工专项扣除、调薪弹层和少量残余动作提示；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第八批 | `src/pages/hr/HrSalaryPage.tsx` | “薪资结构”详情里的关联项目已从双列卡片压成紧凑表格；“薪资项目”“分配员工薪资”两个弹层已把三卡摘要改成单摘要容器；“薪资结构”项目选择改成高密度行式列表；“个税配置”弹层的顶部统计与联调样本预览也继续收成状态条语法，Light / Dark 下 `npm run build` 通过并继续向 `.references/sub2api` 后台页的低卡片密度靠拢 | 仍需继续对照源码收紧员工专项扣除、调薪弹层和其余深层表单节奏；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第七批 | `src/pages/hr/HrSalaryPage.tsx` | “薪资结构”详情顶部四卡、删除校验块和在岗样本说明块已继续压成单状态条 + 单摘要容器；`结构/薪级/社保方案` 三个弹层也已去掉大卡片摘要与大风险卡，统一改成单摘要容器和行内风险列表，Light / Dark 下 `npm run build` 通过并继续向 `.references/sub2api` 后台页的低说明密度靠拢 | 仍需继续对照源码收紧基础配置深层表格细节与剩余帮助文案；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第六批 | `src/pages/hr/HrSalaryPage.tsx` | 已删除“薪资项目”“薪资结构”里不改变决策的整块风险说明卡，并移除“薪资项目”弹窗底部整块保存校验说明，同时把表格里的删除提示压短为单行结果，Light / Dark 下 `npm run build` 通过，页面解释密度继续往 `.references/sub2api` 的后台页语法收紧 | 仍需继续对照源码收紧基础配置深层表格细节、结构详情密度和其余弹层表单节奏；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第五批 | `src/pages/hr/HrSalaryPage.tsx` | “调薪闭环校验”已从四张状态卡 + 双摘要块压成单状态条 + 单摘要容器；“薪资等级”“社保方案”也继续去掉顶部多卡、风险卡、待配卡与重复说明块，统一改成单状态条、单风险摘要、单说明容器和标准表格语法，Light / Dark 下 `npm run build` 通过并继续向 `.references/sub2api` 的后台页密度靠拢 | 仍需继续对照源码收紧基础配置深层表格细节、结构详情密度和弹层表单节奏；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第四批 | `src/pages/hr/HrSalaryPage.tsx` | 调薪申请页签的“联调提示”已压成单说明容器；同时保留第三批中已完成的左侧统计条、右侧详情信息容器和“薪资项目”顶部紧凑状态条，Light / Dark 下 `npm run build` 通过并继续往后台页的单结果容器语法收口 | 仍需继续回炉调薪闭环以及基础配置其余分区的信息密度、局部块比例和表格/详情结构；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第三批 | `src/pages/hr/HrSalaryPage.tsx` | 调薪申请页签左侧统计卡已压成紧凑状态条，右侧详情头部八宫格已压成单结果信息容器；基础配置里的“薪资项目”顶部概览也已改成紧凑状态条，Light / Dark 下 `npm run build` 通过并继续向源码后台页的低卡片密度靠拢 | 仍需继续回炉调薪闭环、联调提示、以及基础配置其余分区的信息密度、局部块比例和表格/详情结构；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第二批 | `src/pages/hr/HrSalaryPage.tsx` | 在第一批壳层回炉基础上，员工薪资页签第一屏的在岗薪资档案、员工薪资详情、薪资档案历史、到手收入测算四块主容器继续统一成轻量 Section 语法，Light / Dark 下 `npm run build` 通过并继续压低页面重量 | 仍需继续回炉调薪申请与基础配置两大分区内部的信息密度、局部块比例和表格/详情结构；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/salary` 第二轮回炉第一批 | `src/pages/hr/HrSalaryPage.tsx` | 薪酬页已移除 `WorkspaceBackdrop / WorkspacePageContent / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，统一替换为页内轻量后台壳层，并把顶部 Hero 与四张概览卡压成 `轻量页头 + 状态/操作条 + Tabs` 语法，Light / Dark 下 `npm run build` 通过并明显脱离旧 Workspace 设计语言 | 仍需继续回炉员工薪资、调薪申请、基础配置三大分区内部的信息密度、局部块比例和表格/详情结构；当前状态维持 `实施中` |
| 2026-04-21 | `/hr/leave/quota` 第二轮回炉 | `src/pages/hr/HrLeaveQuotaPage.tsx` | 假期额度页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 左侧员工列表 + 右侧额度结果区 + `BaseDialog` 结构，最近补齐结果、年度额度总览、额度桶明细和两类调整弹层都已统一压回后台页语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认左右栏比例、结果区表格密度和调整弹层节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/resignation` 第二轮回炉 | `src/pages/hr/HrResignationPage.tsx` | 离职页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 三栏主区（员工列表 / 申请列表 / 申请详情）+ 单交接表 + `BaseDialog` 结构，面谈和确认离职动作也已统一压回后台页语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认三栏比例、交接表密度和详情区节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/transfer` 第二轮回炉 | `src/pages/hr/HrTransferPage.tsx` | 调动页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 三栏主区（员工列表 / 申请列表 / 申请详情）+ `BaseDialog` 结构，详情区已压回后台页语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认三栏比例、申请列表密度和详情区节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/probation` 第二轮回炉 | `src/pages/hr/HrProbationPage.tsx` | 转正页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 三栏主区（员工列表 / 申请列表 / 申请详情）+ `BaseDialog` 结构，创建申请弹层也统一到后台页语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认三栏比例、申请列表密度和创建弹层节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/offer` 第二轮回炉 | `src/pages/hr/HrOfferPage.tsx` | Offer 页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 左侧 Offer 列表 + 右侧详情区 + `BaseDialog` 结构，Offer 内容区和创建弹层都已统一到后台页语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认左侧列表密度、右侧详情比例和创建弹层节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/onboarding` 第二轮回炉 | `src/pages/hr/HrOnboardingPage.tsx` | 入职页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 左侧申请列表 + 右侧详情与任务表 + `BaseDialog` 结构，申请详情、任务办理和创建申请弹层都已统一到后台页语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认左侧列表密度、右侧详情比例和创建弹层节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/recruitment` 第二轮回炉 | `src/pages/hr/HrRecruitmentPage.tsx` | 招聘页已移除 `WorkspaceHeroCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + 单搜索条 + `Tabs` + 单表格容器 + `BaseDialog` 结构，需求、候选人、面试三组主表和新建/拒绝弹层都已统一到后台页语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认顶部状态条比例、三组 Tabs 表格密度和四类弹层节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/employees` 第二轮回炉 | `src/pages/hr/HrEmployeePage.tsx`、`src/pages/hr/HrEmployeeWorkspace.tsx` | 员工主列表已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 单主结果表 + `BaseDialog` 结构；员工子工作区也同步改为轻量头部 + 信息胶囊 + `Tabs` + 单表格容器 + `BaseDialog` + `ConfirmDialog` 语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认顶部信息条比例、Tabs 密度和三类档案弹层节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/headcount` 第二轮回炉 | `src/pages/hr/HrHeadcountPage.tsx` | 编制页已移除 `WorkspaceHeroCard / WorkspaceMetricCard / WorkspaceSectionCard / WorkspaceDialogShell / WorkspaceInlineState / WorkspaceTableStateRow` 等旧工作台壳层，改为轻量页头 + 状态/操作条 + `TablePageLayout` + 单主结果容器 + 右侧紧凑详情栏 + `BaseDialog` 结构，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页语法 | 仍需做最终源码对照走查，确认筛选条比例、右侧详情密度和表格行节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/dashboard` 第二轮回炉 | `src/pages/hr/HrDashboardPage.tsx` | HR 仪表盘已移除 `WorkspaceHeroCard / MetricCard / SectionCard / Backdrop / PageContent` 等旧工作台壳层，改为轻量页头 + 顶部状态/操作条 + 紧凑统计卡 + 快捷入口网格 + 双列表数据区结构，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台仪表盘页语法 | 仍需做最终源码对照走查，确认统计卡比例、快捷入口密度和双列表细节无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/attendance/checkin` 第二轮回炉 | `src/pages/admin/attendance/AttendanceCheckIn.tsx` | 打卡页已移除粉色 Hero、统计卡、右侧多卡摘要与 `Workspace` 壳层，改为轻量页头 + 状态/操作条 + 单主打卡容器 + 紧凑侧栏结构，保留时间、规则、定位和打卡动作四类核心信息，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台页语法 | 仍需做最终源码对照走查，确认动作按钮密度、侧栏信息比例与提示文案层级无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/hr/attendance/rule` 第二轮回炉 | `src/pages/admin/attendance/AttendanceRule.tsx` | 考勤规则页已移除渐变头、右侧摘要卡与 `WorkspaceStatusPanel` 等旧设计语法，改为轻量页头 + 状态/操作条 + 单设置容器结构，保存与编辑动作统一收进顶部操作条，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台设置页 | 仍需做最终源码对照走查，确认设置项密度、开关节奏和帮助文案层级无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/admin/duty-schedule` 第二轮回炉 | `src/pages/DutySchedulePage.tsx` | 排班页已移除 `WorkspaceHero / Workbench / ResultCard / DialogShell / TableStateRow` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选/操作条 + 单结果表结构，新增排班与换班也统一改回常规 `BaseDialog` 语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认表单比例、表格行密度和是否需要补强日历视图无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/admin/visitor` 第二轮回炉 | `src/pages/VisitorPage.tsx` | 访客页已移除 `WorkspaceHero / Workbench / ResultCard / PaginationBar / TableStateRow` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选/操作条 + 单结果表结构，新增预约与取消预约也统一改回常规 `BaseDialog / ConfirmDialog` 语法，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台列表页 | 仍需做最终源码对照走查，确认表单比例、表格行密度与行内操作节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/announcement` `/office/announcement` 第二轮回炉 | `src/pages/AnnouncementPage.tsx`、`src/components/admin/announcements/AnnouncementManageView.tsx`、`src/components/admin/announcements/AnnouncementReadStatusDialog.tsx`、`src/components/common/AnnouncementDetailModal.tsx`、`src/components/common/AnnouncementManageTable.tsx`、`src/components/common/AnnouncementListItem.tsx` | 公告中心用户视图、管理视图、阅读明细、详情弹层与列表行已统一压成参考后台页的 `轻量页头 + 工具条 + 单结果容器 + 标准表格/弹窗` 语法，相关链路中的 `Workspace` 壳层已移除，Light / Dark 下 `npm run build` 通过并明显贴近 `.references/sub2api` 后台页 | 仍需做最终源码对照走查，重点确认顶部铃铛入口、列表行密度和详情弹层比例无偏差；当前状态提升为 `联调中` |
| 2026-04-22 | `/office/contact` 第三轮纠偏第二批 | `src/pages/ContactPage.tsx` | 页头说明已继续减法，筛选条收成单搜索 + 轻操作，左侧部门导航从卡片按钮压回更轻的后台侧列语法；右侧结果表已重组为联系人、组织、联系方式三段双行信息列，行内动作收成 icon-only，详情名片也继续压薄为更克制的字段块。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧侧列宽度、列表密度和详情字段编排；当前状态维持 `联调中` |
| 2026-04-22 | `/admin/asset` 第三轮纠偏第五批 | `src/pages/admin/asset/AssetList.tsx`、`src/pages/admin/asset/AssetForm.tsx` | 二维码弹层已压回轻摘要 + 单二维码主体，日志弹层改成单容器分隔行列表，新增/编辑资产表单彻底收成 `BaseDialog footer + form body`，并把弹层宽度收窄到 `max-w-2xl`。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧日志字段密度和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/admin/visitor` 第三轮纠偏第一批 | `src/pages/VisitorPage.tsx` | 页头英文眉题、说明段和顶部胶囊统计已删除，状态摘要压回单行结果条，筛选区改成单条轻筛选栏，行内动作收成 icon-only 中性操作；新增预约与取消确认弹层同步去掉冗余说明并收窄比例。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表列宽、行密度和少量最终源码细节；当前状态维持 `联调中` |
| 2026-04-22 | `/admin/duty-schedule` 第三轮纠偏第一批 | `src/pages/DutySchedulePage.tsx` | 页头英文眉题、说明段和顶部胶囊统计已删除，状态摘要压回单行结果条，筛选区改成单条轻筛选栏，签到/换班/签退动作收成 icon-only 中性操作；新增排班与换班弹层同步去掉冗余说明并收窄比例。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表列宽、排班明细密度和少量最终源码细节；当前状态维持 `联调中` |
| 2026-04-22 | `/system/log` 第三轮纠偏第一批 | `src/pages/system/OperationLogPage.tsx` | 已把“查看趋势”弹层入口撤回页内，上半区固定展示近 30 天趋势图与趋势汇总，下半区保留筛选后的操作日志列表，形成更贴近源码后台页的上图下表单页结构；筛选、详情与删除链路保持共享语法。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表密度、趋势区留白和详情弹层细节；当前状态维持 `实施中` |
| 2026-04-22 | `/system/audit-log` 第三轮纠偏第一批 | `src/pages/system/AuditLogPage.tsx` | 页头说明、副文案和重摘要胶囊已删除，筛选区、结果头、表格与详情弹层统一收成和 `/system/log` 同一套轻量列表页语法；前后值对比详情保留，但已压回更中性的对照面板。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表密度、前后值列宽和详情编排；当前状态维持 `实施中` |
| 2026-04-22 | `/system/login-log` 第三轮纠偏第一批 | `src/pages/system/LoginLogPage.tsx` | 页头说明、副文案和重摘要胶囊已删除，筛选区、结果头、表格与详情弹层统一收成和 `/system/log` 同一套轻量列表页语法；批量删除、服务端分页和详情查看保留，但已压回更克制的后台列表页节奏。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表密度、详情编排和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/system/online` 第三轮纠偏第一批 | `src/pages/system/OnlineUserPage.tsx` | 页头说明、副文案和重摘要胶囊已删除，顶部状态汇总压成单行结果条，筛选区收成单条轻筛选栏，列表头与行内强退动作统一收成更克制的后台列表页语法；批量强退、当前会话不可操作约束和服务端分页保留。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表密度、会话信息编排和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/system/post` 第三轮纠偏第一批 | `src/pages/system/PostList.tsx` | 页头说明、副文案和重摘要胶囊已删除，顶部状态汇总压成单行结果条，筛选区收成单条轻筛选栏，列表头与编辑/删除弹层统一收成更克制的后台列表页语法；岗位创建、编辑与删除链路保留。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧列表密度、弹层细节和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/system/config` 第三轮纠偏第一批 | `src/pages/system/ConfigList.tsx` | 页头说明、副文案和重摘要胶囊已删除，顶部状态汇总压成单行结果条，筛选区收成单条轻筛选栏，列表头与编辑/删除弹层统一收成更克制的后台列表页语法；内置参数不可删除、缓存清理等链路保留。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧键值表达、列表密度和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-22 | `/system/cache` 第三轮纠偏第一批 | `src/pages/system/CacheMonitor.tsx` | 页面已去掉大 Hero / Workbench / ResultCard / SectionCard 壳层，改为轻量页头 + `TablePageLayout` + 视图切换 + 单容器双栏浏览器语法；概览信息、命令统计、Key 树、详情区与删除确认链路保留，并统一回收到更接近 `.references/sub2api` 的后台复杂页表达。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧双栏比例、浏览器明细密度和少量最终源码细节；当前状态维持 `实施中` |
| 2026-04-21 | `/office/contact` 第三轮纠偏第一批 | `src/pages/ContactPage.tsx` | 联系人页已移除 `WorkspaceBackdrop / HeroMetrics / Workbench / ResultCard / SectionCard / DialogShell` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 左侧部门导航 + 右侧标准结果表结构，联系人详情也改回常规 `BaseDialog` 语法，Light / Dark 下构建通过并明显脱离旧设计语言 | 仍需做最终源码对照走查，确认部门导航比例、结果表密度和名片详情节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-22 | `/office/business-trip` 第三轮纠偏第二批 | `src/pages/BusinessTripPage.tsx` | 顶部摘要已压成单行汇总，结果头改为标题 + 当前筛选 + 共 N 条，列表行动作收成 icon-only 轻动作；删除/提交/取消链路统一改回共享 `ConfirmDialog`，新建弹层移除说明句与分段说明文案，详情弹层的信息卡、附件块和流程轨迹块也继续压薄；列表额外补回了出差人、部门和项目等基础字段。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧筛选条密度、详情字段编排和少量最终源码细节；当前状态维持 `联调中` |
| 2026-04-22 | `/expense/claim` 第三轮纠偏第二批 | `src/pages/ExpenseClaimPage.tsx` | 顶部摘要已压成单行汇总，结果头改为标题 + 当前筛选 + 共 N 条，列表主列补成报销单号、申请人/部门、类别/明细的双行信息列，列表行动作收成 icon-only 轻动作；删除/提交链路统一改回共享 `ConfirmDialog`，新建与详情弹层移除说明句并继续压薄字段卡和明细区。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧筛选条密度、明细行宽比例和详情字段编排；当前状态维持 `联调中` |
| 2026-04-22 | `/payment/request` 第三轮纠偏第二批 | `src/pages/PaymentRequestPage.tsx` | 顶部摘要已压成单行汇总，结果头改为标题 + 当前筛选 + 共 N 条，列表主列补成付款单号、收款方/账户、申请人/部门、类型/日期的双行信息列，列表行动作收成 icon-only 轻动作；删除/提交链路统一改回共享 `ConfirmDialog`，新建与详情弹层移除说明句并继续压薄字段卡和附件区。Light / Dark 下 `npm run build` 通过 | 仍需继续对照 `.references/sub2api` 收紧筛选条密度、字段编排和少量最终源码细节；当前状态维持 `联调中` |
| 2026-04-21 | `/office/business-trip` 第三轮纠偏第一批 | `src/pages/BusinessTripPage.tsx` | 出差申请页已移除 `WorkspaceBackdrop / HeroMetrics / Workbench / ResultCard / TableStateRow / PaginationBar` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选/操作条 + 单容器结果表结构，表单与详情也统一改回常规 `BaseDialog` 语法，Light / Dark 下构建通过并明显脱离旧设计语言 | 仍需做最终源码对照走查，确认详情信息密度、流程轨迹区比例和表单节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/expense/claim` 第三轮纠偏第一批 | `src/pages/ExpenseClaimPage.tsx` | 报销申请页已移除 `WorkspaceBackdrop / HeroMetrics / Workbench / ResultCard / TableStateRow / PaginationBar` 等旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选/操作条 + 单容器结果表结构，报销表单与详情层也改回常规后台弹层语法，报销明细编辑改成紧凑行编辑，Light / Dark 下构建通过并明显脱离旧设计语言 | 仍需做最终源码对照走查，确认表单行高、明细列宽和详情信息密度无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/schedule` 第三轮纠偏第二批 | `src/pages/SchedulePage.tsx` | 日历页继续去掉自定义统计胶囊、重皮肤 FullCalendar 和多段右侧摘要，改成单筛选/操作条 + 中性日历画布 + 标准列表 + 单 `Agenda` 侧栏结构，新建/详情也已改回常规 `BaseDialog` 语法，Light / Dark 下构建通过并继续向源码后台页收敛 | 仍需做最终源码对照走查，确认 FullCalendar 栅格细节、列表密度和 `Agenda` 侧栏比例无偏差；当前状态维持 `联调中` |
| 2026-04-21 | `/payment/request` 第三轮纠偏第一批 | `src/pages/PaymentRequestPage.tsx` | 付款申请页已移除 `WorkspaceBackdrop / HeroMetrics / Workbench / ResultCard / TableStateRow / PaginationBar` 旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果表结构；列表区统一收敛到标准 CRUD 页语法，表单与详情弹层保持同一套轻量语法，Light / Dark 下构建通过并明显脱离旧设计语言 | 仍需做最终源码对照走查，确认详情弹层比例、行内信息密度和表单节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/schedule` 第三轮纠偏第一批 | `src/pages/SchedulePage.tsx` | 日历页已移除 `WorkspaceBackdrop / HeroMetrics / Workbench / ResultCard / SectionCard / SegmentedControl / IconButton / TableStateRow` 旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器内容区结构；左侧统一收敛为日程列表 + 日历画布两段轻量容器，右侧保留紧凑侧栏摘要，新建/详情抽屉同步去 Workspace 化，Light / Dark 下构建通过并明显脱离旧设计语言 | 仍需做最终源码对照走查，确认 FullCalendar 皮肤细节、列表密度与抽屉节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/meeting-room` 第三轮纠偏第一批 | `src/pages/MeetingRoomPage.tsx` | 会议室页已移除 `WorkspaceBackdrop / HeroMetrics / SectionCard / SegmentedControl / StatusPanel / EmptyPanel` 旧工作台壳层，改为轻量页头 + `TablePageLayout` + 单筛选条 + 单容器结果区结构，会议室列表进一步从单列长结果行收敛为单容器内的紧凑三列密排，我的预订与统计区也统一收成后台页语法，周排期与预订/管理弹层同步去 Workspace 化，Light / Dark 下构建通过并明显脱离旧设计语言 | 仍需做最终源码对照走查，确认统计表细节、周排期比例与弹层节奏无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/templates` 第三轮纠偏第二批 | `src/pages/TemplateLibrary.tsx` | 模板库页已移除 `WorkspaceBackdrop / HeroCard / Workbench / ResultCard / SectionCard` 旧工作台壳层，收成轻量页头 + 摘要条 + 单筛选条 + 左侧轻筛选 + 右侧单容器结果区，默认切换为更接近源码后台页的列表视图，预览与创建弹窗也同步压平，Light / Dark 下构建通过并明显脱离旧设计语言 | 仍需做最终源码对照走查，确认卡片视图比例、预览弹窗细节与模板管理链路衔接无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/workflow/performance` 第三轮纠偏第一批 | `src/pages/PerformanceStats.tsx` | 性能页已从分析工作台拼装结构收成轻量页头 + 筛选条 + 单容器结果区 + slim 侧栏，流程分布、日期趋势与结果表都改为后台页语法，Light / Dark 下构建通过，页面已明显靠近源码后台页 | 仍需做最终源码对照走查，确认趋势列宽、表格密度和侧栏比例无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/workflow/alerts` 第三轮纠偏第一批 | `src/pages/AlertList.tsx` | 告警页已从“监控工作台 + 焦点卡 + 建议卡”收成轻量页头 + 筛选条 + 单容器结果列表 + slim 侧栏，异常解决弹窗也继续压轻，Light / Dark 下构建通过，页面已明显靠近源码后台列表页 | 仍需做最终源码对照走查，确认列表列宽、侧栏比例和少量行内细节无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/workflow/deploy` 第三轮纠偏第二批 | `src/pages/DeployManagement.tsx`、`src/components/deploy/DeployWindowManagement.tsx`、`src/components/deploy/DeployApprovalManagement.tsx`、`src/components/deploy/VersionRollbackManagement.tsx`、`src/components/deploy/DeployStatistics.tsx` | 审批、回滚、统计三个治理模块的主视图也已从摘要卡/说明面板收成工具栏 + 单容器 + 列表行语法，窗口编辑、审批详情与回滚弹窗继续压轻，Light / Dark 下构建通过，整页已接近源码后台页的紧凑语法 | 仍需做最终源码对照走查，确认列宽、间距和个别弹窗细节无偏差；当前状态提升为 `联调中` |
| 2026-04-21 | `/workflow/deploy` 第三轮纠偏第一批 | `src/pages/DeployManagement.tsx`、`src/components/deploy/DeployWindowManagement.tsx` | 页面壳层已收成轻量页头 + 紧凑模块切换条，默认窗口模块已改为工具栏 + 列表行语法，Light / Dark 下构建通过，打开部署页第一屏已不再是旧的 Deploy Workspace 设计语言 | 仍需继续回炉审批、回滚、统计三个子模块的顶部结构和信息密度，页面状态维持 `实施中` |
| 2026-04-21 | `/workflow` 第三轮回炉第一批 | `src/pages/WorkflowCreate.tsx` | 旧 `WorkspaceBackdrop / WorkspaceHeroCard / WorkspaceSectionCard` 已移除，页面改为轻量页头 + 单容器分栏入口结构，Light / Dark 下构建通过，入口页终于脱离原有 Workspace 设计语言 | 仍需继续对照 `.references/sub2api` 微调按钮细节、右栏密度与入口比例，当前状态维持 `联调中` |
| 2026-04-21 | `/workflow/monitor` 第三轮纠偏第一批 | `src/pages/WorkflowMonitor.tsx` | 自定义监控工作台、说明块和多层大卡片已收成 `工具栏 + 单容器双栏 + 列表行` 的后台页语法，Light / Dark 下构建通过，页面主体已明显向源码靠拢 | 仍需继续对照 `.references/sub2api` 微调趋势列宽、告警信息密度和监控批次页面间比例，当前状态维持 `联调中` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏第八批 | `src/components/WorkflowBuilder.tsx`、`src/pages/WorkflowDesign.tsx` | 页头已去除状态胶囊并收敛为轻量标题区，节点卡已从多枚标签、警告提示与条件块收口为标题 + 元信息 + 摘要文本表达，Light / Dark 下构建通过，页面重量进一步下降并进入联调中 | 仍需对照 `.references/sub2api` 做最终视觉走查；若无新的比例或密度偏差，再转 `已完成` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏第七批 | `src/components/WorkflowBuilder.tsx`、`src/pages/WorkflowDesign.tsx` | 审批人/用户/部门选择器的标签、滚动列表、选中态和已选项样式已进一步压轻，全局配置区字段标签与分组密度也继续收紧，Light / Dark 下页面继续减重并构建通过，但仍未达到最终 99% 源码对齐验收 | 仍需继续回炉字段编排节奏、节点信息表达和更细的状态/交互反馈，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏第六批 | `src/components/WorkflowBuilder.tsx`、`src/pages/WorkflowDesign.tsx` | 审批人/用户/部门选择器的搜索框、滚动列表、选中态和已选项胶囊已继续压轻，Light / Dark 下右侧配置面板的 section 节奏也进一步统一并构建通过，但仍未达到最终 99% 源码对齐验收 | 仍需继续回炉字段编排节奏、节点信息表达和更细的状态/交互反馈，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏第五批 | `src/components/WorkflowBuilder.tsx`、`src/pages/WorkflowDesign.tsx` | 右侧属性面板与全局配置面板中的大批说明性小字、辅助提示段落和部分区块间距已继续压掉，Light / Dark 下配置面板更克制并构建通过，但仍未达到最终 99% 源码对齐验收 | 仍需继续回炉字段编排节奏、节点信息表达和更细的状态/交互反馈，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏第四批 | `src/components/WorkflowBuilder.tsx`、`src/pages/WorkflowDesign.tsx` | 右侧属性面板与全局配置面板中的说明性小字、辅助提示段落和部分松散间距已继续压掉，Light / Dark 下配置面板更克制并构建通过，但仍未达到最终 99% 源码对齐验收 | 仍需继续回炉字段编排节奏、节点信息表达和更细的状态/交互反馈，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏第三批 | `src/components/WorkflowBuilder.tsx`、`src/pages/WorkflowDesign.tsx` | 右侧属性面板与全局配置面板的标题层级、提示块、文本域、JSON 输入区和浮层阴影已继续压轻，Light / Dark 下设计器深层表单重量继续下降并构建通过，但仍未达到最终 99% 源码对齐验收 | 仍需继续回炉属性面板字段编排、节点信息表达和更细的状态/交互反馈，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏第二批 | `src/pages/WorkflowDesign.tsx`、`src/components/WorkflowBuilder.tsx` | 页头、工具栏、节点卡、快捷新增菜单、拖拽落点、画布浮层与属性侧栏已继续压轻，Light / Dark 下页面重量明显下降并构建通过，但仍未达到最终 99% 源码对齐验收 | 仍需继续回炉属性侧栏深层表单密度、节点信息表达和更细的设计器交互反馈，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第三轮纠偏瘦身 | `src/pages/WorkflowDesign.tsx` | 页面外层摘要卡、说明卡和右侧上下文卡已删除，Light / Dark 下仅保留轻量标题区与设计器本体并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉侧栏内部表单密度、节点细节和更深层设计器交互，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第二轮回炉第二批 | `src/pages/WorkflowDesign.tsx`、`src/components/WorkflowBuilder.tsx` | 属性侧栏与节点卡比例已进一步统一到同一套 Light / Dark 轻量设计工作台语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉侧栏内部表单密度、节点细节和更深层设计器交互，页面状态继续保持 `实施中` |
| 2026-04-21 | `/forms` 第二轮回炉第二批 | `src/pages/FormDesign.tsx`、`src/components/FormBuilder.tsx` | 字段卡与预览卡已进一步统一到同一套 Light / Dark 轻量设计工作台语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉字段编辑密度、预览细节和更深层表单编辑交互，页面状态继续保持 `实施中` |
| 2026-04-21 | `/forms` 第二轮回炉第一批 | `src/pages/FormDesign.tsx`、`src/components/FormBuilder.tsx` | 页面壳层、组件库列、画布头部与设计/预览容器已统一到同一套 Light / Dark 轻量设计工作台语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉字段卡比例、预览细节和更深层表单编辑交互，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/design` 第二轮回炉第一批 | `src/pages/WorkflowDesign.tsx`、`src/components/WorkflowBuilder.tsx`、`src/components/WorkflowSettingsModal.tsx` | 页面壳层、设计器工具栏、画布容器与设置弹窗已统一到同一套 Light / Dark 轻量复杂页语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉属性侧栏、节点卡比例和更深层设计器交互，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/performance` 第二轮回炉 | `src/pages/PerformanceStats.tsx` | 性能工作台、效率概览、趋势区、风险焦点与结果表已统一到同一套 Light / Dark 轻量分析语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉图表适配批次整体比例、趋势细节和监控统计页面之间的联动节奏，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/monitor` 第二轮回炉 | `src/pages/WorkflowMonitor.tsx` | 监控工作台、趋势面板、治理建议与双列告警区已统一到同一套 Light / Dark 轻量复杂页语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉监控批次整体比例、趋势表达细节和页面间联动节奏，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/alerts` 第二轮回炉 | `src/pages/AlertList.tsx`、`src/components/common/BaseDialog.tsx` | 告警工作台、结果列表、侧栏焦点与解决弹窗已统一到同一套 Light / Dark 轻量监控语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉监控批次页面之间的比例、联动节奏与更细的告警信息层级，页面状态继续保持 `实施中` |
| 2026-04-21 | `/workflow/deploy` 第二轮回炉补齐统计模块 | `src/pages/DeployManagement.tsx`、`src/components/deploy/DeployStatistics.tsx` | 页面壳层与四个治理子模块已完成同一套 Light / Dark 轻量治理语法并构建通过，但未达到最终 99% 源码对齐验收 | 仍需继续回炉整体比例、信息层级和交互节奏，页面状态继续保持 `实施中` |
| 2026-04-20 | 台账标准纠偏并回退历史完成项 | `docs/前端设计语言重构台账.md`、此前所有被标记为 `已完成` 的共享层与页面 | 已明确“构建通过 / 第一轮收口 / 可用”不再视为完成；当前无有效已完成项 | 后续只有达到 99% 源码对齐的页面，才允许重新写入完成记录 |

## Phase 2 占位
| 页面组 | 路由 | 实现文件 | 页面模板类型 | 当前状态 | 目标模板 | 依赖共享组件 | Dark Mode 状态 | 进度状态 | 完成日期 | 验收结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Phase 2 移动端 | `/dashboard` | `src/mobile/pages/MobileDashboard.tsx` | 门户仪表盘页 | 暂未纳入首期 | 参考移动端导航与卡片页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/messages` | `src/mobile/pages/MobileMessages.tsx` | 标准列表页 | 暂未纳入首期 | 参考移动端消息页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/meeting-room` | `src/mobile/pages/MobileMeetingRoom.tsx` | 复杂工作台页 | 暂未纳入首期 | 参考移动端预约页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/profile` | `src/mobile/pages/MobileProfile.tsx` | 标准信息页 | 暂未纳入首期 | 参考移动端个人中心 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/reimbursement/request` | `src/mobile/pages/MobileReimbursement.tsx` | 表单页 | 暂未纳入首期 | 参考移动端表单页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/schedule` | `src/mobile/pages/MobileSchedule.tsx` | 日历调度页 | 暂未纳入首期 | 参考移动端日历页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/tasks` | `src/mobile/pages/MobileTasks.tsx` | 标准列表页 | 暂未纳入首期 | 参考移动端任务页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/workflow/monitor` | `src/mobile/pages/MobileWorkflowMonitor.tsx` | 复杂工作台页 | 暂未纳入首期 | 参考移动端监控页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/vehicle/booking` | `src/mobile/pages/vehicle/MobileVehicleBooking.tsx` | 表单页 | 暂未纳入首期 | 参考移动端预约页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
| Phase 2 移动端 | `/hr/leave/application` | `src/mobile/pages/MobileLeaveApplication.tsx` | 表单页 | 暂未纳入首期 | 参考移动端表单页 | 移动端 Layout、ThemeProvider | 未实施 | 未开始 | - | 不在首期 | Phase 2 占位 |
