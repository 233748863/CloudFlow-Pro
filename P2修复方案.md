# CloudFlow Pro P2 风险修复方案

- 编写日期：2026-05-13
- 适用基线：`dev` 分支（P0/P1 修复完成后启动）
- 关联文档：[后端审计报告.md](./后端审计报告.md) §13.3、[P0修复方案.md](./P0修复方案.md)、[P1修复方案.md](./P1修复方案.md)
- 修复范围：15 项 P2 风险（去重后）

> **说明**：P2 是 **中期优化** 项，主要聚焦在代码质量、可维护性、安全加固深度。完成 P0/P1 后启动，可作为日常重构与迭代的一部分逐步推进。

---

## 一、修复优先级与建议顺序

按 **重构影响范围 × 收益** 排序，分 3 类推进：

| 类别 | 项目 | 工期 |
|---|---|---|
| **代码质量重构** | P2-1 工作流回调三件套抽公共模块 | 5 天 |
| | P2-2 软删除标志统一 | 3 天 |
| | P2-3 表前缀统一 | 5 天 |
| | P2-11 审批回调 Handler 注册表化 | 2 天 |
| **安全加固深度** | P2-4 限流 IP 来源校验 | 1 天 |
| | P2-5 @RepeatSubmit args 摘要稳定化 | 0.5 天 |
| | P2-6 文件上传兜底校验 | 2 天 |
| | P2-7 密码静默升级审计 | 0.5 天 |
| | P2-8 注册流程 emailDomain 校验 | 1 天 |
| | P2-9 startProcess 权限校验 | 1 天 |
| | P2-13 网关关键路由限流启用 | 1 天 |
| | P2-14 Actuator 访问控制 | 1 天 |
| **业务/部署优化** | P2-10 服务名统一 | 0.5 天 |
| | P2-12 HR 社保比例迁出 yaml | 2 天 |
| | P2-15 Dockerfile HEALTHCHECK + Layered JAR | 1 天 |

总工期约 **25 个工作日**（约 5 周，可分散到 2-3 个迭代）。

---

## 二、P2-1 工作流回调三件套抽公共模块

### 2.1 问题描述

OA / CRM / HR 三个业务服务各自实现了相同结构的工作流回调链路：

```
WorkflowApprovalCallbackStreamConfig
WorkflowCallbackStreamConstants
WorkflowApprovalCallbackStreamConsumer
WorkflowCallbackService / Impl
ApprovalResultHandler
{Business}ApprovalHandler
```

**三套代码 80% 雷同**，每次工作流回调协议升级需改 3 处。

### 2.2 修复思路

新建 `cloudflow-common-workflow-callback` 模块，沉淀通用框架，业务服务仅实现 `ApprovalResultHandler` SPI。

### 2.3 修复步骤

1. **新模块目录**：
   ```
   cloudflow-common/cloudflow-common-workflow-callback/
   ├── pom.xml
   └── src/main/java/com/cloudflow/common/workflow/callback/
       ├── annotation/ApprovalCallbackHandler.java   (业务 Handler 标记)
       ├── core/WorkflowCallbackStreamConfig.java
       ├── core/WorkflowCallbackStreamConsumer.java
       ├── core/WorkflowCallbackDispatcher.java       (按 businessType 路由)
       ├── core/WorkflowCallbackConstants.java
       ├── spi/ApprovalResultHandler.java             (业务实现接口)
       └── config/WorkflowCallbackAutoConfiguration.java
   ```

2. **Handler 注册机制**：
   ```java
   @ApprovalCallbackHandler(businessType = "BUSINESS_TRIP")
   @Component
   public class BusinessTripApprovalHandler implements ApprovalResultHandler {
       @Override
       public void handle(WorkflowCallbackEvent event) {
           // OA 业务侧仅关心如何更新 biz_business_trip 表
       }
   }
   ```

3. **业务服务接入**：
   - `cloudflow-service-oa` / `cloudflow-service-crm` / `cloudflow-service-hr` 引入新模块依赖
   - 删除三个服务中重复的 Stream 三件套
   - 仅保留业务 Handler 实现

4. **回归测试**：审批回调全链路（起流程 → 审批 → 业务回写）

### 2.4 验证方式

- 三个服务的 `WorkflowCallbackServiceImpl` 与 `WorkflowApprovalCallbackStreamConsumer` 全部删除
- 工作流审批完成后，OA/CRM/HR 业务表正确更新
- 新增业务类型仅需新加一个 `@ApprovalCallbackHandler` 标注的类

### 2.5 风险

- 改造期间需保留兼容代码 1 个迭代
- Redis Stream Consumer Group 名称变更可能丢消息，需停服切换

---

## 三、P2-2 软删除标志统一

### 3.1 问题描述

两套软删除规范并存：

- `del_flag CHAR(1) DEFAULT '0'`（取值 `'0'`/`'2'`）— common / oa 老表
- `deleted TINYINT(1) NOT NULL DEFAULT 0`（取值 0/1）— hr / 新表

MyBatis-Plus 实体上 `@TableLogic` 需要分别配 `value` 和 `delval`，给维护带来负担。

### 3.2 修复思路

统一到 `deleted TINYINT(1) DEFAULT 0`，删除 `del_flag` 列（保留兼容视图）。

### 3.3 修复步骤

1. **统计影响表**：
   ```bash
   grep -rln "del_flag" DB/*.sql
   ```
2. **新增 `deleted` 列 + 数据同步**：
   ```sql
   ALTER TABLE sys_user ADD COLUMN deleted TINYINT(1) NOT NULL DEFAULT 0 AFTER del_flag;
   UPDATE sys_user SET deleted = CASE del_flag WHEN '2' THEN 1 ELSE 0 END;
   ```
3. **应用层 Entity 修改**：
   ```java
   @TableLogic   // 默认 value=0, delval=1
   private Integer deleted;
   ```
4. **逐表灰度切换**，旧应用通过视图兼容：
   ```sql
   CREATE OR REPLACE VIEW v_sys_user AS
   SELECT *, CASE deleted WHEN 1 THEN '2' ELSE '0' END AS del_flag FROM sys_user;
   ```
5. **2 个迭代后删除 `del_flag` 列**

### 3.4 验证方式

- 软删除接口调用，DB 中 `deleted=1`、`del_flag` 同步
- 老应用（未升级）查询仍能正常工作（通过视图兼容）

---

## 四、P2-3 表前缀统一

### 4.1 问题描述

| 当前前缀 | 业务域 | 问题 |
|---|---|---|
| `oa_crm_*` | CRM | 历史遗留（孵化自 OA） |
| `wf_*` + `workflow_*` | 工作流 | 混用 |
| `meeting_room` | OA 会议室 | 单数表名，无前缀 |
| `vehicle_*` | OA 车辆 | 无 `oa_` 前缀 |

### 4.2 修复思路

**新建表用规范前缀，老表保留 + 加视图别名**：

| 业务域 | 规范前缀 |
|---|---|
| 系统底座 | `sys_` |
| 工作流 | `wf_` |
| OA | `oa_` |
| CRM | `crm_` |
| HR | `hr_` |
| 业务流转 | `biz_` |

### 4.3 修复步骤（以 CRM 为例）

1. **重命名表**（停服窗口操作）：
   ```sql
   RENAME TABLE oa_crm_customer TO crm_customer;
   RENAME TABLE oa_crm_contact TO crm_contact;
   ...
   ```
2. **创建兼容视图**（过渡期 2 个迭代）：
   ```sql
   CREATE VIEW oa_crm_customer AS SELECT * FROM crm_customer;
   ```
3. **应用层 `@TableName` 改新名**：
   ```java
   @TableName("crm_customer")
   public class CrmCustomer { ... }
   ```
4. **工作流表统一**：`workflow_template` → `wf_template` 等
5. **OA 杂项表加前缀**：
   - `meeting_room` → `oa_meeting_room`
   - `vehicle_lease` → `oa_vehicle_lease`

### 4.4 验证方式

- `SHOW TABLES LIKE 'oa_crm_%'` 仅返回视图
- 应用层查询新表名，业务功能正常
- 兼容期内老查询走视图也能工作

### 4.5 风险

- 数据库迁移涉及业务停机
- 跨服务的 SQL 直接拼表名（如 HR 引用部门表）需同步改

---

## 五、P2-4 限流 IP 来源校验

### 5.1 问题描述

`RateLimiterAspect.getIpAddr` 从 `X-Forwarded-For` 直接取首位 IP，未校验来源是否真为网关。

业务服务端口 9001-9005 在容器网络可达 → 攻击者直接调业务服务 + 伪造 `X-Forwarded-For` → 限流绕过。

### 5.2 修复步骤

1. **配置可信代理 IP**：
   ```yaml
   cloudflow:
     ratelimit:
       trusted-proxies:
         - 172.20.0.0/16   # docker network
         - 10.0.0.0/8      # 内网
   ```
2. **IP 解析增加校验**：
   ```java
   public String resolveClientIp(HttpServletRequest req) {
       String remote = req.getRemoteAddr();
       if (!isTrustedProxy(remote)) {
           return remote;   // 来源不是网关，忽略 X-Forwarded-For
       }
       String xff = req.getHeader("X-Forwarded-For");
       return StrUtil.isNotBlank(xff) ? xff.split(",")[0].trim() : remote;
   }
   ```
3. **业务服务网络策略**（K8s NetworkPolicy / docker-compose `internal: true`）：限制 9001-9005 仅接受网关来源流量

### 5.3 验证方式

- 从网关访问 → 限流按真实客户端 IP 计算
- 直接 curl 业务服务 9005 端口 → 拒绝连接（NetworkPolicy）或限流按容器 IP 算（无法伪造）

---

## 六、P2-5 @RepeatSubmit args 摘要稳定化

### 6.1 问题描述

`@RepeatSubmit` 防重 key 用 `arg.hashCode()`，对未重写 `hashCode()` 的 POJO 走 identity hash：
- 每次反序列化新对象 → hashCode 不同 → key 不同 → 防重不生效

### 6.2 修复步骤

修改 `RepeatSubmitAspect`：

```java
// 改前
String argsHash = String.valueOf(Arrays.hashCode(args));

// 改后
String argsHash = SecureUtil.md5(JSON.toJSONString(args));
// 或
String argsHash = SecureUtil.md5(
    Arrays.stream(args)
        .map(a -> a == null ? "" : JSON.toJSONString(a))
        .collect(Collectors.joining("|"))
);
```

### 6.3 验证方式

- 单元测试：同 POJO 内容传入两次，hash 相同
- 集成测试：连续两次相同 POST 请求，第二次返回 "REPEAT_SUBMIT"

---

## 七、P2-6 文件上传兜底校验

### 7.1 问题描述

`FileUploadUtils.upload` 未做：
- MIME 类型白名单校验
- 文件扩展名白名单
- 文件大小限制（除 multipart 默认）
- 路径穿越校验（`../../etc/passwd`）

### 7.2 修复步骤

1. **新增 `FileUploadProperties`**：
   ```yaml
   cloudflow:
     upload:
       max-size: 50MB
       allowed-extensions: [jpg, png, pdf, doc, docx, xls, xlsx, zip]
       allowed-mimetypes: [image/jpeg, image/png, application/pdf, ...]
       sanitize-filename: true
   ```
2. **改造 `FileUploadUtils.upload`**：
   ```java
   public static String upload(String baseDir, MultipartFile file, FileUploadProperties props) {
       // 1. 大小校验
       if (file.getSize() > props.getMaxSize().toBytes()) {
           throw new ServiceException("FILE_TOO_LARGE");
       }
       // 2. 扩展名白名单
       String ext = FilenameUtils.getExtension(file.getOriginalFilename()).toLowerCase();
       if (!props.getAllowedExtensions().contains(ext)) {
           throw new ServiceException("FILE_TYPE_NOT_ALLOWED");
       }
       // 3. MIME 校验（用 Apache Tika 探测真实类型，而非信任 Content-Type Header）
       String detected = TikaUtil.detect(file.getInputStream());
       if (!props.getAllowedMimetypes().contains(detected)) {
           throw new ServiceException("FILE_MIME_MISMATCH");
       }
       // 4. 文件名清理（防路径穿越）
       String safeName = sanitizeFilename(file.getOriginalFilename());
       String storedName = IdUtils.simpleUUID() + "." + ext;
       // 5. 写入路径基于 baseDir resolve，不允许绝对路径
       Path target = Paths.get(baseDir).toAbsolutePath().resolve(storedName).normalize();
       if (!target.startsWith(Paths.get(baseDir).toAbsolutePath())) {
           throw new ServiceException("PATH_TRAVERSAL_DETECTED");
       }
       Files.copy(file.getInputStream(), target);
       return storedName;
   }
   ```
3. **OSS 路径同样隔离**：路径前缀 `tenant/{tenantId}/{module}/...`

### 7.3 验证方式

- 上传 `.exe` 文件 → 拒绝
- 上传 50MB+ 文件 → 拒绝
- 文件名含 `../../` → 清理后正常存储
- 上传伪装成 `.jpg` 的可执行文件 → Tika 探测出真实 MIME，拒绝

---

## 八、P2-7 密码静默升级审计

### 8.1 问题描述

`PasswordService` 校验"明文→BCrypt"成功后会**静默升级为"SHA-256(明文)→BCrypt"**，无审计日志，合规上"不可解释"。

### 8.2 修复步骤

```java
public boolean verifyAndUpgrade(SysUser user, String inputPassword) {
    if (bcryptMatches(user.getPassword(), inputPassword)) {
        // 老格式
        String newHash = bcryptEncode(sha256(inputPassword));
        userMapper.updatePassword(user.getId(), newHash);

        // 新增：审计日志
        auditService.record(
            AuditEvent.builder()
                .userId(user.getId())
                .action("PASSWORD_HASH_UPGRADE")
                .detail("从 BCrypt(plain) 升级到 BCrypt(SHA256(plain))")
                .build()
        );
        return true;
    }
    return bcryptMatches(user.getPassword(), sha256(inputPassword));
}
```

### 8.3 验证方式

- 老密码用户登录后，`sys_audit_log` 表新增一条 `PASSWORD_HASH_UPGRADE` 记录

---

## 九、P2-8 注册流程 emailDomain 校验

### 9.1 问题描述

`POST /auth/register` 允许指定 `tenantCode` 注册到任意租户，**未校验 emailDomain 与租户绑定**。

后果：A 公司员工用个人邮箱注册到 B 公司租户。

### 9.2 修复步骤

1. **`sys_tenant` 增加 `allowed_email_domains` 字段**：
   ```sql
   ALTER TABLE sys_tenant ADD COLUMN allowed_email_domains VARCHAR(500)
       COMMENT '允许的邮箱域，逗号分隔，空表示不限制';
   ```
2. **注册校验**：
   ```java
   public void register(RegisterBody body) {
       SysTenant tenant = tenantService.getByCode(body.getTenantCode());
       String domain = body.getEmail().substring(body.getEmail().indexOf('@') + 1);
       if (StrUtil.isNotBlank(tenant.getAllowedEmailDomains())) {
           List<String> allowed = StrUtil.split(tenant.getAllowedEmailDomains(), ",");
           if (!allowed.contains(domain)) {
               throw new ServiceException("EMAIL_DOMAIN_NOT_ALLOWED");
           }
       }
       // ... 后续注册逻辑
   }
   ```
3. **管理后台增加配置入口**：租户管理 → 邮箱域白名单

### 9.3 验证方式

- 租户配置 `@example.com`，用 `user@gmail.com` 注册 → 拒绝
- 未配置（空）→ 不限制（向后兼容）

---

## 十、P2-9 startProcess 权限校验

### 10.1 问题描述

`WorkflowController.startProcess` 仅 `@SaCheckLogin`，**所有登录用户能用任意 processDefKey 发起流程**。

`wf_process_definition` 表已有 `start_permission_type / start_permission_value` 字段但未消费。

### 10.2 修复步骤

1. **Service 层校验**：
   ```java
   public Long startProcess(StartRequest req) {
       WfProcessDefinition def = processDefMapper.selectByKey(req.getProcessKey());
       UserContext user = UserContext.current();

       switch (def.getStartPermissionType()) {
           case "ROLE":
               List<String> requiredRoles = JSON.parseArray(
                   def.getStartPermissionValue(), String.class);
               if (CollectionUtil.isEmpty(CollectionUtil.intersection(
                       user.getRoles(), requiredRoles))) {
                   throw new ServiceException("NO_PERMISSION_TO_START");
               }
               break;
           case "DEPT":
               List<Long> requiredDepts = JSON.parseArray(
                   def.getStartPermissionValue(), Long.class);
               if (!requiredDepts.contains(user.getDeptId())) {
                   throw new ServiceException("NO_PERMISSION_TO_START");
               }
               break;
           case "USER":
               List<Long> requiredUsers = JSON.parseArray(
                   def.getStartPermissionValue(), Long.class);
               if (!requiredUsers.contains(user.getUserId())) {
                   throw new ServiceException("NO_PERMISSION_TO_START");
               }
               break;
           case "ALL":
           default:
               // 全员可发起
       }
       // ... 后续逻辑
   }
   ```
2. **流程模板编辑器** 增加权限配置 UI

### 10.3 验证方式

- 配置 `salary_adjustment_approval` 仅 hr_admin 可发起
- 普通员工调发起 API → 返回 403

---

## 十一、P2-10 服务名统一

### 11.1 问题描述

| 服务 | 当前 service-id |
|---|---|
| auth | `cloudflow-auth` |
| workflow | `cloudflow-service-workflow` |
| **oa** | `cloudflow-oa` ⚠️（缺 `service-` 前缀） |
| crm | `cloudflow-service-crm` |
| hr | `cloudflow-service-hr` |

### 11.2 修复步骤

1. **修改 OA 的 `spring.application.name`**：
   ```yaml
   # cloudflow-service-oa/src/main/resources/application.yml
   spring:
     application:
       name: cloudflow-service-oa   # 改前: cloudflow-oa
   ```
2. **网关路由更新**：
   ```yaml
   - id: cloudflow-service-oa
     uri: lb://cloudflow-service-oa   # 改前: lb://cloudflow-oa
   ```
3. **CRM `@Inner` 注解兼容标记保留 + 新增**：
   ```java
   @Inner(allowedServices = {"cloudflow-service-oa", "cloudflow-oa"})
   // 兼容期保留两者，后续删除 cloudflow-oa
   ```
4. **Nacos 服务列表确认**：旧服务名注销，新服务名注册

### 11.3 验证方式

- Nacos 控制台仅看到 `cloudflow-service-oa`
- 网关路由 `/oa/**` 正常转发
- CRM → OA 内部调用正常

---

## 十二、P2-11 审批回调 Handler 注册表化

### 12.1 问题描述

OA `WorkflowCallbackServiceImpl` 用 `if-else` 或 `switch` 按 `businessType` 分发到 `BusinessTripApprovalHandler` / `KnowledgeApprovalHandler` 等。新增业务必须改 dispatcher 类。

### 12.2 修复步骤（与 P2-1 联动）

1. **Handler 接口**：
   ```java
   public interface ApprovalResultHandler {
       String supportedBusinessType();
       void handle(WorkflowCallbackEvent event);
   }
   ```
2. **Spring 自动注入所有 Handler**：
   ```java
   @Component
   public class WorkflowCallbackDispatcher {
       private final Map<String, ApprovalResultHandler> handlers;

       public WorkflowCallbackDispatcher(List<ApprovalResultHandler> all) {
           this.handlers = all.stream().collect(
               Collectors.toMap(ApprovalResultHandler::supportedBusinessType, h -> h));
       }

       public void dispatch(WorkflowCallbackEvent event) {
           ApprovalResultHandler h = handlers.get(event.getBusinessType());
           if (h == null) {
               throw new ServiceException("NO_HANDLER_FOR: " + event.getBusinessType());
           }
           h.handle(event);
       }
   }
   ```
3. **新增业务仅需新建 Handler 类 + 实现接口**

### 12.3 验证方式

- 启动时 dispatcher 打印所有注册的 businessType
- 添加新 Handler 后无需改 dispatcher

---

## 十三、P2-12 HR 社保比例迁出 yaml

### 13.1 问题描述

`cloudflow-service-hr.yaml` 中硬编码社保比例：

```yaml
cloudflow.hr.payroll:
  social-insurance:
    pension-enterprise: 0.16
    pension-personal: 0.08
    medical-enterprise: 0.08
    ...
  housing-fund: 0.12
  tax-threshold: 5000
```

问题：
- 社保比例**按城市不同**（北上广深 vs 二三线）
- 比例**每年可能调整**
- 改 yaml 需运维操作权限，HR 自助调整不便

### 13.2 修复思路

迁移到 `hr_payroll_rate` 表，按地区 + 生效日期管理：

```sql
CREATE TABLE hr_payroll_rate (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    tenant_id BIGINT NOT NULL,
    city_code VARCHAR(20) NOT NULL,
    effective_date DATE NOT NULL,
    expire_date DATE,

    pension_enterprise DECIMAL(5,4),
    pension_personal DECIMAL(5,4),
    medical_enterprise DECIMAL(5,4),
    medical_personal DECIMAL(5,4),
    unemployment_enterprise DECIMAL(5,4),
    unemployment_personal DECIMAL(5,4),
    workinjury_enterprise DECIMAL(5,4),
    maternity_enterprise DECIMAL(5,4),
    housing_fund DECIMAL(5,4),
    tax_threshold DECIMAL(10,2),

    create_by VARCHAR(64), create_time DATETIME,
    update_by VARCHAR(64), update_time DATETIME,

    UNIQUE KEY uk_tenant_city_effective (tenant_id, city_code, effective_date)
);
```

### 13.3 修复步骤

1. 建表 + 初始化默认数据（用当前 yaml 值作为默认 `city_code=DEFAULT`）
2. 新增 `HrPayrollRateService.getEffectiveRate(tenantId, cityCode, date)`，按生效日期匹配
3. 改造 `PayrollCalculator`，从 Service 获取比例而非 `@Value`
4. 管理后台增加配置入口
5. yaml 中保留 `default-city-code: DEFAULT` 作为兜底

### 13.4 验证方式

- 5 月发的工资用 5 月生效的比例（even if `effective_date='2026-04-01'`）
- 北京员工与上海员工使用不同比例

---

## 十四、P2-13 网关关键路由限流启用

### 14.1 问题描述

`RateLimitConfig` 声明了 `ipKeyResolver` / `userKeyResolver` Bean，但 **`application.yml` 中未配置任何 RouteDefinition 的 `RequestRateLimiter` 过滤器**——限流形同摆设。

### 14.2 修复步骤

1. **登录接口防爆破**：
   ```yaml
   spring:
     cloud:
       gateway:
         routes:
           - id: auth-login
             uri: lb://cloudflow-auth
             predicates:
               - Path=/auth/login
             filters:
               - StripPrefix=1
               - name: RequestRateLimiter
                 args:
                   redis-rate-limiter.replenishRate: 5    # 每秒 5 个 token
                   redis-rate-limiter.burstCapacity: 10   # 桶容量 10
                   key-resolver: "#{@ipKeyResolver}"
   ```
2. **注册/验证码接口同样限流**：
   ```yaml
   - id: auth-register
     ...
     filters:
       - name: RequestRateLimiter
         args:
           redis-rate-limiter.replenishRate: 2
           redis-rate-limiter.burstCapacity: 5
   - id: auth-captcha
     ...
     filters:
       - name: RequestRateLimiter
         args:
           redis-rate-limiter.replenishRate: 10
           redis-rate-limiter.burstCapacity: 20
   ```
3. **业务接口按用户限流**：
   ```yaml
   - id: workflow-start
     ...
     filters:
       - name: RequestRateLimiter
         args:
           redis-rate-limiter.replenishRate: 20
           redis-rate-limiter.burstCapacity: 50
           key-resolver: "#{@userKeyResolver}"
   ```

### 14.3 验证方式

- 1 秒内 ab 压测 `/auth/login` × 100，期望 5+ 个 429
- Redis 中观察到 `request_rate_limiter.{IP}.tokens` key

---

## 十五、P2-14 Actuator 访问控制

### 15.1 问题描述

`management.endpoints.web.exposure.include = health,info,metrics,prometheus`，9000 端口对外暴露：
- `http://gateway:9000/actuator/prometheus` 公网可达 → 指标完全公开
- 任意访问者拉取业务流量、JVM 状态

### 15.2 修复思路

**两层方案**（择一）：

**方案 A：独立 management 端口（推荐）**
```yaml
management:
  server:
    port: 9100   # 与业务端口隔离
    address: 0.0.0.0
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
```
Prometheus 抓取 9100，9100 仅内网可达。

**方案 B：网关层拦截 `/actuator/**`**
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: block-actuator
          uri: no://op
          predicates:
            - Path=/actuator/**
          filters:
            - SetStatus=403
```

### 15.3 修复步骤

1. 6 个业务服务的 yaml 增加 `management.server.port`
2. `docker-compose.yml` 不对外暴露 9100
3. Prometheus scrape config 指向 `service:9100`

### 15.4 验证方式

- 公网 `curl http://gateway:9000/actuator/prometheus` → 404
- 内网 `curl http://auth:9100/actuator/prometheus` → 200

---

## 十六、P2-15 Dockerfile HEALTHCHECK + Layered JAR

### 16.1 问题描述

- Dockerfile 没有 `HEALTHCHECK` 指令（依赖 docker-compose 的 healthcheck）
- 没有 Layered JAR 优化，每次代码改动重建整个 jar 层 → 缓存失效

### 16.2 修复步骤

```dockerfile
# 构建阶段：分层
FROM maven:3.8-openjdk-17 as builder
COPY cloudflow-backend/ /app
WORKDIR /app
RUN mvn clean package -DskipTests -pl cloudflow-gateway -am

# 拆分 jar 层
FROM eclipse-temurin:17-jre-alpine as layer
WORKDIR /tmp
COPY --from=builder /app/cloudflow-gateway/target/*.jar app.jar
RUN java -Djarmode=layertools -jar app.jar extract

# 运行阶段
FROM eclipse-temurin:17-jre-alpine
RUN addgroup -S appuser && adduser -S appuser -G appuser
USER appuser
WORKDIR /app

# 按层 COPY（依赖层稳定，业务代码层频繁变化）
COPY --from=layer --chown=appuser:appuser /tmp/dependencies/ ./
COPY --from=layer --chown=appuser:appuser /tmp/spring-boot-loader/ ./
COPY --from=layer --chown=appuser:appuser /tmp/snapshot-dependencies/ ./
COPY --from=layer --chown=appuser:appuser /tmp/application/ ./

HEALTHCHECK --interval=30s --timeout=5s --start-period=60s --retries=3 \
    CMD wget --no-check-certificate --spider -q http://localhost:9000/actuator/health || exit 1

ENTRYPOINT ["java",
    "-XX:+UseContainerSupport",
    "-XX:MaxRAMPercentage=70.0",
    "-Djava.security.egd=file:/dev/./urandom",
    "org.springframework.boot.loader.launch.JarLauncher"]
```

### 16.3 验证方式

- 重建镜像：仅业务代码改动时，前 3 层走缓存
- `docker inspect` 显示 HEALTHCHECK 状态变化

---

## 十七、总修复时间线

```
Week 1-2 (重构基础):
  P2-1 工作流回调三件套抽公共模块 (5d)
  P2-11 Handler 注册表化 (2d)（与 P2-1 联动）
  P2-2 软删除标志统一 (3d)

Week 3 (重构延展):
  P2-3 表前缀统一 (5d)

Week 4 (安全加固):
  P2-4 限流 IP 来源校验 (1d)
  P2-5 @RepeatSubmit 摘要稳定 (0.5d)
  P2-6 文件上传兜底校验 (2d)
  P2-7 密码升级审计 (0.5d)
  P2-8 注册 emailDomain (1d)
  P2-9 startProcess 权限 (1d)

Week 5 (网关/部署):
  P2-13 网关限流启用 (1d)
  P2-14 Actuator 访问控制 (1d)
  P2-10 服务名统一 (0.5d)
  P2-12 HR 社保比例迁表 (2d)
  P2-15 Dockerfile HEALTHCHECK+Layered (1d)
```

---

## 十八、配套机制

1. **代码评审**：P2-1/P2-3 这类大重构需独立 PR + 2 人评审
2. **金丝雀部署**：P2-3 表前缀变更先在 staging 验证 1 周
3. **回滚预案**：所有 DB 改动保留 backup 表 30 天
4. **审计报告同步**：每完成一项，在 `后端审计报告.md` §13.6 修复记录表追加一行

---

## 十九、附录：快速命令清单

```bash
# 扫描重复的工作流回调代码
find cloudflow-backend -name "WorkflowCallbackService*.java" -o \
                       -name "WorkflowApprovalCallbackStreamConfig.java"

# 扫描软删除标志使用
grep -rn "del_flag\|@TableLogic" cloudflow-backend/ --include="*.java"

# 扫描表前缀分布
grep -h "CREATE TABLE" DB/*.sql | awk '{print $3}' | sort -u

# 验证服务名统一
grep -rn "spring.application.name\|lb://" config/ cloudflow-backend/

# 验证 Actuator 暴露
grep -rn "management.endpoints.web.exposure" cloudflow-backend/ config/

# 扫描 HEALTHCHECK 缺失
find docker -name "Dockerfile" -exec grep -L "HEALTHCHECK" {} \;
```

---

## 二十、CRM 模块整改阶段 1–3 联动（2026-05-13 已落地）

CRM 模块整改阶段 1–3（见 `docs/CRM-模块评估与整改方案.md`）已落地。与本 P2 文档的关系：

### 20.1 直接关系

| P2 条目 | 联动情况 |
|---|---|
| P2-1 工作流回调三件套抽公共模块 | **强相关**。CRM 阶段 3 新增了**第二条 Stream**（`crm:stream:events`，独立于 workflow 回调流），并在 OA 侧的 `ContractApprovalHandler` 直接 `redisStreamUtil.publishGlobal(...)`。建议 P2-1 推进时把 publish/subscribe 两端都纳入抽象，避免 CRM/OA 两侧重复散写 Stream 代码 |
| P2-2 软删除标志统一 | CRM 新增表 `oa_crm_handover_task`、`oa_crm_approval` 都使用 `del_flag CHAR(1)` 沿用 CRM 既有约定；P2-2 切到统一标志时一并迁移 |
| P2-3 表前缀统一 | 阶段 2/3 新增 3 张表（`oa_crm_handover_task`、`oa_crm_approval`、外加 `oa_crm_receivable.invoice_id` 字段、`oa_contract.source_*` 字段）。前两张表沿用 `oa_crm_*` 前缀；P2-3 统一改名时**多 2 张表**要带上 |
| P2-4 限流 IP 来源校验 | 无直接关系 |
| P2-7 工作台聚合接口加缓存 | 阶段 1 已经把"工作台聚合"独立出 `CrmCustomerWorkspaceServiceImpl`（705 行），P2-7 加缓存时只需在该类入口套 `@Cacheable`，比原来糅合在 1080 行单类时容易得多 |
| P2-9 startProcess 权限校验 | CRM 阶段 3 新增 4 类 `RemoteWorkflowService.startProcess` 调用（`customer_claim_review` / `customer_level_change` / `opportunity_downgrade_review` / `crm_refund_review`），P2-9 收敛 startProcess 权限时这 4 类需一并覆盖 |
| P2-10 服务名统一 | 无直接关系；CRM 新增的 `@FeignClient(name="cloudflow-service-crm/hr")` 都使用规范名 |
| P2-11 审批回调 Handler 注册表化 | **强相关**。CRM 现有 6 个 `ApprovalResultHandler`：原 2 个（`QuoteApprovalHandler`、`RenewalApprovalHandler`）+ 阶段 3 新增 4 个（`CrmCustomerClaimApprovalHandler`、`CrmCustomerLevelApprovalHandler`、`CrmOpportunityDowngradeApprovalHandler`、`CrmRefundApprovalHandler`）。新增的 4 个共享基类 `AbstractCrmApprovalHandler`，已经做了一半的注册表化工作，P2-11 落地时可直接复用 |

### 20.2 阶段 3 引入的工作流定义清单

CRM 现需要工作流引擎支持的 `processDefKey`（建议在 P2-9 时统一审计权限矩阵）：

| processDefKey | 关联业务 | businessType（用于 stream 回调） |
|---|---|---|
| `quote_approval` | 报价审批（既有） | `crm_quote` |
| `customer_renewal_review` | 续约审批（既有） | `crm_renewal` |
| `customer_claim_review` | 客户领取/公海释放（新） | `crm_customer_claim` |
| `customer_level_change` | 客户分级变更（新） | `crm_customer_level` |
| `opportunity_downgrade_review` | 商机降级关闭（新） | `crm_opportunity_downgrade` |
| `crm_refund_review` | 退款（新） | `crm_refund` |

> 后 4 个流程定义需在工作流引擎中事先导入，否则 `RemoteWorkflowService.startProcess` 会失败。当前 `CrmApprovalServiceImpl.startWorkflow` 已对失败做日志降级，不会回滚审批流水。

### 20.3 时间线

不占用 P2 的任何一波。P2-1 / P2-7 / P2-11 推进时与阶段 3 改动有显著协同收益。

> 本节仅作进度同步。详细内容见 `后端审计报告.md` §8.10、§9.11 与 §13.6。

---

*文档版本：v1.0 — 2026-05-13。每完成一项 P2，请同步更新本文档对应章节为"✅ 已完成"并在审计报告 §13.6 追加修复记录。*
